# Tasks: add-delta-scan-execution

## Phase 2: Implementation (Group A) — Wire the format-reader seam into production pushdown
- [x] 1.1 Add `partition_columns: Vec<String>` to `JoinSpec`, serde-defaulted and skipped when empty; assert an Iceberg join spec's serialization is byte-identical
- [x] 1.2 Build the per-request `TableScanResolver`: one exhaustive `CatalogKind` match, one catalog session per request, Unity `CatalogTableIdent` recovery from the recorded dotted identifier, `format_reader` + `resolve_scan` [expert]
- [x] 1.3 Route `handle_pushdown`'s single-table path through the resolver; thread the resolved `CatalogKind` from `dispatch` through `handle_pushdown_request`; populate `CommonScanSpec.partition_columns` from the `ResolvedScan`
- [x] 1.4 Route every join leg through the resolver: replace `JoinSideResolution`'s Iceberg-typed session with the resolver, and populate each side's `partition_columns` (fact side into the common spec, broadcast side into `JoinSpec`)
- [x] 1.5 Delete the `dispatch` Unity Catalog refusal; replace `unity_kind_pushdown_is_refused_not_iceberg_routed` with a routing assertion; update the construction-site probe's doc comment to name the scan-source construction site instead of the refusal
- [x] 1.6 Collapse `resolve_file_list` into `IcebergFormatReader::resolve_scan` and delete it; migrate `tests/common/e2e_harness.rs`, `tests/catalog_session_signatures.rs`, `tests/e2e_scan_test.rs`, `file_resolution_tests.rs`, and `format/iceberg_tests.rs` onto the seam; update both surface probes to 25 in-crate and 15 external items
- [x] 1.7 Pin capability advertisement as catalog-kind-blind with a compile-time signature probe

## Phase 2: Implementation (Group B) — Apply Delta deletion vectors at scan time
- [x] 2.1 Add `scan/deletion_vectors.rs` (+ sibling `deletion_vectors_tests.rs`): resolve a descriptor to its sidecar path or inline payload, serve pre-fetched bytes through a read-only `StorageHandler` shim whose unsupported operations return clean errors and never panic, decode through `DeletionVectorDescriptor::read`, and reject a decoded set whose length disagrees with the declared cardinality [expert]
- [x] 2.2 Replace `applicable_positional_delete`'s `(path, size)` result with a delete-mechanism classification that keeps both Iceberg refusals; extend Phase A to fetch each unique sidecar once under the shared limiter and merge Delta positions into the one position map; leave Phase B and the row-selection and access-plan builders untouched [expert]
- [x] 2.3 Integration tests over the vendored `table-with-dv-small` fixture on a local filesystem store: 10 physical rows yield 8, the two deleted values are absent, a mixed Iceberg-and-Delta shard, shared-sidecar read-once, bounded concurrency, and each malformed-container refusal

## Phase 2: Implementation (Group C) — Materialize partition values in the scan
- [x] 3.1 Split the logical schema into file fields and partition fields at registration, keep `TableProvider::schema()` in declared order, attach `table_partition_cols`, and remap the projection indices from declared order to `file ++ partition` order [expert]
- [x] 3.2 Populate each `PartitionedFile.partition_values` from its `FileEntry`, converting the protocol's string serialization to the column's declared Arrow type, mapping an absent or empty value to NULL, and failing cleanly on a value the type cannot represent [expert]
- [x] 3.3 Apply the same registration path to both sides of a broadcast join, reading the broadcast side's partition columns from `JoinSpec`
- [x] 3.4 Integration tests over the vendored `basic_partitioned` fixture: per-file constants across six files, the Hive default-partition file yielding NULL, a filter and a GROUP BY over the partition column, a partition column physically present in the file, and an unpartitioned scan proving no plan-shape change

## Phase 2: Implementation (Group D) — End-to-end coverage on the Unity Catalog suite
- [x] 4.1 Give the suite's virtual schema a CONNECTION carrying the MinIO endpoint and static storage credentials, and confirm the shared harness provisions the scan script
- [x] 4.2 Add the round-trip scenarios to `tests/e2e_unity_test.rs`: delete-free `multi_part_stats`, deletion-vector `table_with_dv`, column-mapped `cm_id_mode` and `cm_name_mode`, partitioned `basic_partitioned` with filter and GROUP BY, a grouped aggregate, an ORDER BY with LIMIT, a broadcast join whose broadcast side is partitioned with a captured pushdown SQL assertion, and the fail-loud refusals for `unshredded_variant` and `stats_all_types`

## Phase 2: Implementation (Group E) — Verification and hygiene
- [x] 5.1 Verify how a percent-encoded Delta `add.path` flows from log replay to file registration; if it reaches object storage undecoded, open a tracked issue and cite it inline in the `delta-table-planning` spec delta
- [x] 5.2 Run the Iceberg E2E suites unchanged to prove the rewiring is byte-identical, then the full checklist below

## Phase 4: Code Review
- [x] 4.rev Code review of all changed files — 24 findings (standard: 22, expert: 2)

## Phase 4: Review Fixes
- [x] 4.1 Move involved-table identifier validation behind the catalog kind: validate every identifier the request will resolve inside `TableScanResolver::for_request`'s one exhaustive `CatalogKind` match — `parse_table_ident` for `IcebergRest`, `unity_table_ident` for `UnityCatalogNative` — ahead of any catalog contact, delete both `parse_table_ident` calls from `pushdown/mod.rs`, and decide the Unity identifier shape in `unity_table_ident` alone [expert]
- [x] 4.2 Rebuild `PartitionedScanSchema::split` without the `file_count` subtraction and the `Option<FieldRef>`/`expect`: collect file fields and partition slots in one pass, refuse a partition column matching a second declared field, compute each partition column's scan index from `file_fields.len()`, assert every declared column maps to a distinct scan index, and add unit tests for both duplicate-name schemas [expert]

- [x] 4.3 Restore `JoinSpec`'s struct-level doc comment verbatim from `git show HEAD:crates/lakehouse-engine/src/scan/spec.rs`, placing it above the derive attribute so the `storage` field's "see the struct doc" reference resolves again
- [x] 4.4 Restore `build_broadcast_join_sql`'s doc comment verbatim from `git show HEAD:crates/lakehouse-engine/src/adapter/pushdown/joins/sql_builders.rs`, and append one sentence stating that each side's `partition_columns` ride in that side's own spec block
- [x] 4.5 Restore the seven collaterally-deleted doc comments verbatim from `git show HEAD:<path>` in crates/lakehouse-engine/src/scan/object_store_tests.rs (`spec_with_join`, `adls_spec_with_join`), crates/lakehouse-engine/src/scan/spec_tests.rs (`join_block_round_trips_through_split_and_merge`, `join_spec_omitting_post_join_limit_deserializes_to_none`), crates/lakehouse-engine/tests/scan_join_test.rs (`join_spec`, `a_dimension_side_read_failure_redacts_the_dimension_sides_credential`), and crates/lakehouse-engine/tests/e2e_unity_test.rs (`create_unity_virtual_schema`, amended to state the password now also carries the MinIO endpoint and static storage credentials)
- [x] 4.6 In crates/lakehouse-engine/src/adapter/mod.rs, introduce `struct ResolvedConnectionConfig { catalog_uri, storage, creds, allow_http, catalog_kind }`, return it from `resolve_connection_config`, pass it as one argument to `handle_pushdown_request` and on to `handle_pushdown`, and delete the `#[allow(clippy::too_many_arguments)]` on `handle_pushdown_request`
- [x] 4.7 In crates/lakehouse-engine/src/adapter/pushdown/joins/planning.rs, replace `ResolvedJoinSide::new`'s 8 parameters with `(table_name: String, table_identifier: String, resolved: ResolvedScan)`, move the field destructuring inside it, update `resolve_one_join_side` to pass the `ResolvedScan` whole, update the call in joins_tests.rs, and delete the `#[allow(clippy::too_many_arguments)]`
- [x] 4.8 Rename the field `iceberg_ident` to `table_identifier` on both `JoinLeaf` and `ResolvedJoinSide`, and the `resolve_one_join_side` parameter likewise, using Serena's `rename_symbol`; update all references across crates/lakehouse-engine/src/adapter/pushdown/ and crates/lakehouse-engine/tests/ (including `joins/mod.rs`'s `identifiers` collection)
- [x] 4.9 In crates/lakehouse-engine/src/adapter/pushdown/, move `encode_initial_default` next to its only consumer in `format/iceberg.rs`, move `empty_result_sql` into its own `empty_result.rs` submodule with sibling `empty_result_tests.rs`, rename the remainder of `file_resolution.rs` to `shard_paths.rs` with sibling `shard_paths_tests.rs`, update the `mod`/`use` lines in pushdown/mod.rs and joins/mod.rs, and split `file_resolution_tests.rs` to match the per-submodule test rule
- [x] 4.10 In crates/lakehouse-engine/src/adapter/pushdown/pushdown_tests.rs, delete the dead `#[allow(clippy::too_many_arguments)]` on `seam_handle_pushdown` (5 arguments, below the lint's threshold)
- [x] 4.11 Add `#[tokio::test] async fn a_two_leg_join_resolves_both_legs_on_one_catalog_session()` in crates/lakehouse-engine/src/adapter/pushdown/pushdown_tests.rs driving `handle_pushdown` via `seam_handle_pushdown` with a two-table join request against a `RecordingCatalog`, asserting `catalog.targets()` is exactly `[ICEBERG_CONFIG_TARGET, <leg 1 loadTable>, <leg 2 loadTable>]`; add the `UnityCatalogNative` twin in crates/lakehouse-engine/src/adapter/pushdown/scan_resolution_tests.rs resolving two identifiers on one Unity resolver and asserting exactly two `unity-catalog/tables/...` targets and no repeated auth target
- [x] 4.12 In crates/lakehouse-engine/src/adapter/adapter_tests.rs, rename `unity_kind_pushdown_routes_to_the_delta_format_reader` to `unity_kind_pushdown_routes_to_the_unity_catalog_loader`, reword its assertion message to "must reach the Unity Catalog load-table call", add `assert!(message.contains("unity-catalog/tables"))`, and point the plan's "Planning: The Delta reader is reached from production pushdown" scenario row at `every_request_shape_resolves_through_the_format_reader_seam`
- [x] 4.13 In crates/lakehouse-engine/src/adapter/pushdown/scan_resolution_tests.rs, change the vacuous refusal assertion (lines ~104-107) to `err.to_string().contains(&format!("'{unresolvable}'"))`, non-vacuous for all three loop inputs
- [x] 4.14 In crates/lakehouse-engine/src/adapter/pushdown/scan_resolution_tests.rs, reduce `an_iceberg_identifier_resolves_through_the_iceberg_reader_with_no_partition_columns` to what the resolver alone owns (the `IcebergRest` kind reaches the Iceberg reader and `partition_columns` comes back empty), deleting the `table_root`/`files`/`effective_storage`/`logical_schema`/`name_mapping` assertions already covered by `format/iceberg_tests.rs`
- [x] 4.15 In crates/lakehouse-engine/src/adapter/pushdown/format/iceberg_tests.rs, reword the doc at lines ~95-102 to state what the test actually asserts (the reader resolves the fixture's files, table root, effective storage, field-id-bound logical schema, and name mapping, and adds no partition columns), naming `dispatch_golden_tests.rs` as where encoding byte-identity is pinned
- [x] 4.16 In crates/lakehouse-engine/src/adapter/pushdown/format/iceberg_tests.rs, delete `loopback_catalog` and route its tests through `super::super::test_support::RecordingCatalog`; rename the local `snapshotless_load_table_body` at line ~39 to `name_mapped_load_table_body` so it no longer collides with `test_support_tests.rs`'s helper of the same name
- [x] 4.17 In crates/lakehouse-engine/src/scan/positional_deletes.rs, delete the inline comment inside `partitioned_files` (lines ~844-847) that restates the method's own doc comment
- [x] 4.18 In crates/lakehouse-engine/src/scan/positional_deletes_tests.rs, delete the vacuous `assert!(!err.contains("#320"), ...)` block (lines ~350-353) in `only_iceberg_equality_and_puffin_delete_mechanisms_are_refused`, which checks the PUFFIN refusal for text only the removed Delta refusal ever cited
- [x] 4.19 In crates/lakehouse-engine/tests/scan_deletion_vectors.rs, drop the `expected_cause` element from the `corruptions` tuples and its substring assertion in `malformed_deletion_vector_containers_fail_the_scan_without_panicking`; keep the loop and assert instead that the message names the affected data file and that no batch was emitted for each corruption
- [x] 4.20 In crates/lakehouse-engine/tests/e2e_unity_test.rs, delete `unity_virtual_schema_connection_carries_minio_storage_credentials` (lines ~546-569) and move its doc comment's credential rationale onto `unity_delta_delete_free_table_returns_its_rows`; update the plan's Scenario Coverage row for the storage-credentials scenario to name that test
- [x] 4.21 In crates/lakehouse-engine/tests/e2e_unity_test.rs, immediately after collecting `cm_ids` (lines ~894-910), assert its values are distinct so the semi-join ground truth is proven equal to a real join
- [x] 4.22 In crates/lakehouse-engine/tests/e2e_unity_test.rs, replace the `split_at(join_idx)` string-offset assertions (lines ~879-891) with assertions over the parsed spec: extract the `"join":{…}` object from `pushed` and assert its `table_root` names `basic_partitioned` while the surrounding common spec's `table_root` names `cdf-column-mapping-id-mode` (corrected from the finding's original `files`-list wording after a live E2E run showed neither side's `files` array entries carry the table name as a substring — only `table_root` does, and the sharded fact side carries no top-level `files` field at all when file assignment flows through `LAKEHOUSE_DISTRIBUTE_FILES`)
- [x] 4.23 In crates/lakehouse-engine/tests/e2e_unity_test.rs, extend the module doc sentence (lines ~20-23) to state that the CONNECTION password supplies no CATALOG-auth field but does carry the MinIO endpoint and static storage credentials the UDF-side scan reads through
- [x] 4.24 In crates/lakehouse-engine/tests/catalog_session_signatures.rs, retitle the covered-scenario line (lines ~22-26) to "the Iceberg scan source carries a shared catalog session" and add a sentence pointing the façade-departure claim at tests/pushdown_public_surface.rs and src/adapter/pushdown_surface_probe_tests.rs

## Phase 5: Verification
- [x] 5.a Automated checks (build, test, e2e-unity, e2e-iceberg, lint, format)
- [x] 5.b Scenario coverage audit
- [x] 5.c Manual verification

## Phase 6: Verification Report
- [x] 6.1 Generate verification-report.md
