# Plan Review Findings: add-native-unity-catalog-client (round 2)

<!-- Overwrites the STALE 0-findings certification left by an earlier (storage-backend-enum)
     cycle. This is a fresh round-2 pass over the TRAIT-BASED revision. The two round-1 BLOCKERs
     (async-trait ban; deleted `resolve_table_schema` + missing module-structure delta) are
     re-checked below and are resolved for the homes they addressed. Two NEW blockers of the same
     class — `resolve_table_schema` is recorded in two further live features the round-1 fix did
     not reach — are raised under Requirement Quality. -->

## Summary
- Axes checked: 6/6
- Total findings: 2 (Blockers: 2, Advisory: 0)
- Intent Fidelity blockers: 0

## Round-1 Blocker Recheck

- Resolved: [REQUIREMENT_CONFLICT] async-trait ban (Option A, boxed futures) — plan task 1.1 now reads "Do NOT use `#[async_trait]` and do NOT add `async-trait` to the crate manifest"; § Dependencies (line 111) states the crate "gains no manifest line" and makes the trait dyn-compatible via `Pin<Box<dyn Future<…> + Send + '_>>`; decision-log `[plan-review]` records Option A. `crates/lakehouse-catalog/tests/catalog_crate_boundary.rs` is left unedited and still lists `"async-trait"` in `FORBIDDEN_DIRECT_DEPENDENCIES` (line 25), so the ban stays true. The mechanism is coherent: the boxed-future return is dyn-compatible, and the error type `UdfError` (`exasol_udf_sdk::error::UdfError`) is already in use in `lakehouse-catalog` (`session.rs`, `namespace.rs`, `auth.rs`, `vended.rs`), so `Result<CatalogListing, UdfError>` names an in-scope type.
- Resolved: [REQUIREMENT_CONFLICT] deleted `resolve_table_schema` broke the in-crate probe + missing module-structure delta — the new CHANGED delta `vs-adapter/pushdown-module-structure/spec.md` exists and is in plan § Features (line 102); its arithmetic matches the code (in-crate probe `src/adapter/pushdown_surface_probe_tests.rs` verified at 22 items incl. `resolve_table_schema` → 21; external probe `tests/pushdown_public_surface.rs` verified at 12 → 11; `resolve_file_list` alone keeps `pub`); task 3.3 now edits all three consumer files, and § Dead Code Removal reflects it. Resolved **for the two façade probes**. The deletion's other recorded homes are NOT covered — see the two new blockers below.

## Intent Fidelity

no objection — axis checked. The verbatim ask ("the same Catalog abstraction … a common trait … a common interface") is still operationalized by ONE `CatalogClient` trait with `list_tables`/`load_table`, both kinds implementing it, one construction site, and one listing pipeline. All fixed interview/decision points (single shared trait, `IcebergRestCatalogClient` composing `CatalogSession`, listing-only scope, Option A, third-selector design) are honored unchanged. No drift, creep, or silent reduction introduced by the revision.

## Feasibility

no objection — axis checked. The boxed-future mechanism is buildable with no new dependency (`UdfError` in scope in `lakehouse-catalog`; `reqwest 0.12`/`serde`/`url` already present). The `#325` fixture-harness dependency is named with branch/commit in § Dependencies. The DECIMAL descriptor advisory is applied without regression — `unity-catalog-client` Background, Scenario 1, and the get-table scenario now specify the FULL parameterized Spark type (name + precision/scale from `type_precision`/`type_scale` or `type_text`), so the `DECIMAL(p,s)` mapping is testable.

## Requirement Quality

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: plan.md § Features (lines 92-102, no `pushdown-catalog-session` row) and task 3.3 (line 129); recorded `specs/vs-adapter/pushdown-catalog-session/spec.md` scenario "CatalogSession is public and every file-resolution entry point takes one" (line 76) and scenario "createVirtualSchema resolves every table's schema on one shared session" (lines 60-70); test `crates/lakehouse-engine/tests/catalog_session_signatures.rs`
- Issue: Deleting `resolve_table_schema` (task 3.3) invalidates the live recorded feature `vs-adapter/pushdown-catalog-session`, which the plan carries NO delta against. Two of its scenarios name the deleted function normatively: (1) "CatalogSession is public and every file-resolution entry point takes one" states `*AND* resolve_table_schema SHALL likewise take &CatalogSession as its first parameter and SHALL NOT construct a session of its own` (line 76) — after deletion this `SHALL` clause references a function that no longer exists; (2) the whole scenario "createVirtualSchema resolves every table's schema on one shared session" (lines 60-70) describes an adapter-side schema loop calling `resolve_table_schema` per table on one shared `CatalogSession`, the exact mechanism the plan removes and relocates into `IcebergRestCatalogClient::list_tables`. The test `catalog_session_signatures.rs` explicitly declares (its lines 19-23) that it covers both of these `vs-adapter/pushdown-catalog-session` scenario rows, and task 3.3 edits that file to drop the `resolve_table_schema` proof — deleting a recorded scenario's test with no authorizing delta. The plan mis-attributes this edit: task 3.3 says the `catalog_session_signatures.rs` change is "recorded by the `vs-adapter/pushdown-module-structure` delta", but that delta covers only the `pushdown::<name>` façade re-export set, not `pushdown-catalog-session`'s behavioral signature/one-session scenarios. At `/speq:record` the recorded library would retain a `SHALL` clause naming a deleted function and a scenario describing a deleted mechanism, with its test removed — a silent recorded-spec change, the same defect class round-1 BLOCKER-2 gated for the façade probes.
- Fix: Add a CHANGED delta `vs-adapter/pushdown-catalog-session` to the plan (create `specs/_plans/add-native-unity-catalog-client/vs-adapter/pushdown-catalog-session/spec.md` and add a `pushdown-catalog-session | CHANGED` row to plan.md § Features). In it: (a) SUPERSEDE the line-76 clause of "CatalogSession is public and every file-resolution entry point takes one" so that after deletion only `resolve_file_list` remains taking `&CatalogSession` and `resolve_table_schema` is removed from the file-resolution entry-point set; (b) SUPERSEDE or relocate the scenario "createVirtualSchema resolves every table's schema on one shared session", stating the one-session-per-enumeration, empty-namespace-no-grant, skip-non-loadable, two-grants-on-OAuth-mode, and grant-failure-before-loop guarantees move from the adapter schema loop into `IcebergRestCatalogClient::list_tables` (tested by `crates/lakehouse-catalog/src/client_tests.rs::enumeration_builds_exactly_one_session` and `empty_namespace_builds_no_session_and_no_grant`). In task 3.3, correct the attribution so the `catalog_session_signatures.rs` edit (dropping the `schema_resolution_entry_point_takes_a_shared_session` proof and its covered-scenario doc line) is recorded by this new `pushdown-catalog-session` delta, not by `pushdown-module-structure`.

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: plan.md § Features (lines 92-102, no `create-virtual-schema` row), § Patterns (line 66 "One case-fold home"), § Dead Code Removal (line 163); recorded `specs/vs-adapter/create-virtual-schema/spec.md` scenario "Create virtual schema enumerates every table in the configured namespace" (line 73, and Background line 45)
- Issue: The plan moves the Iceberg column-name case-fold out of `resolve_table_schema` into the shared listing pipeline — § Dead Code Removal (line 163): "the Exasol-mapping and uppercasing half moves into the shared pipeline"; § Patterns (line 66): "the shared pipeline folds every declared name, both kinds" — and deletes `resolve_table_schema`. But the live recorded feature `vs-adapter/create-virtual-schema` pins that fold to that exact function: `*AND* that fold SHALL be owned by exactly ONE site, resolve_table_schema (adapter/pushdown/file_resolution.rs), which produces the (name, Exasol type) pairs the response's column list is built from, and no other code path SHALL declare a differently-cased name` (line 73). After the plan, `resolve_table_schema` no longer exists and the single fold owner is the shared pipeline (which must also fold the new Unity path's names), so this `SHALL` clause names a deleted function and the wrong site. The plan carries NO delta against `vs-adapter/create-virtual-schema`, so the "one fold home = `resolve_table_schema`" invariant would record as false. (Background line 45 likewise cites `resolve_table_schema` at `file_resolution.rs:610-644` and `adapter/mod.rs:255/551/576` as the sole fold/column-declaration site.)
- Fix: Add a CHANGED delta `vs-adapter/create-virtual-schema` to the plan (create `specs/_plans/add-native-unity-catalog-client/vs-adapter/create-virtual-schema/spec.md` and add a `create-virtual-schema | CHANGED` row to plan.md § Features). SUPERSEDE the line-73 clause so the single fold owner is the shared `CatalogClient` listing pipeline (the one home that folds every declared name for BOTH catalog kinds), replacing the `resolve_table_schema` naming, while keeping "owned by exactly ONE site" and "no other code path SHALL declare a differently-cased name" intact — including the `ß`→`SS` / `straße`→`STRASSE` full-Unicode expansion and the no-collision-check trade-off (line 74) the plan promises to keep byte-identical. Note in the delta that the enumeration mechanism moves behind the trait but the enumerated tables, declared names/types, `TABLE_MAP`, warnings, and errors stay byte-identical.

## Task Breakdown

no objection — axis checked. The empty-namespace guarantee migration is now explicitly named (task 3.3 and § Dead Code Removal line 162) with a source and destination test, and the OSS inline-columns precondition ordering (`3.1 → 3.2 → 3.3`, task 4.2 for E2E) is stated. Task 3.3 remains large but is a single expert task with a byte-identical acceptance gate; the sole traceability defect (the `catalog_session_signatures.rs` edit lacking an authorizing delta) is captured in the first Requirement-Quality blocker rather than duplicated here.

## Design Depth

no objection — axis checked. `IcebergRestCatalogClient` composing `CatalogSession` (decision [10]) preserves the empty-namespace-no-grant and one-session guarantees; task 1.2's private session-taking helper is now DISTINCT from the trait `load_table`, and decision [9]'s `load_table` warrant is restated as the user-requested single-table load and #319/#320 scan source (round-1 advisory applied). Source-tagged `ColumnSourceType` keeps one Exasol-mapping home and the one-way crate dependency intact. No new shallowness, leakage, or boundary violation introduced.

## Prose Quality

no objection — axis checked. The plan § Summary is now five sentences, each ≤25 words, leading with the load-bearing conclusion ("Both catalog kinds share ONE `CatalogClient` operation surface"), per the round-1 advisory. Decision [8] Rationale opens with its conclusion. No new BLUF, terseness, or ambiguity regressions in the revised prose.
