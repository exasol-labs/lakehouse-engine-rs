# Tasks: change-sigv4-region-inference

## PR Lifecycle
- [x] resolved
- [x] implemented
- [x] version-bumped
- [ ] tested-green
- [ ] recorded
- [ ] pr-ready

## Phase 2: Implementation (Group A: Signing-region resolver and both catalog signing paths)
- [x] 1.1 Write failing unit tests in `crates/lakehouse-catalog/src/sigv4_tests.rs` for `ConnectionCreds::sigv4_signing_region`. Cover the scenario host matrix: standard Glue hosts (incl. uppercase, `:443` port, `ap-southeast-2`) match; GovCloud, China, FIPS, VPC interface, dual-stack, private host, `http` scheme, userinfo form, trailing-dot host, multi-label region, empty URI, unparseable URI all fail. Also cover: no-region Glue endpoint derives; stated-region-differs Glue endpoint still derives; non-standard endpoint with stated region returns stated value; empty-region non-Glue returns `None`.
- [x] 1.2 Implement `sigv4_signing_region` and private `glue_endpoint_region` in `crates/lakehouse-catalog/src/sigv4.rs` with `url::Url`, per the Design host rule. Doc comment per Key Interfaces; name no Exasol CONNECTION mechanism. [expert]
- [x] 1.3 Add `pub(crate) fn required_signing_region` in `sigv4.rs`, returning the credential-safe refusal error.
- [x] 2.1 Change `CatalogAuth::Sigv4` to `Sigv4 { region: String }` in `crates/lakehouse-catalog/src/auth.rs`; make `resolve_catalog_auth` fill it from `required_signing_region(creds, catalog_uri)?`.
- [x] 2.2 In `crates/lakehouse-catalog/src/iceberg_io.rs`, sign with the region the variant carries in `authed_get_json`; update redaction match arm to `Sigv4 { .. }`; update the match in `resolve_load_table_prefix` (`session.rs:149`).
- [x] 2.3 In `crates/lakehouse-catalog/src/namespace.rs`, resolve region once in `list_namespace_tables` via `required_signing_region` before the first request; thread `region: &str` through `list_in_namespace_signed` and `signed_get_json`; delete `&creds.region` from that `sign_request` call.
- [x] 2.4 Update tests naming the unit variant: `session_tests.rs:187`, `session_tests.rs:340`, `auth_tests.rs:366`.
- [x] 2.5 Add tests in `auth_tests.rs`: `sigv4_auth_carries_region_derived_from_glue_endpoint`, `sigv4_auth_derives_region_even_when_a_different_region_is_stated`, `sigv4_auth_refuses_without_signing_region`. SigV4 branch makes no network call.
- [x] 2.6 Add `sigv4_request_is_signed_for_the_carried_region` in `iceberg_io_tests.rs`: local TCP listener (`namespace_tests.rs:74-143` pattern), `creds.region` empty, `CatalogAuth::Sigv4 { region: "eu-west-1".into() }`, assert `Authorization` header contains `/eu-west-1/glue/aws4_request`.
- [x] 2.7 Add `signed_enumeration_is_signed_for_the_resolved_region` and `signed_enumeration_refuses_without_signing_region` in `namespace_tests.rs`, same capture pattern against `list_in_namespace_signed` and `list_namespace_tables`.
- [x] 2.8 In `crates/lakehouse-catalog/tests/catalog_public_surface.rs`, add `connection_creds_sigv4_signing_region_is_reachable` and `signing_region_steps_are_not_public` (asserts from `sigv4.rs` source: no `pub fn glue_endpoint_region`, no `pub fn required_signing_region`, no `CONNECTION`/`UdfContext` token).

## Phase 2: Implementation (Group B: Adapter guard, operator docs, and live Glue proof)
- [x] 3.1 In `crates/lakehouse-engine/src/adapter/connection.rs`, pass `&uri` from `read_connection` into `validate_creds` and on to `validate_sigv4_creds`. Count `region` missing only when `creds.sigv4_signing_region(catalog_uri)` is `None`.
- [x] 3.2 Append the Glue-endpoint clause from the Design table when the error names `region`.
- [x] 3.3 Rewrite the `REQUIRED_KEY` doc comment (`connection.rs:17-22`) to state the conditional `region` rule.
- [x] 3.4 In `crates/lakehouse-engine/src/adapter/connection_tests.rs`, update `sigv4_requires_access_secret_region`: cite the new scenario title, assert the Glue-endpoint clause when `region` is named.
- [x] 3.5 Add `sigv4_region_required_for_non_standard_glue_hosts` in `connection_tests.rs`: GovCloud, China, FIPS, VPC interface, dual-stack, private-host, `http` addresses; each rejected naming `region`, no key leaked.
- [x] 3.6 Add four tests in `connection_tests.rs`: `sigv4_region_derived_from_standard_glue_endpoint_is_accepted`, `sigv4_derived_region_places_no_store_under_vending`, `sigv4_cross_region_glue_and_s3_is_supported`, `sigv4_stated_region_used_for_non_standard_endpoint`.
- [x] 4.1 In `docs/catalogs.md`, update the `region` field-table row (line 41), § "Connection fields" (line 56), § "AWS Glue Iceberg REST (SigV4)" (lines 92 and 114) per the plan's Impact/Migration guidance.
- [x] 5.1 In `crates/lakehouse-engine/tests/cloud_e2e_test.rs`, add `CloudEnv::catalog_connection_password_without_region` (`region: String::new()`); let `setup_cloud_vs` take the password to use.
- [x] 5.2 Add `cloud_sigv4_region_derived_from_glue_endpoint_lists_table`: skip when `GLUE_CATALOG_URI` doesn't begin with `format!("https://glue.{AWS_REGION}.amazonaws.com/")`; create CONNECTION `GLUE_CATALOG_CREDS_NO_REGION` and a virtual schema; assert `SYS.EXA_ALL_TABLES` lists the configured table; read no data file.

## Phase 4: Review Fixes
- [x] 4.1 In `crates/lakehouse-catalog/src/namespace.rs`, add a private struct `SignedEnumeration<'a> { catalog_uri: &'a str, prefix: &'a str, creds: &'a ConnectionCreds, region: &'a str }`. Turn `list_in_namespace_signed` into a method `fn list_in_namespace_signed<'s>(&'s self, ns: &'s NamespaceIdent) -> Pin<Box<dyn Future<Output = Result<Vec<TableIdent>, UdfError>> + Send + 's>>` whose recursion calls `self.list_in_namespace_signed(&child)`. Turn `signed_get_json(url, creds, region)` into the method `self.signed_get_json(url)`. In `list_namespace_tables`, build the struct in the SigV4 branch from `catalog_uri`, `&prefix`, `creds`, and `&region`, then call the method. In `crates/lakehouse-catalog/src/namespace_tests.rs`, change `signed_enumeration_is_signed_for_the_resolved_region` to build `SignedEnumeration { catalog_uri: &catalog_uri, prefix: "catalogs/123456789012", creds: &creds, region: "eu-west-1" }` and call `.list_in_namespace_signed(&ns)`. Keep every URL, signing, error-text, and best-effort child-listing behavior unchanged, and keep every existing assertion.
- [x] 4.2 In `crates/lakehouse-catalog/src/sigv4.rs`, change `const MISSING_SIGNING_REGION` to `pub(crate) const MISSING_SIGNING_REGION`. In `crates/lakehouse-catalog/src/auth_tests.rs` `sigv4_auth_refuses_without_signing_region` and `crates/lakehouse-catalog/src/namespace_tests.rs` `signed_enumeration_refuses_without_signing_region`, replace the expected string literal with `crate::sigv4::MISSING_SIGNING_REGION`. Leave the literal in `crates/lakehouse-catalog/src/sigv4_tests.rs` `required_signing_region_refuses_without_a_signing_region` as the only text pin, because that pin proves the message carries no credential and no URI.
- [x] 4.3 In `crates/lakehouse-catalog/src/session_tests.rs` `catalog_session_resolve_sigv4_no_config_roundtrip`, add `creds.region = String::new();` after `creds.use_sigv4 = true;`, so the asserted `"us-east-1"` can only come from the Glue endpoint. Keep the existing assertion and its message.
- [x] 4.4 In `crates/lakehouse-engine/src/adapter/connection_tests.rs` `sigv4_requires_access_secret_region`, add `assert!(!msg.contains("https://glue.<region>.amazonaws.com"), "must not state the Glue-endpoint alternative when region is not named: {msg}");` to the "Missing access_key" case and to the "Missing secret_key" case.
- [x] 4.5 In `crates/lakehouse-engine/src/adapter/connection_tests.rs`, delete the `// region intentionally absent` line inside the `serde_json::json!` literal in `sigv4_region_required_for_non_standard_glue_hosts`, `sigv4_region_derived_from_standard_glue_endpoint_is_accepted`, and `sigv4_derived_region_places_no_store_under_vending`. Leave the pre-existing one at line 961 alone.
- [x] 4.6 In `crates/lakehouse-engine/tests/cloud_e2e_test.rs`, add a private struct `CloudVsTarget<'a> { conn_name: &'a str, vs_name: &'a str, password: CatalogConnectionPassword }`. Change the signature to `fn setup_cloud_vs(conn: &mut ExaConn, env: &CloudEnv, target: &CloudVsTarget)`, reading `target.conn_name`, `target.vs_name`, and `&target.password` in the body. Update all three callers (`cloud_smoke_projection_filter_query`, `cloud_sigv4_region_derived_from_glue_endpoint_lists_table`, `cloud_perf_grouped_aggregate_smoke`) to build a `CloudVsTarget` with their current values.
- [x] 4.7 In `crates/lakehouse-engine/tests/cloud_e2e_test.rs`, delete `setup_cloud_vs_vended`. In `cloud_scan_reads_with_vended_credentials`, call `setup_cloud_vs` instead, passing conn name `CLOUD_CATALOG_CONN_VENDED`, virtual schema name `format!("{CLOUD_VS_NAME}_VENDED")`, and password `env.catalog_connection_password_vended()`, in the parameter shape task 4.6 gives `setup_cloud_vs`.
- [x] 4.8 In `crates/lakehouse-engine/tests/cloud_e2e_test.rs`, add `fn vs_table_name(glue_table: &str) -> String` that returns the uppercased last dot-separated component. Rewrite `vs_table` as `format!("{CLOUD_VS_NAME}.{}", vs_table_name(glue_table))`. Replace the `vended_table` block's derivation with `vs_table_name(&env.glue_table)`, keeping its `{CLOUD_VS_NAME}_VENDED.` prefix. Replace `expected_table`'s derivation with `vs_table_name(&env.glue_table)`. Move the existing "The adapter uppercases the table's last component." note from inside `vs_table` into a doc comment on `vs_table_name`.
- [x] 4.9 In `crates/lakehouse-engine/tests/cloud_e2e_test.rs` `cloud_sigv4_region_derived_from_glue_endpoint_lists_table`, delete the inline comment `// List tables only — no SELECT against the table, so no data file is read.`

## Phase 5: Verification
- [x] V.1 Run automated checks (build, test, e2e, cloud e2e opt-in, lint, format)
- [x] V.2 Scenario coverage audit against plan's Verification > Scenario Coverage
- [x] V.3 Manual verification steps
