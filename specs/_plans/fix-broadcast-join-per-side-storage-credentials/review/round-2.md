# Plan Review Findings: fix-broadcast-join-per-side-storage-credentials (round 2)

## Summary
- Axes checked: 6/6
- Total findings: 6 (Blockers: 2, Advisory: 4)
- Intent Fidelity blockers: 0

## Round-1 Blocker Recheck

- **Resolved (substance): [UNSTATED_ASSUMPTION] The scan's object-store access set was enumerated wrongly in both directions.** Every routing claim in the new evidence table was re-derived against the tree and holds. Verified exact: `positional_deletes.rs:663-665` (`object_meta_for` at `:215-225`, doc sentence verbatim at `:213-214`); the `!logical_schema.is_empty()` guard at `raw_scan.rs:204-205` with the `else` branch at `:207-217` calling `ListingOptions::infer_schema` on the FIRST file only (`:176-181`); `url.rs` `is_collection()` at `:204-206`, the skip-head branch at `:266-274`, `store.head` at `:276`, the `NotFound` fallback at `:282-291`, and `store.list(Some(&full_prefix))` at `:397-398`. The load-bearing derived claim also holds: `ListingTableUrl::try_new` sets `prefix = Path::from_url_path(url.path())` (`url.rs:150-158`) and `list_with_cache` uses `table_base_path` unchanged when `prefix` is `None` (`:387-394`), so the `NotFound`-triggered listing prefix is the first data file's own exact path — never a parent. Task 3.2's `Some(prefix)` rule, task 3.3's two listing tests, and the routing scenario's new normative clause (`scan-execution-join/spec.md:56`) are all present. **One evidence cell inside this fix is false** — raised fresh as the Feasibility BLOCKER below.
- **Resolved: [UNSTATED_ASSUMPTION] A failed reproduction gate had no route for the artifacts that assume it passes.** Task 1.3 now routes an ALLOWED read back to `/speq:plan` revision and tables the four contingent artifacts; decision `[12]` records the contingency and rejects both hedged-scenario alternatives.
- **Resolved: [AMBIGUOUS_REQUIREMENT] Three passages gave three different answers on whether escalation unblocks the fix work.** One rule now appears identically at plan.md:204, task 1.3, and § Parallelization:273 — an ALLOWED read HALTS groups B–F, escalation alone does not unblock. "or the plan has escalated" is gone.
- **Resolved (interface): [REQUIREMENT_CONFLICT] The prescribed `build_side_store` signature could not satisfy the prescribed union redaction.** § Migration and task 4.2 both now read `build_side_store(&ScanSide, budget, all_secrets: &[String])`, task 4.1 computes the union once in `build_session_context`, and both places state the structural reason. The interface is now capable of union redaction; **nothing proves it happens** — raised as the Task Breakdown BLOCKER.
- **Resolved: [TRACEABILITY_GAP] The plan's central property had no falsifiable test.** Task 5.6 is present, `[expert]`, and sequenced after 5.1. Its substitution for the review's prescribed fix is justified and correct: `impl Display for AmazonS3` prints `AmazonS3({bucket})` and nothing else (`object_store-0.13.2/src/aws/mod.rs:88-92`, cited range exact), and `SpecSizedObjectStore`'s `Display` (`scan/object_store.rs:320-324`) wraps it, so two same-bucket stores are indistinguishable that way; the registry returns `Arc<dyn ObjectStore>` over a private `inner` with no `Any` supertrait, so no no-network alternative exists. The test falsifies the collapse: `side_store_url` (`scan/object_store.rs:411-420`) derives the key from the side's first FILE URI and never reads `storage.endpoint`, so two endpoint-divergent same-bucket sides share one registry key and `validate_sides_share_one_store` (`:444-475`) accepts them — a dimension store built from `common.storage` therefore sends the dimension read to the FACT listener and the assertion fails.

## Intent Fidelity

No objection — axis checked. Every element of the request is still present and none of the interviewed decisions is re-litigated: `JoinSpec::storage` from `dimension.effective_storage` (2.1, 2.3), per-side registration replacing `spec.common.storage` (4.1–4.3), the per-bucket prefix-routing decorator over one inner store per side (task 3), always-route with no credential comparison, error-on-unroutable, both comment rewrites, both guard reconciliations, and the replacement of `s3_sides_differing_in_credentials_are_accepted`. The explicit refusals the user demanded — no rejection of divergent joins, no N-scan fallback — hold in § Non-Goals and § Consequences. Round-1's SCOPE_REDUCTION advisory is discharged: decision `[13]` records the deferred live Databricks E2E with its rationale, its stand-in, and its residual risk, and § Non-Goals names Databricks explicitly. The Iceberg-citation rule is met, and the multi-bucket deviation is a named tracked exception (task 6.1, `#TRACKED-MULTI-BUCKET` placeholder) rather than a silent gap.

## Feasibility

#### [UNSTATED_ASSUMPTION] BLOCKER

- Location: plan.md § Iceberg table-spec compliance ¶1, evidence-table row 1 (line 50); `datafusion-scan/scan-execution-join/spec.md` Background bullet 5 (line 17)
- Issue: the fix for round-1 BLOCKER 1 introduces a false evidence claim into the one passage the user's conditional risk acceptance rests on. Row 1 certifies "**Zero** `head` per data file (production path, `logical_schema` non-empty) … Regression-tested by `tests/scan_no_head_test.rs` (`scan_uses_spec_size_and_issues_no_head`, `scan_issues_no_head_for_delete_files`)", and the delta records the same as "a property regression-tested by `tests/scan_no_head_test.rs`". Neither test covers that path, and the first asserts the opposite of row 1's "Routed by: n/a — no store call is made". The suite's only spec builder, `raw_spec` (`tests/scan_no_head_test.rs:261-272`), closes with `..Default::default()`, and `logical_schema` defaults empty — so all three tests run row 2's schema-inference branch. `scan_uses_spec_size_and_issues_no_head` (`:504`) asserts `discover_counts.forwarded_to_inner() >= 1` ("discovery scan must issue at least one HEAD to the inner store", `:527-530`) and `spec_counts.served_from_spec() >= 1` ("spec-size scan must answer the per-file HEAD from the spec size", `:549-553`). What the suite proves is that a head is answered from the size index instead of the network — not that no head is issued, and nothing at all about the production path. The production path is head-free by inspection (`raw_scan.rs:214` is the only `infer_schema` call site in `crates/`, and it is skipped), so no routing conclusion falls; but the plan certifies a guard that does not exist, and the delta merges that false attribution into the permanent library. Two smaller inaccuracies ride in the same table: row 3 cites `positional_deletes.rs:670` as a general footer range read, but it sits inside `if let Some(deletes) = … && !deletes.is_empty()` (`:667-669`), so it fires only for delete-carrying files; and `ensure_positional_delete` runs to `:247`, not `:246`.
- Fix: In `datafusion-scan/scan-execution-join/spec.md` Background bullet 5, delete the clause "a property regression-tested by `tests/scan_no_head_test.rs`". In plan.md § Iceberg table-spec compliance ¶1, move the three `scan_no_head_test.rs` test names out of row 1 into row 2's Evidence cell, and state there that the single schema-inference `head` is answered from that side's size index rather than reaching the network (`scan/object_store.rs:344-366`), which is why the `NotFound` trigger cannot fire for a spec-indexed file. Replace row 1's Evidence with code-inspection support only: `scan/positional_deletes.rs:663-665` via `object_meta_for` (`:215-225`), plus the statement that no test currently guards the production path's zero-head property. Correct row 3 to say `positional_deletes.rs:670` fetches the footer only for delete-carrying data files (guarded at `:667-669`), and change `:232-246` to `:232-247` in the Deletion Vectors row.

#### [EFFORT_MISESTIMATION] ADVISORY

- Location: plan.md § Implementation Tasks task 5.6 (lines 250-254)
- Issue: task 5.6 is feasible and correctly reasoned, but three unstated mechanics decide between a fast falsifiable gate and a multi-minute stall that a later engineer deletes. (a) The task says the listener "records the requests it receives" and never says it must answer. `object_store` 0.13.2 retries `HttpErrorKind::Connect | Request` (`client/retry.rs:433-443`) up to `max_retries: 10` inside a 180 s `retry_timeout` with backoff to a 15 s cap (`:252-259`, `client/backoff.rs:40-47`), so an accept-then-drop listener turns the second read into a retry storm; a 4xx is not retried (`:406-420`). (b) "issue one read" must be `get`, never `head`: `SpecSizedObjectStore::get_opts` short-circuits `head: true` for an indexed path with zero I/O (`scan/object_store.rs:344-366`), so a HEAD-based assertion passes vacuously against both endpoints. (c) `.with_endpoint` is applied only under `if storage.path_style` (`scan/object_store.rs:170-174`) and `allow_http` gates reqwest's `https_only` at request time — a dimension `StorageProps` that drops either silently targets `https://<bucket>.s3.<region>.amazonaws.com`, so neither listener is contacted and the test's meaning evaporates. The cited precedent is real but narrower than stated: `file_resolution.rs:1789-1830` is a single-shot **catalog** fake that answers full HTTP and records nothing, and `crates/lakehouse-engine/Cargo.toml:55-59` names it in the dev-dependency comment for `net`/`io-util`.
- Fix: In plan.md task 5.6, add three sentences. State that each loopback listener MUST accept in a loop and answer every request with `HTTP/1.1 404 Not Found` and `Content-Length: 0`, and MUST return the request line it saw (via the spawned task's join handle or an `mpsc`), because the `file_resolution.rs` fake records nothing and a dropped connection triggers `object_store`'s 10-attempt / 180 s connect retry. State that the two reads MUST be `get`, never `head`, naming the `SpecSizedObjectStore::get_opts` head short-circuit as the reason. State that both sides' `StorageProps` MUST keep `allow_http: true` and `path_style: true`, since `.with_endpoint` is applied only under `path_style`. Add that the reads are wrapped in `tokio::time::timeout` exactly as `shared_bucket_join_store_answers_both_sides_sizes_from_the_spec` (`scan/object_store.rs:975-1006`) already does.

Otherwise no objection — axis checked. Task 5.6's premise and falsification are verified above. Task 1.4's self-flagged unverified "6 rows" is **correct**: `ORDERDATE_LOWER_BOUND` is `2024-01-05` (`e2e_join_test.rs:56`), `O_ORDERDATE` is `BASE_DATE + (order_key - 1)` (`tests/common/seed.rs:1025-1026`), `FACT_ORDERS_ROWS` is 10 and `DIM_CUSTOMER_ROWS` 5, so keys 5..=10 qualify and every one matches a customer — the existing suite states the same conclusion inline at `e2e_join_test.rs:281-282`. Task 1.1's and 1.4's citations check out (`seed_star_schema` at `seed.rs:1053`, `build_seed_catalog` `:277`, `build_seed_catalog_with_auth` `:291`, `join_query` at `e2e_join_test.rs:118`, `expected_join_rows_with_fact_where` at `:207`, `e2e_broadcast_join_result_correct` at `:274`). One latent enumeration omission is immaterial: `url.rs:418` is a second `store.list` site in `list_with_cache`'s `Some(cache)` arm, keyed by `table_base_path`, which equals `full_prefix` when `prefix` is `None`; no code in `crates/` configures a list-files cache, so the arm is unreachable and its routing conclusion identical.

## Requirement Quality

No objection — axis checked. Round-1's line-citation advisory is discharged, and both of the reviser's counter-corrections are right: `sql_builders.rs:665` is the `///` separator and `:666-670` is the withdrawn storage paragraph; `join_scan.rs:81-84` is the storage-backend paragraph, `:85` the `//` separator, `:86-89` the semaphore paragraph to keep verbatim. Also verified exact: the `JoinSpec` construction expression at `sql_builders.rs:683-690` with `build_broadcast_join_sql` at `:671` and its doc from `:656`; `scan/mod.rs:223-225` for the injected-`build_session` quotation; `datafusion-execution-54.1.0/src/object_store.rs:212` for the pre-registered `LocalFileSystem`; `object_store.rs:139-143` for the `host_str()` requirement; `planning.rs:920` for the ADLS test. `speq feature validate` reports 0 errors library-wide. Every DELTA scenario across all seven deltas has a § Verification § Scenario Coverage row, and no delta contradicts another or the recorded library.

## Task Breakdown

#### [TRACEABILITY_GAP] BLOCKER

- Location: plan.md § Verification § Scenario Coverage (five rows), § Test Disposition, § Implementation Tasks group 5
- Issue: five of the fourteen tests § Verification names exist in no task, and the most important of them is the only proof of round-1 BLOCKER 4's requirement. Two deltas make union redaction normative — `scan-execution-join`'s "MUST NOT contain a storage access key, secret key, session token, or SAS token from EITHER side's storage backend" and `scan-execution-memory-and-credentials`' "the redaction set for such a message is every side's `secret_values()`, not the fact side's alone". Their mapped test, `unreadable_join_file_error_redacts_both_sides_credentials`, does not exist and no task creates it. The nearest existing test is `join_unreadable_file_errors_without_secrets` (`tests/scan_join_test.rs:477`), which builds both sides from one `storage()` helper (`:166`) and asserts a single literal — `!text.contains("TOPSECRETVALUE")` (`:508-511`) — so it stays green under fact-only redaction. BLOCKER 4 therefore made union redaction possible without making it falsifiable, which is the same defect shape as round-1 BLOCKER 5. Four more rows have no producing task: `join_registers_each_side_against_its_own_backend` (nearest existing is `join_executes_inner_equi`, `:283`); `each_side_store_gets_the_full_connection_budget`, mapped to the connection-concurrency delta's new "SHALL apply that same budget of N to EACH object store" clause, while task 5.1's enumerated assertions omit the budget and the existing `build_s3_store_applies_spec_connection_budget` (`scan/object_store.rs:682`) is whole-spec; `broadcast_carries_each_sides_own_storage`, where task 5.4 only says to apply § Test Disposition's `sql_builders.rs` rows and those name just the three goldens; and `adls_sides_on_two_accounts_are_rejected`, which contradicts § Test Disposition's KEEP row for the existing `adls_sides_on_different_storage_accounts_are_rejected` (`planning.rs:920`).
- Fix: In plan.md, add a sub-task to group 5 that renames and extends `tests/scan_join_test.rs::join_unreadable_file_errors_without_secrets` (`:477`) into `unreadable_join_file_error_redacts_both_sides_credentials`: give the two sides DISTINCT secret values in `join_spec` (`:180`) instead of the shared `storage()` helper (`:166`), and assert BOTH secret literals are absent from the message; add a matching REPLACE row to § Test Disposition. Name the task that produces `join_registers_each_side_against_its_own_backend` and add its RESTATE row to § Test Disposition. Add `each_side_store_gets_the_full_connection_budget` to task 5.1's assertion list, asserting each side's inner store receives the full budget N. Add `broadcast_carries_each_sides_own_storage` explicitly to task 5.4. Reconcile the ADLS name: either restore `adls_sides_on_different_storage_accounts_are_rejected` in § Verification § Scenario Coverage, or change § Test Disposition's row from KEEP to RENAME.

Round-1's Group D/E granularity advisory stays open by the reviser's declared choice and is not re-raised; Group E's parenthetical is now honest, and Group D's "4.3, 4.4 independent of both" remains false because 4.4 edits `scan/object_store.rs` alongside 4.1 and 4.2. Task 5.6's sequencing is sound: Group E follows Groups B, C, D, and 5.6 follows 5.1 in the same test module.

## Design Depth

#### [INFORMATION_LEAKAGE] ADVISORY

- Location: plan.md § Implementation Tasks task 3.1 (line 228); § Migration (`side_size_index` row, line 175)
- Issue: round-1 raised this advisory; the revision neither applied it nor recorded it among the declared deferrals, so it is silently open. "The `object_store::Path` DataFusion will request for this file entry" remains one decision with three homes. `index_file_sizes` derives it as `reconstruct_abs_uri` → `ListingTableUrl::parse` → `.prefix().clone()` (`scan/object_store.rs:283-295`), `object_meta_for` re-derives it identically (`scan/positional_deletes.rs:215-225`), and task 3.1 adds a third in `store_router.rs`. Nothing enforces agreement, and the invariant is brittle in a way that now converts a mismatch into a hard error rather than a wrong size: `Path::from(&str)` percent-encodes while `ListingTableUrl::parse().prefix()` percent-decodes, so a router keyed the other way false-errors on any table with encoded partition values. Round-1's secondary objection is weaker than stated and does not need action — `index_file_sizes` has no `entry.deletes` loop, but the derivation it applies is `reconstruct_abs_uri`-based and `DeleteFileRef.path` is documented as relative-to-`table_root` "exactly like `FileEntry::path`" (`scan/spec.rs:520-523`), so applying that derivation with the side's own root is correct for delete files.
- Fix: In plan.md, add a task exporting one `pub(crate) fn requested_path(entry_path: &str, table_root: &str) -> Result<ObjectStorePath, UdfError>` from `scan/object_store.rs` that performs `reconstruct_abs_uri` then `ListingTableUrl::parse(..).prefix().clone()`, and restate task 3.1, the § Migration `side_size_index` row, and `scan/positional_deletes.rs`'s `object_meta_for` to call it for data files and delete files alike. If the plan instead accepts the duplication, record that as a decision-log entry naming the percent-encoding asymmetry as the accepted risk, so the deferral is explicit rather than silent.

#### [TACTICAL_SHORTCUT] ADVISORY

- Location: plan.md § Iceberg table-spec compliance ¶1 (line 58); § Patterns ("Exact-membership first, prefix second", line 104); decision-log.md § [2] "Rule 2's reachability"
- Issue: the revision resolves round-1's completeness-vs-fallback contradiction honestly, but the surviving justification for keeping rule 2 does not hold. All three passages say rule 2's one covered shape is "a `files` entry whose path ends in `/`, which makes `is_collection()` list a directory", and plan.md:58 keeps rule 2 "because it converts that one malformed-spec shape into a served read rather than a hard error". That shape routes by rule 1, not rule 2. Task 3.1 derives each side's owned path set through the same `ListingTableUrl::parse(..).prefix()` conversion that produces `full_prefix`, so a trailing-slash entry lands in the owned set as the same `Path` the listing requests — an exact match. Rule 2 is therefore unreachable in every shape, malformed included, and is retained purely because the interview prescribed longest-root-prefix matching. That prescription settles the decision; the stated reason does not survive it.
- Fix: In plan.md § Iceberg table-spec compliance ¶1 and § Patterns, and in decision-log.md § [2]'s "Rule 2's reachability" paragraph, delete the claim that rule 2 covers a trailing-slash `files` entry and the "converts that one malformed-spec shape into a served read" justification. Replace both with the single accurate statement: rule 2 is unreachable through the current scan for every spec shape, because task 3.1 derives each side's owned path set through the same `ListingTableUrl` prefix conversion the listing path uses, and it is retained solely because the interview prescribed longest-root-prefix matching.

Otherwise no objection — axis checked. Decision `[3]`'s router-outside/sized-store-inside layering, decision `[5]`'s ADLS-container reasoning, and decision `[6]`'s narrowed guard all still verify against the tree. Task 3.2's ten methods remain exactly `object_store` 0.13.2's trait surface. Construction stays at the session entry point, no cycle is added, and the VS layer gains only a field assignment, so the thin-VS boundary holds. Task 5.6's placement in `object_store.rs` is forced rather than chosen: `build_session_context` is `pub(super)` (`:35-38`), so no integration test can reach it.

## Prose Quality

#### [PROSE_BLOAT] ADVISORY

- Location: plan.md § Non-Goals (line 29)
- Issue: round-1 flagged this passage as a ~95-word semicolon chain carrying six exclusions and the reviser deferred the reformatting — but the A3 fix appended a seventh exclusion with its own nested explanation to the same chain, so one sentence now runs past 110 words and carries seven independent exclusions plus a rationale, a motivation, a CI constraint, a stand-in, and a cross-reference. That is the deferred finding made materially worse by an unrelated fix, not merely left alone. The Databricks deferral is the one exclusion a reviewer is most likely to need, and it is buried at the end of the longest sentence in the document.
- Fix: Convert plan.md § Non-Goals to a bulleted list, one exclusion per bullet, with the Databricks E2E deferral as its own bullet whose rationale is a single sentence pointing at decision-log `[13]`.

Otherwise no objection — axis checked. Round-1's § Impact and decision-log `[2]` length findings stay open by the reviser's declared choice and are not re-raised. The new prose — task 1.3's contingency table, decision `[12]`, decision `[13]`, and the five `[plan-review]` findings — is BLUF-compliant, evidence-dense, and states its direction change before its rationale.
