# Open Questions: fix-connection-credential-exposure

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan fix-connection-credential-exposure` locally, or re-run `/speq:plan-pr fix-connection-credential-exposure` after commenting.

Round 2 review found 7 BLOCKERs (0 Intent-Fidelity) after round 1's 8 were fixed. Full detail: `review/round-2.md`.

- [ ] **Does the leak actually reach `EXA_USER_PROFILE_LAST_DAY.SQL_TEXT`?** The plan's own task 1.2 says this is still open, but § Summary and the new `scan-spec-credential-reference` spec already assert it as fact. The live probe only confirmed the profiling row is readable — not that it holds the VS-rewritten SQL (which carries the credential) rather than the user's original `SELECT` text. If it's the latter, the profiling-leak claim is false and the E2E positive control (round-2 finding, § Feasibility) passes on a vulnerable build too. Needs one more live probe before the claim can ship.
- [ ] **Where does task 1.3b's verification gate actually run?** § Dependencies, § Parallelization, and § Manual Testing disagree — as placed, it has no least-privilege user, no grant, and no test binary yet (they're provisioned later in the same group). Pick: run it self-contained at the head of Group E (provision inline via `exapump`), or move it after the tasks that provision its prerequisites.
- [ ] **`StorageCreds` field visibility** — three artifacts require mutually exclusive things: `connection-credentials` spec says one reader crosses the crate boundary (needs `pub` fields or accessors), task 2.1 mirrors a private-fields-plus-accessors pattern, and the named verification test asserts the fields are *not* public. Pick one shape (round-2 recommends `pub` fields, matching `StorageProps`) and propagate it.
- [ ] **How do the 11 sibling spec deltas actually get merged?** They correct `## Background` bullets and descriptions, but `/speq:spec-merge` only has actions for `## Scenarios` (NEW/CHANGED/REMOVED) — a trial merge left every falsified recorded bullet standing verbatim beside a contradicting new scenario. Needs either a `## Recorder Notes` section with exact strike/replace text per feature, or moving the corrections into `DELTA:CHANGED` scenarios.
- [ ] **8 more recorded locations across 6 features still assert the pre-fix credential guarantees**, uncovered by any delta (`pushdown-catalog-session`, `pushdown-module-structure`, `pushdown-planning-selectlist-expressions`, `pushdown-planning-single-group-agg-scalar-over-aggregate`, `pushdown-module-dedup-consolidation`, `storage-backend-enum`, `catalog-crate-structure`). Full list and exact fix in round-2.md's Requirement Quality section.
- [ ] **3 `DELTA:CHANGED` reproductions silently drop or weaken recorded clauses** they replace wholesale (`storage-backend-enum`'s golden-fixture-stability clause, `pushdown-planning-cloud-credentials`'s addressing-supersession clause, and a "no change to any assertion" clause in two other features that the fixture regeneration now falsifies). These need the dropped text restored with the actual change carved out, not silently deleted.

Advisory findings (not blocking, but worth a look before merge): unpatched `docs/install.md`/`docs/catalogs.md` connection recipes still omit the new grant statement; join-side storage-reference dedup is unspecified; two features will cross the 10-scenario reorg threshold on record with no plan-side heads-up; several sibling deltas quote recorded text that doesn't match byte-for-byte. Full list in round-2.md.
