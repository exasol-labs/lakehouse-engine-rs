# Open Questions: fix-connection-credential-exposure

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan fix-connection-credential-exposure` locally, or re-run `/speq:plan-pr fix-connection-credential-exposure` after commenting.

Round 2 review found 7 BLOCKERs (0 Intent-Fidelity) after round 1's 8 were fixed. Full detail: `review/round-2.md`.

- [x] **Does the leak actually reach `EXA_USER_PROFILE_LAST_DAY.SQL_TEXT`?** RESOLVED, live probe against the Docker stack, re-run in round 3 with a genuinely least-privilege user: **no.** `EXA_USER_PROFILE_LAST_DAY` — the user's OWN-session view, read BY that user with no `ACCESS ANY CONNECTION` and no `SELECT ANY DICTIONARY` — returned 9 rows carrying only the user's own literal statement, never the VS-rewritten pushdown SQL, while the same query's `EXPLAIN VIRTUAL` output carried the credential in plain sight. `EXA_DBA_AUDIT_SQL` recorded the same negative result but is NOT least-privilege-readable at all: that user was refused it with SQL state `42500`, since it is gated behind `SELECT ANY DICTIONARY`. Plan claims corrected accordingly (task 1.2, § Summary, § Design, § Impact, § Sealed vended envelope, task 7.3, task 7.4, § Manual Testing) and the profiling-based E2E positive control removed rather than kept — see decision-log entry [16] and the round-3 `[plan-review]` entry that corrects its view names. `EXPLAIN VIRTUAL` and pushdown-path errors remain the real, unaffected leak surfaces.
- [x] **Where does task 1.3b's verification gate actually run?** RESOLVED: self-contained at the head of Group E — 1.3b now provisions its own throwaway user and grant inline via `exapump` (mirroring 1.3a's style), rather than depending on task 7.2's shared harness grant or task 7.3's test binary, neither of which exists yet at that point in the sequence. § Dependencies, § Parallelization, task 1.3b's own text, and § Manual Testing all updated to agree.
- [x] **`StorageCreds` field visibility** — RESOLVED: the nine fields are `pub`, mirroring `StorageProps`; see plan.md § Key interfaces (the `StorageCreds` row), task 2.1, and decision-log entry [15] — the `storage_creds_fields_are_not_public` test is replaced by `storage_creds_declares_exactly_the_nine_pub_storage_fields`.
- [x] **How do the 11 sibling spec deltas actually get merged?** RESOLVED as a scheduled task: plan.md task **8.1** (Group F, the last group `/speq:implement` runs) writes `recorder-checklist.md` with exact strike/replace text in two named sets — nine non-sibling locations and the ten sibling recorded bullets — converts the 2 scenario-level miscategorized cases (`pushdown-agg-sql-consolidation:33`,`:46`) to `DELTA:CHANGED`, and fixes the 10 divergent sibling-delta titles. **Round 3 correction:** the earlier claim that `speq-implement-pr`'s pipeline runs and confirms 8.1 before `/speq:record` was FALSE — that pipeline has no such step. **Round 4 correction:** the replacement claim, that a single `grep` makes the gate self-enforcing regardless of whether the checklist runs, was also false — nothing invokes it after `/speq:record`, its exit-code convention was inverted, and it covered 12 of the 19 sentences task 8.1 strikes. The gate is now § Post-Record Verification: a MANUAL sweep, one check per struck sentence, run by the operator or the orchestrating session after `/speq:record` returns and before the PR is marked ready — it depends on someone actually running it, and that dependency is now stated rather than claimed away.
- [x] **8 more recorded locations across 6 features still assert the pre-fix credential guarantees**, uncovered by any delta. RESOLVED as a scheduled task: plan.md task **8.2** (Group F) authors the missing/extended deltas round-2 specifies, re-verified against the current library state at implementation time, and updates § Features' closing count.
- [x] **3 `DELTA:CHANGED` reproductions silently drop or weaken recorded clauses** they replace wholesale. RESOLVED IN PLANNING, not deferred: round 3 found that deferring the repair to Group F left the plan's own requirement artifacts forbidding, for the whole of Groups C–E, the fixture regeneration Group C performs. All four restorations are now applied to the delta files themselves (`storage-backend-enum`, `pushdown-planning-cloud-credentials`, `pushdown-format-neutral-resolution`, `scan-module-structure`); task **8.3** is reduced to a read-and-confirm pass that gates nothing.

Advisory findings (not blocking, but worth a look before merge): unpatched `docs/install.md`/`docs/catalogs.md` connection recipes still omit the new grant statement; join-side storage-reference dedup is unspecified; two features will cross the 10-scenario reorg threshold on record with no plan-side heads-up; several sibling deltas quote recorded text that doesn't match byte-for-byte. Full list in round-2.md.
