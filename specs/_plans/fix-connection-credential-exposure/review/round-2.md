# Plan Review Findings: fix-connection-credential-exposure (round 2)

## Summary
- Axes checked: 6/6
- Total findings: 17 (Blockers: 7, Advisory: 10)
- Intent Fidelity blockers: 0

## Round-1 Blocker Recheck

- **Partly resolved: [HIDDEN_DEPENDENCY] Task 1.3's gate was circular and unexecutable.** 1.3a is split out, was run live (Lua `CONN_PROBE`, script-scoped grant, runtime VARCHAR name, revoke → `22001`), sits in § Parallelization Group A, and § Manual Testing row 3 now names the probe commands rather than a test binary. The revert rule is stated (§ Dependencies: "A 1.3b failure REVERTS tasks 3.x and 4.x"). The residual gate 1.3b, however, is placed where it cannot run and pointed at a binary that does not exist yet — see the Feasibility HIDDEN_DEPENDENCY on task 1.3b.
- **Resolved: [NFR_IGNORED] Catalog-auth secrets would have materialized inside the scan UDF.** The scan side now derives from a nine-field `StorageCreds` projection and never constructs a `ConnectionCreds`; verified independently that `storage_block` (`crates/lakehouse-engine/src/adapter/connection.rs:360-382`) reads exactly those nine fields plus the `allow_http` parameter and no `use_sigv4`/`use_vended_credentials`, so the projection is sufficient and the "field-for-field EQUAL" claim holds. `vs-adapter/connection-credentials-catalog-auth` gains a `DELTA:NEW` scenario with a source-level probe clause; the false citation is gone from `vs-adapter/scan-spec-credential-reference`. Residual error-path gap in the Requirement Quality COMPLETENESS_GAP on the unparsable CONNECTION password.
- **Partly resolved: [UNSTATED_ASSUMPTION] Every absence assertion could pass on an empty surface.** Positive controls are now normative in the right places (`e2e-harness/e2e-harness` § Scenario "A least-privilege user…" clauses 45-46, task 6.1's name-present assertion, task 3.4's `"connection":{"name":` assertion) and the profiling view's determinism was settled live with `FLUSH STATISTICS`. A second vacuity vector the positive control cannot catch remains — see the Feasibility UNSTATED_ASSUMPTION on the profiling surface.
- **Resolved: [REQUIREMENT_CONFLICT] Moving parse_creds/storage_block reversed a recorded prohibition.** Nothing moves. decision-log `[12]` quotes `catalog-crate-structure:68` verbatim (checked against the recorded file — exact match) and supersedes `[7]` on the move half only; `[7]` carries the matching "SUPERSEDED IN PART" note. The `pub`-set widening is recorded in `vs-adapter/catalog-crate-public-surface-extensions`, which the recorded feature establishes as the mechanism that supersedes the "exactly these items SHALL be `pub`" enumeration (five prior extensions did the same). Resolving this did resolve blocker 2 with the same change. The chosen shape introduces one new conflict — see the Requirement Quality REQUIREMENT_CONFLICT on `StorageCreds` field visibility.
- **Not resolved: [REQUIREMENT_CONFLICT] The delta set was far too narrow for what the change falsifies.** § Features grew 6 → 35 and every round-1-named clause is now touched, but the corrections that carry this blocker's fix are written into delta `## Background` bullets and delta descriptions, which the merge procedure has no action for — see the Requirement Quality REQUIREMENT_CONFLICT on Background-only supersession. Eight further recorded locations across six features are still untouched — see the REQUIREMENT_CONFLICT on the incomplete sweep. Three `DELTA:CHANGED` reproductions silently delete or self-falsify recorded clauses — see the REQUIREMENT_CONFLICT on the reproductions.
- **Resolved: [COMPLETENESS_GAP] Three of the five redaction feed sites were unaccounted for — in fact seven.** All seven line references verified against the tree (`object_store.rs:66`, `join_scan.rs:48`, `partial_agg.rs:70`, `partial_agg.rs:125`, `raw_scan.rs:54`, `raw_scan.rs:224`, `positional_deletes.rs:630`, the last cited as `:629`). The no-accessor rule is normative in three deltas (`scan-spec-credential-reference` § Scenario "Error redaction…" clause 2, `storage-backend-enum` clause 30, `scan-execution-memory-and-credentials` clause 47), task 4.3 names all seven, task 4.5 adds the raw-scan and partial-aggregate redaction tests, and § Verification carries rows for both. The compile-time claim is specified, not merely asserted; the probe's stated home is wrong — see the Task Breakdown AMBIGUOUS_REQUIREMENT on task 6.2.
- **Resolved: [TRACEABILITY_GAP] Task 3.3 pointed at a file that does not exist and missed both join sides.** Every seam verified real: `pushdown/mod.rs:249` (`let storage = &effective_storage;`), `:349` (`storage: &StorageBackend`), `:379` (`storage: storage.clone()`), `joins/sql_builders.rs:603` (fact), `:762` (dimension), `joins/planning.rs:219` (`effective_storage`). Those are the only three spec-storage population sites in `adapter/pushdown/`. The readers' exemption is correct for the stated reason — both return `storage.clone()` on the static path (`format/iceberg.rs:102-110`, `format/delta_format_reader.rs:92-95`), so the UDF's re-derivation reproduces `conn.storage` exactly. Split into 3.3a/3.3b. Unstated signature work in the Feasibility EFFORT_MISESTIMATION on task 3.3b.
- **Resolved: [TASK_GRANULARITY] The headline guarantee test asserted on its own fixture.** `scan_storage_for` is named in § Key interfaces, required by `scan-spec-credential-reference` § Scenario "One pure function selects the wire variant for every builder path", and task 6.1 drives every builder path through it under both settings of `use_vended_credentials` with a positive control. The derived path list matches the tree (`RequestShape` has exactly `Grouped`, `GroupByWrapper`, `SingleGroupAgg`, `RowScan`), and task 3.4's eighteen fixture names match the eighteen credential-bearing files one-for-one (24 files, 18 with `access_key`, 6 `empty_*`). One internal contradiction in 3.4 — see the Task Breakdown AMBIGUOUS_REQUIREMENT on task 3.4.

## Intent Fidelity

[no objection — axis checked: the Summary now scopes the closure ("Closes issue #135 for CONNECTION-supplied standing credentials"), task 1.5 establishes whether the reporting deployment had vending enabled, and #378 is cited in the spec that owns the residual; the second deliverable is present and traceable in three places — plan.md § Rotation Runbook (five rows, including the `client_secret`-specific row), task 7.4's `docs/security.md` § Rotation, and decision-log `[10]` — with `ALTER CONNECTION` / `EXA_DBA_CONNECTIONS` / `CREATE OR REPLACE` all recorded as live-verified; no decision from the headless framing is re-litigated. One factual claim inside the Summary is challenged on the Feasibility axis, not here — it concerns evidence, not scope]

## Feasibility

#### [HIDDEN_DEPENDENCY] BLOCKER
- Location: plan.md § Dependencies "Ordering" bullet 2; § Parallelization Group E and its intra-group order; § Manual Testing row "Gate 1.3b"; task 1.3b
- Issue: the residual gate cannot execute where the plan puts it, and the round-1 defect is displaced rather than removed. Three statements disagree. § Dependencies says 1.3b "sits at the head of Group E, immediately before task 7.3". § Parallelization says "Within Group E: 1.3b FIRST, alone", with `7.2 → 7.3` afterwards. § Manual Testing gives 1.3b exactly one vehicle: `cargo test --features exasol-e2e --test e2e_credential_exposure_test` — the binary task 7.3 creates later in the same group, and it needs the least-privilege user and the script-scoped grant that task 7.2 provisions, also later. So at its stated position 1.3b has no user, no grant, and no binary. This is the same failure round-1 named ("a gate mapped to a binary that does not exist at gate time"), moved from 1.3a to 1.3b, and it governs a revert of two whole task groups.
- Fix: In plan.md, choose one and make all three sections agree. Either (a) give 1.3b a self-contained vehicle at the head of Group E — state the provisioning explicitly (create the least-privilege user, issue `GRANT ACCESS ON CONNECTION <c> FOR SCRIPT <schema>.LAKEHOUSE_SCAN`, run one existing `exasol-e2e` binary's query as that user via `exapump`, then revoke) and replace the § Manual Testing "Gate 1.3b" command with those statements; or (b) move 1.3b to run after 7.2 and 7.3, rewrite § Dependencies bullet 2 and § Parallelization's intra-group order to `7.2 → 7.3 → 1.3b`, and state that the revert rule still applies at that point. Do not leave "FIRST, alone" beside a command that only exists after 7.3.

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: plan.md § Summary; § Design "Is `EXPLAIN VIRTUAL` the only surface?" row; § Impact "Fixed" paragraph; task 1.2; `vs-adapter/scan-spec-credential-reference/spec.md:3` and § Background bullet 3; `e2e-harness/e2e-harness/spec.md` § Scenario "A least-privilege user…" clause 46
- Issue: the plan asserts a leak surface it has not established, and the E2E assertion built on it is vacuous-by-construction in a way the new positive control cannot detect. § Summary states a `SELECT`-only user "can today read a standing `access_key` and `secret_key` … out of `EXA_USER_PROFILE_LAST_DAY.SQL_TEXT`". What the live probe actually established is different: § Design records that the user "read its own profiled statement's full `SQL_TEXT`" — readability of the profiling row, not that the row's `SQL_TEXT` holds the VS-REWRITTEN pushdown SQL that carries the credential. The plan itself leaves the question open, in task 1.2: "Record whether the REWRITTEN statement appears as its own row or only the user's original `SELECT`, because that scopes what the fix can claim." If the answer is "only the user's original `SELECT`", then (i) § Summary and `scan-spec-credential-reference:3` — a NEW feature, so this sentence lands in the permanent library — assert a leak that never existed, and (ii) clause 46's assertion is green against the vulnerable build too: the positive control matches rows by the comment the USER's statement carries, which is precisely the text that structurally cannot contain the credential. That is the round-1 "green from day one" defect reappearing one column over.
- Fix: Settle the question in plan.md before Group A and thread the answer through. State, in § Design's surface table, whether the VS-rewritten statement text appears in any `EXA_USER_PROFILE_LAST_DAY` or `EXA_DBA_AUDIT_SQL` row readable by the least-privilege user, with the probe output as evidence. If it does NOT: delete `EXA_USER_PROFILE_LAST_DAY.SQL_TEXT` and `EXA_DBA_AUDIT_SQL.SQL_TEXT` from § Summary, § Impact "Fixed", `scan-spec-credential-reference:3` and its § Background bullet 3, delete clause 102's profiling limb and `e2e-harness` clause 46, and record the negative result in `docs/security.md` (task 7.4) so the surface enumeration stays honest. If it DOES: change `e2e-harness` clause 46 and task 7.3 so the positive control matches on a literal that appears in the REWRITTEN text — the connection name or the `LAKEHOUSE_SCAN` call — and not on the user's own comment, so the absence assertion is scoped to rows that can carry a credential.

#### [HIDDEN_DEPENDENCY] BLOCKER
- Location: plan.md § Key interfaces rows 6-7 (`ResolvedScanStorage`, `resolve_scan_storage`, both `pub(crate)`); task 4.2; task 4.3; § Test Disposition row "The four threaded signatures"
- Issue: the resolved pair is threaded through two `pub` entry points that twelve external test binaries call, and it is declared `pub(crate)` with `resolve_scan_storage` as its ONLY constructor — so the threading cannot compile and the callers cannot supply the argument. `run_raw_scan_with_session` (`crates/lakehouse-engine/src/scan/raw_scan.rs:48`, `pub`, secret set read at `:54`) and `run_join_scan_with_session` (`scan/join_scan.rs:42`, `pub`, union read at `:48`) are both named by task 4.2/4.3 and are both part of the crate's external facade: `tests/scan_two_arg.rs`, `scan_telemetry.rs`, `scan_join_test.rs`, `scan_column_binding.rs`, `scan_partition_values.rs`, `scan_footer_refetch_observable.rs`, `scan_positional_deletes.rs`, `scan_deletion_vectors.rs`, `scan_batch_loop.rs`, `scan_no_head_test.rs`, `scan_name_mapping.rs`, and `scan_facade_reachability.rs` reach them through `use lakehouse_engine::scan::…`. A `pub(crate)` parameter type in a `pub` signature also trips `private_interfaces`, and § Checklist requires `cargo clippy --all-targets` at zero warnings. § Test Disposition calls these signatures MECHANICAL and notices only the name pin in `scan_surface_probe_tests.rs:22`.
- Fix: In plan.md § Key interfaces, decide the visibility and state it. Either declare `ResolvedScanStorage` `pub` with a second `pub` constructor for the inline case and replace "the ONLY constructor" with "the only constructor that reads a CONNECTION", or keep the type `pub(crate)` and give the two external-facade entries a `pub` parameter shape instead — `run_raw_scan_with_session(…, storage: &StorageBackend, …)` and `run_join_scan_with_session(…, fact: &StorageBackend, dimension: Option<&StorageBackend>, …)`, both types already `pub`. Name the chosen shape in task 4.2 and task 4.3, and add a § Test Disposition row for the twelve `tests/scan_*.rs` binaries whose call sites gain the new argument.

#### [EFFORT_MISESTIMATION] ADVISORY
- Location: plan.md task 3.3b; § Test Disposition
- Issue: 3.3b's one line hides a signature change through the join builder chain that no Test Disposition row covers. `scan_storage_for` takes a `&ConnectionCreds` and a connection name, and neither join builder has either: `join_fan_out_scan_spec(primary: &ResolvedJoinSide, projection, filter, emit_exa_types, join, tuning)` (`joins/sql_builders.rs:580-587`) and `build_broadcast_join_sql(sides: &JoinSides, rendered, window, tuning, udf_name, distribute_udf_name)` (`:716-723`) both receive only sides plus tuning. The task says the choice comes "from its own `ResolvedJoinSide::effective_storage` plus the one resolved connection config both sides share" without naming how that config reaches either function, which callers change, or whether it rides on `JoinScanTuning`/`JoinSides`.
- Fix: In plan.md task 3.3b, name the carrier explicitly (add the connection name plus `use_vended_credentials` to `JoinScanTuning`, or add one parameter to both functions) and list the call sites that change. Add a § Test Disposition row for `joins/sql_builders_tests.rs` and the join golden path covering those signature edits.

#### [HIDDEN_DEPENDENCY] ADVISORY
- Location: plan.md task 7.1, task 7.4, § Impact "Breaking" paragraph 1; `docs/install.md:170-185`; `docs/catalogs.md` (8 `CREATE OR REPLACE CONNECTION` recipes)
- Issue: the grant is threaded into the installer template, the harness, § Impact and the runbook, but not into the two documents an operator actually copies from — and § Impact presents the coverage as complete. `docs/install.md:170` reads "Two statements finish the install: a catalog `CONNECTION`, then the Virtual Schema over it", followed by a `CREATE OR REPLACE CONNECTION` block; `docs/catalogs.md` carries eight more (lines 67, 93, 119, 131, 166, 197, 237, 261). After this change that walkthrough yields a deployment that plans and then fails at scan time, and no task edits either file beyond task 7.4's "cross-reference it". § Impact also states the grant is needed by "every user or role that queries a virtual schema", while the plan elsewhere relies on the E2E suite continuing to work as `sys` without one.
- Fix: In plan.md task 7.4, require `docs/install.md` § "Point the VS at your data" to become three statements — the CONNECTION, the `GRANT ACCESS ON CONNECTION <c> FOR SCRIPT <schema>.LAKEHOUSE_SCAN TO <user-or-role>`, and the Virtual Schema — with the `CREATE OR REPLACE` warning beside it, and require the same grant line plus warning in each of `docs/catalogs.md`'s eight recipes. In § Impact "Breaking" paragraph 1, add that a user holding `ACCESS ANY CONNECTION` (a DBA such as `sys`) needs no per-script grant, so the requirement binds every NON-DBA grantee.

#### [UNSTATED_ASSUMPTION] ADVISORY
- Location: plan.md task 7.2; `e2e-harness/e2e-harness/spec.md` § Scenario "Every E2E binary provisions the scan path…" clause 33
- Issue: the ordering instruction names a SQL builder rather than the execution site, misses a second connection-creating site, and the clause's stated purpose does not follow. `tests/common/stack.rs:370-384` is `build_create_connection_sql`, a builder; the statement is executed at `tests/common/e2e_harness.rs:267-268` inside `create_virtual_schema_with_password`, which its own doc comment describes as re-issuing the connection ("harmless to repeat") and which runs once per binary and once per Virtual Schema. `cloud_e2e_test.rs:326` creates a second CONNECTION outside the shared definition. Separately, clause 33 justifies the grant as making it so "no binary provisions the grant … only because its caller is a DBA" — but every binary still connects as `sys`, so issuing the grant changes nothing about what those binaries prove; only the new least-privilege binary exercises it.
- Fix: In plan.md task 7.2, name `tests/common/e2e_harness.rs:267-268` as the site — issue the grant immediately after each `conn.execute(&create_conn_sql)`, so every re-issue is followed by a fresh grant — and add `cloud_e2e_test.rs:326`'s own connection to the same rule. In `e2e-harness/e2e-harness` clause 33, replace the rationale with the true one: the grant must survive every connection re-issue, and the suite's own binaries connect as `sys`, so the grant's necessity is proven by the least-privilege binary alone.

## Requirement Quality

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: plan.md task 2.2 and § Key interfaces row 4; `vs-adapter/connection-credentials/spec.md` § Scenario "One storage-credential projection and one selector serve both readers" clause 3; plan.md § Verification row "catalog-crate-public-surface-extensions" (test `storage_creds_fields_are_not_public`)
- Issue: three artifacts of this plan require mutually unsatisfiable things about `StorageCreds`' field visibility. `connection-credentials` clause 3 is normative: "the adapter's own CONNECTION-to-backend entry point SHALL reach that selector through the projection … and the nine storage field spellings SHALL have exactly ONE reader". Task 2.2 operationalizes it: "Repoint `parse_creds` to read its nine storage fields through `StorageCreds::from_json`". `parse_creds` lives in `lakehouse-engine` and constructs a 17-field `ConnectionCreds` literal, so it must read those nine values across the crate boundary — which needs `pub` fields or nine accessors. But § Verification names the test `storage_creds_fields_are_not_public`, task 2.1 says to mirror `StaticStoreAddress` (private fields plus two `pub fn` accessors, `storage.rs:271-286`), and `catalog-crate-public-surface-extensions` clause 1 admits to the public surface "exactly ONE type … plus its deserialization … and its ONE backend selector" — no accessors. An implementer must break one of the three: duplicate the nine spellings in `parse_creds` (losing the one-reader property decision `[12]` rests on), add nine unenumerated accessors (failing the probe that names the `pub` set), or publish the fields (failing the named test).
- Fix: Decide it in plan.md § Key interfaces row 4 and propagate. Preferred: declare `StorageCreds`' nine fields `pub`, mirroring `StorageProps` (`crates/lakehouse-catalog/src/creds.rs:181-195`), which already carries credentials with public fields; delete the § Verification test name `storage_creds_fields_are_not_public`; and state in task 5.1 that the redacting manual `Debug` is what guards the payload. Otherwise name the exact reachability the projection publishes (nine accessors, or one `pub fn` declared beside both types in `lakehouse-catalog` that fills a `ConnectionCreds`' storage half), add it to task 2.3's enumerated `pub` set, and to `catalog-crate-public-surface-extensions` clause 1.

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: plan.md § Features and § Consequences last row; all 34 existing-feature deltas' `## Background` sections and descriptions; conflicts with the merge procedure in `/speq:spec-merge` and `references/delta-template.md`
- Issue: the corrections that carry round-1 blocker 5's fix are written where the merge has no action for them, so recording this plan leaves the false sentences standing with a contradicting scenario appended beside them. `/speq:spec-merge` defines exactly three actions, all on scenarios — `DELTA:NEW` appends a scenario, `DELTA:CHANGED` replaces a scenario with the same name, `DELTA:REMOVED` deletes one — and `references/delta-template.md` rule 2 states that a delta for an existing feature carries "Only `## Scenarios` section with markers around changes". Nothing merges a description or a `## Background` bullet, and rewording during merge is listed as an anti-pattern. Yet the supersession is Background-only for at least eleven of the falsified claims: `pushdown-planning:12`, `:26`, `:28`; `pushdown-planning-join:22`, `:47`; `unity-catalog-vended-credentials:7`; `work-unit-sharding:54`; `scan-execution:20`; `scan-execution-spec-reconstitution` § Background:9-13; `pushdown-agg-sql-consolidation:33`, `:46`; plus the ENTIRE correction in all eleven sibling deltas, each of which puts "**SUPERSEDES this feature's recorded Background bullet …**" in its Background and states only its own new scenario. Two independent checks agree on the outcome: the recorded library shows prior plans' Background bullets APPENDED rather than substituted (`specs/datafusion-scan/scan-execution-spec-reconstitution/spec.md:24-82`), and a trial `speq record` against a copy of the tree left every one of the eleven recorded bullets verbatim — e.g. `specs/vs-adapter/pushdown-planning-grouped-agg/spec.md:79` "Credentials MUST NOT appear in any returned SQL or error message." next to a newly appended scenario stating a vended credential does appear. The plan's own goal ("every recorded feature whose credential claim this change falsifies given its own delta") is not met by an append: the library ends up self-contradictory on a security claim, which is worse than the round-1 state, not better.
- Fix: In plan.md, add a `## Recorder Notes` section listing, per feature, every recorded Background bullet and description phrase this change falsifies — `file:line`, the exact text to strike, and the exact replacement — and state that the DELTA markers act on scenarios only, so those edits are applied by hand during `speq record`. Record the same in decision-log as a new `[plan-review]` entry. Where the false text is a SCENARIO clause, move the correction into a `DELTA:CHANGED` on that host scenario instead of a Background bullet plus a `DELTA:NEW` (required at minimum for `pushdown-agg-sql-consolidation:33` and `:46`). Delete the H1 and description blocks the ten renamed sibling deltas carry — `pushdown-planning-count-distinct` says "Pushdown Planning — COUNT(DISTINCT)" against the recorded "Pushdown Planning — Single-Group COUNT(DISTINCT)", and nine more diverge the same way — since neither merges and a renamed title misleads the reader about which feature is being amended.

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: plan.md § Features (35 deltas); conflicts with eight recorded locations in six features carrying no delta or an unamended sibling scenario
- Issue: the sweep is broader but still incomplete, and every gap is the same class round-1 named. Verified verbatim against the recorded library:
  - `specs/vs-adapter/pushdown-catalog-session/spec.md:40`, `:49`, `:57` — no delta at all. `:57` reads "the storage block each table produces MUST be identical to the pre-refactor output for that table field for field, the ONLY permitted difference being the externally-tagged backend wrapper"; a second enclosing `connection`/`inline` wrapper is a further difference. `:40` and `:49` carve out only "the `storage` value's variant tag".
  - `specs/vs-adapter/pushdown-module-structure/spec.md:93` — no delta. Carves out "that `storage` value's variant tag, whose tagged payload `vs-adapter/storage-backend-enum` requires to be byte-identical to the untagged encoding".
  - `specs/vs-adapter/pushdown-planning-selectlist-expressions/spec.md:132` — no delta. "*AND* every existing `dispatch_golden` fixture MUST remain byte-identical", plus § Background:116-119 "a diff in any of them is a regression rather than an expected update". Task 3.4 regenerates eighteen of them.
  - `specs/vs-adapter/pushdown-planning-single-group-agg-scalar-over-aggregate/spec.md:216` — no delta. "the grouped planner's rendered SQL MUST remain byte-identical … asserted by full-string equality against the existing committed `dispatch_golden` fixtures", plus § Background:122-125.
  - `specs/vs-adapter/pushdown-module-dedup-consolidation/spec.md:105` and `:117` — the feature HAS a delta, but only its first scenario is `DELTA:CHANGED`. Both of these name `group_by_fallback.sql` and `multi_count_distinct_decline.sql` — both in task 3.4's regeneration list — and require "every other byte of both goldens … unchanged" with a variant-tag-only carve-out.
  - `specs/vs-adapter/storage-backend-enum/spec.md:148` — the feature HAS a delta, but this third scenario is unamended: "byte-identical to its pre-delta output, because an S3 CONNECTION produces the same S3 backend and therefore the same `storage` value" — the reason clause is exactly what this change falsifies. `:135` and `:152` in the same scenario carry unscoped "no storage credential … SHALL appear in any returned SQL string or error message" prohibitions that the delta scopes nowhere.
  - `specs/vs-adapter/catalog-crate-structure/spec.md:106` — no delta: "no catalog secret, no live bearer token, and no vended STS key SHALL appear in any returned SQL string or error message". A vended STS key does appear there. The plan corrects four other features for exactly this class ("four correct vended-credential prohibitions that were never true for the returned SQL") and leaves this one.
- Fix: Author the missing deltas and clauses, and add each to plan.md § Features: `vs-adapter/pushdown-catalog-session` (`DELTA:CHANGED` on the scenarios owning `:40`, `:49`, `:57`, widening the carve-out from "variant tag" to "the `storage` value"), `vs-adapter/pushdown-module-structure` (`:93`), `vs-adapter/pushdown-planning-selectlist-expressions` (`:132`), `vs-adapter/pushdown-planning-single-group-agg-scalar-over-aggregate` (`:216`), and `vs-adapter/catalog-crate-structure` (`:106`, scoped to name the #378 vended exception). Extend the existing `pushdown-module-dedup-consolidation` delta with `DELTA:CHANGED` scenarios covering `:105` and `:117`, and the existing `storage-backend-enum` delta with a `DELTA:CHANGED` covering `:135`, `:148`, and `:152`. Apply the § Recorder Notes treatment required by the preceding finding wherever the false text is a Background bullet rather than a scenario clause.

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: `vs-adapter/storage-backend-enum/spec.md` § Scenario "The scan-spec wire carries the backend as a tagged variant"; `vs-adapter/pushdown-planning-cloud-credentials/spec.md` § Scenario "One concept-level call resolves the effective scan storage from a loadTable response"; `vs-adapter/pushdown-format-neutral-resolution/spec.md:28`; `datafusion-scan/scan-module-structure/spec.md:17`
- Issue: `DELTA:CHANGED` replaces a scenario wholesale, so a clause the reproduction omits is deleted from the library — and two reproductions keep a clause the same change falsifies. Three defects, all verified against the recorded text:
  - `storage-backend-enum` drops recorded `:122` entirely: "the S3 encoding MUST be byte-identical to before this delta, so every committed golden SQL fixture, every scan-spec JSON fixture, and the existing external-vantage serde test all pass UNEDITED". The delta's clause 3 keeps only the encoding half; the "pass UNEDITED" half — precisely the half the eighteen regenerations falsify — vanishes with no declaration. The same reproduction weakens recorded `:123`'s "SHALL be EXTENDED rather than replaced" to "SHALL be KEPT" and drops its class-bounding cross-reference, and widens the recorded `WHEN` from "an Azure backend is serialized" to "a storage backend is serialized" — a scope change the delta's Background does not declare.
  - `pushdown-planning-cloud-credentials` drops recorded `:243`'s trailing "; the recorded clause forbidding 'any other CONNECTION-derived value' is SUPERSEDED for addressing alone" without note.
  - `pushdown-format-neutral-resolution:28` and `scan-module-structure:17` each reproduce "every existing test MUST pass with no change to any assertion or expected value" verbatim inside a scenario whose `GIVEN` names the golden dispatch fixtures — eighteen of which change their expected value under task 3.4, and one of which (`common_blob_wire_is_byte_stable`) § Test Disposition amends.
- Fix: In `vs-adapter/storage-backend-enum`, restore recorded `:122` and `:123` inside the `DELTA:CHANGED` scenario with only the named change — replace "every committed golden SQL fixture … pass UNEDITED" with the eighteen-regenerated/six-unchanged split, keep "EXTENDED rather than replaced" and the class-bounding cross-reference verbatim, and restore the `WHEN` to the Azure backend or declare the widening in the delta's Background. In `pushdown-planning-cloud-credentials`, restore the dropped `:243` fragment. In `pushdown-format-neutral-resolution` and `datafusion-scan/scan-module-structure`, amend the "no change to any assertion or expected value" clause in the reproduced scenario to carve out the eighteen regenerated fixtures and `common_blob_wire_is_byte_stable`'s pinned bytes.

#### [COMPLETENESS_GAP] ADVISORY
- Location: `vs-adapter/scan-spec-credential-reference/spec.md` § Scenario "The scan UDF resolves the referenced CONNECTION…" clause 5; plan.md task 4.1, task 4.4
- Issue: the one error path where the WHOLE CONNECTION password — catalog secrets included — is in hand at message-construction time is unspecified. `ctx.connection()` returns the full password text into the UDF; only after a successful JSON parse does the nine-field projection bound what the UDF holds. The spec covers the "cannot be resolved" case (missing grant, absent connection) and task 4.4 tests it, but neither covers a password that is not valid JSON, or one that parses and carries no storage field. Those are the paths where a `format!`-built error can echo the password verbatim, and no redaction set exists yet at that point because it is derived from the resolved backend.
- Fix: Add a clause to `vs-adapter/scan-spec-credential-reference` § Scenario "The scan UDF resolves the referenced CONNECTION…" requiring that an unparsable CONNECTION password, and one carrying no storage field, each produce a `UdfError` naming the connection name and carrying neither the password text nor any substring of it. Add the two matching cases to task 4.4 and a § Verification row for them.

#### [COMPLETENESS_GAP] ADVISORY
- Location: `vs-adapter/scan-spec-credential-reference/spec.md` § Scenario clause 44 and § Scenario "The scan UDF resolves…" clause 4; plan.md § Patterns row "Resolve-once-at-the-boundary"
- Issue: whether two identical join-side references collapse to one lookup is nowhere decided. Clause 44 requires "a storage reference PER SIDE … even when both name the same CONNECTION"; clause 4 requires resolution "EXACTLY ONCE per invocation … BOTH sides of a join spec in that one step"; § Patterns claims "one CONNECTION is read once per invocation". Since both join sides are planned under one CONNECTION today, the common case is two references with the same name, and the plan does not say whether `resolve_scan_storage` issues one `ctx.connection()` call or two.
- Fix: State the rule in plan.md § Key interfaces row 7 and add a clause to the same scenario: `resolve_scan_storage` SHALL issue one `ctx.connection()` call per DISTINCT connection name in the spec, so two same-named references resolve from one read. Add the case to task 4.4's join test.

#### [COMPLETENESS_GAP] ADVISORY
- Location: the eleven sibling scoping deltas; `vs-adapter/pushdown-planning-empty-result/spec.md:15` versus `:25`; `vs-adapter/pushdown-planning-join-fallback/spec.md:20`
- Issue: the shared Background boilerplate was applied without reading three of its targets. (a) `pushdown-planning-empty-result:15` claims the recorded bullet "is FALSE for a vended storage credential" and replaces it with "a CONNECTION-supplied storage credential is carried as a connection REFERENCE and does not appear in the returned SQL" — while its own scenario at `:25` states the empty path "SHALL carry NO scan-spec storage value of any kind, so it contains neither a credential nor a connection reference". The recorded bullet it supersedes reads "in any empty-result SQL string" and was TRUE. (b) Nine of the eleven quote a sentence the target does not contain: eight recorded files say "any returned SQL or error message" (no "string") — `pushdown-planning-grouped-agg:79`, `-grouped-agg-wrapper-fallback:94`, `-count-distinct:81`, `-order-by-capability:96`, `-aggregate-extensions:13`, `-expression-aggregate:23`, `-capability-extensions:22` — and `-empty-result:40` says "any empty-result SQL string"; only `-file-encoding:21` and `-topn:87` match. A hand-applied strike keyed on the quoted sentence would not find its target. (c) `pushdown-planning-join-fallback:20` quotes only the first half of recorded `:68` and drops ", and MUST NOT be repeated per shard"; its scenario never restates the per-shard rule, unlike the parent and join deltas, which both add "The once-per-fan-out rule is UNCHANGED".
- Fix: Rewrite `pushdown-planning-empty-result`'s Background bullet to state that its recorded claim stays TRUE on this path because the empty-result SQL carries no storage value, and delete the "FALSE for a vended storage credential" boilerplate there. Correct the quoted sentence in each of the nine deltas to the recorded wording byte-for-byte. Add the per-shard clause to `pushdown-planning-join-fallback`'s scenario, matching the parent delta's wording.

## Task Breakdown

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: plan.md task 6.2; § Verification rows naming `both_storage_fields_are_declared_as_scan_storage` and `scan_storage_declares_no_secret_values_method`
- Issue: the file the probe is assigned to cannot express the assertion. `crates/lakehouse-engine/src/scan_surface_probe_tests.rs` is, by its own header, "a pure `use` list with no behavior" whose whole mechanism is failing to compile when an item is narrowed; it holds no `#[test]` function and no source-reading helper. "Assert that `ScanStorage` declares no `secret_values` method" is a negative claim no `use` statement makes, and the engine crate has no `declaration_body`/`source` helper of the kind `crates/lakehouse-catalog/tests/catalog_public_surface.rs:540-594` uses. The compile-time guarantee itself does not depend on this probe, but the guard against a later re-addition does.
- Fix: In plan.md task 6.2, name the mechanism. Either add a source-level probe to the engine crate mirroring `catalog_public_surface.rs:540-594` (its own `source()`/`declaration_body` helper reading `scan/spec.rs`, asserting the `ScanStorage` block declares no `fn secret_values` and that both `storage` fields name `ScanStorage`) in a new `_tests.rs` file, or keep `scan_surface_probe_tests.rs` for the two type pins expressible as compile-time checks (`fn _pin(s: &CommonScanSpec) -> &ScanStorage { &s.storage }`) and place the negative assertion in the source-level probe. Update the two § Verification rows to the file that actually hosts each.

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: plan.md task 3.4; § Test Disposition row 1
- Issue: the task requires two incompatible things of the golden fixtures. It says to add "a sibling helper producing the reference variant, so `dispatch_golden_tests.rs` renders the reference variant for the static case and the inline variant for the vended case", then requires "each regenerated fixture contains `"connection":{"name":` and no sentinel credential value" for all eighteen. No golden fixture renders a vended spec today — all twenty-four come from `test_support::sample_storage` (`test_support_tests.rs:369-378`, the origin of the `minioadmin` strings) — so either some fixtures carry the inline variant and the blanket assertion is false, or none do and the "inline variant for the vended case" instruction has no fixture.
- Fix: In plan.md task 3.4, state that all eighteen regenerated fixtures render the REFERENCE variant and that the inline vended encoding is covered by `pushdown_tests.rs` alone (`vended_storage_is_emitted_inline_and_static_storage_is_emitted_as_a_reference`), and delete the "inline variant for the vended case" clause — or name the fixtures that carry the inline variant and exempt them from the `"connection":{"name":` assertion.

#### [HIDDEN_DEPENDENCY] ADVISORY
- Location: plan.md § Features; § Checklist; `/speq:spec-merge` § Check Library Thresholds
- Issue: recording this plan trips the library-organization threshold on two features, and nothing in the plan says so. `specs/vs-adapter/pushdown-planning-grouped-agg` and `specs/vs-adapter/pushdown-planning-capability-extensions` each hold ten scenarios today; each delta appends one, taking both to eleven, past the ">10 scenarios per spec" signal at which the merge procedure must return to the orchestrator for a user decision BEFORE archiving. `pushdown-planning-join-fallback` reaches exactly ten. So `/speq:record` will halt mid-way on a plan whose § Checklist implies a clean finish.
- Fix: Add a line to plan.md § Features stating that `pushdown-planning-grouped-agg` and `pushdown-planning-capability-extensions` reach eleven scenarios on record and will trigger the per-spec reorganization signal, and name the intended answer (split, or accept eleven), so the recorder has the decision rather than raising it cold.

## Design Depth

[no objection — axis checked: the wrapper enum still makes "a name AND a credential" unrepresentable (decision `[6]`); the storage-only projection makes the catalog-auth exclusion structural rather than conventional, and `storage_block`'s verified nine-field read confirms the projection is the whole input to the selection rule, so one decision keeps one home; decision `[13]` records the round-1 `ResolvedScanSpec` advisory as considered and bounded, and its argument — that the unresolved variant is unreachable because the wire type exposes no accessor — holds on its own terms, though the `pub(crate)`-versus-`pub` question raised on the Feasibility axis must be settled before the compile-time guard it relies on can be written; the tactical vended shortcut keeps its scheduled follow-up (#378) with candidate remediations recorded]

## Prose Quality

#### [PROSE_BLOAT] ADVISORY
- Location: plan.md § Summary; § Design "The contract permits exactly one remedy" and "Sequencing"; `vs-adapter/scan-spec-credential-reference/spec.md` § Background (27 bullets)
- Issue: round-1's prose findings were applied in part — the "rather than assumed" construction is gone from all but two lines and the inverted "buys the whole leak" lead-in is fixed — but the two structural caps still fail, and the file the round-1 fix named grew instead of splitting. § Summary's second sentence runs about 75 words against the guardrails' 25-word cap. § Design keeps bolded arguments where the fix asked for statement headings ("**The contract permits exactly one remedy.**", "**Sequencing.**"). `scan-spec-credential-reference` § Background went from 20 bullets to 27 with the longest ones intact — bullets 22 and 25 each run past 120 words in multi-clause sentences — and the narrowness-of-the-vended-residual argument is still restated in three places (`scan-spec-credential-reference:29`, `pushdown-planning-cloud-credentials:10`, plan.md § Impact) rather than stated once and cited.
- Fix: Split plan.md § Summary's second sentence into three, each under 25 words. Rewrite the two § Design lead-ins as statement headings ("One remedy survives the pushdown contract", "The Azure deferral condition is discharged"). Split every `scan-spec-credential-reference` § Background bullet longer than three lines into single-idea bullets under the 25-word sentence cap, and reduce the vended-narrowness argument to one home with a one-line citation from the other two.
