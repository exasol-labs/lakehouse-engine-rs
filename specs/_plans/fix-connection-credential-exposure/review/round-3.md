# Plan Review Findings: fix-connection-credential-exposure (round 3)

## Summary
- Axes checked: 6/6
- Total findings: 20 (Blockers: 8, Advisory: 12)
- Intent Fidelity blockers: 0

## Premortem

Three failure stories, each routed into the taxonomy below.

1. **Group D does not compile and the plan stalls mid-implementation.** `ResolvedScanStorage` is `pub(crate)` with `resolve_scan_storage` as its only constructor, but task 4.2 threads it through `run_raw_scan_with_session` — `pub`, called by twelve external test binaries — and task 4.3 repoints `join_scan.rs:48`, inside `run_join_scan_with_session`, also `pub`. The implementer either publishes the type unplanned or duplicates a constructor, and the compile-time guard decision `[13]` rests on is improvised. → Feasibility `[HIDDEN_DEPENDENCY]`.
2. **The release ships, and the first non-DBA user's query fails anyway.** The installer template emits a role and one `ACCESS ON CONNECTION ... FOR SCRIPT` grant. Nobody established whether a user querying a virtual schema also needs `EXECUTE ON SCRIPT <schema>.LAKEHOUSE_SCAN`, which 1.3a's probe user held explicitly. The operator follows `docs/install.md` and `docs/security.md` and still gets the scan-time error the plan promised the grant would prevent. → Feasibility `[UNSTATED_ASSUMPTION]`.
3. **`/speq:record` runs and the library is left self-contradictory on a security claim — the exact round-2 outcome.** Task 8.1 writes hand-edit instructions into a `## Recorder Notes` section, but `/speq:spec-merge` has three scenario-only actions, no step that reads such a section, and an anti-pattern forbidding rewriting during merge; `speq-implement-pr`'s pipeline has no step between `/speq:implement` and `/speq:record` to enforce the gate. Ten sibling features keep "Credentials MUST NOT appear in any returned SQL or error message" beside a new scenario saying a vended credential does appear. → Feasibility `[HIDDEN_DEPENDENCY]`, Requirement Quality `[COMPLETENESS_GAP]`.

## Round-2 Blocker Recheck

- **Resolved: [HIDDEN_DEPENDENCY] Task 1.3b could not run where the plan put it.** § Dependencies bullet 2, § Parallelization Group E intra-order, task 1.3b, and § Manual Testing row "Gate 1.3b" now agree on option (a): self-contained inline provisioning via `exapump`, no dependency on 7.2 or 7.3. No section still carries "immediately before task 7.3". Residual scope understatement raised as ADVISORY below, not as a reopened blocker.
- **Resolved: [UNSTATED_ASSUMPTION] The profiling leak surface was asserted, not established.** The live probe was run and the answer is negative. Applied in § Summary, § Design row 4, task 1.2, task 7.3, task 7.4, § Manual Testing, `scan-spec-credential-reference:3` and § Background bullet 3, and `e2e-harness` clauses 17-18 and 45. **Reopened in part**: one plan.md location was missed and the evidence trail contradicts itself — see Feasibility `[UNSTATED_ASSUMPTION]` below.
- **NOT resolved: [HIDDEN_DEPENDENCY] `pub(crate)` resolved pair threaded through `pub` facade entry points.** § Key interfaces still declares `ResolvedScanStorage` and `resolve_scan_storage` `pub(crate)` with "the ONLY constructor"; task 4.2 still names `run_raw_scan_with_session` (verified `pub` at `crates/lakehouse-engine/src/scan/raw_scan.rs:48`); task 4.3 still repoints `join_scan.rs:48` inside `run_join_scan_with_session` (verified `pub` at `scan/join_scan.rs:42`); no § Test Disposition row covers the twelve external binaries' call sites. `grep` across every plan artifact finds no mention of `run_join_scan_with_session`, `private_interfaces`, or `scan_facade_reachability`. No decision-log entry records the finding. Re-raised below.
- **Resolved: [REQUIREMENT_CONFLICT] `StorageCreds` field visibility.** Decision `[15]` settles it as nine `pub` fields; § Key interfaces row, task 2.1, task 2.3, and § Verification agree, and the `storage_creds_fields_are_not_public` test name is gone. Residual: the delta that records public-surface additions does not enumerate the fields — ADVISORY below.
- **NOT resolved: [REQUIREMENT_CONFLICT] Background-only supersession does not merge.** Deferred to task 8.1 rather than fixed. The deferral mechanism does not exist (Feasibility below) and 8.1's own scope omits the ten sibling bullets that carry the bulk of the correction (Requirement Quality below).
- **Deferred, scope incomplete: [REQUIREMENT_CONFLICT] Eight further recorded locations uncovered.** Task 8.2 names all eight and re-verification is required. Acceptable as a scheduled task, contingent on the Group F orchestration gate being real.
- **NOT resolved: [REQUIREMENT_CONFLICT] Three `DELTA:CHANGED` reproductions drop or self-falsify recorded clauses.** Deferred to task 8.3, which runs in Group F — after the Groups C/D work those very clauses forbid. Re-raised below.

## Intent Fidelity

[no objection — axis checked: the two stated intents are both in scope and traceable. #135's standing-credential case is closed by the reference path (§ Summary, task 1.5 ties the closure to the case the issue reports); #378 is closed inside this plan by the sealed envelope, with decision `[14]` superseding `[2]` on the vended half only and `[2]` carrying the matching supersession note. The verification-discipline intent is honoured for the load-bearing probes — 1.3a's script-scoped grant with a runtime VARCHAR name, 1.4's `CREATE OR REPLACE` versus `ALTER` grant behaviour, decision `[17]`'s four-way live comparison of `ACCESS ANY CONNECTION` / `PUBLIC` / per-user / role, and `EXA_DBA_CONNECTIONS` carrying no password column all carry probe output and SQL states. Nothing is silently deferred or reinterpreted; three verification-discipline failures exist but are gaps in evidence, not in scope, so they are raised on Feasibility]

## Feasibility

#### [HIDDEN_DEPENDENCY] BLOCKER
- Location: plan.md § Key interfaces rows for `ResolvedScanStorage` and `resolve_scan_storage`; task 4.2; task 4.3; § Test Disposition row "The four threaded signatures" and row "The 15 `tests/scan_*.rs` integration binaries"
- Issue: the plan as written does not compile, and this is round-2's third blocker carried forward untouched. § Key interfaces declares `struct ResolvedScanStorage` and `resolve_scan_storage` as `pub(crate)`, with `resolve_scan_storage` "the ONLY constructor". Task 4.2 threads the resolved pair through "the `pub` host-test entry `run_raw_scan_with_session`" — verified `pub async fn` at `crates/lakehouse-engine/src/scan/raw_scan.rs:48`, reading `spec.common.storage.secret_values()` at `:54`. Task 4.3 repoints `join_scan.rs:48`, which is inside `pub async fn run_join_scan_with_session` (`scan/join_scan.rs:42`, reading `spec.common.all_secret_values()`) — a function task 4.2 never names at all, though its secret-set read is deleted by the same task group. Twelve external binaries reach both through `use lakehouse_engine::scan::…`: `scan_batch_loop.rs`, `scan_column_binding.rs`, `scan_deletion_vectors.rs`, `scan_facade_reachability.rs`, `scan_footer_refetch_observable.rs`, `scan_join_test.rs`, `scan_name_mapping.rs`, `scan_no_head_test.rs`, `scan_partition_values.rs`, `scan_positional_deletes.rs`, `scan_telemetry.rs`, `scan_two_arg.rs`. A `pub(crate)` parameter in a `pub` signature trips `private_interfaces`, and § Checklist demands `cargo clippy --all-targets` at zero warnings; the callers cannot construct the argument either, because the only constructor is `pub(crate)`. § Test Disposition calls these signatures MECHANICAL and lists the fifteen `tests/scan_*.rs` binaries as gaining only the `Inline` wrapper. No plan artifact mentions `run_join_scan_with_session`, `private_interfaces`, or `scan_facade_reachability.rs`, and no `[plan-review]` decision-log entry records the round-2 finding.
- Fix: In plan.md § Key interfaces, decide the visibility and state it. Either declare `ResolvedScanStorage` `pub` and replace "the ONLY constructor of `ResolvedScanStorage`" with "the only constructor that reads a CONNECTION", adding the second `pub` constructor the external binaries call; or keep the type `pub(crate)` and give the two facade entries `pub` parameter shapes — `run_raw_scan_with_session(…, storage: &StorageBackend, …)` and `run_join_scan_with_session(…, fact: &StorageBackend, dimension: Option<&StorageBackend>, …)`. Name `run_join_scan_with_session` explicitly in task 4.2 beside `run_raw_scan_with_session`, name the chosen shape in tasks 4.2 and 4.3, and add a § Test Disposition row for the twelve external binaries whose call sites gain the new argument. Record the resolution as a `[plan-review]` entry in decision-log.md.

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: plan.md § Impact "Breaking" paragraph 1; § Migration row 1; task 7.1; task 7.4; task 1.3b; `vs-adapter/scan-spec-credential-reference/spec.md` § Scenario "The scan script requires its own script-scoped connection access grant" clauses 113-114 and § Background bullet 22
- Issue: the plan never establishes whether a user querying a virtual schema must also hold `EXECUTE ON SCRIPT <schema>.LAKEHOUSE_SCAN`, yet both possible answers falsify text the plan already commits to. 1.3a's probe user held `CREATE SESSION`, `EXECUTE ON SCRIPT <schema>.CONN_PROBE`, and the connection grant, and invoked the script DIRECTLY — so it answers nothing about a UDF reached through VS-rewritten pushdown SQL. Task 1.3b's throwaway user holds "only `SELECT` on that VS" plus the connection grant, and no `EXECUTE`. Meanwhile § Impact asserts "`EXECUTE` on `LAKEHOUSE_SCAN` therefore becomes security-relevant" and Background bullet 22 opens "A user who can execute `LAKEHOUSE_SCAN` and holds the script-scoped grant" — both premised on `EXECUTE` being a separate grantable prerequisite. If it is: task 7.1's template, § Migration, § Impact, and clause 114 are all incomplete, an operator following them gets exactly the scan-time failure the grant was added to prevent, and 1.3b fails for a missing grant line while its own failure rule orders a revert of tasks 3.x and 4.x. If it is not: the delegation-residual argument in § Impact and Background bullet 22 needs rewording, since "can execute `LAKEHOUSE_SCAN`" then means "can query the virtual schema". CLAUDE.md requires this class of claim to be checked against a running instance, not assumed.
- Fix: Add the question to task 1.3a as a second probe on the same throwaway objects — issue the `ACCESS ON CONNECTION ... FOR SCRIPT` grant WITHOUT `EXECUTE ON SCRIPT` and record whether a VS-rewritten pushdown statement still resolves — and record the SQL state and message either way in the verification report. Then thread the answer: if `EXECUTE` is required, add `GRANT EXECUTE ON SCRIPT <schema>.LAKEHOUSE_SCAN TO LAKEHOUSE_ENGINE_ROLE_<schema>` to task 7.1's template, to § Migration row 1, to § Impact "Breaking" paragraph 1, to task 7.2's harness grant, to task 1.3b's provisioning, and to `scan-spec-credential-reference` clause 113; if it is not, rewrite § Impact "Not contained, and named" and Background bullet 22 to say "a user who can query the virtual schema" instead of "a user who can execute `LAKEHOUSE_SCAN`".

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: plan.md § Impact "Fixed, with a bounded guarantee" paragraph (line 222); plan.md § Design row "Is `EXPLAIN VIRTUAL` the only surface?"; plan.md task 1.2 versus decision-log `[16]` and open-questions.md line 7; plan.md § Manual Testing row "Profiling/audit never carry the rewritten pushdown SQL"; `vs-adapter/scan-spec-credential-reference/spec.md` § Background bullet 3
- Issue: decision `[16]`'s correction is not applied everywhere it claims, and the evidence behind it is internally inconsistent — three defects on the one claim the round-2 revision was supposed to settle.
  - **Missed location.** decision `[16]` states "Every citation of `EXA_USER_PROFILE_LAST_DAY.SQL_TEXT` / `EXA_DBA_AUDIT_SQL.SQL_TEXT` as a credential leak surface or assertion target is corrected in plan.md (§ Summary, § Design, § Impact, task 1.2, task 7.3, task 7.4, § Manual Testing)". § Impact line 222 still reads: "it defeats a plaintext read or grep of `EXPLAIN VIRTUAL` output, **profiling and audit `SQL_TEXT`**, and pushdown-path error text". That names as a defeated surface exactly what the same section, § Sealed vended envelope, and the spec Background say was never a surface. § Impact is the section the writing guardrails route into the PR body.
  - **Which view was read.** decision `[16]` and open-questions.md line 7 both name `EXA_DBA_PROFILE_LAST_DAY` for the 9-row result. Task 1.2, § Design row 4, § Impact, task 7.4, and `scan-spec-credential-reference:11` all attribute the same 9-row result to `EXA_USER_PROFILE_LAST_DAY` — the view whose readability by a least-privilege user is the whole point. § Manual Testing's reproduction command queries `EXA_DBA_PROFILE_LAST_DAY`, so following it does not reproduce the claim the plan makes.
  - **Under which privilege.** § Design row 4 asserts the user's query appeared in `EXA_USER_PROFILE_LAST_DAY` "and in `EXA_DBA_AUDIT_SQL` — both readable without `ACCESS ANY CONNECTION` or `SELECT ANY DICTIONARY`". Task 1.2 says the audit read was done "as `sys`", and `scan-spec-credential-reference:11` says `EXA_DBA_AUDIT_SQL` "carries `SQL_TEXT` under `SELECT ANY DICTIONARY`" while asserting in the same bullet that a user holding neither privilege selected from it. The spec bullet contradicts itself, and it is a NEW permanent feature file.
- Fix: In plan.md § Impact line 222, delete "profiling and audit `SQL_TEXT`," from the list of surfaces the envelope defeats and append the standing correction ("profiling and audit `SQL_TEXT` were verified live never to carry the rewritten pushdown SQL, so they are not a surface the envelope needs to defeat"). Settle the view question: re-run the probe against `EXA_USER_PROFILE_LAST_DAY` as the least-privilege user and record its row count, or change task 1.2, § Design row 4, § Impact, task 7.4, and `scan-spec-credential-reference:11` to name `EXA_DBA_PROFILE_LAST_DAY` as the view actually queried; make § Manual Testing's command name the same view as the claim. Delete "both readable without `ACCESS ANY CONNECTION` or `SELECT ANY DICTIONARY`" from § Design row 4 and the "a user holding neither ... selected ... from both" construction from `scan-spec-credential-reference:11`, replacing them with what the probe recorded: which principal read which view.

#### [HIDDEN_DEPENDENCY] BLOCKER
- Location: plan.md task 8 header; § Parallelization "Group E → Group F" note; task 8.1's `## Recorder Notes` premise
- Issue: Group F's entire gating mechanism names steps that do not exist in the skills it delegates to, so the round-2 blocker it was created to close stays open. Task 8's header states the group "MUST be orchestrated by the `speq-implement-pr` skill's own pipeline (the step immediately before it invokes `/speq:record`)", and § Parallelization states "`speq-implement-pr`'s own pipeline MUST run it — and confirm it completed — before invoking `/speq:record`". `speq-implement-pr`'s pipeline is fixed and reads: resolve target → blocker check → implement → bump version → commit evidence → test+record gate → PR. There is no step between `/speq:implement` and `/speq:record` other than a version bump, a commit, and running the test suites, and this plan cannot add one. Separately, task 8.1's premise is that the gap "must be closed by giving `/speq:record` an explicit, hand-executable instruction" in a plan.md section — but `/speq:spec-merge` reads plan.md only under "Load Plan Context", defines exactly three scenario-only DELTA actions, lists "Rewriting scenario wording during merge" as an anti-pattern, and finalizes by archiving the plan directory. Nothing in the merge procedure reads a `## Recorder Notes` section or applies a hand edit to a recorded `## Background` bullet. The plan's own trial-merge evidence says the false bullets survive verbatim; nothing in the fix changes that.
- Fix: Replace the unenforceable orchestration MUST. In plan.md task 8's header and § Parallelization's Group E → Group F note, state that Group F runs as the final task group inside `/speq:implement` like every other group, and drop the claim that `speq-implement-pr`'s pipeline runs or confirms it. Then close the merge gap with a mechanism that exists: either (a) convert every Background-only correction into a `DELTA:CHANGED` on the scenario that owns the falsified claim — adding the scenario where none owns it — so `/speq:spec-merge`'s three actions carry the whole correction, or (b) require task 8.1 to write the strike/replace checklist into a file `recorder-agent` is told to execute and add an explicit instruction to plan.md § Features naming that file, then state in § Checklist a post-record verification step (`grep` for each struck sentence across `specs/`) that fails if any survives. Name the chosen mechanism in decision-log.md as a `[plan-review]` entry.

#### [EFFORT_MISESTIMATION] ADVISORY
- Location: plan.md task 1.3b; § Dependencies "Ordering" bullet 2; § Manual Testing row "Gate 1.3b"
- Issue: "SELF-CONTAINED" covers the throwaway user and grant, not the environment the probe needs. Task 1.3b says "stand up (or reuse) a VS over a real seeded table" and "the `.so` is rebuilt", but names no actor for the Docker stack, the Iceberg seed, `make cross-udf-build`, the BucketFS upload, or the recreation of the `LAKEHOUSE_SCAN` script DDL — all of which the E2E harness normally owns and all of which must happen before the first statement in § Manual Testing's row runs. A gate that governs a revert of two whole task groups should not have unnamed prerequisites.
- Fix: In plan.md task 1.3b, list the prerequisite steps explicitly — `make cross-udf-build`, upload the `.so` to BucketFS, recreate the `LAKEHOUSE_SCAN` script DDL, bring up the Docker stack and seed the table (naming the existing script or make target that does it) — and note that only the user and grant provisioning is inline `exapump` work. Mirror the list in § Manual Testing's "Gate 1.3b" row.

#### [HIDDEN_DEPENDENCY] ADVISORY
- Location: plan.md task 7.4; § Impact "Breaking" paragraph 1; `docs/install.md:170-185`; `docs/catalogs.md:56` and its eight `CREATE OR REPLACE CONNECTION` recipes
- Issue: round-2's advisory stands, and the sealed-envelope revision added a second document defect. Task 7.4 still asks only to "cross-reference it from `docs/install.md` and `docs/catalogs.md`", so the two documents an operator copies from still yield a deployment that plans and then fails at scan time. New: `docs/catalogs.md:56` states "If you turn `use_vended_credentials` on without SigV4, you can omit all static S3 fields", which — read against the same page's "Local / generic Iceberg REST (no auth)" recipe — documents the no-auth + vending combination this plan now REFUSES at plan time. No task corrects that sentence. § Impact also still says the grant binds "every user that queries a virtual schema" without noting that a DBA holding `ACCESS ANY CONNECTION` needs none, which is why the E2E suite passes as `sys` today.
- Fix: In plan.md task 7.4, require `docs/install.md` § "Point the VS at your data" to become three statements — the CONNECTION, the role plus `GRANT ACCESS ON CONNECTION <c> FOR SCRIPT <schema>.LAKEHOUSE_SCAN TO <role>`, and the Virtual Schema — with the `CREATE OR REPLACE` warning beside it; require the same grant line and warning in each of `docs/catalogs.md`'s eight recipes; and require `docs/catalogs.md:56` to be rewritten to state that vending requires a catalog-auth mode carrying secret material. In § Impact "Breaking" paragraph 1, add that a grantee holding `ACCESS ANY CONNECTION` needs no per-script grant, so the requirement binds every non-DBA grantee.

## Requirement Quality

#### [COMPLETENESS_GAP] BLOCKER
- Location: plan.md task 8.1's "At minimum this covers" list; the eleven sibling deltas' § Background supersession bullets
- Issue: task 8.1's minimum scope mislabels one set of locations as another and, in doing so, omits the larger half of the round-2 blocker it exists to close. The parenthetical reads "the eleven sibling deltas' Background-only corrections (`pushdown-planning:12`, `:26`, `:28`; `pushdown-planning-join:22`, `:47`; `unity-catalog-vended-credentials:7`; `work-unit-sharding:54`; `scan-execution:20`; `scan-execution-spec-reconstitution` § Background:9-13)". Those are nine locations in six features, and NONE of them is a sibling delta — the eleven siblings are `pushdown-planning-file-encoding`, `-topn`, `-join-fallback`, `-grouped-agg`, `-grouped-agg-wrapper-fallback`, `-count-distinct`, `-order-by-capability`, `-aggregate-extensions`, `-expression-aggregate`, `-capability-extensions`, and `-empty-result`. Ten of those carry their ENTIRE correction in a non-merging Background bullet beginning "**SUPERSEDES this feature's recorded Background bullet …**", against these recorded targets, verified verbatim in the library: `pushdown-planning-file-encoding:21`, `-topn:87`, `-join-fallback:68`, `-grouped-agg:79`, `-grouped-agg-wrapper-fallback:94`, `-count-distinct:81`, `-order-by-capability:96`, `-aggregate-extensions:13`, `-expression-aggregate:23`, `-capability-extensions:22`. None appears in 8.1's list. 8.1's only other sibling instruction is cosmetic ("correct the ten renamed sibling deltas' titles/descriptions"), and its catch-all covers "further Background-only falsification introduced by task-group 1–7's actual implementation" — new falsifications, not these existing ten. An implementer following 8.1 literally produces Recorder Notes for nine locations and leaves ten unscoped security prohibitions standing in the recorded library beside contradicting new scenarios — round-2's blocker, unchanged.
- Fix: In plan.md task 8.1, split the "At minimum" list into two named sets. Set 1, the nine non-sibling Background-only locations already listed, relabelled as such. Set 2, the ten sibling recorded bullets named individually with their `file:line` — `pushdown-planning-file-encoding:21`, `-topn:87`, `-join-fallback:68`, `-grouped-agg:79`, `-grouped-agg-wrapper-fallback:94`, `-count-distinct:81`, `-order-by-capability:96`, `-aggregate-extensions:13`, `-expression-aggregate:23`, `-capability-extensions:22` — each with the exact recorded sentence to strike and the scoped replacement each sibling delta's Background already drafts. State that `pushdown-planning-empty-result:40` needs NO strike, because its delta records the recorded claim as staying true on that path.

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: `vs-adapter/pushdown-format-neutral-resolution/spec.md:28`; `datafusion-scan/scan-module-structure/spec.md:17`; `vs-adapter/storage-backend-enum/spec.md` § Scenario "The scan-spec wire carries the backend as a tagged variant"; plan.md task 8.3; § Parallelization "Group E → Group F"
- Issue: the plan ships spec deltas whose normative clauses forbid what its own tasks require, and schedules the repair for the last task group — after the tasks those clauses govern have run. `pushdown-format-neutral-resolution:28` reads "*AND* every existing test MUST pass with no change to any assertion or expected value", inside a `DELTA:CHANGED` scenario whose `GIVEN` names "the golden dispatch fixtures". `scan-module-structure:17` reads "*THEN* every test MUST pass with no change to any test assertion or expected value". Task 3.4 regenerates eighteen of those fixtures and § Test Disposition amends `common_blob_wire_is_byte_stable`'s pinned bytes — both in Group C. Task 8.3 corrects the clauses in Group F, gated behind "Group E → Group F". So for the entire duration of Groups C, D, and E the plan's own requirement artifacts state the opposite of its tasks, and an implementer reading the delta for the feature they are touching gets a normative prohibition on the change they were told to make. Deferring a requirement correction until after the requirement has been violated does not resolve a `[REQUIREMENT_CONFLICT]`; it schedules its concealment.
- Fix: Apply task 8.3's three corrections to the delta files NOW, during planning, rather than as an implementation task: in `pushdown-format-neutral-resolution` and `datafusion-scan/scan-module-structure`, amend the "no change to any assertion or expected value" clause to carve out the eighteen fixtures task 3.4 regenerates and `common_blob_wire_is_byte_stable`'s pinned bytes; in `vs-adapter/storage-backend-enum`, restore recorded `:122` as the eighteen-regenerated/six-unchanged split, restore `:123`'s "SHALL be EXTENDED rather than replaced" wording and its class-bounding cross-reference verbatim, and restore its `WHEN` to name the Azure backend or declare the widening in the delta's Background; in `vs-adapter/pushdown-planning-cloud-credentials`, restore the dropped trailing fragment of recorded `:243`. Reduce task 8.3 to a re-verification step ("confirm each restored clause still matches the recorded text at record time") and remove it from the Group F gate.

#### [COMPLETENESS_GAP] BLOCKER
- Location: plan.md § Key interfaces `ResolvedConnectionConfig` row; task 3.2; § Sealed vended envelope "Threat model" paragraph; `vs-adapter/scan-spec-credential-reference/spec.md` § Scenario "One pure function selects the wire variant for every builder path" clause 50; § Verification row "A vended query under a catalog-auth mode with no secret material is refused at plan time"
- Issue: the predicate that decides whether the sealed envelope's guarantee applies has no owner, no test, and a criterion that does not match the rationale it is sold on. The rule is stated identically in four places — the key is `Some` "iff the catalog-auth mode carries secret material (non-empty `token`, or non-empty `client_secret`, or `use_sigv4` with non-empty `access_key`/`secret_key`)" — and is computed in `resolve_connection_config` (`adapter/mod.rs`), while the code that acts on it, `scan_storage_for` (`adapter/pushdown/support.rs`), sees only `Option<&SealedStorageKey>`. Three consequences.
  - **No test.** § Verification's only refusal row names `scan_storage_for_refuses_vending_without_catalog_auth_secret_material`, which exercises `scan_storage_for` given `None` — not the predicate that decides `None`. The `connection-credentials` row names `resolved_config_carries_the_catalog_connection_name`, which covers the name, not the key. No named test asserts the key is absent for no-auth and present for each of the three secret-bearing modes. A defect there ships an envelope under a guessable key, silently: the exact false guarantee the refusal exists to prevent, and the exact objection under which decision `[2]` originally rejected the envelope.
  - **Non-emptiness is not entropy.** § Sealed vended envelope claims "The guarantee is scoped per catalog-auth mode: it holds whenever the CONNECTION carries genuine secret material". The implemented test is a non-empty string check. A `token` of `"x"` passes it and ships the envelope under a one-character-derived key.
  - **The gate refuses configurations whose password does carry secret material.** `deploy/scripts/install.sh:1425`'s own next-step template creates `LAKEHOUSE_CATALOG_CREDS` with `USER ''` and a password of `{"warehouse", "region", "access_key", "secret_key"}` — no `token`, no `client_secret`, no `use_sigv4`. That is a high-entropy password that the predicate classifies as no-auth. § Sealed vended envelope's justification says a no-auth CONNECTION password "may carry no secret at all (e.g. only `{"warehouse":"…"}`)", which is not what the gate tests.
- Fix: In plan.md § Key interfaces, give the predicate a named home — a function in `crates/lakehouse-engine/src/scan/sealed.rs` or beside `derive_sealed_storage_key`, taking the parsed `ConnectionCreds` and returning `bool` — and name it in task 3.2 as the single site that decides. Add to task 3.2's test list, and to § Verification, a case per auth mode asserting the key's presence: absent for no-auth, present for a non-empty `token`, present for a non-empty `client_secret`, present for `use_sigv4` with both keys, absent for each of those with the field empty. In § Sealed vended envelope and in `scan-spec-credential-reference` clause 50, replace "carries genuine secret material" with what the gate actually tests — a non-empty catalog-auth secret field — and state that the bound therefore also depends on the operator's own secret strength. Decide and state whether the gate should widen to accept a password carrying non-empty static `access_key`/`secret_key` without SigV4, given the installer's own default template produces exactly that shape.

#### [COMPLETENESS_GAP] ADVISORY
- Location: `vs-adapter/catalog-crate-public-surface-extensions/spec.md` clause 29 and § Background bullet 20; decision-log `[15]`; plan.md task 2.3 and § Verification row for `catalog-crate-public-surface-extensions`
- Issue: decision `[15]` publishes nine `pub` fields, and the delta whose purpose is to record public-surface additions does not admit them. Clause 29 enumerates the addition as "exactly ONE type … plus its deserialization from a JSON credential document and its ONE backend selector", while § Background bullet 20 states "The `pub` enumeration at `catalog-crate-structure` is exhaustive and the probe names every member, so admitting a type without editing both is a silent widening". Task 2.3's `storage_creds_declares_exactly_the_nine_pub_storage_fields` and § Verification's row for it therefore have a test with no requirement, and the recorded exhaustive enumeration will be out of date on merge — the silent widening the feature exists to prevent.
- Fix: In `vs-adapter/catalog-crate-public-surface-extensions/spec.md` clause 29, extend the admitted surface to name the nine `pub` fields, and add a clause requiring the probe to assert the type declares exactly those nine and no more, citing decision `[15]`'s reason (`parse_creds` reads them across the crate boundary; the payload is guarded by the redacting manual `Debug`, not by visibility).

#### [COMPLETENESS_GAP] ADVISORY
- Location: the ten sibling deltas' § Background supersession bullets; `vs-adapter/pushdown-planning-join-fallback/spec.md:20`; `vs-adapter/pushdown-planning-empty-result/spec.md:15`
- Issue: round-2's quoted-text advisory is unfixed, and task 8.1 now makes it load-bearing. Seven sibling deltas quote their target as "Credentials MUST NOT appear in any returned SQL string or error message." while the recorded text has no "string": `pushdown-planning-grouped-agg:79`, `-grouped-agg-wrapper-fallback:94`, `-count-distinct:81`, `-order-by-capability:96`, `-aggregate-extensions:13`, `-expression-aggregate:23`, `-capability-extensions:22` — verified against the recorded library. `pushdown-planning-join-fallback:20` quotes only the first half of recorded `:68` and drops ", and MUST NOT be repeated per shard", and its scenario never restates the per-shard rule that the parent and join deltas both keep. `pushdown-planning-empty-result:15` quotes "any returned SQL string" against a recorded "any empty-result SQL string". A strike/replace checklist keyed on a quoted sentence that does not appear in the target file does not find it.
- Fix: Correct the quoted sentence in each of the seven deltas to the recorded wording byte-for-byte, restore the dropped ", and MUST NOT be repeated per shard" fragment to `pushdown-planning-join-fallback:20` and add the per-shard clause to its scenario matching the parent delta's wording, and correct `pushdown-planning-empty-result:15`'s quotation to "any empty-result SQL string or error message".

#### [COMPLETENESS_GAP] ADVISORY
- Location: `vs-adapter/scan-spec-credential-reference/spec.md` § Scenario "The scan UDF resolves the referenced CONNECTION without contacting the catalog" clauses 64-68; plan.md task 4.1, task 4.4; § Patterns row "Resolve-once-at-the-boundary"
- Issue: two round-2 advisories on the same scenario are unaddressed. (a) The one error path where the WHOLE CONNECTION password is in hand at message-construction time is still unspecified: `ctx.connection()` returns the full password text, and only a successful JSON parse bounds what the UDF holds. The scenario covers "cannot be resolved" and the sealed-envelope AEAD failure, but not a password that is not valid JSON, nor one that parses and carries no storage field — the paths where a `format!`-built error can echo the password verbatim, with no redaction set yet derived. (b) Whether two identical join-side references collapse to one lookup is still undecided: clause 46 requires a reference per side "even when both name the same CONNECTION", clause 67 requires resolution "EXACTLY ONCE per invocation", and § Patterns claims "one CONNECTION is read once per invocation" — but nothing says whether `resolve_scan_storage` issues one `ctx.connection()` call or two for the common case where both sides share a CONNECTION.
- Fix: Add a clause to that scenario requiring an unparsable CONNECTION password, and one carrying no storage field, each to produce a `UdfError` naming the connection and carrying neither the password text nor any substring of it; add the two matching cases to task 4.4 and a § Verification row. Add a second clause stating that `resolve_scan_storage` SHALL issue one `ctx.connection()` call per DISTINCT connection name in the spec, and add that case to task 4.4's join test.

## Task Breakdown

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: plan.md § Verification, Scenario Coverage row "scan-spec-credential-reference: One pure function selects the wire variant for every builder path"
- Issue: the test that verifies the plan's central selection rule is named for the behaviour decision `[14]` superseded. The row names `scan_storage_for_selects_reference_when_vending_is_disabled_and_inline_when_enabled`. Under the current design `scan_storage_for` returns `Sealed` when vending is enabled and a key exists, the named refusal error when it does not, and § Key interfaces states "It can NEVER return `Inline`"; `scan-spec-credential-reference` clause 54 makes that normative — "the function MUST NOT return the plaintext `Inline` variant on any input". An implementer writing the test the § Verification table names writes an assertion that the function returns `Inline` under vending: the exact regression the sealed-envelope design exists to prevent, asserted as the expected result. This is the last leftover of the pre-sealed design in the plan's traceability table, and § Verification is what an implementer works from.
- Fix: In plan.md § Verification, rename that test to `scan_storage_for_selects_reference_when_vending_is_disabled_and_sealed_when_enabled` and add `scan_storage_for_never_returns_inline` beside it. Add both names to task 3.3a's test list, and grep the plan for any other `_inline_when_enabled` or `emitted_inline` construction before revising.

#### [TRACEABILITY_GAP] ADVISORY
- Location: plan.md task 7.1; decision-log `[17]`; § Impact "Breaking" paragraph 1; § Migration row 1; `vs-adapter/scan-spec-credential-reference/spec.md` clauses 113-114
- Issue: the role-based grant design is a breaking deployment contract with no normative home. Decision `[17]` and task 7.1 specify a named role `LAKEHOUSE_ENGINE_ROLE_<schema>`, its schema qualification to avoid cross-install collision, the grant to the installing user, the "one `GRANT <role> TO <user>` thereafter" onboarding path, and the `CREATE ROLE`-has-no-`IF NOT EXISTS` / check-`EXA_ALL_ROLES`-first rule. The spec scenario says only "`TO <user-or-role>`" and "the installer's next-step template SHALL emit that grant". § Verification's E2E row names `next_step_template_emits_the_scan_script_grant_and_the_replace_warning`, while § Manual Testing requires `install.test.sh` to assert `CREATE ROLE`, the role grant, the installing-user grant, and two warning lines. So the recorded library will carry a test and an installer behaviour with no requirement behind them, and a future planner reading the spec cannot tell the role pattern is the deliberate default.
- Fix: Add clauses to `vs-adapter/scan-spec-credential-reference` § Scenario "The scan script requires its own script-scoped connection access grant" requiring the installer template to emit a deployment-scoped role qualified by the schema name, the connection grant to that role once, the role grant to the installing user, the `CREATE ROLE` non-idempotency warning naming the `EXA_ALL_ROLES` pre-check, and the direct per-user grant as the documented exception. Rename the § Verification test to match what `install.test.sh` asserts.

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: plan.md task 6.2; § Verification rows naming `both_storage_fields_are_declared_as_scan_storage` and `scan_storage_declares_no_secret_values_method`
- Issue: round-2's advisory stands unchanged. `crates/lakehouse-engine/src/scan_surface_probe_tests.rs` is, by its own header, a pure `use` list with no `#[test]` function and no source-reading helper; its mechanism is failing to compile when an item is narrowed. "Assert that `ScanStorage` declares no `secret_values` method and no payload accessor" is a negative claim no `use` statement expresses, and the engine crate has no `declaration_body`/`source()` helper of the kind `crates/lakehouse-catalog/tests/catalog_public_surface.rs:540-594` uses.
- Fix: In plan.md task 6.2, name the mechanism — add a source-level probe to the engine crate in its own `_tests.rs` file, with a `source()`/`declaration_body` helper reading `scan/spec.rs`, asserting the `ScanStorage` block declares no `fn secret_values` and no payload accessor and that both `storage` fields name `ScanStorage`; keep `scan_surface_probe_tests.rs` for the compile-time type pins only. Update the two § Verification rows to the file that hosts each.

#### [HIDDEN_DEPENDENCY] ADVISORY
- Location: plan.md § Features; § Checklist; `/speq:spec-merge` § Check Library Thresholds; `speq-implement-pr` SKILL.md step 6
- Issue: round-2's advisory stands, and the headless pipeline makes it consequential. `specs/vs-adapter/pushdown-planning-grouped-agg` and `specs/vs-adapter/pushdown-planning-capability-extensions` each hold ten scenarios; each delta appends one, taking both past the ">10 scenarios per spec" signal at which `/speq:spec-merge` must return to the orchestrator for a user decision before archiving. `speq-implement-pr` step 6 answers that question automatically: "If it raises its library-threshold split question, **answer yes** automatically (split)". So a headless run will split two recorded features with no guidance from the plan on where the split boundary falls or what the new sibling is called.
- Fix: Add a line to plan.md § Features stating that `pushdown-planning-grouped-agg` and `pushdown-planning-capability-extensions` reach eleven scenarios on record, that `pushdown-planning-join-fallback` reaches exactly ten, and naming the intended split for each — which scenarios move, and the sibling feature name — so the recorder executes a decision rather than inventing one.

#### [TASK_GRANULARITY] ADVISORY
- Location: plan.md § Parallelization "Within Group C"; task 8.2's "Add each new/extended delta to plan.md § Features"; plan.md § Features closing paragraph
- Issue: two small ordering and bookkeeping defects. (a) The Group C intra-order reads "3.1 → 3.3c → 3.2 · 3.3a · 3.3b → 3.4" — listing 3.3a and 3.3b as concurrent — while its own prose says "3.3a and 3.3b are file-disjoint … once `scan_storage_for` exists, so 3.3a lands the function first". A parallelization claim the same sentence contradicts. (b) Task 8.2 adds five new and two extended deltas and instructs the implementer to add each to § Features, but § Features' closing paragraph states fixed arithmetic — "Twenty-nine of the thirty-five deltas exist because …" — which task 8.2 does not tell anyone to update, and `speq plan validate` reports "Validated 35 delta spec(s)" today.
- Fix: In § Parallelization, rewrite the Group C order as "3.1 → 3.3c → 3.3a → 3.2 · 3.3b → 3.4". In task 8.2, add an explicit instruction to update § Features' closing count paragraph to the post-8.2 totals.

#### [TRACEABILITY_GAP] ADVISORY
- Location: decision-log.md § Review Findings opening line; the absence of round-2 `[plan-review]` entries
- Issue: decision-log.md records "Round 1 (`review/round-1.md`): 8 BLOCKERs, 12 ADVISORY, 0 Intent-Fidelity blockers. All 8 blockers resolved below" and carries a `[plan-review]` entry per round-1 blocker. Round 2's seven blockers have no such entries. Three appear only as tasks 8.1-8.3, one is captured by decision `[15]`, one by `[16]`, and the facade-visibility blocker is recorded nowhere at all. Round-2's own fix text asked for one ("Record the same in decision-log as a new `[plan-review]` entry"). The plan-review method requires a later round to judge each prior blocker "from the revised artifacts plus the `[plan-review]` entries in `decision-log.md`"; with the entries absent, this round had to reconstruct the resolution of each from the artifacts alone, which is how the facade blocker survived undetected into round 3.
- Fix: Add a `Round 2 (review/round-2.md): 7 BLOCKERs, 10 ADVISORY, 0 Intent-Fidelity blockers` line to decision-log.md § Review Findings and one `[plan-review]` entry per round-2 blocker, each with its Finding and Direction change, including the facade-visibility blocker once this round's fix settles it.

## Design Depth

#### [INFORMATION_LEAKAGE] ADVISORY
- Location: plan.md § Key interfaces rows `ResolvedConnectionConfig` and `scan_storage_for`; task 3.2; task 3.3a
- Issue: one security decision has two homes. "Does this CONNECTION carry key material strong enough for the envelope's guarantee to hold" is computed in `resolve_connection_config` (`adapter/mod.rs:203-220`) as a four-clause condition over `ConnectionCreds`, then transported to `scan_storage_for` (`adapter/pushdown/support.rs`) as an `Option`, where the refusal that the condition justifies is actually raised. The `Option` carries the outcome but not the reason, so the error message `scan_storage_for` must produce — "the no-auth + vending combination and both remedies" — is written from a fact the function cannot see, and a later change to the condition (adding a mode, tightening a field check) changes what that error means without touching the function that emits it. The § Design diagram makes this visible: the "sealed key present?" test appears at the `scan_storage_for` node although nothing there computes it. Per the design-philosophy diagnostic, exactly one module should own each significant design decision.
- Fix: In plan.md § Key interfaces, name the predicate as its own function beside `derive_sealed_storage_key` in `crates/lakehouse-engine/src/scan/sealed.rs`, taking `&ConnectionCreds` and returning `bool`, with a doc comment stating the design intent (which auth modes carry key material and why the envelope's guarantee is scoped to them). Have `resolve_connection_config` call it and `scan_storage_for` cite it in the refusal error's provenance. This shares a fix with the Requirement Quality `[COMPLETENESS_GAP]` on the same predicate.

[no further objection — axis checked: `ScanStorage` still makes "a name AND a credential" unrepresentable and exposes no secret or payload accessor, so the five spec-reading redaction sites fail to compile rather than silently disarming (decision `[6]`, and the compile-time guard decision `[13]` rests on, contingent on the visibility blocker above being settled); `scan/sealed.rs` is a genuinely deep module — HKDF parameters, nonce policy, AEAD, and the `base64(nonce ‖ ciphertext)` framing have exactly one home shared by both callers, and the § Sealed vended envelope section records why widening `Inline` to carry ciphertext was declined rather than leaving two payload semantics under one tag; `StorageCreds` makes the catalog-auth exclusion structural rather than conventional; decision `[12]`'s dependency direction holds — `lakehouse-catalog` gains a credential type and one selector and names no Exasol delivery mechanism, and the adapter→scan direction the sealing module introduces points inward, not outward]

## Prose Quality

#### [PROSE_BLOAT] ADVISORY
- Location: plan.md § Summary; § Design "**The contract permits exactly one remedy.**" and "**Sequencing.**"; § Impact "Breaking" paragraph 1; `vs-adapter/scan-spec-credential-reference/spec.md` § Background
- Issue: round-2's prose finding is unaddressed and the round-2-to-now revision made the worst offender worse. The guardrails set a hard two-sentence cap on § Summary and a 25-word cap on sentences; § Summary is now one paragraph of six sentences and roughly 340 words, several sentences past 60 words, with the sealed-envelope mechanism, the threat-model bound, and the no-auth refusal all folded into it. § Design still opens two arguments with bolded lead-ins where the guardrails ask for statement headings that form a scan path. § Impact "Breaking" paragraph 1 runs eleven sentences covering the role design, the `PUBLIC` rejection, the repository's current state, task 1.3b's residual, the no-fallback rule, and the vended binding — six ideas in one paragraph against the one-idea rule. `scan-spec-credential-reference` § Background is 26 bullets, several past 100 words in multi-clause sentences.
- Fix: Cut plan.md § Summary to two sentences — the replacement and the two issues closed — and move the envelope mechanism, the bound, and the refusal into § Design, which already states each. Rewrite the two § Design lead-ins as statement headings ("One remedy survives the pushdown contract", "The Azure deferral condition is discharged"). Split § Impact "Breaking" paragraph 1 into three paragraphs: the grant requirement, the grantee model, and the vended binding. Split every `scan-spec-credential-reference` § Background bullet longer than three lines into single-idea bullets under the 25-word sentence cap.
