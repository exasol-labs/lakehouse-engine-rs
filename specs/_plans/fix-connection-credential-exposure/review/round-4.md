# Plan Review Findings: fix-connection-credential-exposure (round 4)

## Summary
- Axes checked: 6/6
- Total findings: 7 (Blockers: 3, Advisory: 4)
- Intent Fidelity blockers: 0

## Round-3 Blocker Recheck

Each of round 3's eight BLOCKERs was re-checked against the revised artifacts, the live recorded library, and the source tree — not against `decision-log.md`'s claim of resolution.

1. **RESOLVED — [HIDDEN_DEPENDENCY] `pub(crate)` resolved pair across the `pub` facade entries.** Verified in the source: `run_scan_one` (`crates/lakehouse-engine/src/scan/mod.rs:253`), `run_raw_scan_with_session` (`scan/raw_scan.rs:48`) and `run_join_scan_with_session` (`scan/join_scan.rs:42`) are all `pub async fn`; `run_scan_dispatch` (`:270`) is private and `build_session_context` (`scan/object_store.rs:38`) is `pub(super)` — exactly as § Key interfaces, task 4.2 and § Test Disposition now state. The third site the finding did not name, `run_scan_one`, is threaded in all four places (§ Key interfaces row, task 4.1, task 4.2, § Test Disposition "The five threaded signatures"), and its injected `build_session` closure bound is named in task 4.2. `grep` over `crates/lakehouse-engine/tests/` returns exactly the twelve binaries § Test Disposition lists. `ResolvedScanStorage` is `pub` with `from_backends` as the second `pub` constructor, and `resolve_scan_storage` is re-described as "the only constructor that READS A CONNECTION" — no artifact still says "the ONLY constructor". Two residual defects raised as ADVISORY below, neither reopening the blocker.
2. **RESOLVED — [UNSTATED_ASSUMPTION] `EXECUTE ON SCRIPT` never checked.** Settled live and the "if it is not" branch taken consistently: § Impact "Not contained, and named", `scan-spec-credential-reference` Background bullet 23, task 1.3a's second probe, and task 7.4 all now say "a user who can query the virtual schema". No `EXECUTE` grant was added to task 7.1's template, § Migration, task 7.2, or spec clause 125 — correct under that branch.
3. **RESOLVED, with one evidence defect — [UNSTATED_ASSUMPTION] wrong profiling view.** § Impact line 232 no longer lists profiling/audit among the surfaces the envelope defeats; § Design row 4, task 1.2, task 7.4, § Manual Testing (now two rows, one per view and principal) and `scan-spec-credential-reference:11-12` (split into a profiling bullet and an audit bullet) all name `EXA_USER_PROFILE_LAST_DAY` for the least-privilege read and reserve `sys` for the audit read. The "both readable without …" construction is gone. The row-count arithmetic inside that evidence does not hold — ADVISORY below.
4. **NOT FULLY RESOLVED — [HIDDEN_DEPENDENCY] Group F's gate.** The unenforceable `speq-implement-pr` MUST is deleted and the pipeline is now described accurately (verified against `implement-pr/SKILL.md`). The replacement mechanism does not hold up: see BLOCKER 1 below.
5. **RESOLVED — [COMPLETENESS_GAP] task 8.1's two-set split.** Verified against the live recorded library: all ten SET-2 `file:line` citations are exact, and each names a bullet matching the wording task 8.1 predicts. `grep -rn '^\* Credentials MUST NOT appear in any returned SQL' specs/` returns exactly twelve hits — the ten siblings plus `pushdown-planning:28` and `pushdown-planning-join:22` — matching § Checklist's count. All nine SET-1 citations resolve to real Background text at the stated lines. All ten siblings carry a "SUPERSEDES this feature" bullet the replacement can be copied from.
6. **RESOLVED — [REQUIREMENT_CONFLICT] the three `DELTA:CHANGED` reproductions.** All four restorations are present in the delta files now, not deferred: `storage-backend-enum` carries the eighteen-regenerated / six-`empty_*`-unchanged split, `:123`'s "SHALL be EXTENDED rather than replaced" wording with its CLASS FOUR cross-reference, and a Background bullet declaring the `WHEN` widening; `pushdown-planning-cloud-credentials:102` reproduces recorded `:243` byte-for-byte including the trailing "SUPERSEDED for addressing alone"; `pushdown-format-neutral-resolution:28` and `scan-module-structure:17` both carry the "EXCEPT for the two edits … and no others" carve-out. Task 8.3 is now a read-and-confirm pass and § Parallelization says it gates nothing.
7. **RESOLVED IN THE PLAN, NOT IN EVERY ARTIFACT — [COMPLETENESS_GAP] the entropy gate.** The predicate has one named home, one caller, a truth table and four `adapter_tests.rs` cases; the widened password-content criterion is stated identically in § Design's diagram, § Sealed vended envelope, § Impact "Breaking" 2, § Migration, § Manual Testing, tasks 3.2 / 3.3a / 3.3c / 6.1 / 7.4, and `scan-spec-credential-reference` clauses 55-57 and 104. `install.sh:1425`'s template was verified to emit exactly the `{"warehouse","region","access_key","secret_key"}` shape the widening exists to admit, and `seed.rs:279` / `stack.rs:410` / `stack.rs:370` all check out. Two artifacts still carry the superseded mode-based criterion — BLOCKERS 2 and 3 below.
8. **RESOLVED — [REQUIREMENT_CONFLICT] the selection test named for the forbidden regression.** § Verification now names `scan_storage_for_selects_reference_when_vending_is_disabled_and_sealed_when_enabled` and `scan_storage_for_never_returns_inline`; both appear in task 3.3a. `grep` across every live plan artifact finds no `_inline_when_enabled` or `emitted_inline` construction outside `review/round-2.md` and `review/round-3.md`.

`speq plan validate fix-connection-credential-exposure` passes over all 35 deltas. The twelve round-3 ADVISORY findings are out of scope for this round and are not re-raised.

## Intent Fidelity

[no objection — axis checked: both stated intents remain in scope and traceable. #135's standing-credential case closes through the reference path and #378's vended residual through the sealed envelope, with task 1.5 tying #135's closure to the case the issue reports. The round-3 fixes narrowed no scope: the `EXECUTE ON SCRIPT` fix removed a false prerequisite rather than dropping a requirement, and the entropy-gate widening refuses strictly less than the criterion it replaced. The verification-discipline intent is honoured for every round-3-added Exasol claim — the `EXECUTE ON SCRIPT` probe names the privileges held, the query run, the `EXA_DBA_OBJ_PRIVS` confirmation and the outcome; the profiling probe names the view, the principal, the flush, the row count and the audit view's `42500` refusal. No claim added by the round-3 fixes rests on memory or documentation alone. One arithmetic defect inside the profiling evidence is raised on Requirement Quality, not here: it does not change the negative result the plan depends on]

## Feasibility

#### [HIDDEN_DEPENDENCY] BLOCKER
- Location: plan.md § Checklist last row; § Features paragraph at line 218; task 8 header (line 322); § Parallelization "Group E → Group F" note (line 398); open-questions.md line 10
- Issue: the replacement gate for round-3's Group F blocker is claimed to be self-enforcing and is not, in three separate ways — and all four locations state the strongest form of the claim. (a) **Nothing runs it.** `/speq:implement` Phase 5a is the only step that executes plan `## Verification > Checklist` commands, and it runs before `/speq:record`; `speq-implement-pr`'s pipeline (resolve target → blocker check → implement → bump version → commit evidence → test+record gate → PR) has no step after `/speq:record` at all, which is the same absence round-3's finding established. A row labelled "AFTER `/speq:record`" therefore has no executor. (b) **Where it IS executed it fails by construction.** Run at Phase 5a the recorded library is unmerged, so the grep returns the twelve matches this review confirmed live, against an expected "ZERO matches". Phase 5a branches on exit code, and `grep` exits 1 on zero matches — so the desired outcome reads as a failure and the undesired one as a pass. (c) **It covers twelve of nineteen strikes.** `grep -rn '^\* Credentials MUST NOT appear in any returned SQL' specs/` matches the ten SET-2 siblings plus `pushdown-planning:28` and `pushdown-planning-join:22` — verified. It matches none of the other seven SET-1 locations (`pushdown-planning:12`, `:26`; `pushdown-planning-join:47`; `unity-catalog-vended-credentials:7`; `work-unit-sharding:54`; `scan-execution:20`; `scan-execution-spec-reconstitution` § Background:9-13) and nothing in the CATCH-ALL. Yet § Features says "the gate holds whether or not the checklist was executed", § Parallelization says the grep "fails if any sentence 8.1's checklist struck still stands in `specs/`", task 8's header says "The grep is what makes the gate real: it does not depend on any agent reading any instruction", and open-questions.md repeats it. Round-3's blocker was that Group F's gate named an enforcement that does not exist; the replacement names an enforcement that exists but does less than a third of what all four locations claim.
- Fix: In plan.md § Checklist, remove the "AFTER `/speq:record`" row from the table `/speq:implement` Phase 5a executes and place the sweep in a new § Post-Record Verification subsection instead, stating that it is run by the operator or the orchestrating session after `/speq:record` returns and before the PR is marked ready. Give that subsection one command per struck sentence class, not one: the current pattern for the twelve, plus a fixed-string grep for each of the seven SET-1 sentences task 8.1 strikes, each with its expected zero-match outcome and the note that `grep` exits 1 on success. Then correct the three overclaims — in § Features line 218, task 8's header, and § Parallelization's Group E → Group F note — to state exactly what the sweep covers ("the nineteen sentences task 8.1 strikes; a CATCH-ALL entry has no automated sweep and depends on the checklist being executed") and delete "so the gate holds whether or not the checklist was executed" and "it does not depend on any agent reading any instruction". Mirror the corrected wording in open-questions.md line 10. Record the correction as a `[plan-review]` entry in decision-log.md.

[no further objection — axis checked: the five threaded signatures and their visibilities were verified in the source and match plan.md exactly; the twelve external binaries were enumerated by `grep` and match § Test Disposition; `install.sh:1425`, `seed.rs:279`, `stack.rs:370` and `stack.rs:410` all carry the text the plan cites; `/speq:spec-merge` does read plan.md under "Load Plan Context", so § Features' checklist instruction is at least reachable by `recorder-agent`, which is the mechanism round 3's own Fix prescribed]

## Requirement Quality

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: `specs/_plans/fix-connection-credential-exposure/pr-description-draft.md` lines 3, 17 and 22
- Issue: the artifact that carries this change's breaking contract to operators still states the criterion decision `[14]` was amended away from. Line 17 is headed "**No-auth + vending is refused at plan time.**" and asserts "That one combination now fails the pushdown request with a named error stating both remedies (configure catalog authentication, or disable `use_vended_credentials`)"; line 22 repeats "No-auth + vending no longer plans (see above)". Under the widened gate none of that is true: the predicate tests the PASSWORD, so a no-auth catalog whose CONNECTION carries a non-empty `secret_key` — the exact shape `deploy/scripts/install.sh:1425`'s own template emits, verified live in this review — PLANS and seals. What is refused is a password carrying none of the six secret-bearing fields, regardless of catalog-auth mode. The remedy list is also short one entry: `scan-spec-credential-reference` clause 106 requires the error to name "configure catalog authentication (or supply the CONNECTION's own storage secret), or disable `use_vended_credentials`". Line 3 compounds it by dating the draft to "the plan state after the round-2 revision". The round-3 `[plan-review]` entry enumerates where the widened criterion was applied and does not name this file, so the sweep simply missed it — and this is the one artifact whose text reaches a human as the description of what ships.
- Fix: In `pr-description-draft.md`, rewrite line 17's heading to "**Vending on a CONNECTION whose password carries no secret material is refused at plan time.**" and its body to state the password-content criterion (refused only when `token`, `client_secret`, `secret_key`, `session_token`, `account_key` and `sas_token` are all empty or absent; a non-empty `access_key` alone does not satisfy the gate), name all three remedies, and state that the installer's own default no-auth template shape is admitted rather than refused. Replace line 22's bullet with "Vending on a CONNECTION password carrying no secret material no longer plans (see above)". Update line 3 to name the current plan state (round-3 revision, widened entropy gate) instead of the round-2 revision.

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: `decision-log.md` entry `[14]`, the `**Decision:**` bullet (line 119) and the `**Rationale:**` bullet (line 121); `/speq:spec-merge` § "Promote ADRs to Permanent Decision Log"
- Issue: the amendment to `[14]` was made as a blockquote header note, but the field the merge tool reads is the `Decision` bullet — so the permanent decision library will record the superseded criterion as the accepted one. `[14]` carries `Promotes to ADR: yes`, and `/speq:spec-merge`'s promotion mapping fills the ADR's **Decision** field "from the entry's Decision bullet" and **Context** "from the entry's Rationale + Alternatives". Neither field reads a blockquote. `[14]`'s Decision bullet still reads "The key exists iff the catalog-auth mode carries genuine secret material (bearer `token`, OAuth2 `client_secret`, or SigV4 `access_key`/`secret_key`); for a no-auth catalog with vending enabled, `scan_storage_for` REFUSES to plan", and its Rationale still reads "every auth mode except no-auth carries genuine secret material in that password, and the one mode that does not is refused for vending". The round-3 `[plan-review]` entry that widened the criterion ALSO carries `Promotes to ADR: yes`, so one merge produces two ADR blocks in `specs/_decision/NNN-fix-connection-credential-exposure.md` stating contradictory gate criteria, with the architectural decision of record carrying the wrong one — the permanent-library-contradicts-shipped-behaviour defect this plan's own § Consequences row exists to avoid, on the same security gate. The plan's convention of leaving superseded entries unedited works for `[2]` and `[7]`, whose whole decision was replaced by a later entry a reader will find; it does not work here, because `[14]` remains the standing decision and only one clause inside it changed.
- Fix: In `decision-log.md` entry `[14]`, edit the `Decision` bullet so the gate clause reads "The key exists iff the CONNECTION PASSWORD carries secret material — any non-empty `token`, `client_secret`, `secret_key`, `session_token`, `account_key`, or `sas_token`, a non-empty `access_key` alone excluded; when it carries none and vending is enabled, `scan_storage_for` REFUSES to plan with a named `UdfError` stating the combination and all three remedies." Edit the Rationale sentence "every auth mode except no-auth carries genuine secret material in that password, and the one mode that does not is refused for vending" to the password-content form, keeping the existing blockquote header note as the history pointer. Then set the round-3 `[plan-review]` entry "The entropy gate had no owner, no test, and a criterion that refused the installer's own default" to `Promotes to ADR: no`, so the merge emits one ADR for this gate rather than two that disagree.

#### [UNSTATED_ASSUMPTION] ADVISORY
- Location: plan.md § Design row "Is `EXPLAIN VIRTUAL` the only surface?" (line 34); task 1.2 (line 277); § Manual Testing row "Profiling never carries the rewritten pushdown SQL" (line 468); `decision-log.md` `[16]` (line 159) and the round-3 `[plan-review]` entry on the profiling evidence (line 226); `vs-adapter/scan-spec-credential-reference/spec.md:11`
- Issue: the probe record the round-3 fix rewrote into five places does not add up. Each states "selected 9 rows (one per execution-graph part: `COMPILE / EXECUTE`, `PUSHDOWN`, `INSERT`, two `SCAN`, two `GROUP BY`)". That enumeration names seven parts, or eight if `COMPILE / EXECUTE` is two. Task 1.2 orders an implementer to "Re-run BOTH halves … and record both in the verification report", so the row count is a comparison target; a re-run producing seven or eight rows cannot be judged against a record whose own parts and count disagree. The negative result the plan depends on — that every `SQL_TEXT` is the user's own literal statement — is unaffected, which is why this is advisory rather than blocking, but it is the second consecutive round in which this one probe's write-up carried an internal inconsistency.
- Fix: In plan.md § Design row 4, task 1.2, and § Manual Testing's profiling row, and in `decision-log.md` `[16]` and the round-3 entry, and in `scan-spec-credential-reference:11`, either correct the part enumeration so it sums to nine or drop the parenthetical count and state only "one row per execution-graph part, every row's `SQL_TEXT` byte-identical to the user's own literal statement". Change task 1.2's re-run instruction to record the observed part names rather than to match a fixed row count.

#### [TRACEABILITY_GAP] ADVISORY
- Location: plan.md § Verification row "scan-spec-credential-reference: One named predicate decides whether the sealed envelope's guarantee can hold" (line 417); open-questions.md lines 5 and 14
- Issue: two bookkeeping gaps the round-3 revision left behind. (a) That § Verification row lists `crates/lakehouse-engine/src/scan/sealed_tests.rs` in its Test Location column but names only the four `sealing_key_*` cases, all of which task 3.2 places in `adapter_tests.rs`. Task 3.3c's predicate truth table — the test that exercises the predicate itself rather than the resolved config's reaction to it — has no name anywhere in the traceability table, so `/speq:implement`'s Phase 5b scenario-coverage audit cannot check it. (b) open-questions.md still opens "Round 2 review found 7 BLOCKERs (0 Intent-Fidelity) after round 1's 8 were fixed" and its closing advisory list still points at `round-2.md`. Round 3's twelve advisories — the set this project's convention carries into the PR for the human — are not listed anywhere a PR reader will find them.
- Fix: In plan.md § Verification, add the predicate truth-table test to that row's Test Name column under a concrete name (e.g. `connection_password_carries_key_material_truth_table`) and add the same name to task 3.3c's test list. In open-questions.md, replace the round-2 header line with a round-3 line naming its 8 BLOCKERs and 12 ADVISORY findings and `review/round-3.md`, and rewrite line 14's advisory summary from round-3's advisory set with a pointer to `review/round-3.md`.

## Task Breakdown

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: plan.md task 4.2 final sentence; § Test Disposition row "The twelve external test binaries that call a `pub` facade entry" (line 356)
- Issue: the compile-time pin the round-3 visibility fix promises cannot be written as instructed, and one call-site change is understated. Task 4.2 says "Add `ResolvedScanStorage`, `ResolvedScanStorage::from_backends`, and `run_join_scan_with_session` to `scan_surface_probe_tests.rs`'s `use` list, so a later narrowing of the type fails to compile there", and § Test Disposition says `scan_facade_reachability.rs` "additionally gains the two new names in its reachability list". Both files are, by their own headers, pure `use` lists — verified — and Rust cannot import an inherent associated function: `use lakehouse_engine::scan::ResolvedScanStorage::from_backends;` does not compile. Only the type can be pinned that way; pinning the constructor needs a reference in a function body or a typed binding. `run_join_scan_with_session` is also already present in both `use` lists (`scan_surface_probe_tests.rs` line 23, `scan_facade_reachability.rs` line 19), so that third instruction is a no-op. Separately, the twelve-binaries row says "each call site gains one argument", but `scan_batch_loop.rs` and `scan_footer_refetch_observable.rs` call `run_scan_one` with an injected `build_session` closure (`impl Fn(&ScanSpec, u64)`), so those two also gain a widened closure signature — a change task 4.2 mentions and § Test Disposition does not.
- Fix: In plan.md task 4.2, replace the `use`-list instruction with "add `ResolvedScanStorage` to `scan_surface_probe_tests.rs`'s `use` list, and pin `from_backends` with a typed binding in a small `#[test]` in the same file (`let _: ResolvedScanStorage = ResolvedScanStorage::from_backends(…, None);`), because an inherent associated function cannot be imported"; drop `run_join_scan_with_session`, which both probes already name. Apply the same correction to § Test Disposition's `scan_facade_reachability.rs` sentence. In the twelve-binaries row, add that `scan_batch_loop.rs` and `scan_footer_refetch_observable.rs` additionally widen their injected `build_session` closure to take the resolved pair.

[no further objection — axis checked: Group F's intra-order (8.2 · 8.3 before 8.1) is stated with a reason that matches 8.1's catch-all; task 8.3 is a read-and-confirm pass in both its own text and § Parallelization; task 8.1's two named sets total nineteen locations that all resolve against the live library; the Group C intra-order and § Features count paragraph remain as round 3 left them, which that round raised as ADVISORY and this round does not reopen]

## Design Depth

[no objection — axis checked: the round-3 fixes reduced complexity rather than adding it. The entropy predicate now has exactly one home (`scan/sealed.rs`, beside the derivation it gates) and exactly one caller (`resolve_connection_config`), with `scan_storage_for` citing it by name so the reason travels with the `Option`, which closes round-3's `[INFORMATION_LEAKAGE]` advisory on the same predicate. Publishing `ResolvedScanStorage` did not weaken the compile-time guard decision `[13]` rests on: `ScanStorage` still exposes no `secret_values()` and no payload accessor, and `from_backends` reads no wire value and contacts no CONNECTION, so it cannot become a production resolution path. Adding `scan/sealed.rs` and `scan/storage_ref.rs` does not falsify recorded `datafusion-scan/scan-module-structure`, whose Background states "the exact submodule list is a design decision recorded in the plan, not a normative contract" and whose new-submodule rule (private `mod`, public items re-exported flat) both new files follow. The predicate's home in the scan module rather than the adapter is round-3's own prescribed fix and is not re-litigated]

## Prose Quality

#### [PROSE_UNCLEAR] ADVISORY
- Location: plan.md § Impact "Fixed, with a bounded guarantee" paragraph (line 232)
- Issue: the round-3 correction was spliced in ahead of the clause it was meant to follow, and the result attaches a justification to the wrong claim. The paragraph now reads "… it does not claim to withstand offline cryptanalysis of the ciphertext against a low-entropy password. Profiling and audit `SQL_TEXT` were verified live never to carry the rewritten pushdown SQL, so they are not a surface the envelope needs to defeat — acceptable because the vended values are short-lived and prefix-scoped, and because the key material is exactly what `ACCESS ON CONNECTION` already reveals." The "acceptable because" clause justifies the BOUND on the guarantee, not the profiling negative; as written it reads as an argument for why profiling not being a surface is acceptable, which is not a claim anyone needs justified. § Impact is the section the writing guardrails route into the PR body.
- Fix: In plan.md § Impact, split that sentence: end the bound sentence with "… against a low-entropy password — acceptable because the vended values are short-lived and prefix-scoped, and because the key material is exactly what `ACCESS ON CONNECTION` already reveals." Then start a new sentence: "Profiling and audit `SQL_TEXT` were verified live never to carry the rewritten pushdown SQL, so they are not a surface the envelope needs to defeat."

[no further objection — axis checked: round 3's `[PROSE_BLOAT]` finding on § Summary, the § Design bolded lead-ins, § Impact "Breaking" paragraph 1, and `scan-spec-credential-reference` § Background stands unfixed by design and is not re-raised. The round-3 revision did not lengthen § Summary further, and the newly added prose — § Sealed vended envelope's two criterion paragraphs, § Impact "Breaking" paragraph 2, task 8's header — leads with its conclusion in each case]
