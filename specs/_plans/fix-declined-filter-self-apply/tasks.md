# Tasks: fix-declined-filter-self-apply

## Phase 2: Implementation (Group A)
- [x] 2.1 Add `pub(super) fn datafusion_renderable` to `pushdown/support.rs` + unit tests (rendering predicate, SECOND(ts,3) arity decline, trivially-true TRUE literal, strip_table_alias invariance) + `crates/vs-expression` `second_with_precision_declines_for_datafusion_renders_for_exasol`
- [x] 2.2 Add `CLAUDE.md` subsection recording the corrected protocol fact (no Exasol-side re-check/fallback once a capability is advertised) — plain fact, no issue number

## Phase 2: Implementation (Group B, sequential — all edit joins/sql_builders.rs)
- [x] 2.3 `render_broadcast_join`: distinguish absent/trivially-true filter from declined; decline → `Ok(None)` fallthrough to N-scan; update doc comment; unit test `broadcast_declines_on_unrenderable_filter_stays_eligible_when_absent`
- [x] 2.4 [expert] Reclassify declined side-local conjuncts as residual at `build_n_scan_join_sql`'s two render sites only (`side_local_filter`/`cross_side_residual_filter` bodies UNCHANGED); add `renderable_only`/`declined_only` screens in `joins/rendering.rs`; three-outcome gate (Some/trivially-true/unrenderable→`join_render_decline`) gated on non-suppressing `render_expression_qualified`; update doc comments per plan; unit tests: `declined_side_local_conjunct_partitions_to_residual`, its rendering complement, `join_side_pruning_input_unchanged_when_df_render_declines`, `trivially_true_residual_emits_no_outer_where_and_does_not_error`
- [x] 2.5 [expert] Self-apply declined single-table filter via qualified wrapper: compute decline once in `handle_pushdown`, thread original (non-rewritten) filter as new param into `build_dispatch_sql` → `qualified_single_table_fallback_pushdown`/`build_qualified_single_table_fallback_sql`; route decline ahead of routing classifier; render via `render_df_filter_qualified` against `LHS_T0`; same three-outcome gate; fan-out spec's `filter` MUST be `None` on decline route; fix `SELECT *` projection override (full `col_types` row) per plan; full call-site census (12 sites, all pass `None` for new params except `pushdown/mod.rs:239`); update mirrors at `pushdown/mod.rs:871,907,941,972,1002,1043` and `topn.rs:495-505` (reproduce decline branch or doc why not); unit tests: `single_table_wrapper_renders_declined_predicate_in_exasol_dialect`, `declined_filter_routes_every_dispatch_shape_to_qualified_wrapper`, `trivially_true_filter_omitted_without_wrapper`, `declined_filter_with_absent_select_list_projects_full_row`, `iceberg_pruning_input_unchanged_when_df_render_declines`, `nested_like_decline_routes_to_wrapper_where`, `declined_like_on_integer_column_routes_to_wrapper_where`, `declined_like_on_unresolvable_column_routes_to_wrapper_where`

## Phase 2: Implementation (Group C)
- [x] 2.6 Add golden fixtures `filterless_request_emits_unchanged_sql_at_all_three_sites` and `rendering_filter_emits_unchanged_wrapper_free_scan` to `dispatch_golden.rs`

## Phase 2: Implementation (Group D)
- [x] 2.7 E2E single-table coverage in `e2e_capability_test.rs` (SECOND arity 0-row, LIKE-decimal 3-row, INSTR 3-arg, aggregate-before-filter, ORDER BY/LIMIT-before-filter, `e2e_declined_filter_select_star_returns_full_row_shape`); assert EXPLAIN VIRTUAL shows wrapper WHERE not scan-spec filter
- [x] 2.8 E2E broadcast coverage in `e2e_join_test.rs`: below-threshold inner equi-join with declined side-local conjunct → correct rows + N-scan wrapper plan (not broadcast block)
- [x] 2.9 E2E N-scan per-leg coverage in `e2e_join_test.rs`: above-threshold/3-table join with rendering + declined side-local conjuncts → correct rows, declined conjunct in outer WHERE, rendering conjunct still in its leg's scan-spec filter
- [x] 2.10 E2E terminal-case coverage: both-dialects-unrenderable predicate (advertised FN_CAST to INTERVAL/GEOMETRY/HASHTYPE/TIMESTAMP WITH LOCAL TIME ZONE) → clean error, no rows, no credential leakage; if unreachable via Exasol's parser, record finding in verification-report.md and cover via unit test on wrapper's error path instead

## Phase 2: Implementation (Group E)
- [x] 2.11 Correct every doc comment asserting the disproven backstop (census by symbol): `pushdown/mod.rs` module header, `support.rs` `like_subject_type_guard`/`string_function_arg_type_guard`/rewrite-primitive doc, `joins/rendering.rs` `render_df_filter_qualified`/`side_local_filter`/`cross_side_residual_filter`, `joins/sql_builders.rs` `RenderedJoinPushdown::filter` field doc, `adapter/capabilities.rs` FN_CAST comment, `crates/vs-expression/src/lib.rs` `render_df_filter_safe`/`render_df_filter_exasol_safe`. No behavior change.

## Phase 4: Review Fixes
- [x] 4.1 [expert] Extract the duplicated three-outcome self-apply gate in `joins/sql_builders.rs` into one private `render_self_applied_where(tree, alias_of, subject)` helper beside `join_render_decline`, documenting why the error is keyed on the non-suppressing renderer; replace both `match` blocks (`build_n_scan_join_sql`, `build_qualified_single_table_fallback_sql`) with a call to it and delete both inline comment copies, keeping both error message texts byte-identical
- [x] 4.2 [expert] Delete the `projection_override` parameter from `qualified_single_table_fallback_pushdown` and derive the full-base-row projection inside it from `col_types` when `declined_filter.is_some()`; fold its doc paragraph into the `declined_filter` paragraph; drop the `full_base_row` construction and the trailing argument at all five call sites in `pushdown/mod.rs` and `joins/sql_builders.rs`
- [x] 4.3 In `render_self_applied_where` (`crates/lakehouse-engine/src/adapter/pushdown/joins/sql_builders.rs`), append the offending predicate tree JSON (`serde_json::to_string(tree).unwrap_or_default()`) to the both-dialects-unrenderable error message; leave `join_render_decline`'s own template/prefix and all existing assertions on it unchanged; extend `single_table_wrapper_errors_when_declined_predicate_renders_in_neither_dialect` to assert the message contains `no_such_node_type_in_either_dialect`
- [x] 4.4 In `crates/lakehouse-engine/tests/e2e_capability_test.rs`, tighten `e2e_both_dialects_unrenderable_predicate_errors_without_rows` to a single assertion `msg.to_ascii_lowercase().contains("neither dialect")`, deleting the `HASHTYPE` and `applied nowhere` OR-alternatives; verify against the live Docker Exasol stack and adjust the substring if Exasol's error wrapping changes it
- [x] 4.5 In `crates/lakehouse-engine/tests/e2e_join_test.rs`, add `e2e_broadcast_declined_filter_excludes_rows` (same below-threshold two-table join as `broadcast_declined_filter_join_query` but with `SECOND(o.O_ORDERDATE, 3) = 1`, false for every seeded row) asserting zero rows and `has_n_scan_wrapper(&pushed, 2)`; also add `assert!(pushed.contains("SECOND("), ...)` to `e2e_broadcast_declined_filter_falls_back_to_n_scan_and_filters`
- [x] 4.6 In `crates/vs-expression/src/lib.rs`, rewrite the comment on the CAST test (~2243-2248) to state the translator declines these targets with no Exasol-side re-check backstop, and rename `cast_to_unsupported_target_falls_back` to `cast_to_unsupported_target_declines`, updating any in-file reference
- [x] 4.7 In `crates/vs-expression/src/lib.rs`, rewrite the comment on `exasol_df_filter_suppresses_trivially_true` (~4983-4988) to state a trivially-true filter is a correct no-op omission, distinct from a genuine decline (which the caller must self-apply since Exasol never re-applies an advertised capability); remove "correctness backstop" wording
- [x] 4.8 In `crates/lakehouse-engine/src/adapter/pushdown/support.rs`, rewrite the `pushdown_translates_or_omits_predicate` scenario header to "Filter predicate is pushed into the scan spec (translatable) or kept out of it (untranslatable) — never mistranslated, never omitted from the query", rewrite the `render_df_filter_safe`-returns-`None` line to state it keeps the predicate out of the SCAN SPEC only, and rewrite the trailing line to state the scan SQL stays valid without a scan-spec filter while the adapter self-applies the predicate, cross-referencing `declined_filter_routes_every_dispatch_shape_to_qualified_wrapper`; drop "correctness backstop"; no assertion changes
- [x] 4.9 In `crates/lakehouse-engine/src/adapter/pushdown/support.rs`, extend `datafusion_renderable_answer_unchanged_by_strip_table_alias` with a second fixture that RENDERS and carries `tableAlias` (predicate_greater on qualified column `O`/`TS`), asserting `datafusion_renderable` is `true` both before and after `strip_table_alias` and that the two answers are equal
- [x] 4.10 In `crates/lakehouse-engine/src/adapter/capabilities.rs`, replace the FN_CAST comment (~61-64) with a present-tense statement of CAST's advertised faithful target set and that a target renderable under neither dialect fails the query because an advertised capability is never re-applied by Exasol; remove the "now … rather than" change narrative and the test-name reference

## Phase 5: Verification
- [x] 5.1 Build: `make cross-musl-udf-build` — exit 0
- [x] 5.2 Test: `cargo test` — 0 failures
- [x] 5.3 Test (e2e): `make test-e2e` (manual docker compose stack) — 0 failures, 0 skips (200 tests across 8 binaries)
- [x] 5.4 Lint: `cargo clippy --all-targets` — 0 errors/warnings
- [x] 5.5 Format: `cargo fmt` — no changes
- [x] 5.6 Golden-SQL stability: `git diff -- crates/lakehouse-engine/src/adapter/pushdown/dispatch_golden.rs` — no change to any rendering-filter fixture
- [x] 5.7 Manual testing per plan's § Manual Testing table (9 rows incl. CLAUDE.md grep and TEMP_DB_RAM_PEAK decline-path memory cost) — all confirmed live against Docker Exasol; see verification-report.md
