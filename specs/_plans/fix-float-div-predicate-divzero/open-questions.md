# Open Questions: fix-float-div-predicate-divzero

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan fix-float-div-predicate-divzero` locally, or re-run `/speq:plan-pr fix-float-div-predicate-divzero` after commenting.

- [ ] Task 1.2's (and task 4.5's) guarded-division measurement query uses a divisor that is identically zero on every row (`L_LINENUMBER - L_LINENUMBER`), so the guard conjunct `L_LINENUMBER <> 0` excludes nothing and the run only re-proves task 1.1's unguarded result. Round 2's proposed fix: swap the divisor to `(L_LINENUMBER - 1)` (zero on 10 of the 20 fixture rows, non-zero on the other 10, giving a 0.5 true-ratio for the left conjunct, above DataFusion's 0.2 `PRE_SELECTION_THRESHOLD`) so the guarded outcome is actually observable, in both conjunct orders, with guarded and unguarded row counts recorded separately. Confirm this divisor swap (or propose another) before task 1.2/4.5 run.
- [ ] Task 4.10's "no leftover `(#TODO-…)` token" grep gate matches its own definition text and other non-citation mentions (in `plan.md`, `decision-log.md`, and `review/round-1.md`) and can never pass as written — 18 matches today, and at least 5 remain even after every real citation site is replaced. Round 2's proposed fix: scope the grep to `--include='spec.md'` under the plan directory only (the two spec deltas the recorder actually merges), extend the replacement list to include `decision-log.md` alongside `plan.md` and the two deltas, and state that `review/` files are historical and never edited. Confirm this narrower gate scope before task 4.10 is executed at implementation time.
