# Tasks: fix-nested-aggregate-pushdown

Scope for this run: spike only (Task 1). Stop after capture + write-up so the
fix approach can be aligned on before Tasks 2-6 are implemented.

## Phase 2: Implementation (Group A)
- [x] 1. Diagnostic spike — capture the real `pushdownRequest` JSON for the Q7 nested-aggregate shape. [expert]

## Deferred (not started this run — pending alignment on fix approach)
- [ ] 2. Root-cause write-up + fix-family selection.
- [ ] 3. Implement the behavioral fix. [expert]
- [ ] 4. Host unit test for the composed-request guard.
- [ ] 5. E2E regression test for the Q7 nested-aggregate shape.
- [ ] 6. Verification gate.
