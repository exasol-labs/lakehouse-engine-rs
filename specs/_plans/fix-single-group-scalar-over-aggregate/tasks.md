# Tasks: fix-single-group-scalar-over-aggregate

## Phase 1: Correctness floor (Group A)
- [x] 1.1 Add a depth-insensitive `function_aggregate` subtree probe to `project_columns` (`adapter/pushdown/support.rs`), applied to each select-list item before the node-type match, setting `needs_full_fallback = true` on a hit. Verify the top-level-aggregate outcome is unchanged and that all eighteen `testdata/dispatch_golden/` fixtures stay byte-identical. [expert]
- [x] 1.2 Add unit tests in `support_tests.rs` proving the widening fires for a nested aggregate under each pushable node family — `function_scalar`, `function_scalar_cast`, `function_scalar_case`, an arithmetic node, and a predicate node — and does NOT fire for a scalar item with no nested aggregate.
- [x] 1.3 Add a `dispatch_golden` fixture for a single-table request whose select list is `ROUND(SUM(col), 2)` and which the decomposition declines (inner `DISTINCT`), asserting the qualified-wrapper SQL rather than a per-shard projection.

## Phase 2: Shared decomposition owner (Group B)
- [x] 2.1 Create `adapter/pushdown/scalar_over_agg.rs` plus `scalar_over_agg_tests.rs`, moving `sentinelize_aggregates`, `sentinel_column_node`, `agg_sentinel_token`, `classify_scalar_over_aggregate`, `render_scalar_over_merge`, and `fold_aggregate_plan` out of `grouped_agg.rs` at `pub(super)` visibility. Change `render_scalar_over_merge` to take the merged `PARTIAL_*` expressions as a parameter instead of calling `merge_select_items`, so the new module names neither planner. Repoint `grouped_agg.rs` and register the submodule in `mod.rs`. [expert]
- [x] 2.2 Prove the move changed no output: `grouped_aggregate.sql`, `grouped_all_agg_kinds.sql`, and `group_by_fallback.sql` must match byte-for-byte, and every existing grouped scalar-over-aggregate unit test must pass unedited.

## Phase 3: Single-group decomposition (Group C)
- [x] 3.1 Add `SingleGroupItem::ScalarOverAggregate { select_index, node, declared_type }` and extend `detect_aggregates` to classify such an item via `scalar_over_agg::classify_scalar_over_aggregate`, declining the whole detection when it declines. Keep the `groupBy` gate, add no `aggregationType` check, and keep the function's signature. [expert]
- [x] 3.2 Widen `ordinary_plans` to fold every nested aggregate into the returned list, deduplicated by `AggregatePlan` equality through `scalar_over_agg::fold_aggregate_plan`. Signature unchanged; output unchanged for every select list with no nested aggregate. [expert]
- [x] 3.3 Add `single_group_plan_types(pushdown_req, items)`, aligned 1:1 with `ordinary_plans(items)`: each slot takes the `selectListDataTypes` entry of a top-level occurrence at its own ordinal, and the default otherwise. Derive the alignment from the folded plan list rather than re-running the fold.

## Phase 4: Single-group decomposition (Group D)
- [x] 4.1 Change `build_scan_driving_sql` / `build_aggregate_scan_sql` (`support.rs`) so the outer merge SELECT items arrive ready-to-emit from the caller and `aggregate_types` means per-plan `EMITS` types. Assemble the merge SELECT in `mod.rs`'s `SingleGroupAgg` arm: a bare aggregate through the existing cast-merge helper, a scalar-over-aggregate item through `render_scalar_over_merge`, each cast to its own item's declared type, in `selectList` order. [expert]
- [x] 4.2 Add the `ScalarOverAggregate` arm to `empty_agg_sql` (`empty_result.rs`): the item's zero-row value cast to its own declared type through the shared declared-type CAST helper, keeping the one-row single-group empty shape.

## Phase 5: Single-group decomposition (Group E)
- [x] 5.1 Update the exhaustive-match and helper sites the new variant reaches — `single_group_agg_tests.rs`'s `agg_of`/`distinct_of` helpers, `empty_result.rs`, `support.rs`'s distinct-fan-out builders — and the external callers of the changed `build_scan_driving_sql` signature: `tests/scan_plan_shape.rs`. Full census in plan.md's *Call-Site Census*. Also delete the dead `aggregate_exasol_types` (`support.rs`) once its callers are repointed by 3.3/4.1, per plan.md's Dead Code Removal table.

## Phase 6: Verification fixtures & unit tests (Group F)
- [x] 6.1 `dispatch_golden` fixtures for the new decomposed shapes: a lone `ROUND(SUM(col), 2)`, a deduplicating `COUNT(*)` + `ROUND(SUM(col) / COUNT(*), 2)`, an interleaved list, a scalar-wrapped `VARIANCE`, and the empty-result one-row shape.
- [x] 6.2 Unit tests per new-feature scenario in `single_group_agg_tests.rs`, `scalar_over_agg_tests.rs`, `empty_result_tests.rs`, and `request_shape_tests.rs`.

## Phase 7: E2E verification (Group G)
- [x] 7.1 E2E: the issue #194 repro (`ROUND(SUM(l_quantity), 2)` over TPC-H `LINEITEM`) and the issue #188 repro (`ROUND(VARIANCE(c_acctbal), 4)` over `CUSTOMER`), each asserted against the native-schema oracle, plus regression assertions that bare `VARIANCE` and `ROUND(VAR_SAMP(...), 4)` still return their current values.
- [x] 7.2 E2E for the floor: a scalar-wrapped aggregate over a join (the shape `carries_aggregation_clause` does not recognise), and a scalar-wrapped `COUNT(DISTINCT)` — both must return the single-node result, not a per-shard row set.
- [x] 7.3 `EXPLAIN VIRTUAL` assertion that the #194 query now emits a non-empty `"aggregates"` and `"projection":[]`, and no `"expr"` containing an aggregate.

## Phase 8: Verification
- [x] 8.1 Run `cargo test` (host unit + integration, excluding e2e) — 1593 passed, 0 failed
- [x] 8.2 Run `cargo clippy --all-targets` and `cargo fmt --check` — both clean
- [x] 8.3 Run `make test-e2e` — 296 passed, 0 failed, live Docker Exasol stack

## Phase 9: Review Fixes (Expert)
- [x] 9.1 Split the two defaults in `single_group_plan_types` (`single_group_agg.rs`): a slot reached only through a nested `ScalarOverAggregate` seeds `"DOUBLE PRECISION"` (matching `scalar_over_agg::fold_aggregate_plan` and the grouped planner), while `"VARCHAR(2000000)"` stays only as the fallback for a top-level `Aggregate` with no `selectListDataTypes` entry. Update the three affected `single_group_plan_types` tests and add a regression test that `ROUND(MIN(<expr>), 2)` emits a numeric `"PARTIAL_min_0"` EMITS type. All twenty-four `testdata/dispatch_golden/` fixtures stay byte-identical. [expert]
- [x] 9.2 Break the `single_group_agg` ↔ `grouped_agg` planner cycle: move `merge_select_items`, `cast_merge_items`, `stddev_of`/`StatMergeFragments`, and `parse_agg_item` with `EXPR_CAPABLE_AGG_KINDS`, `STAT_AGG_KINDS`, `arg_column_or_expr`, `column_from_first_arg` into `scalar_over_agg.rs`; repoint both planners plus `support_tests.rs`, so neither planner names the other and the shared module names neither. Every golden fixture byte-identical, every existing unit test unedited. [expert]
- [x] 9.3 Add `support::declared_select_type(pushdown_req, select_index)` as the single owner of the `selectListDataTypes` → `exasol_type_from_json` → `"VARCHAR(2000000)"` lookup, and replace the copies in `single_group_agg.rs` (`detect_aggregates`, `single_group_plan_types`), `empty_result.rs` (`empty_agg_sql`), `grouped_agg.rs`, and the inline lookup in `support.rs`. All twenty-four golden fixtures byte-identical. [expert]
- [x] 9.4 Replace the `request_limit`/`aggregate_types`/`merge_select` parameter bundle of `build_scan_driving_sql` and `build_aggregate_scan_sql` (`support.rs`) with a single `Option<&AggregateMergeInputs>`, re-exported on the `pushdown` façade, so "row scans read neither" is unrepresentable and an aggregate spec with absent inputs or an empty merge SELECT can never render `SELECT  FROM`. Update every call site (`mod.rs`, `joins/sql_builders.rs`, `support_tests.rs`, `test_support_tests.rs`, `topn_tests.rs`, `tests/scan_plan_shape.rs`) and compile with `cargo test --no-run --all-targets`. [expert]

## Phase 10: Review Fixes (Standard)
- [x] 10.1 Rewrite the doc comment on `ensure_ground_truth_lineitem_table` (`tests/e2e_join_test.rs`, lines 883-899) to drop the claim that `detect_group_by_aggregates` declines a scalar-over-aggregate select list or hard-fails with "Expected number of columns is 5 but pushdown query has 6"; state instead that the ground truth must be computed by Exasol over native data so it is an oracle independent of the pushdown path under test.
- [x] 10.2 Rewrite the comment at `adapter/pushdown/mod.rs` lines 474-477 to state the per-plan declared-type invariant without naming the deleted `aggregate_exasol_types`.
- [x] 10.3 Change the doc comment on `empty_grouped_sql` (`adapter/pushdown/empty_result.rs`) to name `group_key_exasol_types` and `GroupedAggregateDetection::plan_types` as the two type sources, dropping `aggregate_exasol_types`.
- [x] 10.4 In `adapter/pushdown/grouped_agg.rs`, delete the `plan_types` doc-comment sentence naming the deleted `aggregate_exasol_types` (line ~105-107), and reword the two comments at lines ~680 and ~719 to say "the caller's per-plan `aggregate_types` list, resolved from `selectListDataTypes`" instead of naming it.
- [x] 10.5 Reword the closing parenthetical of the doc comment on `empty_agg_sql_scalar_over_aggregate_item_does_not_shift_a_later_bare_aggregate_type` (`adapter/pushdown/empty_result_tests.rs`) to state the shift rule without naming the deleted `aggregate_exasol_types`.
- [x] 10.6 Module doc on `adapter/pushdown/scalar_over_agg.rs`: skipped — moot. The expert pass (9.2) already relocated `parse_agg_item` (and its siblings) into this module and rewrote the module doc to state the accurate invariant ("names NEITHER planner, and both name it"); there is no remaining cross-planner dependency and no stale claim to fix.
- [x] 10.7 Rename `fold_keeps_distinct_aggregates_in_separate_slots` to `fold_keeps_structurally_different_aggregates_in_separate_slots` (`adapter/pushdown/scalar_over_agg_tests.rs`) — the folded aggregates carry no `distinct: true`, so the old name collided with the codebase's DISTINCT domain term.
- [x] 10.8 Remove the unused `select_index` field from `SingleGroupItem::ScalarOverAggregate` (`adapter/pushdown/single_group_agg.rs`) and its construction/pattern sites in `single_group_agg.rs`, `single_group_agg_tests.rs`, and `empty_result_tests.rs`; confirmed via `cargo test -p lakehouse-engine` that no other construction site remains.
- [x] 10.9 `adapter/pushdown/dispatch_golden_tests.rs`: change `dispatch_sql_widened` to derive its dispatch inputs through `project_columns(pushdown_req, base_col_types())` instead of hardcoding `projection_widened = true` with empty projection vectors, asserting the fixture's own widening; `nested_aggregate_decline.sql` stays byte-identical.
- [x] 10.10 `adapter/pushdown/support_tests.rs`: add `assert_widens_to_full_base_row(select_item, declared_type, why)` and rewrite the six near-identical widening tests to call it, keeping each test's name and doc comment.
- [x] 10.11 `adapter/pushdown/support_tests.rs`: restore the issue-#198 doc comment above `aggregate_merge_renders_request_limit_when_some`, deleted when the test gained its `merge_select` argument.
- [x] 10.12 `adapter/pushdown/mod.rs` / `single_group_agg_tests.rs`: no live `selectList` shape reaches the `single_group_merge_select`-`None` else-arm through `detect_aggregates` (the hypothesized DataFusion/Exasol dialect divergence in CHAR/VARCHAR CAST rendering does not actually cause a decline — both dialects always succeed, just with different text). Added a comment on the `else` arm recording this, and a direct unit test (`merge_select_declines_when_the_scalar_structure_fails_to_render`) pinning `single_group_merge_select`'s own None-producing contract at the function level.
- [x] 10.13 Correct `plan.md`'s "Façade" paragraph in the Call-Site Census section: it claimed no façade item was added and that `vs-adapter/pushdown-module-structure` needed no delta, which the expert-fix pass's addition of `AggregateMergeInputs` (task 9.4) contradicts. Updated to state the addition and point at the new spec delta authored for it.
