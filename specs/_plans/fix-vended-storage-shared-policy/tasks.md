# Tasks: fix-vended-storage-shared-policy

## Phase 2: Implementation (Group A — shared derivations, no behaviour change)
- [x] 1.1 Add `scheme_of`, `location_host`, `adls_account_name` to `storage.rs` as `pub(crate)`, moving bodies of `vended.rs::anchor_host`, `unity/vended.rs::scheme_of`, `unity/vended.rs::location_host` verbatim; unify the two ADLS account-name error texts into one neutral text naming the location and the host.
- [x] 1.2 Repoint `resolve_vended_storage`'s inline scheme computation, `adls_backend_from_vended`, `resolve_uc_vended_storage`, `s3_from_vended`, `adls_from_vended` at the shared derivations; delete `vended.rs::anchor_host`, `unity/vended.rs::scheme_of`, `unity/vended.rs::location_host`. Keep `vended.rs::lowercase_scheme`.
- [x] 1.3 Add `storage_tests.rs` unit tests for the three shared derivations, including the container-userinfo host case and the unlabelled-host account-name error.

## Phase 2: Implementation (Group B — shared construction and gates; sequential 2.3 → 2.1 · 2.2 → 2.4)
- [x] 2.3 Add `pub struct StaticStoreAddress` with `Default` and `impl From<&ConnectionCreds>`, re-exported from `lib.rs`. Both fields NON-`pub`, with `pub fn endpoint(&self) -> &str` / `pub fn region(&self) -> &str` accessors. [expert]
- [x] 2.1 Add `pub(crate) struct VendedS3` and `pub(crate) fn adls_backend` to `storage.rs`, with the `abfs://` consent gate inside `adls_backend`, one neutral refusal text naming the `abfs://` scheme, the location, and `ALLOW_HTTP`. [expert]
- [x] 2.2 Add `pub(crate) fn s3_backend` to `storage.rs` carrying the plaintext-endpoint gate, the address rule, and the `path_style` derivation; do NOT port the "store address undetermined" error. [expert]
- [x] 2.4 Add `storage_tests.rs` unit tests for the address precedence matrix, the `path_style` composition matrix, the `abfs`/`abfss` gate, the plaintext-endpoint gate on a CONNECTION-supplied endpoint, and the both-empty-address success case.

## Phase 2: Implementation (Group C — repoint selectors; independent within group)
- [x] 3.1 Extract `iceberg_vended_s3` and `iceberg_vended_sas` in `vended.rs` from `s3_backend_from_vended`/`adls_backend_from_vended` bodies, keeping host-suffixed SAS selection and required-key error texts byte-identical; delete both `*_from_vended` functions. [expert]
- [x] 3.2 Add the `StaticStoreAddress` parameter to `resolve_vended_storage`, move the `abfs` gate out of its `match` arm, dispatch to `s3_backend`/`adls_backend`. [expert]
- [x] 3.3 Update `vended_tests.rs` per plan § Test Disposition. [expert]
- [x] 4.1 Extract `uc_vended_s3` and `uc_vended_sas` in `unity/vended.rs`, keeping missing-credential error texts byte-identical; delete `s3_from_vended` and `adls_from_vended`.
- [x] 4.2 Add the `StaticStoreAddress` parameter to `resolve_uc_vended_storage`, dispatch to `s3_backend`/`adls_backend`; update its doc comment to state credentials stay vended-only while addressing may come from the CONNECTION.
- [x] 4.3 Update `unity/vended_tests.rs` per plan § Test Disposition, adding the `abfs://` gate case and the both-empty-address success case.

## Phase 2: Implementation (Group D — probes and call sites; task 5 and task 6 independent)
- [x] 5.1 Generalise `storage_backend_variant_names()` into one extractor parameterised by enum name; add `VendedBackendKind` extraction. [expert]
- [x] 5.2 Replace the two per-selector `StorageBackend`-variant probes with: one construction probe over `storage.rs`; one per-selector `VendedBackendKind`-dispatch probe each over `vended.rs` and `unity/vended.rs`; one set-equality probe over the two enums' extracted names. [expert]
- [x] 5.3 Add `static_store_address_is_reachable_and_declares_no_credential_field` — one test, used unchanged in both Verification rows — constructing the type from the external vantage through `Default` and `From<&ConnectionCreds>`, asserting over `struct StaticStoreAddress`'s own declaration that it names no field spelled `access_key`, `secret_key`, `session_token`, `token`, `account_key`, `sas_token`, or `password`. [expert]
- [x] 5.4 Add `StaticStoreAddress` and its conversion to the `use` list and the arity-pin tests.
- [x] 5.5 Add `shared_vended_policy_steps_are_not_public`, asserting no `CATALOG_SOURCES` file declares `pub fn s3_backend`, `pub fn adls_backend`, `pub fn scheme_of`, `pub fn location_host`, or `pub fn adls_account_name`, that none declares `pub struct VendedS3`, and that `lib.rs` re-exports none of the six. Correct `demoted_and_deleted_functions_are_not_declared_public` in the SAME task: `s3_backend_from_vended` is now a DELETED predecessor, add its sibling `pub fn adls_backend_from_vended` beside it and rewrite the doc comment to state the shared `s3_backend`/`adls_backend` in `storage.rs` replaced both, crate-privacy asserted by `shared_vended_policy_steps_are_not_public`. Assert each shared step in exactly ONE of the two tests. [expert]
- [x] 5.6 Add `static_store_address_fields_are_not_public`, asserting from `storage.rs`'s production source that `struct StaticStoreAddress`'s declaration keeps BOTH fields non-`pub`.
- [x] 6.1 `resolve_file_list` (`crates/lakehouse-engine/src/adapter/pushdown/file_resolution.rs`) passes `&StaticStoreAddress::from(creds)`; update its doc comment's "reading no CONNECTION storage field" sentence to the credentials/addressing split.
- [x] 6.2 `probe_vended_credential` (`crates/lakehouse-engine/tests/e2e_lakekeeper_test.rs`) passes `&StaticStoreAddress::from(creds)`.
- [x] 6.3 `cloud_e2e_test.rs`: change the `client.region`/`s3.endpoint` assertion to a report, leaving the key-pair assertions hard.
- [x] 6.4 Add `vended_addressing_prefers_the_connection_endpoint_and_region` to `crates/lakehouse-engine/src/adapter/pushdown/file_resolution_tests.rs` per plan task 6.4 (single-shot loopback catalog fake, no snapshot, CONNECTION endpoint/region win over different vended values). [expert]

## Phase 3: Verification
- [ ] 7.1 Run `cargo test` (0 failures)
- [ ] 7.2 Run `cargo clippy --workspace --all-targets -- -D warnings` (0 errors/warnings)
- [ ] 7.3 Run `cargo fmt --check` (no changes)
- [ ] 7.4 Run `make cross-musl-udf-build` (exit 0)
- [ ] 7.5 Run `EXASOL_CONTAINER=lakehouse-engine-rs-2-exasol-1 make test-e2e` (0 failures)
- [ ] 7.6 Scenario coverage audit against plan § Verification > Scenario Coverage

## Phase 4: Review Fixes
- [x] 4.1 Collapse `storage.rs::adls_account_name` to `fn adls_account_name(location: &str) -> Result<&str, UdfError>` (private, not `pub(crate)`), deriving the host internally via `location_host(location)` and keeping the refusal text byte-identical (still naming both the location and the derived host); update `adls_backend` to call `adls_account_name(location)?` and drop its now-unused `host` local; update `adls_account_name_reads_the_hosts_leading_label` and `adls_account_name_errs_when_the_host_has_no_leading_label` in `storage_tests.rs` to pass the location alone, dropping the paired-host tuples but keeping both unlabelled-host locations and every existing assertion; leave the `pub fn adls_account_name` entry in `shared_vended_policy_steps_are_not_public` in place. [expert]
- [x] 4.2 In `crates/lakehouse-catalog/src/unity/vended.rs`, rewrite the module doc (lines 4-13) to state that this module reads the Unity Catalog temporary-table-credentials wire shape ONLY, reduces it to the neutral `VendedS3`/SAS values, and hands them to the shared policy and construction in `storage`, which applies the consent gates and builds the `StorageBackend` variant; drop the "constructs its OWN variant" and "every variant name appears in this selector's own source" claims and replace them with the `VendedBackendKind`-dispatch requirement the current probe enforces. In the `resolve_uc_vended_storage` doc, change "carries no `warehouse`, `region`, or existing `StorageBackend`" to "carries no `warehouse`, no credential, and no existing `StorageBackend`" so it no longer contradicts the store-address sentence that follows it.
- [x] 4.3 In `crates/lakehouse-catalog/src/unity/vended_tests.rs`, change the two assertion messages in `s3_vended_response_terminates_in_s3_backend` (lines 90 and 92) to state what the fixture actually establishes — that with the CONNECTION address unset and no endpoint or region vended, both resolve empty — and remove the "no CONNECTION endpoint read" / "no CONNECTION region read" claims.
- [x] 4.4 In `crates/lakehouse-engine/tests/e2e_lakekeeper_test.rs`, keep the `endpoint`/`region`/`access_key`/`secret_key` emptiness assertion in `lakekeeper_vended_creds_projection_filter` exactly as it is, and rewrite its doc comment (lines 348-353) and its assertion message (lines 370-372) to say that a static key pair would be an unread credential, while a static `endpoint` or `region` would OVERRIDE the vended store address — so this fixture must carry neither, or the row set below would no longer prove the scan reached the store through the vended resolution alone.
- [x] 4.5 In `crates/lakehouse-engine/tests/cloud_e2e_test.rs`, rename `cloud_glue_vends_s3_key_pair_and_store_address` to `cloud_glue_vends_the_s3_key_pair_for_the_table_location`, and update the literal test name embedded in its `println!` report prefix (line 769) to the new name so the report line stays greppable by test name.
- [x] 4.6 In `crates/lakehouse-catalog/tests/catalog_public_surface.rs`, rename `resolve_uc_vended_storage_signature_takes_no_connection_value` to `resolve_uc_vended_storage_signature_takes_only_a_credential_free_store_address`, leaving its body, doc comment, and assertions unchanged.
- [x] 4.7 In `crates/lakehouse-catalog/src/vended.rs`, add the dropped `anchor` contract sentence back to `resolve_vended_storage`'s doc comment, stating that `anchor` must be the table's OWN location — the same value `storage_credentials[*].prefix` is matched against — so that a catalog URI is refused as an unsupported scheme rather than silently falling through to the flat `config` map.
- [x] 4.8 In `crates/lakehouse-catalog/src/storage.rs`, extend `adls_account_name`'s CURRENT doc comment (read it first — the expert fix at 4.1 already narrowed its signature and rewrote its opening sentence; append/adjust rather than reverting) with the verbatim-derivation rationale: the label is read from the host byte-exactly because the downstream `adls.account-name` wrong-account guard compares it byte-for-byte against the account parsed out of each file URI (`iceberg-storage-opendal-0.10.0/src/azdls.rs:165`), so case-folding it would fire the guard on the very locations it was derived from.
- [x] 4.9 In `crates/lakehouse-catalog/src/storage.rs`, add a paragraph to the module doc stating that this module is also the single home for vended-storage POLICY and CONSTRUCTION — the `abfs://` and plaintext-endpoint consent gates, the CONNECTION-wins store-address rule, and the two `StorageBackend` constructions both catalog kinds share — and that it lives here because the enum's own module already owns which module may name a variant, so the vended selectors fork only on how a value is read off the wire, never on what makes it acceptable.
- [x] 4.10 In `crates/lakehouse-catalog/src/storage_tests.rs`, extend `s3_backend_gates_a_plaintext_endpoint_the_connection_supplied` to drive the refusal over a case-variant endpoint spelling as well (loop the CONNECTION endpoint over `http://minio:9000` and `HTTP://minio:9000`), asserting the same refusal for both so the gate's case-insensitive scheme match is pinned.
- [x] 4.11 In `crates/lakehouse-catalog/src/storage_tests.rs`, delete `static_store_address_defaults_to_empty_and_takes_the_connections_endpoint_and_region` together with the now-unused `base_creds` import if nothing else in the file uses it, leaving `catalog_public_surface.rs`'s `static_store_address_is_reachable_and_declares_no_credential_field` as the single home for that assertion; run `cargo test -p lakehouse-catalog` and `cargo clippy --workspace --all-targets -- -D warnings` afterwards.
