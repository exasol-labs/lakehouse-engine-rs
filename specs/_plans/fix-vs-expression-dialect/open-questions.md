# Open Questions: fix-vs-expression-dialect

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan fix-vs-expression-dialect` locally, or re-run `/speq:plan-pr fix-vs-expression-dialect` after commenting.

Round 2 of adversarial review found 3 remaining BLOCKER findings (0 Intent-Fidelity issues this round — the scope-drop decision below was applied cleanly, confirmed by independent re-verification). Review is bounded to 2 rounds; what's left needs a human call rather than a third automated revision pass. Full detail: `specs/_plans/fix-vs-expression-dialect/review/round-2.md`.

- [ ] **Sweep-test exception list is incomplete — the requirement as written can't be satisfied.** The rewritten sweep test (task 6) requires every `function_scalar` node's Exasol rendering to equal `<NAME>(<args>)`, except for 4 named exceptions. Two more `function_scalar` families don't fit that shape either: the four now-family names (`CURRENT_DATE`/`SYSDATE`/`CURRENT_TIMESTAMP`/`SYSTIMESTAMP`) render as a bare keyword with no parens, and `CASE` renders `CASE WHEN … END` in both dialects. As written, the implementer either includes these rows and the test fails by construction, or omits them and the sweep is silently incomplete. Round 2's Fix: line spells out the corrected 7-exception list — mechanical to apply.
- [ ] **The regression-guard's advertised guarantee is broader than what it delivers.** Plan text (§ Goals, § Design Decision, § Consequences, and 2 spec deltas) claims the sweep test "fails when a future arm forgets the dialect" / renders it so "no divergence is possible" — i.e. it would catch a brand-new function added later with no dialect branch. It can't: the sweep table only checks names already listed in it. A future `SUBSTRING`/`NVL`/`DATE_BIN` arm added without also adding a sweep row passes every test and silently renders DataFusion SQL on the Exasol path — the exact issue #209 failure mode, unfixed for the *next* function. Two ways to close this:
  - **(a)** Add a task deriving the sweep table and `EXASOL_VERBATIM_FNS` from one single declared source, so a new arm literally cannot exist without a sweep row (real fix, more implementation work), or
  - **(b)** Soften the plan's claims to state what the test actually guarantees — catches a *known* name diverging, not a *future* one — and file a tracked follow-up GitHub issue for the real fix, per this project's no-silent-gap rule.
- [x] ~~Is `decimal_to_varchar_exasol` a real bug or scope creep?~~ **Resolved:** drop it entirely — not reachable today, so it does not belong in this plan. Applied cleanly in the round-2 revision; round 2 independently confirmed no dangling references remain.

**Also flagged, advisory only (not blocking, full text in round-2.md):** the `INSTR`/`strpos` row in § Context/§ Verification is the same "unreachable-today labeled as a current failure" pattern as the resolved decimal item — `strpos` was already fixed by #210, so that row should be relabeled a regression guard, not a repro. The guarded arm needs to physically move above the pre-existing math arms in `lib.rs` for `SIGN` to actually stop rendering `signum(x)` — task 1 currently says to widen the guard in place, which does not fix the plan's own headline defect (the implementer's own test would catch this during implementation, so it is not a shipped-bug risk, just rework if missed). Plus: a missing `NULLIF` scenario, an unchecked `%`-token false-positive risk in the sweep, one wrong SQL state code, and 3 over-length sentences.

Reply with a decision on the two BLOCKER items above (e.g. "apply the mechanical fix for item 1 and use option (b) for item 2"), or resume locally with `/speq:plan fix-vs-expression-dialect`.
