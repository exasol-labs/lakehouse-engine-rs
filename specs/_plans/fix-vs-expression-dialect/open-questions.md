# Open Questions: fix-vs-expression-dialect

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan fix-vs-expression-dialect` locally, or re-run `/speq:plan-pr fix-vs-expression-dialect` after commenting.

- [ ] **Scope-creep on `decimal_to_varchar_exasol`.** The plan presents the adapter-synthesized decimal-to-VARCHAR node's Exasol-dialect rendering as one of three failure paths "verified during planning to fail on live Exasol" (`decision-log.md` § [2]; `plan.md` § Impact ¶2, task 5, § Manual Testing row 6). Independent re-verification found that node is unreachable from every Exasol-dialect consumer today: it is adapter-synthesized only, and its two production producers (`adapter/pushdown/mod.rs:213`, `adapter/pushdown/support.rs:1125`) both terminate in the DataFusion dialect, never the Exasol one. The § Manual Testing repro (`SELECT COUNT(DISTINCT LENGTH(c_decimal_a)) ...`) does not reproduce — that select-list item renders from raw wire JSON and compiles today. Two options:
  - **(a)** Keep it in this plan, but relabel it honestly as unreachable-today defensive hardening (not a verified bug), per the reviewer's proposed fix — cites `mod.rs:213`, `support.rs:1125`, and the `vs-adapter/pushdown-planning-decimal-string-format` spec's scan-path scope.
  - **(b)** Drop it from this plan entirely, since issue #209 is scoped to live compilation failures and this item is neither reported nor currently reachable — track it separately if/when an adapter path changes and makes it reachable.

Full review, including 5 additional (non-blocking-on-intent) BLOCKER and 7 ADVISORY findings the reviewer surfaced — a second REGEXP_LIKE encoding site untouched by the plan, both REGEXP_LIKE acceptance tests exercising a code path the Exasol dialect never reaches, an unverifiable deny-list-based sweep-test requirement, a name-set duplication the design decision claims to eliminate but doesn't, a "five consumer sites" claim contradicted by the plan's own four-row table, and others — is at `specs/_plans/fix-vs-expression-dialect/review/round-1.md`. Those are independent of the scope question above and should be fixed regardless of how the scope question resolves; they are not repeated here since none of them alone said "wrong problem."
