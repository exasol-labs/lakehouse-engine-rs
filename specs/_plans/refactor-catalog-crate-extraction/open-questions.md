# Open Questions: refactor-catalog-crate-extraction

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan refactor-catalog-crate-extraction` locally, or re-run `/speq:plan-pr refactor-catalog-crate-extraction` after commenting.

- [ ] Round 2 review found that fixing round 1's shared-test-fixture blocker (task 3.0) introduced a new one: task 3.0 says to leave `base_creds`/`static_storage` in place in `crates/lakehouse-engine/src/adapter/pushdown/test_support.rs` "for the engine tests that still use them" — but after tasks 3.2/3.3 move their only callers, no engine test calls either, so both become `dead_code` and fail `cargo clippy --workspace --all-targets -- -D warnings` (task 1.1's stated gate and task 7.3's final gate). Confirm the reviewer's proposed fix before it's applied: **delete** `base_creds`/`static_storage` from the engine's `test_support.rs` once their callers move (not copy-and-keep), and account for the ~13 additional cross-module test helpers (9 sentinel constants, `AUTH_PROP_KEYS`, and 3 builders) that `credentials.rs`'s test module splits across `auth`/`session`/`iceberg_io`/`vended` — full detail in `review/round-2.md` under `[UNSTATED_ASSUMPTION] BLOCKER`.
