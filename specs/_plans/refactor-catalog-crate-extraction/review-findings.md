# Code Review Findings: refactor-catalog-crate-extraction

## Summary
- Files reviewed: 34 (3 deleted, 20 modified, 11 new)
- Total findings: 15 (standard: 9, expert: 6)

Verified clean, no finding raised: the ~2,558-line `credentials.rs` move preserved all 52 originating tests (51 into the crate, 1 relocated to `pushdown/mod.rs`) with zero assertion, message, name, or attribute drift — the only body edits are the mechanical `assert_eq!(x, "LIT")` → `assert_eq!(x.as_deref(), Some("LIT"))` adaptation to `extract_vended_keys`' new `Option` return, which is exactly equivalent because the old `unwrap_or_default()` and the new `filter(|s| !s.is_empty())` both collapse absent and empty to the same branch. `resolve_vended_storage`'s consolidation is behavior-identical to the retired five-step sequence field by field (source selection, empty-key fallback, session-token precedence, path-style parse failure, `allow_http`). The plan's visibility split landed exactly: all 13 public items have external callers, all 19 mechanism items are crate-private or tighter, `CatalogSession`'s four fields are private. Both surface-probe counts (22 in-crate, 12 external) match their `use` lists. No leftover reference to the three deleted modules, no unused import, no duplicated fixture, and `Makefile` `VS_SRCS` covers the new crate so neither E2E gate is vacuous.

## Standard fixes

### crates/lakehouse-catalog/src/lib.rs

#### [OUTDATED_COMMENT] Crate doc describes the extraction as still in progress and cites a path that will not exist
- Location: lines 4-6; also `crates/lakehouse-catalog/tests/catalog_public_surface.rs` lines 15-17 and `crates/lakehouse-engine/tests/catalog_session_signatures.rs` lines 19-25
- Issue: `lib.rs` says "This crate is being populated incrementally by `specs/_plans/refactor-catalog-crate-extraction`. See that plan for the catalog surface being carved out and the rationale for the split." The population is complete, so the tense is wrong, and the citation dangles: `/speq:record` archives the plan into gitignored `specs/_recorded/`, making the referenced file unreadable to any future reader. This is the identical defect the plan's own Dead Code Removal table lists for the two pushdown probe doc comments ("archived into gitignored `specs/_recorded/`, unreadable") — task 6.1 removed those two citations while three new ones were introduced elsewhere in the same change.
- Fix: In crates/lakehouse-catalog/src/lib.rs, rewrite the crate doc comment to describe the crate's completed responsibility in the present tense (Iceberg REST catalog access: session, auth, namespace enumeration, vended-storage resolution, SigV4 signing, credential redaction, and the three shared credential types) and delete the `specs/_plans/refactor-catalog-crate-extraction` sentence. In crates/lakehouse-catalog/tests/catalog_public_surface.rs delete the parenthetical `(specs/_plans/refactor-catalog-crate-extraction/plan.md).` from the "Covers the Verification > Scenario Coverage row" note, keeping the scenario text itself. In crates/lakehouse-engine/tests/catalog_session_signatures.rs delete the `(specs/_plans/refactor-catalog-crate-extraction/plan.md):` citation on the "Covers Verification > Scenario Coverage rows" line, keeping the two scenario descriptions. Cite the permanent spec feature names (`vs-adapter/catalog-crate-structure`, `vs-adapter/pushdown-catalog-session`) instead of plan paths.

### crates/lakehouse-catalog/src/test_support.rs

#### [OUTDATED_COMMENT] Module doc is written in the pre-migration tense, cites deleted line numbers, and overstates how many helpers are shared
- Location: lines 4-11
- Issue: three separate inaccuracies in one comment. (1) "the engine declarations are deleted once their last callers move to this crate (task 3.3)" is future tense for work already done — `crates/lakehouse-engine/src/adapter/pushdown/test_support.rs` no longer declares `base_creds` or `static_storage`. (2) It cites those declarations at `(:23, :44)`, line numbers that no longer exist in that file. (3) It claims the other thirteen items "became cross-module the moment that module split four ways across `auth`, `session`, `iceberg_io`, and `vended`" — measured against the landed code, only four of the thirteen did (`BEARER_TOK`, `CLIENT_SECRET`, `OAUTH_ACCESS_TOKEN`, `creds_no_auth`); the remaining nine are reached by exactly one module.
- Fix: In crates/lakehouse-catalog/src/test_support.rs, rewrite the module doc comment to state only what is true after the fix in the finding below: this module holds the test fixtures reached by two or more of the crate's test modules, and each fixture's doc comment names its consumers. Delete the task numbers, the `(:23, :44)` line citations, and the "thirteen items" count.

#### [INFORMATION_LEAKAGE] Nine of fifteen shared-support helpers are reached by only one module
- Location: lines 65-70 (`AUTH_PROP_KEYS`), 92-160 (`make_load_table_result`, `vended_result_flat_config`), 56-58 and 62 (`VENDED_AK`, `VENDED_SK`, `VENDED_TOK`, `VENDED_REGION`); consequence at `crates/lakehouse-catalog/src/auth.rs` lines 17-20
- Issue: the recorded `vs-adapter/pushdown-module-structure` rule places a test helper in a shared `#[cfg(test)]` support module only when more than one submodule reaches it. Measured reach: `base_creds` (3 modules), `creds_no_auth` (3), `CLIENT_SECRET` (3), `static_storage` (2), `BEARER_TOK` (2), `OAUTH_ACCESS_TOKEN` (2) are correctly shared. The other nine are single-module — `AUTH_PROP_KEYS` is read only by `auth.rs`'s own test module (lines 293, 395, 416), and `make_load_table_result` (18 call sites), `vended_result_flat_config`, `VENDED_AK`, `VENDED_SK`, `VENDED_TOK`, `VENDED_REGION` only by `vended.rs`'s test module. (`STATIC_AK`/`STATIC_SK` must stay: `creds_no_auth`, a genuinely shared fixture, reads them.) The `VENDED_SK`/`VENDED_TOK` hits in `redaction.rs` are unrelated string literals, not the constants. The consequence of misplacing `AUTH_PROP_KEYS` is that `auth.rs`'s four `REST_CATALOG_PROP_*` constants carry `pub(crate)` solely to be visible to a test-only module — every production read of them is inside `auth.rs` itself (lines 44, 49, 54, 57).
- Fix: In crates/lakehouse-catalog/src/test_support.rs, relocate `AUTH_PROP_KEYS` into `crates/lakehouse-catalog/src/auth.rs`'s own `mod tests` (dropping the `use crate::auth::{REST_CATALOG_PROP_*}` import from test_support.rs), then narrow all four `REST_CATALOG_PROP_TOKEN`, `REST_CATALOG_PROP_CREDENTIAL`, `REST_CATALOG_PROP_OAUTH2_SERVER_URI`, `REST_CATALOG_PROP_SCOPE` constants at auth.rs lines 17-20 from `pub(crate)` to private. Relocate `make_load_table_result`, `vended_result_flat_config`, `VENDED_AK`, `VENDED_SK`, `VENDED_TOK`, and `VENDED_REGION` into `crates/lakehouse-catalog/src/vended.rs`'s `mod tests`, keeping their values byte-identical. Keep `base_creds`, `static_storage`, `creds_no_auth`, `STATIC_AK`, `STATIC_SK`, `CLIENT_SECRET`, `BEARER_TOK`, and `OAUTH_ACCESS_TOKEN` in test_support.rs. Run `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test -p lakehouse-catalog` — both must stay green, and no test assertion may change.

### crates/lakehouse-catalog/src/vended.rs

#### [OUTDATED_COMMENT] Two moved tests still claim an unconditional static-field guarantee the consolidated function no longer makes
- Location: lines 306-308 (doc comment on `vended_creds_override_static_in_spec`) and line 343 (`// Static infrastructure fields must be preserved.`)
- Issue: before task 4.1, `merge_vended_into_storage` unconditionally copied `endpoint`, `region`, and `path_style` from `base`, and `file_resolution.rs` overlaid the vended values afterwards. The consolidated version at lines 63-77 folds that overlay in, so it now overwrites all three whenever the selected source advertises them. Net production behavior is unchanged, but the two comments state a property of the function that is now only a property of the fixture: the assertions at lines 344-355 pass solely because the hand-built `vended_source(&[...])` omits `s3.endpoint`, `client.region`, and `s3.path-style-access`. A reader is told `merge_vended_into_storage` preserves static infrastructure fields; only `allow_http` is still unconditionally taken from `base`.
- Fix: In crates/lakehouse-catalog/src/vended.rs, reword `vended_creds_override_static_in_spec`'s doc comment (lines 306-308) to say that a vended source advertising only the three S3 key fields leaves `endpoint`, `region`, `path_style`, and `allow_http` at their `base` values, and replace the line-343 comment with one stating that these fields fall through to `base` because this fixture advertises none of them. Do not change any assertion.

### crates/lakehouse-catalog/src/sigv4.rs

#### [MISSING_DOC_COMMENT] Module-level documentation is misattached to a `use` statement
- Location: lines 1-10
- Issue: the module's prose header ("SigV4 request-signing helper for AWS Glue catalog REST requests" plus the three credential-safety guarantees) uses `///`, an outer doc comment, so it documents the following item — `use aws_credential_types::Credentials;` — rather than the module. `sigv4` is therefore the only one of the crate's nine modules with no `//!` header, and its safety guarantees do not appear as module documentation. Carried over from the pre-move `adapter/sigv4.rs`, but the extraction is what gave every sibling module a proper `//!` header, leaving this one inconsistent inside its new home.
- Fix: In crates/lakehouse-catalog/src/sigv4.rs, convert the ten `///` lines at the top of the file to `//!` inner doc comments so they document the module, and add a sentence naming the module's place in the crate (the signing mechanism behind `iceberg_io`'s authenticated GET and `namespace`'s signed enumeration, crate-private by design). Keep the wording of the three credential-safety guarantees unchanged.

### crates/lakehouse-engine/src/scan/emit.rs

#### [UNUSED_FUNCTION] Re-export widens the engine's public API for two functions with no external consumer
- Location: line 234
- Issue: `pub use lakehouse_catalog::{redact_credentials, redact_secret_values};` publishes both names on `lakehouse_engine::scan::emit`, a `pub mod` inside a `pub mod`. Repo-wide, every consumer of that path is in-crate: `adapter/mod.rs:1043,1044`, `scan/positional_deletes.rs:208,209`, `scan/object_store.rs:313,316`, plus `emit.rs`'s own lines 213 and 228. No file under `crates/lakehouse-engine/tests/` names it, and `src/scan_surface_probe.rs` does not list it. The engine's public API now carries a second, unused path to two functions that are already `pub` on `lakehouse-catalog`. The `pub` was justified before the move because the functions were declared here; after it, only the in-crate path is needed, and the plan's "no consumer `use` path may change" requirement is satisfied by `pub(crate)`.
- Fix: In crates/lakehouse-engine/src/scan/emit.rs line 234, narrow the re-export to `pub(crate) use lakehouse_catalog::{redact_credentials, redact_secret_values};`. Confirm `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test --workspace` stay green — the three in-crate `crate::scan::emit::redact_*` call sites and emit.rs's own two uses must still resolve.

### crates/lakehouse-engine/src/adapter/mod.rs

#### [INLINE_COMMENT] Thirteen-line inline block restates rationale already carried by the function it calls
- Location: lines 250-262; same pattern at `crates/lakehouse-engine/src/adapter/pushdown/mod.rs` lines 198-205
- Issue: the block above the hoisted `CatalogSession::resolve` explains the one-session-per-enumeration design, the `(catalog_uri, warehouse)` scoping argument, the failure-surfacing order, and why `list_namespace_tables` is deliberately not folded in. All of it is already stated in `resolve_table_schema`'s doc comment (`pushdown/file_resolution.rs` lines 563-577), which the reader reaches from the call one line below. The same rationale is therefore maintained in two places and will disagree the first time either is edited. `pushdown/mod.rs` lines 198-205 repeat the parse-before-config rationale that `resolve_file_list`'s doc comment already carries at file_resolution.rs lines 190-196.
- Fix: In crates/lakehouse-engine/src/adapter/mod.rs, delete the inline comment block at lines 250-262 and keep the `let session = ...` statement. In crates/lakehouse-engine/src/adapter/pushdown/mod.rs, replace the eight-line block at lines 198-205 with the single guard line `parse_table_ident(&catalog.table)?;` and no comment. If either rationale is not already fully stated in the corresponding doc comment on `resolve_file_list` / `resolve_table_schema` in pushdown/file_resolution.rs, move the missing sentence into that doc comment rather than leaving it inline.

### crates/lakehouse-engine/src/adapter/pushdown/mod.rs

#### [WORK_TRACKING_COMMENT] New comments cite this plan's task numbers and its issue number
- Location: lines 781-790 and 2363; also `crates/lakehouse-catalog/src/test_support.rs` lines 8 and 11, `crates/lakehouse-catalog/src/vended.rs` line 809, `crates/lakehouse-engine/tests/catalog_session_signatures.rs` lines 15, 22 and 25, `crates/lakehouse-engine/tests/shared_type_reexports.rs` line 5
- Issue: eight comments introduced by this change identify code by plan task number or issue number rather than by behavior — `Parse-before-config ordering (issue #204 task 5.1) — regression coverage`, `(task 3.3)`, `(task 3.2)`, `Task 4.2 — resolve_vended_storage behavior-parity tests`, `task 5.1/5.2's &CatalogSession parameter`, `(task 5.1)`, `(task 5.2)`, `Task 2.1 moved StorageProps ...`. Task numbers are scoped to a plan that `/speq:record` archives into gitignored `specs/_recorded/`, so they become unresolvable identifiers. (The `Task 3.3 / 3.4`, `Task 4.1`, `Task 4.3`, `Task 4.4`, `Task 4.5` banners moved verbatim from `credentials.rs` are pre-existing and out of scope for this fix.)
- Fix: In each of the six files and locations listed above, rewrite the comment to name the behavior instead of the task. Use `// Parse-before-config ordering — regression coverage` at pushdown/mod.rs:2363; drop `(task 3.3)` and `(task 3.2)` from test_support.rs lines 8 and 11; use `// resolve_vended_storage behavior-parity: the six absence and precedence cases plus the single-selection guarantee` at vended.rs:809; in catalog_session_signatures.rs replace `task 5.1/5.2's &CatalogSession parameter` with `the &CatalogSession parameter` and drop the `(task 5.1)` / `(task 5.2)` suffixes; in shared_type_reexports.rs open with `StorageProps, CatalogProps, and ConnectionCreds are declared in lakehouse-catalog and re-exported at their pre-move engine paths` with no task number. Do not touch the pre-existing `Task 3.3 / 3.4`, `Task 4.1`, `Task 4.3`, `Task 4.4`, `Task 4.5` banners in auth.rs, iceberg_io.rs, session.rs, redaction.rs, or vended.rs.

#### [ASSERTION_FREE_TEST] Four sentinel assertions in the relocated scan-spec test cannot fail
- Location: lines 802-876, specifically the constants at 806-809 and the loop at 855-866
- Issue: `catalog_auth_secrets_never_in_scan_spec_with_vending` declares `TOKEN_SENTINEL`, `CS_SENTINEL`, `OAUTH_URI_SENTINEL`, and `SCOPE_SENTINEL`, then asserts the serialized `ScanSpec` JSON contains none of them, under the comment "Auth sentinel VALUES must not appear even if a refactor wired them in." Nothing in the test ever puts those four values anywhere: the spec is built from `vended_storage` alone, with no `ConnectionCreds` and no catalog-auth field in the construction path. The loop therefore asserts that four string literals never assigned to anything are absent from a JSON document, which holds for any implementation. The vacuity is pre-existing (byte-identical to the original in `credentials.rs`), but relocating the test is the moment to fix it — the JSON-key assertions above it at lines 843-854 and the two vended-presence assertions at lines 867-875 are the parts that carry real signal.
- Fix: In crates/lakehouse-engine/src/adapter/pushdown/mod.rs, delete the four `TOKEN_SENTINEL` / `CS_SENTINEL` / `OAUTH_URI_SENTINEL` / `SCOPE_SENTINEL` constants and the `for value in [...]` loop that reads them, and delete the now-orphaned `// scope is too short and appears in storage endpoint strings — test absence of the auth value instead (done below via SCOPE_SENTINEL).` comment inside the field-name array, replacing it with a note that `scope` is checked by key name only. Keep the JSON-key assertion loop and both vended-credential presence assertions exactly as they are.

## Expert fixes

### crates/lakehouse-engine/src/adapter/mod.rs

#### [MISSING_BOUNDARY_TEST] An empty namespace now pays a catalog round-trip the per-table loop never made
- Location: lines 263-265
- Issue: the hoisted `CatalogSession::resolve` runs unconditionally between `list_namespace_tables` and `build_virtual_tables`. When `table_idents` is empty — a namespace with no tables, which is a reachable `createVirtualSchema` input — `build_virtual_tables` never invokes the resolver closure, so the pre-refactor code performed zero session builds for that request. The new code performs one, including an OAuth2 client-credentials grant on the OAuth path and a `/v1/config` lookup on every non-SigV4 path. Two consequences the plan's Impact section does not cover: the grant count for an empty namespace goes from 1 to 2 rather than from N+1 to 2, and a `resolve_catalog_auth` failure now fails a request that previously succeeded and returned an empty table list. No test exercises the zero-table namespace, so neither the old nor the new behavior is pinned at that boundary.
- Fix: In crates/lakehouse-engine/src/adapter/mod.rs, make the session build lazy so an empty `table_idents` performs no catalog contact: build it only when `!table_idents.is_empty()`, hold it in a binding the `build_virtual_tables` closure can borrow, and keep the existing `.map_err(|e| redact_error(&storage, e))?` on the build so a grant failure still surfaces before the loop when there is at least one table. Then add a unit test in the same file's `mod tests` named `create_virtual_schema_over_empty_namespace_contacts_no_catalog_session` that drives `handle_create_virtual_schema` (or `build_virtual_tables` plus the guard, whichever is reachable without live infrastructure) with an empty table list against an unreachable `catalog_uri` such as `http://127.0.0.1:1` and OAuth2 creds, asserting the call succeeds with an empty `tables` array rather than returning a transport or grant error — mirroring the network-free technique `pushdown/mod.rs::malformed_table_ident_fails_before_any_catalog_contact` already uses.

### crates/lakehouse-catalog/src/redaction.rs

#### [SHALLOW_MODULE] `redact_catalog_error` is a public pass-through alias for `redact_credentials`
- Location: lines 78-81
- Issue: the whole body is `redact_credentials(msg)`, with an identical `&str -> String` signature and no added behavior. The extraction widened it from the engine's `pub(super)` to `pub` and put it on the crate's public surface alongside `redact_credentials`, so two of the crate's 13 public names are one function and every caller must learn which to reach for. The plan's own Dead Code Removal table already identifies it as "a pass-through" whose "whole body is a call to `redact_credentials` with the same argument", yet the move promoted it instead of collapsing it. The confusion is already live inside the crate: `auth.rs:91` and `auth.rs:150` write `redact_secret_values(&redact_catalog_error(msg), ...)` while the engine writes `redact_credentials(&redact_secret_values(...))` for the same composition, so the alias hides that the two spellings call the same function.
- Fix: Delete `redact_catalog_error` from crates/lakehouse-catalog/src/redaction.rs and remove it from the `pub use redaction::{...}` list in crates/lakehouse-catalog/src/lib.rs line 23. Repoint every call site to `redact_credentials`: crates/lakehouse-catalog/src/auth.rs lines 10, 91, 150; crates/lakehouse-catalog/src/namespace.rs lines 8, 107, 120, 186, 193, 206, 213, 240, 248; and crates/lakehouse-engine/src/adapter/pushdown/file_resolution.rs lines 9, 261, 277, 393 (doc reference), 402, 433, 442, 454, 463, 526, 533. Remove `redact_catalog_error` from the `use lakehouse_catalog::{...}` list in crates/lakehouse-catalog/tests/catalog_public_surface.rs line 22 and update that file's header from 12 public items to 11. Update the `vs-adapter/catalog-crate-structure` spec delta under specs/_plans/refactor-catalog-crate-extraction/ wherever it enumerates the crate's public surface, so the item count and the named items match. Confirm no error message text changes: `cargo test --workspace` must pass with no assertion edit.

### crates/lakehouse-catalog/src/vended.rs

#### [IMPLEMENTATION_COUPLED_TEST] Moved unit tests pin the private mechanism steps the consolidation was meant to free
- Location: lines 142, 186, 213, 238, 284, 294, 321, 379, 415, 451-456, 473-479, 493-495, 532, 683, 719, 740
- Issue: seventeen call sites across the moved test module assert against `extract_vended_keys`, `merge_vended_into_storage`, `select_credential_source`, `extract_vended_region`, `extract_vended_endpoint`, and `extract_vended_path_style` — the six private mechanism steps — rather than against `resolve_vended_storage`, the concept-level function task 4.1 made the public surface. The plan's own Context names test coupling to `extract_vended_keys`' signature as the reason the consolidation was blocked for two releases: "None of that can be fixed while a probe asserts `extract_vended_keys`' signature." Task 6.1 removed that coupling at the probe level; these seventeen assertions reinstate it one level down, so the six private functions still cannot be renamed, merged, or reshaped without editing tests. Concretely: `extract_vended_region` and `extract_vended_endpoint` are now single-caller one-line wrappers around `vended_config_value` that only a test keeps distinguishable, and `extract_vended_keys` exists only to hand `merge_vended_into_storage` a three-tuple it destructures immediately. The scenarios these tests carry are real and four of them are not covered by the seven new `resolve_vended_storage_*` tests (storage-credentials-over-config precedence, longest-prefix-wins, flat-config fallback, S3-location anchoring), so the coverage must be preserved, not dropped.
- Fix: In crates/lakehouse-catalog/src/vended.rs, rewrite each mechanism-level test to call `resolve_vended_storage(&result, &base, anchor)` and assert on the returned `StorageProps` field instead of on a tuple element or an `Option` — for example `extract_vended_keys_uses_storage_credentials_over_config` becomes an assertion that `merged.access_key == "VENDED_AK"` and `merged.secret_key == "VENDED_SK"` with `base` supplying distinguishable static values, preserving each test's existing custom assertion message and its scenario doc comment verbatim. Rename each rewritten test so the name states the observable behavior rather than the private function (`vended_storage_prefers_storage_credentials_over_flat_config`, `vended_storage_longest_matching_prefix_wins`, `vended_storage_falls_back_to_flat_config`, `vended_storage_uses_flat_config_when_no_storage_credentials`, `vended_storage_anchor_is_the_s3_table_location`, `vended_storage_adopts_endpoint_and_path_style_from_flat_config`, `vended_storage_adopts_endpoint_from_storage_credentials`, `vended_storage_keeps_static_endpoint_and_path_style_when_absent`, `vended_storage_adopts_region_from_flat_config`), and update the `vs-adapter/pushdown-planning-cloud-credentials` spec delta's Verification test names to match. Where a test needs the pre-merge tuple to distinguish "absent" from "empty", express that through `base` values that differ from every vended sentinel instead of reaching for the private function. Then inline `extract_vended_keys`, `extract_vended_region`, `extract_vended_endpoint`, and `extract_vended_path_style` into `merge_vended_into_storage` as direct `vended_config_value(vended, "<key>")` calls, deleting the four now-callerless functions, and delete the two `include_str!` assertions in crates/lakehouse-catalog/tests/catalog_public_surface.rs that check for `pub fn extract_vended_keys` and `pub fn merge_vended_into_storage`, replacing them with one assertion that no source file declares `pub fn merge_vended_into_storage` or `pub fn select_credential_source`. `cargo test -p lakehouse-catalog` and `make test-e2e-lakekeeper` must both stay green, and `resolve_vended_storage`'s behavior must not change in any field.

### crates/lakehouse-catalog/src/creds.rs

#### [UNUSED_VARIABLE] `CatalogProps.uri` is written by every producer and read by none
- Location: line 159
- Issue: the field is set at `crates/lakehouse-engine/src/adapter/connection.rs:189` (`catalog_block`), `crates/lakehouse-catalog/src/namespace.rs:84`, `crates/lakehouse-engine/src/adapter/pushdown/joins/planning.rs:336`, `crates/lakehouse-engine/src/adapter/pushdown/mod.rs:2397`, `crates/lakehouse-engine/tests/common/e2e_harness.rs:351`, and `crates/lakehouse-engine/tests/shared_type_reexports.rs:43`. Its only read anywhere in either crate is an `assert_eq!(catalog.uri, ...)` inside a `connection.rs` unit test at line 493. Every consumer that needs the catalog URI receives it as a separate `catalog_uri: &str` parameter — `build_rest_catalog(catalog_uri, catalog, ...)`, `CatalogSession::resolve(catalog_uri, ...)`, `list_namespace_tables(&catalog_uri, ...)` — so the field is a second copy of a value always passed alongside it, free to disagree with it and never consulted when it does. This is precisely the argument the plan uses to justify dropping the `catalog_uri` parameter from `resolve_file_list` and `resolve_table_schema` ("a second copy of a value the session already carries, free to disagree with it"); moving `CatalogProps` into the new crate is the point at which the same reasoning should have been applied to the field. The field is `pub` on a public type in the crate's surface, so it also advertises a value the crate never uses.
- Fix: Delete the `uri` field from `CatalogProps` in crates/lakehouse-catalog/src/creds.rs (keeping `warehouse` and `table`) and update its doc comment to state that the catalog URI is not carried here because every consumer receives it as an explicit parameter. Remove `uri:` from every struct literal: crates/lakehouse-engine/src/adapter/connection.rs:189 (and drop the now-unused `uri` parameter from `catalog_block`, updating its two callers in crates/lakehouse-engine/src/adapter/mod.rs), crates/lakehouse-catalog/src/namespace.rs:84, crates/lakehouse-engine/src/adapter/pushdown/joins/planning.rs:336, crates/lakehouse-engine/src/adapter/pushdown/mod.rs:2397, crates/lakehouse-engine/tests/common/e2e_harness.rs:351, crates/lakehouse-engine/tests/shared_type_reexports.rs:43. Replace the `assert_eq!(catalog.uri, "http://catalog.example.com")` assertion at crates/lakehouse-engine/src/adapter/connection.rs:493 with the equivalent assertion on `resolved.uri`, which is a different struct and is genuinely read at adapter/mod.rs:192. Correct the reference to `catalog_props.uri` in the doc comment at crates/lakehouse-catalog/src/vended.rs:259 to name the catalog URI rather than the removed field. Update the `vs-adapter/catalog-crate-structure` spec delta if it enumerates `CatalogProps`' fields. Verify with `cargo test --workspace`, `make cross-musl-udf-build`, and `make test-e2e` that the `ScanSpec` JSON encoding is untouched — `CatalogProps` is not a `ScanSpec` field, and `crates/lakehouse-engine/src/scan/spec.rs::common_blob_wire_is_byte_stable` must still pass unedited.

### crates/lakehouse-engine/Cargo.toml

#### [UNNEEDED_DEPENDENCY] `reqwest` remains a direct dependency of the engine with no user left
- Location: lines 67-71
- Issue: the `[dependencies]` entry `reqwest = { version = "0.12" }` and its four-line comment survive the extraction, but the code that justified them left. Every `reqwest::` reference in `crates/lakehouse-engine/src/` is gone — the only two matches are prose inside a doc comment at `src/scan/object_store.rs:109,111`. The self-issued signed `loadTable` GET the comment names ("Async reqwest client for the self-issued signed load_table GET (task 4.1)") now lives in `lakehouse-catalog`'s `iceberg_io::authed_get_json` and `session::CatalogSession`. Every remaining runtime use is under `crates/lakehouse-engine/tests/`, covered by the separate `[dev-dependencies]` entry at line 90. The declaration therefore does nothing except pin a version in a second place and keep a comment that describes code in another crate.
- Fix: In crates/lakehouse-engine/Cargo.toml, delete the four-line comment at lines 67-70 and the `reqwest = { version = "0.12" }` entry at line 71 from the `[dependencies]` table. Leave the `[dev-dependencies]` entry at line 90 untouched — `tests/common/seed.rs` uses the async `reqwest::Client` and `tests/common/{stack,lakekeeper}.rs` and `tests/common/e2e_harness.rs` use the blocking client, all of which the dev-dependency's `features = ["blocking"]` covers. Verify with `cargo build -p lakehouse-engine`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `make cross-musl-udf-build` that both the library and the test crates still compile and the `.so` still links.

### crates/lakehouse-catalog/Cargo.toml

#### [INFORMATION_LEAKAGE] Four dependency versions are pinned outside the workspace mechanism, with a comment asserting cross-manifest agreement
- Location: lines 21-31
- Issue: `aws-sigv4 = { version = "1.4", ... }`, `aws-credential-types = { version = "1.2" }`, `aws-smithy-runtime-api = { version = "1", features = ["client"] }`, and `reqwest = { version = "0.12" }` are declared with literal versions, and the comments justify that as "version copied verbatim from crates/lakehouse-engine/Cargo.toml so the workspace keeps a single resolved version of each." The workspace's own mechanism for exactly that guarantee is `[workspace.dependencies]` in the root `Cargo.toml`, which already carries `iceberg`, `serde`, `tokio`, `url`, `exasol-udf-sdk`, and eleven others, and every member reaches them with `{ workspace = true }`. Copying a version literal instead means nothing enforces the agreement the comment promises: for `reqwest` there are now genuinely two independent `"0.12"` pins (this manifest and the engine's dev-dependency), and for the three `aws-*` crates the comment describes a source manifest that no longer declares them at all, so it documents a synchronization relationship that has ceased to exist. Either version drifting is a silent single-crate change with no compiler or test signal.
- Fix: Add `aws-sigv4 = { version = "1.4", default-features = true }`, `aws-credential-types = { version = "1.2" }`, `aws-smithy-runtime-api = { version = "1", features = ["client"] }`, and `reqwest = { version = "0.12" }` to `[workspace.dependencies]` in the root Cargo.toml, placed with the existing pins and each carrying a one-line note on what needs it (SigV4 signing for the Glue REST catalog; the async catalog HTTP client). Change crates/lakehouse-catalog/Cargo.toml lines 21-31 to reference all four as `{ workspace = true }` and delete both copy-provenance comments, replacing them with the MSRV note the engine manifest carried ("MSRV 1.91.1 — compatible with rustc 1.94 that builds the UDF .so"). Change the engine's `[dev-dependencies]` `reqwest` entry at crates/lakehouse-engine/Cargo.toml line 90 to `{ workspace = true, features = ["blocking"] }`. Do not touch `Cargo.lock` by hand; confirm with `cargo tree -p lakehouse-catalog --depth 1` that the resolved versions are unchanged, that `git diff Cargo.lock` shows no version movement, and that `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, and `make cross-musl-udf-build` all stay green. Note the caution already recorded in the root Cargo.toml: a `features`/`default-features` edit there that does not move `Cargo.lock` does not rotate the CI cache key, so state in the PR description that these four pins moved.
