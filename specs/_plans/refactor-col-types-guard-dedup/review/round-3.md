# Plan Review Findings: refactor-col-types-guard-dedup (round 3)

## Summary
- Axes checked: 6/6
- Total findings: 11 (Blockers: 1, Advisory: 10)
- Intent Fidelity blockers: 0

## Round-2 Blocker Recheck

- **Resolved: [COMPLETENESS_GAP] Neither new test can compile where the plan puts it.** Re-derived independently, not taken from the planner's self-report. Built a faithful module tree (`adapter::pushdown::{support, joins::planning}`, `involved_table_columns` declared `pub(super)` in a private `mod planning`, the existing `use planning::{involved_table_columns, …}` binding retained) and compiled the prescribed shape with `rustc --edition 2021 --test`: `#[cfg(test)] pub(in super::super) fn involved_table_columns_for_test(request, table_name) { planning::involved_table_columns(request, table_name) }` compiles, and a test in `support` reaching it via `super::super::joins::involved_table_columns_for_test` passes. No `E0252` (the new name differs from the existing binding) and no `E0364` (a function is not a re-export). `Json` in `joins/mod.rs` is `use serde_json::Value as Json`, so no private-type-in-public-interface error. `involved_table_columns`' own declaration at `joins/planning.rs:359` is untouched, so the delta's "no PRODUCTION item's visibility widens" clauses hold. One residual is raised as ADVISORY 1, not a re-blocker: the prescribed visibility is one level wider than needed.
- **Resolved: [REQUIREMENT_CONFLICT] The recorded `MUST NOT be merged` scope fence is left standing.** Verified against the recorded library: `specs/datafusion-scan/type-mapping-module-structure/spec.md:56` still carries the `MUST NOT be merged into one table-selector-parameterized builder` clause, and the delta now supersedes it with a second `DELTA:CHANGED` block on the relocation scenario. Diffed both delta scenarios clause-by-clause against the recorded text: exactly four clauses differ — one in the relocation scenario (the merge fence) and three in the classifier scenario (`pub` rationale, rewiring prohibition, doc-comment clause) — which is exactly what the delta's line-17 summary and decision [11] claim. No other clause changed silently. `speq feature validate` exits 0 with no errors. Decision [5]'s misread "recorded clause requires both signatures to hold" is gone; grep confirms no recorded clause constrains either signature.

## Round-2 Advisory Verification

Twelve of the fourteen ADVISORY fixes land faithfully or as sound, self-consistent deviations; two carry residuals raised below as new findings.

- Two-captures-plus-one-inference restatement: applied in delta line 79, decision [6], and § Impact ¶2. Verified.
- Task 3 fixture work named: asset (`tests/common/seed.rs::seed_typed_distinct_probe`), command, and non-commit disposal all exist as described — but the capture channel and the consumer list are wrong (BLOCKER 1, ADVISORY 8).
- Builder signature bounds: re-derived by compiling the stated signature with both wrappers under `-D warnings` — `impl FnOnce(&[Json]) -> Option<&Json>` plus `str::to_uppercase` / `str::to_ascii_uppercase` as `fold_case` compiles and yields `STRASSE` versus `STRAßE`. Verified.
- Row-2 grep gate: `grep -cF 'find(|(n, _)| *n == name)' … support.rs` returns 3 (669, 827, 1009) exactly as stated; line 1120's `n == name` closure is correctly excluded. Verified, with the escaping residual in ADVISORY 4.
- Row-3 grep gate: the planner's deviation from the finding's "five today" is correct — the `Some("DATE")` pattern returns 7 hits at 674, 677, 811, 828, 1013, 1014, 1015. Verified by running it.
- Row 7 deleted rather than replaced: sound. Rows 5 and 6 baselines check out (`exasol_type_from_json` 2 hits in `planning.rs`, `to_ascii_uppercase` 1 hit at line 373).
- Third delta for `spec.md:90`: authored, one clause split into two, nothing else in that scenario altered. Verified by diff — but the delta drops the recorded Background (ADVISORY 2), and the same defect class survives in a second feature (ADVISORY 3).
- `support.rs:884` / `support.rs:6075` repoint: both cross-references exist and say what the plan quotes. Verified.
- Decision [6] task numbers, § Verification row 5's test name (`classify_exa_type_matches_pushdown_guard_predicates` at `types/mapping.rs:1176`), the single sequential chain, the `fold_case`-removal sentence in task 4 and decision [4], and the `pub` rationale rewrite: all applied. Verified.
- § Impact ¶2: four sentences, longest 24 words. Verified.
- Delta lines 17/19 rewrite: line 19 now says the delta supersedes scenario clauses and quotes the rewiring clause; line 17's four-clause enumeration matches the diff. Verified.

## Intent Fidelity

No objection — axis checked. The revision added no work outside the ask. The third delta is the plan's own motivating defect applied to the library, prescribed by round 2's own Fix line; task 3 is CLAUDE.md § Verification discipline applied to a claim the plan publishes. Issue #265's two non-goals (`HashMap`, coercion-policy trait) are still carried verbatim, the interview's "full scope now" is still executed in full, and nothing in scope was dropped this round.

## Feasibility

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: plan.md task 3; § Manual Testing row 4; `vs-adapter/pushdown-module-structure/spec.md` Background bullet at line 79
- Issue: task 3's gate reads a field its named command never prints, so the plan's one live-verification step is untestable as written — and it is the round-1-BLOCKER remediation the whole reachability claim rests on. Task 3 says: "Capture the delivered name twice: from `SYS.EXA_ALL_COLUMNS` for the virtual table, and from `involvedTables[].columns[].name` in the `EXPLAIN VIRTUAL` payload the same run prints." `involvedTables[].columns[].name` is the adapter's INPUT — the pushdown request Exasol sends — and no output of that run contains it. Verified three ways: `scripts/capture-pushdown-payload.sh:38-39` runs only `capture_pushdown_payload`, which prints the SQL, `explain_virtual_sql`'s result, and the execution response; `explain_virtual_sql` (`tests/common/e2e_harness.rs:282`) returns Exasol's `EXPLAIN VIRTUAL` result set, i.e. the adapter-GENERATED SQL, which `docs/debugging-pushdown.md` and the test's own module doc both describe as "the SQL the adapter generates, including the literal scan-spec JSON"; and `adapter/mod.rs` logs no request (its single `udf_log!` at line 269 is the skipped-non-Iceberg-table warning). The failure mode is not a stall: the delivered name IS observable in that same payload, in the scan-spec `"projection":[…]` array and the `EMITS ("NAME" TYPE, …)` clause, which carry the request's column names verbatim (`src/adapter/pushdown/testdata/dispatch_golden/single_group_row_scan.sql`). The likely substitute is worse than the correct route — `SYS.EXA_ALL_COLUMNS` is capture (1)'s own channel and speaks only to the STORED name, so an implementer who falls back to it reports the gate closed while the inference the delta says task 3 closes stays unmeasured.
- Fix: In plan.md task 3, replace "from `involvedTables[].columns[].name` in the `EXPLAIN VIRTUAL` payload the same run prints" with the constructs that payload actually carries: the scan-spec JSON's `"projection"` entry and the generated `EMITS (…)` column list for the `straße` column, both of which reproduce the request-delivered name. State the halt criterion against those two constructs plus `SYS.EXA_ALL_COLUMNS`, and state that `SYS.EXA_ALL_COLUMNS` alone does NOT close the inference. Apply the same correction to § Manual Testing row 4's expected output and to `vs-adapter/pushdown-module-structure`'s Background bullet 79, which currently says task 3 "captures `involvedTables[].columns[].name` … through a real `createVirtualSchema`".

#### [UNSTATED_ASSUMPTION] ADVISORY
- Location: decision-log.md decision [9], final sentence of Rationale
- Issue: the new supporting claim is false. Decision [9] rejects narrowing `classify_exa_type` and `ExaTypeClass` in this plan partly "because `crates/lakehouse-engine/tests/` compiles as an external crate against the lib, so a visibility narrowing is a compile risk this pure refactor should not absorb". Grep over `crates/lakehouse-engine/tests/` returns no reference to either item, so `pub(crate)` breaks no target there. The conclusion survives on the API-neutrality reason alone, so this is a false premise inside a sound decision — the same defect class (an unverified claim recorded as fact) the plan exists to remove.
- Fix: In decision-log.md decision [9], delete the `tests/` compile-risk sentence or replace it with the verified statement: no target under `crates/lakehouse-engine/tests/` references `classify_exa_type` or `ExaTypeClass`, and `pub` is retained solely to keep the diff API-neutral.

#### [UNSTATED_ASSUMPTION] ADVISORY
- Location: plan.md task 3 (revert rationale); decision-log.md `[plan-review]` entry "Task 3's fixture work, its asset, and its disposal are named"
- Issue: the named fixture-consumer set is wrong in both places. Both state that "`typed_distinct_probe`'s 12-row column vectors are the single source of truth for `e2e_capability_test`, `e2e_count_distinct_test`, and `e2e_scan_test`". Grep for `E2E_TYPED_TABLE` / `typed_distinct_probe` over `crates/lakehouse-engine/tests/` returns `e2e_capability_test.rs`, `e2e_count_distinct_test.rs`, and `e2e_capture_pushdown.rs`. `e2e_scan_test.rs` does not use the probe at all — it seeds `seed_events`, `seed_renamed_column`, and `seed_added_columns_initial_default`. The revert decision still holds on the two suites that do assert against the probe, but the list names a non-consumer and omits `e2e_capture_pushdown`, which is task 3's own capture vehicle.
- Fix: In plan.md task 3 and in the matching decision-log `[plan-review]` entry, replace `e2e_scan_test` with `e2e_capture_pushdown` in the consumer list.

## Requirement Quality

#### [COMPLETENESS_GAP] ADVISORY
- Location: `specs/_plans/refactor-col-types-guard-dedup/vs-adapter/pushdown-planning-like-type-coercion/spec.md` § Background
- Issue: the new third delta carries none of the recorded feature's Background, so recording it risks deleting 19 bullets of a behavioral feature. Counted per delta: `vs-adapter/pushdown-module-structure` keeps 43 of 43 recorded bullets verbatim and adds 12; `datafusion-scan/type-mapping-module-structure` keeps 8 of 8 and adds 6; the new like-type-coercion delta keeps 0 of 19 and carries only its 7 new bullets. `/speq:spec-merge`'s marker table governs SCENARIOS only (`DELTA:NEW` / `CHANGED` / `REMOVED`), so Background reconciliation is the recorder's judgement, and the archived precedent is mixed — `002-refactor-pushdown-collect-walk`'s delta carried new-only bullets and the library kept the union, while `2026-07-28-fix-210-string-functions-type-blind`'s decimal-string-format delta has none of its 3 bullets in the library today. Two of the 19 at risk are load-bearing: recorded lines 28 and 37 record the accepted trade-off that a fail-safe decline can lose a pushdown that previously rendered and succeeded.
- Fix: In the `vs-adapter/pushdown-planning-like-type-coercion` delta, prepend the recorded feature's 19 Background bullets verbatim (from `specs/vs-adapter/pushdown-planning-like-type-coercion/spec.md` § Background) ahead of the 7 new bullets, matching the Background shape this plan's other two deltas already use.

#### [COMPLETENESS_GAP] ADVISORY
- Location: `specs/vs-adapter/pushdown-planning-string-fn-type-coercion/spec.md:83` (recorded) versus plan.md § Features and task 1
- Issue: the stale mirror-convention clause the plan fixes for the LIKE guard survives verbatim for the string-function guard. Line 83 reads "*AND* the name lookup SHALL uppercase the argument's column name before matching, mirroring `extract_all_column_types`'s uppercasing, so a case-mismatched name resolves rather than spuriously declining". That is `coerce_string_position_arg`'s lookup — one of the three guards task 1 rewires onto `column_exa_type` — so after this plan the guard mirrors nothing; it calls the one owner. The plan's own argument for the third delta ("leaving the identical stale citation standing in the permanent library would fix the defect in the code and preserve it in the spec") applies here unchanged, minus the drifted line number. The plan neither amends nor excludes it.
- Fix: In plan.md § Features, add a CHANGED delta for `vs-adapter/pushdown-planning-string-fn-type-coercion` amending the line-83 clause to name `column_exa_type` as the normalization's owner, and cite it in task 1 — or state the exclusion in § Non-Goals with the reason that this clause carries no line number and so cannot drift.

#### [REQUIREMENT_CONFLICT] ADVISORY
- Location: `datafusion-scan/type-mapping-module-structure/spec.md` delta § Background (the "pass-through" bullet) versus the same file's amended relocation clause
- Issue: the delta contradicts itself as it will be recorded. Its Background carries verbatim "A helper whose whole body is a call to another helper with the same arguments is a pass-through, the shallow-module red flag from `/speq:design-philosophy`. Every consolidation below deletes the duplicate rather than keeping it as a wrapper." The amended relocation clause then records a consolidation that keeps two wrappers: "each a thin wrapper supplying its own table selection and case fold to the shared builder". The reconciling distinction — a pass-through forwards the SAME arguments, a partial application supplies different ones — exists only in the sibling `vs-adapter/pushdown-module-structure` delta's Background, so the type-mapping feature file alone reads as self-contradictory.
- Fix: In the `datafusion-scan/type-mapping-module-structure` delta, amend the pass-through Background bullet's second sentence to except a wrapper that supplies arguments the shared helper does not choose, naming the two `col_types` wrappers as that case.

#### [REQUIREMENT_CONFLICT] ADVISORY
- Location: plan.md task 1 and task 4; `vs-adapter/pushdown-module-structure/spec.md` Background bullet 84 and clauses at lines 104, 117, 121; decision-log.md `[plan-review]` entry "Both new tests reach `involved_table_columns` through a `#[cfg(test)]` wrapper function"
- Issue: the prescribed spelling widens the wrapper one level past the narrowest visibility that compiles, against this feature's own recorded rule. `pub(in super::super)` written in `joins/mod.rs` resolves relative to `joins`, so it means `pub(in crate::adapter)`, not `pub(in adapter::pushdown)` — verified with `rustc`: a test in a sibling `adapter::other` module reaches the wrapper under `pub(in super::super)` and is denied it (`E0603`) under `pub(super)`, while `support` reaches it under both. The spelling was lifted from `sql_builders.rs`, one module deeper, where the identical tokens do mean `pub(in adapter::pushdown)`. This feature's recorded Background rule is "A cross-submodule private helper widens to the narrowest visibility that compiles (`pub(super)`)", and delta clause 117 leans on that same narrowest-visibility reasoning for the builder in the very sentence that introduces the wrapper.
- Fix: In plan.md tasks 1 and 4, in the `vs-adapter/pushdown-module-structure` delta (bullet 84 and the clauses at lines 104, 117, 121), and in the matching decision-log `[plan-review]` entry, change the wrapper's declaration to `#[cfg(test)] pub(super) fn involved_table_columns_for_test(request: &Json, table_name: &str) -> Vec<(String, String)>` in `joins/mod.rs`, and state that `pub(super)` there means `pub(in adapter::pushdown)` and is the narrowest visibility that reaches `support`.

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: plan.md § Manual Testing rows 2 and 3
- Issue: the two rows need opposite readings of `\|`, and one reading makes row 3 pass vacuously on today's tree. Row 2 is a `-F` fixed-string count, so a literal backslash breaks it: `grep -cF 'find(\|(n, _)\| *n == name)' … support.rs` returns 0, while the unescaped form returns the stated 3 (669, 827, 1009). Row 3 needs `\|` to survive as BRE alternation: the escaped form returns the stated 7 hits (674, 677, 811, 828, 1013, 1014, 1015), and the unescaped form returns 0 — so a reader who takes `\|` as Markdown table escaping runs a gate that already reports "No hits at all" before the refactor and proves nothing. Both readings are defensible from the source, and each breaks one row.
- Fix: In plan.md § Manual Testing, make each row's shell form unambiguous: write row 2 as `grep -cF 'find(|(n, _)| *n == name)' …` and note that the pipes are literal characters of the search string, and rewrite row 3 with `grep -rEn 'starts_with\("VARCHAR"\)|starts_with\("CHAR"\)|starts_with\("DECIMAL"\)|Some\("DATE"\)' crates/lakehouse-engine/src/adapter/` so alternation does not depend on backslashes surviving Markdown. Keep both stated baselines (3 and 7).

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: plan.md § Manual Testing row 4 (second command)
- Issue: the command cannot run. `scripts/capture-pushdown-payload.sh` requires exactly one positional SQL argument — `if [ $# -ne 1 ]; then echo "usage: …" >&2; exit 1; fi` at lines 22-25 — and sets `CAPTURE_SQL="$1"` itself at line 38. Row 4 invokes it as `CAPTURE_SQL='SELECT * FROM {table} WHERE "STRAßE" LIKE '\''a%'\''' scripts/capture-pushdown-payload.sh` with no argument, so it exits 1 with the usage message. Task 3 states the correct positional form, so the plan's two statements of its own gate disagree.
- Fix: In plan.md § Manual Testing row 4, change the second command to the positional form `scripts/capture-pushdown-payload.sh 'SELECT * FROM {table} WHERE "STRAßE" LIKE '\''a%'\'''`, matching task 3.

#### [COMPLETENESS_GAP] ADVISORY
- Location: plan.md § Design → Context, task 1, and § Dead Code Removal row 2; `vs-adapter/pushdown-module-structure/spec.md` Background bullet 85 and clause at line 102
- Issue: the plan's motivating defect is not where the plan says it is, and the delete list omits its actual location. The only `support.rs:411` citation in the crate is an inline body comment at `support.rs:659-661` — "Uppercase the subject name before lookup, matching `extract_all_column_types`'s uppercasing (`support.rs:411`)" — inside `guard_like_subject`, directly above the lookup block `column_exa_type` replaces. Task 1 enumerates three locations (592, 811, 985), none of them 661, and then says the deletion is "including `guard_like_subject`'s stale `support.rs:411` citation", as though it were among them. Verified line by line: 592 is `like_subject_type_guard`'s doc comment (the pass at line 635, not one of the three guards) and carries no line number; 811 is `is_bare_decimal_column`'s; 985 is `coerce_string_position_arg`'s; `guard_like_subject` itself carries no doc-comment mirror note at all. So § Dead Code Removal row 2's "same three guards (`support.rs:592`, `811`, `985`)" attributes a note to a function outside the three, and delta bullet 85 and clause 102 will record in the permanent library that a DOC COMMENT cites the stale line number when an inline comment does. Separately, the 592 note is a parenthetical inside the sentence that introduces `like_subject_type_guard`'s four-row dispatch table, so "delete the three mirror-note doc lines" taken literally orphans that table.
- Fix: In plan.md task 1 and § Dead Code Removal row 2, add `support.rs:659-661` as the stale `support.rs:411` citation's location, describing it as an inline comment inside the lookup block `column_exa_type` replaces; attribute `support.rs:592` to `like_subject_type_guard` rather than to one of the three guards; and state that the 592 note is removed as a parenthetical, keeping its sentence and the dispatch table it introduces. Correct `vs-adapter/pushdown-module-structure`'s Background bullet 85 and clause 102 to say the stale line-number citation sits in an inline comment, not a doc comment.

## Task Breakdown

No objection — axis checked. Every delta scenario has an implementing task (helper → 1, builder → 5 with its characterization test at 4, classifier → 2, the amended LIKE clause → 1, the amended type-mapping clauses → 2 and 5), and every task implements something in scope; task 3 produces the evidence a delta Background bullet cites, which the plan states. The single chain 1 → 2 → 4 → 5 → 6 with task 3 alongside task 1 is the shape round 2 prescribed, and its shared-file rule for `support.rs` (6,137 lines, verified) covers tasks 1, 2, 4, and 5. Task 3's only edit target is `crates/lakehouse-engine/tests/common/seed.rs`, which no other task touches. Every test name and line number the plan cites resolves against the tree, including `classify_exa_type_matches_pushdown_guard_predicates` (`types/mapping.rs:1176`), `like_guard_unresolvable_column_declines` (`support.rs:4134`), and `string_fn_guard_resolves_case_mismatched_column_name` (`support.rs:6077`).

## Design Depth

No objection beyond ADVISORY 1 above — axis checked. The two-seam split (`column_exa_type` resolves, `classify_exa_type` classifies) keeps the lookup miss out of the classifier as the recorded #176 contract requires, and the guards' retained `type == "column"` test is justified by a real behavioral difference (pass-through versus decline), not by inertia. The `fold_case` parameter is the one configuration parameter added, and both the filed issue and decision [4] now state that closing the divergence removes it — the scheduled follow-up `/speq:design-philosophy` asks for. Dependency direction is unchanged (`adapter` → `types`). Confirmed no fourth node-based `col_types` lookup exists that the three-guard inventory misses: the only other folds in `support.rs` are a `dataType` string fold (758), a function-name fold (777), `extract_all_column_types` (446), `project_columns` (1188), and the collect walk (1343).

## Prose Quality

#### [PROSE_BLOAT] ADVISORY
- Location: decision-log.md, the `[plan-review]` entries added or rewritten this round
- Issue: governed decision-log prose runs 2-3x past the 25-word sentence cap in the entries this round produced. Worst cases, measured: 76 words in "Task 3's fixture work, its asset, and its disposal are named" (the Direction change sentence enumerating the seeder edit, the excluded Spark job, and the capture command); 69 words in "The merged builder's signature carries its bounds"; 65 words in "The type-mapping delta's supersession bullet describes a scenario clause"; 56 words each in the scope-fence and grep-gate entries. `/speq:writing-guardrails` governs decision-log Rationale and Finding prose and caps sentences at 25 words. Round 2 fixed the same defect in § Impact, where the fix landed cleanly — every sentence there is now under 25 words.
- Fix: In decision-log.md, split the three worst sentences — the 76-word Direction change in "Task 3's fixture work, its asset, and its disposal are named", the 69-word one in "The merged builder's signature carries its bounds", and the 65-word one in "The type-mapping delta's supersession bullet describes a scenario clause" — into sentences of at most 25 words each, one claim per sentence.
