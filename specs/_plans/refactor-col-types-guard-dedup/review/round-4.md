# Plan Review Findings: refactor-col-types-guard-dedup (round 4)

## Summary
- Axes checked: 6/6
- Total findings: 9 (Blockers: 1, Advisory: 8)
- Intent Fidelity blockers: 0

## Round-3 Blocker Recheck

- **NOT RESOLVED: [UNSTATED_ASSUMPTION] Task 3's live-capture gate reads a field the capture command never prints.** Re-derived independently against the tree, not taken from the planner's self-report. The round-3 diagnosis was right and the substitute channel is wrong. Task 3 now reads the delivered name from "the scan-spec JSON's `"projection"` entry and the generated `EMITS (…)` column list". Both constructs exist in the printed payload (`src/adapter/pushdown/testdata/dispatch_golden/single_group_row_scan.sql` carries `"projection":["REGION","AMOUNT"]` and `EMITS ("REGION" VARCHAR(2000000), …)`), so the channel is readable — but neither carries the delivered name. Both pass it through the Unicode `str::to_uppercase`, the exact fold whose divergence the gate exists to measure. Detail in BLOCKER 1.

## Round-3 Advisory Verification

Eight of the ten advisory fixes land faithfully, including all four documented deviations from the prescribed Fix text. Two carry residuals raised below as new findings.

- **Deviation 1, inline-comment range 660-662 instead of the finding's 659-661: CORRECT.** Verified line by line — 659 is blank, 660-662 is the three-line comment block, and the `support.rs:411` token sits on 661. `grep -rn 'support.rs:411' crates/lakehouse-engine/src` returns exactly that one line, so the "sole carrier" claim holds. `support.rs:592` is `like_subject_type_guard`'s doc comment, its declaration is at 635, and the parenthetical spans 592-593 inside the sentence introducing the four-row table at 596-600 — the attribution and the parenthetical-only delete are both right.
- **Deviation 2, one shared escaping convention instead of the finding's exact text: PARTLY CORRECT.** Ran all four forms. Row 2's bare-pipe `-F` form returns 3 at lines 669, 827, 1009; the backslash form returns 0. Row 3's `grep -rEn` bare-pipe form returns 7 at 674, 677, 811, 828, 1013, 1014, 1015, all in `support.rs`; the backslash form returns 0. Both stated baselines are correct and the GFM-cell-splitting reason for not copying the finding's spelling is sound. The residual: the new table-wide convention silently breaks a third row that was previously fine (ADVISORY 3).
- **Deviation 3, fix relocated to the `[plan-review]` API-neutrality entry instead of decision [9]: CORRECT.** Decision [9] never carried the compile-risk claim; grep confirms the only surviving instance of "compiles as an external crate" is inside the round-3 Finding text at decision-log line 182, where restating the refuted claim is the point. The verified replacement landed at line 159. Residual: it landed beside a sentence it duplicates verbatim (ADVISORY 8).
- **Deviation 4, authoring the fourth delta rather than taking the exclusion option: CORRECT.** Recorded `specs/vs-adapter/pushdown-planning-string-fn-type-coercion/spec.md:83` reads exactly as quoted and is `coerce_string_position_arg`'s lookup clause, so the stale-owner defect was real. The new delta amends that one clause and carries all 12 recorded Background bullets verbatim plus 7 new ones (19 total) — `comm` against the recorded file shows zero recorded bullets dropped.
- Fixture consumer list: `grep -rln 'E2E_TYPED_TABLE\|typed_distinct_probe' crates/lakehouse-engine/tests/` returns `e2e_capability_test.rs`, `e2e_capture_pushdown.rs`, `e2e_count_distinct_test.rs`. Task 3 and the matching decision-log entry now name exactly those three. Verified.
- Like-type-coercion delta Background: 19 recorded bullets prepended verbatim, 26 total, zero dropped by `comm`. Verified.
- Type-mapping pass-through bullet: now excepts a wrapper supplying arguments the shared helper does not choose, naming both `col_types` wrappers. Verified — with a recording-mechanics residual (ADVISORY 6).
- Wrapper visibility: `#[cfg(test)] pub(super)` in `joins/mod.rs` is now the spelling in plan.md tasks 1 and 4, delta bullet 84, delta clauses 104/117/121, and the round-2 decision-log entry. Re-derived: written in `joins/mod.rs`, `pub(super)` is `pub(in adapter::pushdown)`, which reaches `adapter::pushdown::support` as a descendant. Verified.
- Row 4 positional form: matches task 3 and the script's `[ $# -ne 1 ]` contract. Verified.
- Prose split: the three named sentences went 76w → 29w, 69w → 24w, 65w → 30w. Two overshoot the 25-word cap by 4 and 5 words. Not re-raised on its own.
- `speq feature validate` exits with 0 errors (warnings only, all pre-existing AND-step counts).

## Intent Fidelity

No objection — axis checked. The round added no work outside the ask. The fourth delta is round 3's own prescribed Fix, applying this plan's motivating defect to the second feature whose recorded clause names the wrong owner. Issue #265's two non-goals (`HashMap`, coercion-policy trait) are carried verbatim in plan.md § Non-Goals and decision [8]. The interview's "full scope now" is still executed in full: lookup helper (task 1), builder merge (task 5), classifier rewiring (task 2). Nothing in scope was deferred.

## Feasibility

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: plan.md task 3 (capture (b)); plan.md § Manual Testing row 4; `vs-adapter/pushdown-module-structure/spec.md` Background bullet 79; decision-log.md decision [6] and `[plan-review]` entry "Task 3's live capture reads the generated payload's projection and EMITS list"
- Issue: the replacement capture channel cannot observe the delivered name, so the gate is blind to the one distinction it exists to make. Task 3 asserts of the scan-spec `"projection"` entry and the `EMITS (…)` list: "Both reproduce the request-delivered column name verbatim." Both instead reproduce it after `str::to_uppercase`. Traced end to end. `extract_projection` (`support.rs:1072-1077`) calls `project_columns(pushdown_req, extract_all_column_types(request))`, and `extract_all_column_types` folds every name with `.to_uppercase()` (`support.rs:446`). Inside `project_columns`, a `SELECT *` (`selectList` absent or null) takes `full_row()`, whose names come straight from that Unicode-folded `all_cols` (`support.rs:1130-1137`); an explicit select-list `column` node takes `e["name"].as_str().map(|s| s.to_uppercase())` (`support.rs:1185-1190`). The `EMITS` names are those same projection items (`ProjectionItem::emit_name`, `scan/spec.rs:103-108`). The scan-spec `"filter"` is no escape either: `vs-expression`'s `"column"` arm folds with `.to_uppercase()` before quoting (`crates/vs-expression/src/lib.rs:632-636`). Confirmed with `rustc` that `"STRAßE".to_uppercase()` is `STRASSE`. So on a single-table capture the payload prints `STRASSE` whether Exasol delivered `STRAßE` or `STRASSE` — the gate returns the same reading under both hypotheses, halts the plan on the outcome the delta predicts, and can never refute the inference it is stated to close. This is the round-1-BLOCKER remediation the whole reachability claim rests on, and it is now unfalsifiable rather than merely unreadable.
- Fix: In plan.md task 3, replace capture (b) with a read that does not pass through `str::to_uppercase`. Use `SYS.EXA_ALL_COLUMNS` for the VIRTUAL table's own adapter-declared `straße` column as the discriminating read: `build_virtual_tables` declares the name verbatim, so the served name is Exasol's own fold of an adapter-declared JSON `"name"`, which is exactly the inference's content. Delete the claim that `SYS.EXA_ALL_COLUMNS` "alone does NOT close the inference" and state instead the one step it leaves open — that the pushdown request carries the stored name, which capture (1) already established for ASCII names. If a DELIVERED-name read is still wanted, state that the only generated-SQL construct preserving it is one built from `involved_table_columns`' ASCII fold (`joins/rendering.rs:38-39` feeding `project_columns`' `full_row`), which needs a two-table join capture, not the single-table `SELECT *` row 4 states. Apply the same correction to § Manual Testing row 4's command and expected output, to `vs-adapter/pushdown-module-structure` Background bullet 79 ("both of which reproduce the request's column name verbatim"), to decision [6], and to the `[plan-review]` entry naming the projection and EMITS channels.

#### [UNSTATED_ASSUMPTION] ADVISORY
- Location: plan.md task 1 and § Dead Code Removal row 2 (the `support.rs:884` repoint); § Manual Testing row 9; `vs-adapter/pushdown-planning-string-fn-type-coercion/spec.md` Background bullet 35
- Issue: the prescribed repoint sends a reader to a helper that owns nothing the comment describes, and the gate enforcing it contradicts a delta bullet. `support.rs:884-886` reads "`fn_name` is uppercased before matching, mirroring the uppercase-then-look-up convention of [`guard_like_subject`] / [`extract_all_column_types`]" — that is `string_position_args` folding a FUNCTION name to match its per-function argument-index table. `column_exa_type` folds a COLUMN name and scans `col_types`; it owns no function-name normalization, so naming it as this comment's convention owner substitutes one misdirection for another. Meanwhile § Manual Testing row 9 makes "a surviving `mirroring extract_all_column_types` claim in either cross-reference" a gate failure, while the string-fn delta's Background bullet 35 records that "`extract_all_column_types` still produces the list and still uppercases" — so the gate forces deletion of a reference the plan's own delta certifies as still accurate.
- Fix: In plan.md task 1 and § Dead Code Removal row 2, split the two cross-references. Repoint only `support.rs:6075`'s test doc comment ("mirroring `extract_all_column_types`") at `column_exa_type`, since it describes a column-name lookup. For `support.rs:884`, instruct deleting the trailing "mirroring the uppercase-then-look-up convention of [`guard_like_subject`] / [`extract_all_column_types`]" clause and keeping the `fn_name` sentence, because no helper this plan creates owns function-name folding. Narrow § Manual Testing row 9's expectation to at least 5 `column_exa_type` lines (declaration, three call sites, the 6075 repoint) and drop the two-cross-reference wording.

## Requirement Quality

#### [REQUIREMENT_CONFLICT] ADVISORY
- Location: plan.md task 1 and § Dead Code Removal row 2 (`support.rs:811`); decision-log.md `[plan-review]` entry "The stale `support.rs:411` citation is an inline comment at `support.rs:660-662`"
- Issue: the line number given for the note to DELETE names the line the plan elsewhere requires be KEPT. Task 1 says "`is_bare_decimal_column`'s closing sentence (`support.rs:811`)" and row 2 repeats "THREE doc comments — … `is_bare_decimal_column`'s (`support.rs:811`)". Verified line by line: 811 is `/// \`DECIMAL(p,0)\`, so \`.starts_with("DECIMAL")\` also matches them (harmless: the`, and the closing mirror sentence "Mirrors [\`guard_like_subject\`]'s uppercase + \`col_types\` lookup." sits at 812-813. Line 811 is precisely the rationale line task 2 must reword and `vs-adapter/pushdown-module-structure` delta clause 102 requires be preserved with `ExaTypeClass::Decimal` named in place of the predicate. An implementer following the delete list literally removes the sentence round 1's own BLOCKER 3 established must survive. This round's fix pass corrected the 411 citation from 659-661 to 660-662 with exactly this care and left the sibling citation one-to-two lines off.
- Fix: In plan.md task 1 and § Dead Code Removal row 2, change `is_bare_decimal_column`'s mirror-note location from `support.rs:811` to `support.rs:812-813`, and add that `support.rs:811` is NOT deleted — it is the `.starts_with("DECIMAL")` rationale line task 2 rewords to name `ExaTypeClass::Decimal`. Apply the same correction to the matching decision-log `[plan-review]` entry's "three doc comments (592, 811, 985)" enumeration.

#### [COMPLETENESS_GAP] ADVISORY
- Location: plan.md task 1 and § Dead Code Removal row 2 (`support.rs:985`)
- Issue: the orphaned-table defect the plan fixed for `support.rs:592` survives unfixed for `support.rs:985`. Task 1 says "Keep `coerce_string_position_arg`'s dispatch table and its `(#223)` note" while listing 985 among the doc lines to delete. Verified: 985-986 reads "/// Type-dispatch ONE string-position argument, mirroring [`guard_like_subject`]'s / /// uppercase-then-look-up convention:" — one sentence that is simultaneously the function's summary line, the mirror note, and the colon introducing the four-row table at 988-993. Deleting the line deletes the summary and strands the table, the exact failure the plan spells out for 592 ("remove the parenthetical alone and keep both the sentence and the table"). The mirror text here is a trailing clause, not a parenthetical, so the 592 instruction does not transfer.
- Fix: In plan.md task 1 and § Dead Code Removal row 2, state that `support.rs:985`'s note is removed as the trailing clause ", mirroring [`guard_like_subject`]'s uppercase-then-look-up convention" — keeping "Type-dispatch ONE string-position argument:" and the four-row table it introduces — and that only the clause goes, matching the treatment already stated for `support.rs:592`.

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: plan.md § Manual Testing rows 2, 3, and 7 (the `type-mapping-module-structure` row)
- Issue: the new table-wide escaping convention makes a previously-sound gate vacuous. Rows 2 and 3 now declare "in this table every `\|` is Markdown escaping for ONE bare `|` character typed in the shell". Row 7's command is `grep -n 'not yet wired\|intended first consumer' crates/lakehouse-engine/src/types/mapping.rs`. Under plain `grep` (BRE) a bare `|` is a LITERAL character and `\|` is the alternation operator, so the convention inverts this row: ran both, the backslash form returns 1 hit today (`types/mapping.rs:373`) and the bare-pipe form returns 0 — before AND after task 2. A reader applying the stated convention runs a gate that already reports the passing answer. Rows 2 and 3 escaped this because `-F` and `-E` change what a bare pipe means; the convention was written for those two rows and applied to a table containing a third that needs the opposite reading.
- Fix: In plan.md § Manual Testing, scope the escaping statement to rows 2 and 3 rather than "this table", and rewrite the `type-mapping-module-structure` row as `grep -nE 'not yet wired\|intended first consumer' crates/lakehouse-engine/src/types/mapping.rs`, stating that under `-E` its `\|` is Markdown escaping for one bare `|` alternation operator, consistent with row 3. Add the falsifying baseline: 1 hit today at `types/mapping.rs:373`.

#### [REQUIREMENT_CONFLICT] ADVISORY
- Location: `datafusion-scan/type-mapping-module-structure/spec.md` delta Background bullet 13 and the line-17 summary
- Issue: the delta now amends a RECORDED Background bullet with no supersession marker, while claiming it amends nothing but scenario clauses. Line 17 states "This delta amends FOUR clauses across TWO scenarios of this feature and nothing else." `comm` against `specs/datafusion-scan/type-mapping-module-structure/spec.md` shows one recorded bullet no longer present verbatim: the pass-through bullet, rewritten in place for round 3's ADVISORY. Every other Background change in this plan's deltas uses the explicit "This delta SUPERSEDES the preceding Background bullet «verbatim quote»" form (see `vs-adapter/pushdown-module-structure` bullets 55 and 67-72), which is what makes the change visible to the recorder. Round 2's BLOCKER 2 was this same defect: a line-17 enumeration that under-reported what the delta actually changed.
- Fix: In the `datafusion-scan/type-mapping-module-structure` delta, restore the recorded pass-through bullet verbatim and add a following "This delta SUPERSEDES the preceding Background bullet …" bullet carrying the partial-application exception, matching the supersession form the sibling delta uses. Amend the line-17 summary to state that the delta also supersedes one Background bullet, so the enumeration matches the diff.

#### [REQUIREMENT_CONFLICT] ADVISORY
- Location: `datafusion-scan/type-mapping-module-structure/spec.md` delta, relocation scenario, amended clause at line 35
- Issue: the amended clause restates the contract it forbids itself to restate. It specifies the shared builder — "`support::column_types`, which owns the `involvedTables` walk, the `name`/`dataType` read, and the `exasol_type_from_json` mapping", plus both wrappers' full signatures and files — and then closes with "`vs-adapter/pushdown-module-structure` owns the merged builder's contract; this feature SHALL neither restate it nor forbid it". Both features will therefore carry the same normative statement of what `column_types` owns, with nothing enforcing their agreement — the divergence this plan exists to remove, reintroduced at the spec layer. The delta's own line 18 draws the boundary correctly for the classifier ("This feature keeps owning WHAT `classify_exa_type` returns"), so the pattern is available.
- Fix: In the `datafusion-scan/type-mapping-module-structure` delta's relocation clause, cut the builder's responsibility list and both wrapper signatures. Keep only what this feature owns: that its own `MUST NOT be merged` fence was a scope fence around the relocation, that issue #265 has delivered the merge, that the shared builder is `support::column_types`, and that `vs-adapter/pushdown-module-structure` owns its contract.

## Task Breakdown

#### [TASK_GRANULARITY] ADVISORY
- Location: plan.md § Design → Patterns (last row) versus § Parallelization Group A; plan.md task 1 (the `STRAßE` test) and task 3 (the gate)
- Issue: the plan states an ordering its own parallelization violates. The Patterns row reads "the live reachability capture (task 3) precedes the test", and decision [6] calls test ordering load-bearing. But § Parallelization puts "Group A | Task 1, Task 3", and task 1 adds `column_exa_type_resolves_unicode_folded_list_and_misses_ascii_folded_list`, whose input the plan fixes as "the DELIVERED form `STRAßE`". So one of the two tests the gate is supposed to precede runs concurrently with it. Task 3's halt clause acknowledges the consequence ("revise tasks 1 and 4 to the delivered form before continuing"), which means a failed gate rewrites an already-landed test rather than preventing it. Task 4's test is correctly sequenced after task 3; task 1's is not.
- Fix: In plan.md § Parallelization, either move task 1's `STRAßE`-pinned test into task 4 (leaving task 1 the helper extraction and the comment deletes, which need no delivered-name input), or move task 3 ahead of task 1 as a sole predecessor and drop Group A. Then correct the § Design → Patterns row so "the capture precedes the test" names both tests, not only task 4's.

## Design Depth

No objection — axis checked. The two-seam split holds: `column_exa_type` resolves and `classify_exa_type` classifies, keeping the lookup miss out of a classifier the recorded #176 contract gives a non-optional `&str` (`specs/datafusion-scan/type-mapping-module-structure/spec.md` classifier scenario, verified). The retained `type == "column"` test is justified by a real behavioral split — verified in the code that `guard_like_subject` (`support.rs:653-656`) and `coerce_string_position_arg` (`support.rs:999-1001`) return a non-`column` node unchanged while a miss declines, so absorbing the tag test would convert pass-through to decline. `fold_case` remains the single added configuration parameter, and both the filed issue (task 4) and decision [4] require stating that closing the divergence removes it. Dependency direction unchanged: `adapter` → `types`. Re-confirmed no fourth node-based `col_types` lookup is missed — the three in-scope scans are the only `find(|(n, _)| *n == name)` occurrences (669, 827, 1009), and `project_columns`' closure at 1120 takes an already-resolved `&str`.

## Prose Quality

#### [PROSE_BLOAT] ADVISORY
- Location: decision-log.md line 159, `[plan-review]` entry "`pub` is retained for an API-neutrality reason, not a recorded-clause reason"
- Issue: this round's fix duplicated a sentence verbatim inside the entry it edited. "`pub` is retained solely to keep this refactor's diff API-neutral" appears twice in that single Direction change — once after "The surviving reason is the one that holds…" and again after the new "No compile-risk reason is claimed: grep confirms…" sentence. The relocated fix appended its own restatement beside the existing one instead of replacing the refuted claim in place. `/speq:writing-guardrails` governs decision-log Direction-change prose and bans repetition not required to make the point.
- Fix: In decision-log.md line 159, delete the second occurrence of "`pub` is retained solely to keep this refactor's diff API-neutral." — the sentence immediately preceding "No issue is filed:" — keeping the first occurrence and the new grep-verified compile-risk sentence.
