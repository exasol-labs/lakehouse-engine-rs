# Plan Review Findings: refactor-col-types-guard-dedup (round 6)

## Summary
- Axes checked: 6/6
- Total findings: 3 (Blockers: 1, Advisory: 2)
- Intent Fidelity blockers: 0

## Round-5 Blocker Recheck

- **MOSTLY RESOLVED: [UNSTATED_ASSUMPTION] Task 3 closes the fold link, and the residual is a `ß`-collapse.** The self-contradiction is gone and the arithmetic is now right. Re-derived independently from the source, not from the planner's self-report:
  - Producer/consumer identity: `extract_all_column_types` folds with `str::to_uppercase` (`support.rs:446`), the three guards fold the node name the same way, `involved_table_columns` folds with `to_ascii_uppercase`. Single-table therefore always resolves; the join path resolves iff `to_ascii_uppercase(N) == to_uppercase(N)`.
  - For `N = STRAßE`: `to_uppercase` → `STRASSE`, `to_ascii_uppercase` → `STRAßE` → miss. For `N = straße`: `STRASSE` and `STRAßE` → miss. For `N = NÜM`: both folds are no-ops → resolves, so the plan's `nüm` counter-example is correct. For `N = STRASSE`: both folds agree → resolves.
  - **The named residual is exactly necessary and sufficient.** Any name containing `ß` necessarily diverges, because `to_uppercase` maps `ß` → `SS` and `to_ascii_uppercase` does not; any name without `ß` (in the shapes at issue) necessarily agrees. So "does `involvedTables[].columns[].name` still carry `ß`" is the one refuting branch, and no second branch exists. I also checked the branch the plan does not discuss — whether the expression `column` node's `name` could carry a different form than the `involvedTables` entry — and it is not load-bearing either: with `involvedTables` carrying `ß`, the divergence holds for a node name of `STRAßE`, `straße`, or `STRASSE` alike. The plan's residual statement is minimal and correct. Five rounds on this claim have converged; the arithmetic needs no further round.
  - Task 3 no longer claims both "closes it end to end" and "one step stays outside". It now reads "This task closes that ONE link — the fold — and nothing beyond it", and the capture-(1) disclaimer ("every name it read is ASCII … where the two folds agree") is correct.
  - What is **NOT** resolved is one surviving sentence in the permanent-library artifact. The round-5 fix ordered the "SERVED name rather than the Iceberg one" framing deleted from `vs-adapter/pushdown-module-structure` Background bullet 79; it survives there as capture (1)'s headline. Detail in BLOCKER 1.

## Round-5 Advisory Verification

Six of the seven advisory fixes land whole. One (prose) lands partially.

- **Three partial deletes, and `support.rs:592-593`: CORRECT.** Verified line by line: 592 ends "the subject name is uppercased (mirroring", 593 begins "[`extract_all_column_types`]'s uppercasing convention) and looked up in", 594 is "`col_types`, then dispatched:" (the table intro). 811 is the `.starts_with("DECIMAL")` rationale ending "(harmless: the", 812 opens "trim is a no-op on a scale-0 value). Mirrors [`guard_like_subject`]'s uppercase", 813 is "+ `col_types` lookup." 985 carries the summary plus the mirror clause, 986 the colon, the table 988-993. 660-662 is the inline comment, with `support.rs:411` on 661 — and `grep -rn 'support.rs:411' crates/lakehouse-engine/src` returns exactly that one line, confirming it is the crate's sole carrier. `like_subject_type_guard` is declared at `support.rs:635` as the plan states.
- **Pipe-escaping convention: CORRECT and no longer vacuous.** Row 2 now names the character ("each `\|` renders one PIPE character, typed in the shell with NO backslash"), and rows 3 and 7 cite that rule. Re-ran all three baselines against the tree: row 2's `-F` bare-pipe form returns 3 (669, 827, 1009); row 3's `-rEn` form returns 7 (674, 677, 811, 828, 1013, 1014, 1015, all in `support.rs`); row 7's `-E` form returns 1 at `types/mapping.rs:373`. Row 9's `column_exa_type` count returns 0 today, and `project_columns`' closure at `support.rs:1120` reads `.find(|(n, _)| n == name)` with no `*`, so row 2's exclusion of it is correct.
- **Row 4 constants and decoupled capture SQL: CORRECT.** `e2e_capture_pushdown.rs:26` declares `const VS_NAME: &str = "MY_LAKEHOUSE"`; the `{table}` substitution to `MY_LAKEHOUSE.TYPED_DISTINCT_PROBE` sits at `:54-57` (the plan cites `:55-58`, immaterial); `E2E_TYPED_TABLE = "typed_distinct_probe"` at `seed.rs:2369`; `seed_typed_distinct_probe` at `seed.rs:2652`. The script takes exactly one positional argument (`if [ $# -ne 1 ]`) and sets `CAPTURE_SQL="$1"` itself, so the row's claim about the caller's environment holds. The capture SQL is now `SELECT COUNT(*) FROM {table}` and names no column. Host port 28563 confirmed as `docker-compose.yml`'s `LH_EXASOL_PORT` default.
- **like-type-coercion delta bullet 36 counts: CORRECT.** Now reads FOUR mirror notes, names the inline body comment as the fourth, and splits the two cross-references (one repointed, one's clause deleted).
- **Relocation Scenario Coverage row: CORRECT.** § Verification now carries nine rows covering all seven changed scenarios (three `pushdown-module-structure` NEW, two `type-mapping-module-structure` CHANGED, one each for the two coercion features), plus the integration row. `types/mapping.rs` does carry the relocation tests the new row cites (`exasol_type_to_json_*` at 1015-1090, `exasol_type_from_json_*` at 1108-1161).
- **Wrapper removal: CORRECT and clean.** Detail under Design Depth.
- **Prose: PARTIAL.** § Summary's first sentence is down to 14 words, but the section went from two sentences to three, breaching the guardrails' hard two-sentence cap. Two of the seven decision-log sentences named in round 5 are still over the word cap. ADVISORY 2.
- `speq feature validate`: 0 errors across the library; warnings only, all pre-existing AND-step counts.

## Intent Fidelity

No objection — axis checked. The interview's "full scope now" is still delivered whole: lookup helper (task 1), classifier substitution (task 2), builder merge (task 5). Issue #265's two non-goals (`HashMap`, coercion-policy trait) are carried verbatim in § Non-Goals and decision [8]. The fold-divergence deferral is unchanged from round 5 — declared four times, rationalized on byte-identity, disclosed in § Impact, tracked by the task 4 issue — and the user has left it standing across five directed rounds. Nothing this round narrowed or substituted scope: the two changes (task 3's evidentiary wording, the wrapper removal) are both reductions in artifact surface, not in delivered work.

## Feasibility

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: `specs/_plans/refactor-col-types-guard-dedup/vs-adapter/pushdown-module-structure/spec.md` Background bullet 79 (capture (1)'s headline clause and its closing "so …" clause); secondarily plan.md § Impact paragraph 2 (the word "reachable")
- Issue: the permanent-library bullet asserts and then disclaims the same unmeasured link, and the assertion is the exact framing round 5 ordered deleted. Bullet 79 opens capture (1) as: "**CAPTURE (1), a pushdown request carries Exasol's STORED column name, not the Iceberg one**: … `SELECT COLUMN_SCHEMA, COLUMN_TABLE, COLUMN_NAME FROM SYS.EXA_ALL_COLUMNS` reports … `ID`, `C_VARCHAR`, `C_DECIMAL_A` … — **so Exasol normalizes the adapter-declared name before it ever reaches `involvedTables[].columns[].name`**." Both bolded clauses are request-level claims drawn from a catalog-only read. Verified: capture (1)'s only observation is `SYS.EXA_ALL_COLUMNS`, which reports what the catalog serves, not what Exasol places in a pushdown request field — and I confirmed the repo holds no captured live pushdown request that could (every `involvedTables` occurrence under `crates/**` is a hand-authored `serde_json::json!` fixture, and the ten `testdata` files are generated-SQL goldens). Four sentences later the same bullet says the opposite: "ONE residual therefore stays outside the capture … whether `involvedTables[].columns[].name` holds the SERVED `STRAßE` or the Iceberg `straße` is immaterial", and "NO capture in this plan measures it, and capture (1) CANNOT: every name capture (1) read is ASCII … so it can establish nothing about whether `ß` survives into a request field." So the bullet labels capture (1) as establishing the catalog-to-request identity and then states that capture (1) can establish nothing about that hop. plan.md task 3, decision [6], and the round-5 decision-log entry all carry the corrected scoping ("Exasol normalizes adapter-declared column names", with no request-level claim); bullet 79 is the one artifact that did not get the fix, and it is the one that lands in the permanent library. Separately, plan.md § Impact paragraph 2 opens "One developer-facing consequence, **reachable**:" with no qualifier, in the section an approving architect reads first, while the residual it rests on is unmeasured — the same overclaim, milder.
- Fix: In `vs-adapter/pushdown-module-structure/spec.md` Background bullet 79, rewrite capture (1)'s headline from "a pushdown request carries Exasol's STORED column name, not the Iceberg one" to what the read observed — "Exasol normalizes an adapter-declared column name in its own catalog" — and change the closing clause "so Exasol normalizes the adapter-declared name before it ever reaches `involvedTables[].columns[].name`" to "so Exasol normalizes the adapter-declared name it serves from `SYS.EXA_ALL_COLUMNS`", deleting the reference to the request field. Do not add any new claim; the bullet's later residual statement already carries the correct scoping and needs no edit. In plan.md § Impact paragraph 2, change "One developer-facing consequence, reachable:" to "One developer-facing consequence, reachable under the inference below:" so the section's first clause does not assert measured reachability.

## Requirement Quality

#### [REQUIREMENT_CONFLICT] ADVISORY
- Location: `specs/_plans/refactor-col-types-guard-dedup/vs-adapter/pushdown-module-structure/spec.md` clause 121 (the closing sentence, "Any import of `support::extract_all_column_types` this test needs SHALL be declared INSIDE the `#[cfg(test)] mod tests` block rather than at `planning.rs`'s module level …")
- Issue: a new unconditional `SHALL` is bound to a contingent fact, so it stops describing the code the moment that fact changes. The clause's own reason is "because no production code in that file calls it" — verified true today (`grep -n 'extract_all_column_types' joins/planning.rs` returns nothing). But the `SHALL` is written unconditionally, so once any production code in `planning.rs` calls `extract_all_column_types`, the module-level import becomes the correct spelling and this recorded clause forbids it. That is the same defect class the plan exists to remove — a clause that survives the condition it was written for — reintroduced at the spec layer in this round's own fix. The clause also states a Rust build mechanism (`-D warnings` on an unused import) as a normative requirement rather than as the rationale it is; the feature's sibling bullets (10, 83, 87) state such mechanisms as Background reasons, not as `SHALL`s.
- Fix: In `vs-adapter/pushdown-module-structure/spec.md` clause 121, scope the requirement to its condition: change "Any import of `support::extract_all_column_types` this test needs SHALL be declared INSIDE the `#[cfg(test)] mod tests` block rather than at `planning.rs`'s module level" to "While no production code in `planning.rs` calls `support::extract_all_column_types`, any import of it this test needs SHALL be declared INSIDE the `#[cfg(test)] mod tests` block rather than at `planning.rs`'s module level". Keep the trailing `-D warnings` reason unchanged.

## Task Breakdown

No objection — axis checked. § Parallelization's restructure is internally consistent with the wrapper removal, re-derived against the tree rather than taken from the summary. The support.rs writer set is exactly tasks 1, 2, 5: task 1 adds `column_exa_type` plus its unit test, task 2 rewrites the three guards' arms, task 5 adds `column_types`. Task 4 writes only `joins/planning.rs` and reads `support::extract_all_column_types`, which neither task 1 nor task 2 touches (both leave `extract_all_column_types` at `support.rs:436` unchanged) — so "one gate plus two branches" reflects the real file sharing. Task 5 also writes `planning.rs` (deleting the `exasol_type_from_json` import at `planning.rs:3`), which overlaps task 4's file; the stated ordering (5 after 4, both for the test-before-merge reason and for support.rs) already forbids that overlap, so no rule is missing. All seven changed scenarios have a Scenario Coverage row. Two counting nits observed and deliberately not raised, because both sit in plan-internal prose that no delta records and neither changes a decision: § Consequences row 5 says `involved_table_columns` has "nine test call sites" where the tree has ten (rendering.rs 348/349/373/374/569/570/643/644, sql_builders.rs 1189/1190), and the delta's "four production sites" means four functions across six calls, which the parentheticals make clear.

## Design Depth

No objection — axis checked. The wrapper removal is clean, and the deviation from the round-5 fix text is correct rather than a shortcut. Verified by inspection of the module tree, not from the self-report:
- No stale reference survives. `grep -rn 'involved_table_columns_for_test'` over plan.md and all four deltas returns nothing; the three remaining hits in decision-log.md sit inside the two entries explicitly headed "— SUPERSEDED by round 5" plus the round-5 entry recording the removal, which is the correct disposition.
- Task 4's placement compiles. `joins/planning.rs` declares `involved_table_columns` at `:359` (`pub(super)`), and its `#[cfg(test)]` at `:433` / `mod tests {` at `:434` carries `use super::*;` at `:437`, so `super::involved_table_columns` resolves.
- The in-test import resolves. `extract_all_column_types` is `pub(super)` at `support.rs:436` — i.e. `pub(in adapter::pushdown)` — and `mod support;` at `pushdown/mod.rs:22` is private, hence nameable from any descendant of `adapter::pushdown`; `joins::planning::tests` is one, so `use crate::adapter::pushdown::support::extract_all_column_types;` is legal. No glob collision: `test_support.rs` does not export the name, and the test module's existing `use crate::adapter::pushdown::test_support::*;` therefore cannot conflict.
- The deviation is right and the finding's literal text was wrong. `planning.rs` has zero production references to `extract_all_column_types`, so a module-level `use` would be unused in the non-test lib build and `-D warnings` rejects it. Declaring it inside `mod tests` is the only spelling that compiles under this crate's gate.
- Delta bullet 84's factual basis holds exactly. `joins/mod.rs:10` declares `mod planning;` private, and `involved_table_columns` is re-imported by the **private** `use planning::{…}` at `:28` — not by the `pub(crate) use planning::{…}` at `:14` — so the name is confined to `joins` and its descendants and `support` genuinely cannot reach it. No test-only item and no visibility change in either direction, as clauses 83 and 117 now state.
- One implementation nit, not a finding: task 1 writes the literal slices as `("STRASSE", …)` while `column_exa_type` takes `&[(String, String)]`, so the implementer needs owned `String`s — the existing convention (`decimal_rewrite_col_types()` at `support.rs:4484`, returning `Vec<(String, String)>`) already shows the shape, and the plan states the parameter type on the same line.

## Prose Quality

#### [PROSE_BLOAT] ADVISORY
- Location: plan.md § Summary (sentence count); decision-log.md entries "Task 3 is the chain's first task, so no test is written before its gate runs" and "The relocation clause stops restating the shared builder's contract"; and this round's four new entries
- Issue: the round-5 prose fix traded one cap breach for another and left two of its seven named sentences over the limit. § Summary now reads three sentences ("Give the pushdown type-rewrite guards' … per issue #265." / "Three substitutions do it: …" / "Pure refactor — generated SQL byte-identical."); `/speq:writing-guardrails` calls the Summary two-sentence cap hard, and the pre-fix text met it at two. Of the seven decision-log sentences round 5 named, five are now at or under 25 words but two are not: 33 words in "Task 3 is the chain's first task" ("The alternative the finding also allowed — moving task 1's test into task 4 — was rejected: it leaves task 1 without its own test and splits one helper's extraction from its characterization") and 31 in "The relocation clause stops restating the shared builder's contract" ("Two features would carry the same normative statement of what `column_types` owns, with nothing enforcing their agreement — the dual-ownership divergence this plan exists to remove, reintroduced at the spec layer"). This round's own four new entries add sentences at 28, 27, 27 and 26 words. The pattern is chronic across rounds 3, 5 and 6 — each fix pass writes its own new prose past the cap — and it has never affected a requirement's actionability. It is recorded here for completeness and is not worth a further round on its own.
- Fix: In plan.md § Summary, fold the byte-identity claim into the first sentence — "Give the pushdown type-rewrite guards' `col_types` handling one owner per decision, per issue #265; the generated SQL stays byte-identical." — leaving the section at two sentences with neither over 25 words. In decision-log.md, split the 33-word sentence at its colon and the 31-word sentence at its em-dash, and split the four 26-28-word sentences in this round's new entries at their colon or em-dash boundaries. Drop no claim.
