# Plan Review Findings: refactor-col-types-guard-dedup (round 7)

## Summary
- Axes checked: 6/6
- Total findings: 2 (Blockers: 0, Advisory: 2)
- Intent Fidelity blockers: 0

## Round-6 Blocker Recheck

- **RESOLVED: [UNSTATED_ASSUMPTION] Capture (1) is scoped to the catalog read it performed.** Re-derived against the tree, not from the decision-log entry:
  - Bullet 79's capture (1) headline now reads "CAPTURE (1), Exasol normalizes an adapter-declared column name in its own catalog" and its closing clause "so Exasol normalizes the adapter-declared name it serves from `SYS.EXA_ALL_COLUMNS`". Both request-level claims are gone; `grep -rn 'STORED column name\|not the Iceberg one\|before it ever reaches'` over plan.md, decision-log.md and all four deltas hits only the round-6 decision-log entry that quotes the superseded text, which is the correct disposition.
  - The self-contradiction is gone. Capture (1) now asserts only a catalog observation; the residual sentence four sentences later ("capture (1) CANNOT: every name capture (1) read is ASCII … so it can establish nothing about whether `ß` survives into a request field") no longer contradicts the headline, because the headline no longer claims the request hop.
  - No new inaccuracy introduced. The code-side half of capture (1) re-verified: `adapter/mod.rs:575` declares `"name": name` verbatim and `build_virtual_tables` contains no `to_uppercase`/`to_ascii_uppercase` call, so "declares each column … verbatim … and uppercases nothing" holds. The payload-blindness claim also holds: `support.rs:446` reads `c.get("name")?.as_str()?.to_uppercase()`, so the printed projection cannot discriminate the two hypotheses.
  - The composition still reads coherently after the rescoping: capture (1) establishes that normalization happens at all (catalog), capture (2) the native fold's shape (`ß` survives), the inference bridges native-DDL fold to adapter-declared JSON name, task 3 closes that fold link by reading the served name, and the residual is the catalog→request-field `ß` hop. Nothing in the chain now rests on a claim capture (1) does not support.
  - plan.md § Impact paragraph 2 now opens "One developer-facing consequence, reachable under the inference below:", the prescribed wording. One residual gap in that paragraph is raised as ADVISORY 1 — it is a new observation about what the paragraph omits, not a re-raise of the resolved overclaim.

## Round-6 Advisory Verification

- **Clause 121 SHALL-scoping: CORRECT and coherent.** The clause now opens "While no production code in `planning.rs` calls `support::extract_all_column_types`, any import of it this test needs SHALL be declared INSIDE the `#[cfg(test)] mod tests` block …", keeping the `-D warnings` reason as prescribed. The condition is verifiable and true today: `grep -n 'extract_all_column_types' joins/planning.rs` returns nothing, and `planning.rs:9` imports only `extract_limit` and `order_by_present` across the support boundary. No conflict with the rest of the delta: Background bullet 84 governs WHERE the test is declared (visibility/reachability) and says nothing about import placement, clause 117 governs visibility, and plan.md task 4 states the same placement as a task instruction. Task-level prose may state it unconditionally — the condition holds for the diff being executed; only the recorded clause needed scoping, and only it got it. I also checked the sibling new clauses for the same defect class: clause 101 ("the helper SHALL be declared private to `support`, because its only three consumers live there") is an instance of this feature's own recorded narrowest-visibility-that-compiles rule (bullet 10), not a clause bound to a contingent fact with no governing rule, so it needs no condition.
- **Prose pass: over-corrected as reported, and the plan.md side is now clean.** § Summary is two sentences at 19 and 24 words. Measured every governed plan.md section sentence by sentence (§ Summary, § Impact, § Dependencies): no sentence exceeds 25 words, § Impact paragraph 2 holds at four sentences of 24/21/24/24. Measured every `Finding:` and `Direction change:` sentence in decision-log.md: every sentence in the round-5 and round-6 entries is at or under 25 words, confirming the ten-sentence claim rather than taking it. Both named sentences split without loss — the 33-word one now reads as a rejection plus its reason, the 31-word one as a dual-ownership statement plus its consequence. Over-cap sentences survive only in round-1 through round-4 entries, which no round has claimed to fix; that chronic pattern stays where round 6 left it, unraised.
- **One new inconsistency the prose pass introduced:** the round-5 prose entry now describes a § Summary shape round 6 replaced, with no forward pointer. ADVISORY 2.
- `speq feature validate`: 0 errors across the library; warnings only, all pre-existing AND-step counts.

## Intent Fidelity

No objection — axis checked. Nothing this round touched scope. The three edits are a Background-bullet rescoping, a clause-121 condition, and sentence splits; § Non-Goals, § Features, the six task bodies, and § Parallelization are unchanged from the round-6 state that carried "full scope now" whole (lookup helper task 1, classifier substitution task 2, builder merge task 5). The § Summary fold is a prose merge, not a claim drop: the byte-identity promise survives as a semicolon clause in sentence 1 and is separately carried by § Goals, § Impact, § Verification, and three delta clauses (105, 123, 138). The fold-divergence deferral stands unchanged, disclosed in four places and tracked by the task 4 issue, as the user has left it across six directed rounds.

## Feasibility

#### [UNSTATED_ASSUMPTION] ADVISORY
- Location: plan.md § Impact paragraph 2, sentences 3-4 ("Evidence is two Docker-container captures plus one inference — that Exasol folds an adapter-declared JSON name as it folds a native unquoted DDL identifier. All three sit in `vs-adapter/pushdown-module-structure`'s captured-evidence Background bullet; task 3 closes the inference's fold link as a gate, task 4 files the reconciliation issue.")
- Issue: § Impact enumerates the evidential structure as complete-after-task-3 and omits the one element bullet 79 records as permanently unmeasured. Bullet 79 states "ONE residual therefore stays outside the capture … whether the name still carries `ß` at all rather than arriving already collapsed to `SS` or `ss`. NO capture in this plan measures it". § Impact names two captures plus one inference, says task 3 closes the inference, and stops — so an architect reading only § Impact concludes reachability is fully established once the gate passes. The leading hedge compounds it rather than covering it: "reachable under the inference below" points at the one element task 3 DOES close, leaving the element that stays open unnamed in the section approvers read first. The consequence is bounded — performance-only, and task 1's and task 4's tests take literal slices and a hand-built request rather than a live one, so a false residual costs a mis-scoped tracked issue and an over-specific test doc comment, not a broken gate. This is a disclosure gap in one paragraph, not a defect in the reasoning; the delta, task 3, and decision [6] all carry the residual correctly.
- Fix: In plan.md § Impact paragraph 2, change the leading clause "reachable under the inference below" to "reachable under the inference and residual below", and append one sentence after "task 4 files the reconciliation issue": "One residual stays unmeasured — whether the request field's name still carries `ß` — and the issue task 4 files carries it." Add no other claim; the Background bullet already states it in full.

## Requirement Quality

No objection — axis checked. Clause 121's condition is verified against the tree (see Round-6 Advisory Verification) and the four deltas are otherwise unchanged this round. Re-confirmed the amendment surface is still complete and consistent: the like-coercion delta's clause 52 and the string-fn delta's clause 48 both name `column_exa_type` as the normalization's sole owner and cite no line number; the type-mapping delta's line-18 count still reads four clauses across two scenarios plus one superseded Background bullet, matching its two `DELTA:CHANGED` blocks (relocation clause 36, classifier clauses 48/49/51) and its supersession bullet 14. No delta clause contradicts another: clause 115 requires the builder contain no fold of its own, clause 99 requires the helper fold Unicode, and clause 120 pins the two producers' folds — three statements about three different owners, not one decision stated three times.

## Task Breakdown

No objection — axis checked. Nothing this round touched § Implementation Tasks or § Parallelization, and both remain internally consistent with the round-6 re-derivation: the `support.rs` writer set is tasks 1, 2, 5; task 4 writes only `joins/planning.rs` and reads an `extract_all_column_types` neither task 1 nor task 2 changes; task 3 is the sole gate; all nine Scenario Coverage rows still map onto the seven changed scenarios plus the integration row. One counting nit re-measured and deliberately left unraised, as in round 6: § Consequences row 5 says `involved_table_columns` has "four production plus nine test call sites" where the tree has ten test call sites (`rendering.rs` 348/349/373/374/569/570/643/644, `sql_builders.rs` 1189/1190) and six production calls across four functions. It sits in plan-internal prose that no delta records, and neither figure changes a decision — the argument it supports ("moving it churns import sites for no gain") holds at either count.

## Design Depth

No objection — axis checked. This round changed no module, interface, boundary, or task, so the design surface is exactly the one certified in round 6: two seams (`column_exa_type` resolves, `classify_exa_type` classifies) held apart by the recorded non-optional `&str` contract; both builder wrappers retained as partial applications rather than pass-throughs; no test-only cross-module item; dependency direction `adapter` → `types` unchanged. Clause 121's new conditional wording adds no design obligation — it constrains where one `use` line is written, and it now lapses with its condition instead of outliving it, which removes rather than adds a future edit.

## Prose Quality

#### [PROSE_UNCLEAR] ADVISORY
- Location: decision-log.md, entry "[plan-review] § Summary and seven decision-log sentences split to the 25-word cap" (round 5), Direction-change sentence "The section stays at two sentences plus the byte-identity line."
- Issue: the round-5 entry describes a § Summary shape this round replaced, and it carries no forward pointer, so the file states two different outcomes for the same section with nothing telling a reader which holds. The round-5 text says the first sentence "splits at the colon into two sentences under 25 words: the goal, then the three substitutions" and "The section stays at two sentences plus the byte-identity line" — three sentences. § Summary now carries two sentences, with the byte-identity claim folded into the first as a semicolon clause, exactly as the round-6 entry below records. Every other superseded outcome in this file carries an explicit forward pointer — "Round 5 corrected that count to THREE … see the round-5 entry below", "Round 4 removed the remaining concurrency — see the round-4 entry below", "Round 5 found this fix incomplete … See the round-5 entry below", and five more — so the omission breaks the file's own convention at the one entry this round superseded. No requirement is affected; the defect is in the record, not in what gets built.
- Fix: In decision-log.md, in the round-5 entry "§ Summary and seven decision-log sentences split to the 25-word cap", replace "The section stays at two sentences plus the byte-identity line." with "Round 6 folded the byte-identity claim into the first sentence, leaving the section at two sentences — see the round-6 entry \"§ Summary returns to two sentences, and ten decision-log sentences split\" below." Change nothing else in that entry.
