# Tasks: refactor-col-types-guard-dedup

## Phase 2: Implementation (Group: Gate, no predecessor)
- [x] 2.1 Task 3 — GATE RAN, claim REFUTED, plan corrected. Live capture returned `STRASSE`, not the expected `STRAßE`. Root cause is this crate, not Exasol: `resolve_table_schema` (`file_resolution.rs:640`) applies `f.name.to_uppercase()` to every Iceberg field name before `build_virtual_tables` declares it, and Rust's full-Unicode uppercase maps `ß`→`SS`. Exasol never receives a `ß`-bearing adapter-declared name. Consequence: the fold divergence is unreachable for ANY column name, not merely mis-scoped. plan.md (§ Impact, § Patterns, § Non-Goals, § Consequences, tasks 1/3/4/6, § Parallelization, § Verification, § Manual Testing), the `pushdown-module-structure` delta's captured-evidence bullets and clauses 99/103/120/122, both sibling guard deltas, and decision-log decisions [3]/[4]/[6]/[12]/[13]/[14] are all corrected. Seed.rs temp edit was reverted; task 7 is the committed replacement.

## Phase 2: Implementation (Group: support.rs chain, after gate)
- [x] 2.2 Task 1 — Extract `column_exa_type` helper in support.rs, rewire three guards, delete mirror-note comments, add Unicode/ASCII fold unit test [expert]
- [x] 2.3 Task 2 — Substitute `classify_exa_type`/`ExaTypeClass` for the three guards' starts_with/match-arm predicates, reword `is_bare_decimal_column` doc, update `classify_exa_type` doc comment

## Phase 2: Implementation (Group: planning.rs, after gate — may run parallel to support.rs chain)
- [x] 2.4 Task 4 — Add characterization test pinning each wrapper's OWN fold in planning.rs test module, over a CONSTRUCTED `STRAßE` literal (not a delivered name — task 3 measured `STRASSE`); doc comment must say the literal is constructed and that this is the only assertion distinguishing the two folds. Do NOT invert it into an agreement test. File the GitHub issue as a LOW-priority SIMPLIFICATION removing `column_types`' `fold_case` parameter, not a correctness fix, and cite it in the test — filed as #270

## Phase 2: Implementation (Group: tests/ + Makefile, after gate — may run parallel to support.rs chain and task 4)
- [x] 2.7 Task 7 — Add permanent E2E coverage for a `straße`-named table and column: `seed_non_ascii_identifier` in tests/common/seed.rs under a NEW `e2e_nonascii` namespace, new `tests/e2e_non_ascii_identifier_test.rs` (`non_ascii_table_and_column_stay_queryable`, VS `STRASSE_VS`), and `--test e2e_non_ascii_identifier_test` added to the Makefile `test-e2e` target. Do NOT reuse `typed_distinct_probe` or the `e2e_lakehouse` namespace. DONE — ran live against the Docker Exasol container (`cargo test --features exasol-e2e --test e2e_non_ascii_identifier_test -- --test-threads=1`): 6 passed, 0 failed, including `non_ascii_table_and_column_stay_queryable`. `grep -c 'e2e_non_ascii_identifier_test' Makefile` = 1.

## Phase 2: Implementation (Group: sequential, after both branches)
- [x] 2.5 Task 5 — Merge `extract_all_column_types` and `involved_table_columns` into shared `column_types` builder in support.rs; delete unused import in planning.rs [expert] — DONE. `column_types(request, select_table, fold_case)` added at `support.rs:450`; exactly ONE `involvedTables` walk survives (`support.rs:456`). Both wrappers keep their signatures and files, each supplying its own selector and its own fold (`str::to_uppercase` / `str::to_ascii_uppercase`). Task 4's characterization test passes UNCHANGED — the two folds stayed distinct. Red phase demonstrated: re-adding the `exasol_type_from_json` import to planning.rs fails `-D warnings` with "unused import", proving the mapping moved rather than being copied. `cargo test --workspace` 712 passed / 0 failed; fmt clean; clippy `-D warnings` clean; four join golden-SQL assertions byte-identical. GATE NOTE: `grep -rn 'to_ascii_uppercase' planning.rs` returns 3 lines, not the plan's stated 1 — exactly 1 is the production fold argument (`planning.rs:369`); the other 2 are inside task 4's test doc comment, which task 5 must not edit. The gate's intent (the fold survives as an argument, exactly once in production) holds.
- [x] 2.6 Task 6 — Run full gate (fmt, clippy, cargo test --workspace, make test-e2e including task 7's new binary); confirm byte-identity and no edited PRE-EXISTING assertions. Must follow task 7 as well as task 5. GATE RAN: fmt exit 0, clippy -D warnings exit 0, `cargo test --workspace` exit 0 (699 unit + integration passed across crates, 0 failed), `make test-e2e` exit 0 (8 binaries: 60+16+7+15+6+16+11+59 = 190 passed, 0 failed, including `non_ascii_table_and_column_stay_queryable`). `git diff` reviewed line-by-line for all 5 changed files (Makefile, support.rs, planning.rs, mapping.rs, seed.rs): no pre-existing assertion/expected-value line was removed or altered — all `-` hunks are comment/doc-comment/target-list lines, all `assert_eq!` diff lines are net-new (task 4's characterization test). Grep spot-checks confirmed: `find(|(n, _)| *n == name)` count = 1, `support.rs:411` hits = 0, `column_exa_type` count = 8 (>=5), `to_ascii_uppercase` in planning.rs = 3 total (1 production at line 369, 2 in task 4's doc comment) — matches carried-forward note.

## Phase 4: Review Fixes
- [x] 4.1 In crates/lakehouse-engine/src/adapter/pushdown/support.rs, reword the final sentence of `walk_column_nodes`' doc comment (lines 1316-1318) so the "no test" claim is scoped to `collect_all_column_names`, `collect_column_tables`, and `collect_side_column_names` instead of the whole crate; keep the `MUST NOT be unified` sentence
- [x] 4.2 In crates/lakehouse-engine/src/adapter/pushdown/support.rs, reword `column_exa_type`'s doc comment (line 669) to name the single fold it matches (`extract_all_column_types`' Unicode `to_uppercase` keys), noting `involved_table_columns`' ASCII-folded keys agree for every declarable column name since `resolve_table_schema` Unicode-uppercases names first; keep the `type`-tag paragraph unchanged
- [x] 4.3 In crates/lakehouse-engine/src/adapter/pushdown/joins/planning.rs, rewrite `involved_table_columns`' doc comment (lines 352-360): drop "exactly as the single-table projection does", state the ASCII-only `to_ascii_uppercase` fold and `dataType`-sourced types, and change the summary line to `The (folded name, Exasol type) columns of the named involved table.`; keep the empty-vec and closing partial-application sentences
- [x] 4.4 In crates/lakehouse-engine/tests/common/seed.rs line 2852, define `pub const NONASCII_COL: &str = E2E_NONASCII_TABLE;` instead of repeating the `"straße"` literal; leave the doc comment and use sites (lines 2878, 2900) unchanged
- [x] 4.5 In crates/lakehouse-engine/tests/e2e_non_ascii_identifier_test.rs, delete the second `assert!` (lines 105-108, `pushed_sql.contains(SERVED_NAME)`) and extend the comment at lines 94-98 to record why the `"filter":"` field-presence assertion alone is sufficient (single-predicate WHERE ⇒ a declined guard drops the whole top-level filter and the `skip_serializing_if` field disappears). Substitute NO other substring check (LIKE pattern, column name, `predicate_like` — all equally vacuous inside the flattened echoed `pushdownRequest`); leave assertions 1-4 and the first pushdown assertion untouched; re-run `cargo test --features exasol-e2e --test e2e_non_ascii_identifier_test -- --test-threads=1` against the Docker Exasol container [expert] — DONE. Vacuity of the deleted assertion CONFIRMED empirically, not only by inspection: the live `EXPLAIN VIRTUAL` blob (5,717 bytes, captured via a temporary dump that was then removed) holds 15 `STRASSE` occurrences, 13 of them decision-independent — `"name":"STRASSE_VS"`, `TABLE_MAP{"STRASSE":"e2e_nonascii.straße"}`, the echoed `involvedTables[].name` + `columns[].name`, and Exasol's OWN request filter (`filter.expression.{name,tableName}`, `"type":"predicate_like"`, `filter_expr_string_for_debug:"STRASSE.STRASSE LIKE 'alpha%'"`, `from.name`, `selectList[].tableName`) — so it would have held identically on a decline. `alpha%` appears 4× and `predicate_like` 1× in that same echoed request, confirming the finding's warning that those substitutes are equally vacuous. The KEPT assertion is genuinely discriminating: the blob has exactly ONE string-valued `"filter":"` (the adapter scan spec, `"filter":"(\"STRASSE\" LIKE ''alpha%'')"`) and ONE object-valued `"filter" : {` (Exasol's echoed request), and the latter does not match the assertion's substring. Final state re-run live: 6 passed, 0 failed (`non_ascii_table_and_column_stay_queryable` included); fmt clean, clippy `-D warnings` clean on the E2E target.

## Phase 3: Verification
- [x] 3.1 Run test suite (cargo test --workspace) — 879 passed, 0 failed (post-review-fix re-run)
- [x] 3.2 Run E2E suite (make test-e2e) — 190 passed, 0 failed (post-review-fix re-run)
- [x] 3.3 Run linter (clippy -D warnings) — exit 0
- [x] 3.4 Run formatter check (cargo fmt --check) — exit 0
- [x] 3.5 Scenario coverage audit against plan's Verification table — all scenarios mapped, see verification-report.md
- [x] 3.6 Manual testing commands (grep gates, capture-pushdown-payload.sh) — all confirmed, see verification-report.md
