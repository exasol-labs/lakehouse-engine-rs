# Tasks: refactor-neutralize-scan-spec

## Phase 2: Implementation (Group A — sequential, same file)
- [x] 2.1 Unify the delete mechanism in `crates/lakehouse-engine/src/scan/spec.rs`: add `DeleteMechanism` enum (4 variants), private `DeleteMechanismWire` untagged enum preserving byte-identical Iceberg encoding, retype `FileEntry::deletes` to `Vec<DeleteMechanism>`, delete `DeleteFileRef`/`DeleteFileContentType`, restate the mixed-mechanism refusal gate, prove lossless round trip and byte-identical 2-/3-tuple encodings. [expert]
- [x] 2.2 Neutralize remaining `ScanSpec` fields in `crates/lakehouse-engine/src/scan/spec.rs`: `LogicalField::field_id` → `Option<i32>` + new `physical_name: Option<String>`; add `FileEntry::partition_values` and `CommonScanSpec::partition_columns`; delete `CommonScanSpec::delta`/`FileEntry::delta`/`DeltaTableSpec`/`DeltaFileSpec`/`DeltaColumnMapping`/`DeltaColumnMappingMode`; rework `FileEntryWire`'s `WithDelta` variant; replace `FileEntry::with_delta`; neutralize stale "Iceberg"-only doc comments. Depends on 2.1. [expert]

## Phase 2: Implementation (Group B — parallel, disjoint files)
- [x] 2.3 Generalize the column-binding adapter in `crates/lakehouse-engine/src/scan/field_id_projection.rs`: merge `rename_physical_to_logical`/`resolved_logical_field_ids` into one binding pass, resolution order embedded field-id → declared physical name → `name_mapping` → identity; no `PARQUET:field_id` metadata when `field_id` is `None`; re-key `FieldIdResolution::defaults` by logical name; update construction site `scan/raw_scan.rs:229`; rewrite doc comments for all 3 strategies. [expert]
- [x] 2.4 Dispatch on the delete mechanism in `crates/lakehouse-engine/src/scan/positional_deletes.rs`: generalize `ensure_positional_delete` into a variant match (Iceberg positional passes; equality/Puffin keep exact message text; deletion-vector arm names the data file, cites #320, never echoes `path_or_inline_dv`); keep backstop-before-fetch ordering and dedup keying. [expert]
- [x] 2.5 Build the Iceberg mechanism at plan time in `crates/lakehouse-engine/src/adapter/pushdown/file_resolution.rs`: replace `map_delete_content_type` and `DeleteFileRef` construction with variant construction; in `relativize_shards_to_root` relativize only Iceberg delete-variant paths, leave `path_or_inline_dv` untouched. Also (discovered gap, no other task owns this): add a `DeleteMechanism` accessor in `crates/lakehouse-engine/src/scan/spec.rs` exposing the object-store path a mechanism reads (`None` for `DeltaDeletionVector`, since `path_or_inline_dv` is a UUID token or inline payload, not a path), and use it in `crates/lakehouse-engine/src/scan/store_router.rs:92` and `crates/lakehouse-engine/src/scan/object_store.rs:559` (both currently do `&delete.path` directly), plus `relativize_shards_to_root` here. Update `crates/lakehouse-engine/src/scan/store_router_tests.rs` construction shapes. [expert] (agent's process crashed mid-report after finishing; orchestrator independently verified via `cargo check` error-attribution — zero errors in any of its 6 files, plus the required `relativization_leaves_a_deletion_vectors_path_or_inline_dv_untouched` test exists)
- [x] 2.5b (discovered gap, no task named this site) `build_logical_schema` in `crates/lakehouse-engine/src/adapter/pushdown/mod.rs:809-825` constructs `LogicalField { field_id: f.id, ... }` for every Iceberg field — doesn't compile against `field_id: Option<i32>` and the new `physical_name` field. Fix: `field_id: Some(f.id), physical_name: None` (Iceberg always has a real field-id, never a physical-name binding).
- [x] 2.6 Put the binding key on `LogicalField` in `crates/lakehouse-engine/src/adapter/pushdown/format/delta_schema.rs`: `build_delta_table_schema` returns `(Vec<LogicalField>, Vec<String>)`; populate `field_id`/`physical_name` per column-mapping mode (`id`/`name`/`none`); delete `wire_column_mapping_mode` and `DeltaColumnMapping`/`DeltaTableSpec` construction; keep existing refusal message text unchanged. [expert]
- [x] 2.7 Emit neutral per-file fields in `crates/lakehouse-engine/src/adapter/pushdown/format/delta_replay.rs`: `append_active_files` puts partition values on `FileEntry::partition_values` and the deletion-vector descriptor as one `DeleteMechanism::DeltaDeletionVector`, leaving the list empty of Iceberg variants; keep dedup, explicit-NULL, closed storage-kind refusal, and message text unchanged. [expert]
- [x] 2.9 Update literal `CommonScanSpec` construction sites naming `delta`: `adapter/pushdown/mod.rs:340,353` and `adapter/pushdown/joins/sql_builders.rs:580,593` → `partition_columns: Vec::new()`; confirm spread sites (`joins/sql_builders.rs:1083`, `adapter/pushdown/support.rs:349`) still compile unchanged.

## Phase 2: Implementation (Group C)
- [x] 2.8 Neutralize `ResolvedScan` in `crates/lakehouse-engine/src/adapter/pushdown/format/mod.rs` and `format/delta_format_reader.rs`: replace `ResolvedScan::delta: Option<DeltaTableSpec>` with `partition_columns: Vec<String>`; `IcebergFormatReader` returns empty, `DeltaFormatReader` returns task 2.6's list; retype `read_delta_log`'s third return value; rewrite `resolve_scan`'s doc comment. Depends on 2.6, 2.7. [expert]

## Phase 2: Implementation (Group D)
- [x] 2.10 Add docker-free integration test `crates/lakehouse-engine/tests/scan_column_binding.rs` (mirrors `tests/scan_name_mapping.rs`'s harness): declared-physical-name binding (winning over covering `name_mapping`), identity binding (NULL-fill for absent nullable field, `initial_default` substitution, clean required-absent error).
- [x] 2.11 Update cross-cutting test call sites naming deleted types (construction-shape edits only, no assertion/expected-value/test-name changes except renaming tests stating a removed type): `tests/scan_two_arg.rs`, `scan_plan_shape.rs`, `scan_positional_deletes.rs`, `scan_footer_refetch_observable.rs`, `scan_no_head_test.rs`, `e2e_positional_deletes_test.rs`, `e2e_unity_test.rs`, `src/adapter/pushdown/test_support_tests.rs`. Widened per Group A's compile-gap report — also fix these sibling `_tests.rs`/integration files, same construction-shape-only rule: `src/adapter/pushdown/pushdown_tests.rs`, `topn_tests.rs`, `dispatch_golden_tests.rs`, `joins/joins_tests.rs`, `support_tests.rs` (also reads `.path`/`.content_type` on a delete — needs a variant match, not just a literal tweak), `tests/scan_join_test.rs`, `tests/scan_name_mapping.rs`. (`format/delta_schema_tests.rs` and `src/scan/raw_scan_tests.rs` struck — tasks 2.6 and 2.3 fully handled them, both compile clean.)

## Phase 3: Verification (Group E)
- [x] 3.1 Run the regression gate: `scan_two_arg.rs`, `scan_plan_shape.rs`, `scan_positional_deletes.rs`, `scan_no_head_test.rs`, `scan_name_mapping.rs`, `dispatch_golden_tests.rs` MUST pass with no edit to any assertion/expected value/golden beyond task 2.11's construction shapes; confirm `unity_kind_pushdown_is_refused_not_iceberg_routed` passes unedited; confirm surface probes compile at recorded item counts.
- [x] 3.2 Run full automated checklist (build, host tests, Iceberg E2E, Unity E2E, clippy, fmt, spec validation).

## Phase 4: Review Fixes
- [x] 4.1 Extend `present_field_binds_real_value_not_default` in `crates/lakehouse-engine/src/scan/field_id_projection_tests.rs` with two further `rewrite_with`-driven cases, each carrying a default under the logical column's name and asserting the rewritten expression binds a real `Column` (bare or `CastExpr`-wrapped), never a `Literal`: the step-4 physical-name fallback, and a physical field whose embedded field-id is absent from the logical schema. Extend the doc comment to name all four resolution paths. [expert]
- [x] 4.2 Rewrite `DeleteMechanism::object_store_path`'s doc comment in `crates/lakehouse-engine/src/scan/spec.rs` so the reason for `None` on `DeltaDeletionVector` is that `path_or_inline_dv` is resolved into a path at file registration and never addressed from the delete list itself — never fetched/relativized/claimed for a store HERE — regardless of whether the value looks like a UUID token, an inline payload, or an absolute path (`DeltaDeletionVectorStorage::AbsolutePath`). Apply the same correction to the mirrored doc comment in `crates/lakehouse-engine/src/adapter/pushdown/file_resolution.rs` (~lines 55-57) and to the doc comment + final assertion message of `only_a_delete_file_mechanism_exposes_an_object_store_path` in `crates/lakehouse-engine/src/scan/spec_tests.rs` (~lines 1974-1978, 2007).
- [x] 4.3 In `crates/lakehouse-engine/src/adapter/pushdown/format/delta_replay.rs`, replace `append_active_files`'s construct-then-mutate `FileEntry::with_partition_values(...)` + `entry.deletes = ...` sequence (~lines 183-187) with one `FileEntry { path, size, deletes: ..., partition_values: ... }` struct literal setting all four fields at once, preserving the current evaluation order (partition values read before the deletion vector).
- [x] 4.4 In `crates/lakehouse-engine/src/adapter/pushdown/format/delta_schema_tests.rs`, add `assert_eq!(logical_fields[0].arrow_type, "int32", "a Delta INTEGER column maps to the int32 Arrow tag");` to `none_mode_ignores_a_residual_column_mapping_annotation` (~line 114) and extend its doc comment to state it also pins the `INTEGER` → `int32` Arrow tag mapping.
- [x] 4.5 In `crates/lakehouse-engine/src/scan/store_router_tests.rs`, add a test `a_deletion_vector_contributes_no_owned_path`: build a `RoutedSide` from a single `FileEntry` carrying one `DeleteMechanism::DeltaDeletionVector` (`DeltaDeletionVectorStorage::UuidRelative` with a non-path `path_or_inline_dv` token), asserting the owned-path set contains exactly the data file's path and nothing derived from `path_or_inline_dv`.
- [x] 4.6 In `crates/lakehouse-engine/src/scan/object_store_tests.rs`, add two tests against `validate_uniform_object_store_files`: `a_delete_file_under_a_different_root_is_rejected` (a `FileEntry::with_deletes` whose `DeleteMechanism::IcebergPositionalDelete` path names a different scheme/host than the first absolute path, asserting the error names "delete file"), and `a_deletion_vector_is_not_checked_against_the_object_store_root` (an entry whose only delete is a `DeleteMechanism::DeltaDeletionVector` with a non-URI `path_or_inline_dv`, asserting the call returns `Ok`).
- [x] 4.7 In `crates/lakehouse-engine/src/scan/spec_tests.rs`, rewrite `deletion_vector_storage_kind_outside_the_closed_set_is_refused`'s fixture (~line 1841) as the minimal 3-tuple form `[["f.parquet",1,[{"storage":"puffin","path_or_inline_dv":"x","size_in_bytes":1,"cardinality":1}]]]`, dropping the unrelated `partition_values` member, matching the sibling test `an_iceberg_delete_content_type_outside_the_closed_set_is_refused`'s minimal form.
