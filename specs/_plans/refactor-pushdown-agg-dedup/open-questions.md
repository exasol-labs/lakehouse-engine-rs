# Open Questions: refactor-pushdown-agg-dedup

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan refactor-pushdown-agg-dedup` locally, or re-run `/speq:plan-pr refactor-pushdown-agg-dedup` after commenting.

- [ ] **plan.md § Context and decision-log § [7] state the STDDEV/VARIANCE query failure as settled fact, not as task 1.2's pending capture result.** § Context still reads "The whole query errors where a decline would have returned the correct answer natively," and decision-log § [7]'s Rationale still reads "which DataFusion rejects — the whole query fails" and "so the shape is reachable rather than theoretical." Both are code-inspection conclusions, not verified against a running Exasol instance — and this project's own CLAUDE.md Verification Discipline section requires exactly that check before treating a pushdown-reachability claim as fact ("No assumptions about SQL capabilities, syntax, or pushdown reachability without checking them against a running Exasol instance"). Decision [7] is flagged `Promotes to ADR: yes`, so this unverified claim would enter the permanent spec library via `/speq:record` if left as-is. Should these two locations be walked back to inspection-scoped wording pending task 1.2's actual capture, and should ADR promotion be gated on that capture's result?
- [ ] **Task 1.7 asserts a second, unmeasured reachability claim for the grouped scalar-over-aggregate shape.** Task 1.7 states that `SELECT region, SQRT(STDDEV(a + b)) ... GROUP BY region` is "a real behavior change and today's second failure path," but task 1.2's capture only runs two *ungrouped* queries (`SELECT STDDEV(score + id)`, `SELECT VARIANCE(score * 2)`). Whether Exasol actually pushes the grouped shape is untested by the plan as written, yet § Impact promises "at most one user-visible change" as a single verdict. Should task 1.2 add capture queries for the two grouped paths (`SELECT region, STDDEV(score + id) ... GROUP BY region` and `SELECT region, SQRT(STDDEV(score + id)) ... GROUP BY region`), with § Impact's verdict stated per-path rather than as one blanket claim?
