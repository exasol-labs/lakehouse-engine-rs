# Code Review Findings: refactor-pushdown-agg-dedup

## Summary
- Files reviewed: 12 (8 modified sources, 4 new fixtures)
- Total findings: 6 (standard: 5, expert: 1)

Verified before raising findings, so the implementer agents do not re-litigate them:

- **Byte-identity holds by substitution.** The four extracted statistical arms reproduce the pre-refactor `concat!` text character for character. `numer` carries its own outer parentheses, `stddev_of` adds exactly one pair around its `IS NULL` subject and none around its `GREATEST` argument, and the committed `grouped_all_agg_kinds.sql` shows the resulting `CASE WHEN ((SUM(...` double parenthesis. `emit_null_partial_row`, `partial_row_from_batch`, `partial_emits_items`, and `partial_select_items` each reproduce their prior per-`AggKind` output exactly.
- **The golden gate is genuine, not self-certifying.** All six new fixture tests assert full-string `assert_eq!` against `include_str!`; none uses `.contains(...)`. File mtimes confirm the capture order the plan required: the four fixtures were written at 09:01–09:03, every production edit landed at 09:22 or later (`scan/spec.rs` 09:22, `scan/partial_agg.rs` 09:23, `single_group_agg.rs` 09:40, `grouped_agg.rs` and `support.rs` 10:05). The fixtures also carry no trailing newline, which a hand-written file would.
- **No production visibility widened.** The scan-to-adapter alignment test reaches `crate::scan::build_partial_agg_sql` and `partial_emits_items` through their pre-existing `pub`/`pub(super)`; the diff adds no `pub` to any pre-existing item. The clause `datafusion-scan/scan-partial-agg-column-contract` leaves to reviewer inspection is satisfied.
- **No `PARTIAL_` format literal survives in production code.** Every remaining occurrence is a doc comment or a test expectation.
- **`parse_agg_item`'s new post-condition holds.** `arg_column_or_expr` can never return `(None, None)`, and the statistical branch now unwraps `column_from_first_arg` with `?`, so the doc claim "every plan carries an argument except `AggKind::Count`" is true.
- `cargo test -p lakehouse-engine --lib` (717 passed, 0 failed), `cargo clippy --all-targets` (clean), `cargo fmt --check` (clean).

Inline comments are not raised as findings on their own: the touched files already carry 138 (`e2e_capability_test.rs`) and 151 (`grouped_agg.rs`) of them, so the ban is a settled repository-wide deviation rather than a defect this change introduced. Only a new comment that contradicts or duplicates adjacent code is raised below.

## Standard fixes

### crates/lakehouse-engine/src/adapter/pushdown/dispatch_golden.rs

#### [SHRINKABLE] The 22-argument `build_dispatch_sql` fixture block is now duplicated
- Location: lines 645-687 (`dispatch_sql_with_col_types`), duplicating lines 185-222 (`dispatch_sql_with_pushdown_req`)
- Issue: `dispatch_sql_with_col_types` copies `dispatch_sql_with_pushdown_req`'s entire `build_dispatch_sql` invocation verbatim, differing only in `base_col_types()` → `col_types`. The two copies now independently encode the fixed shard/storage/tuning universe every golden is captured under (`&fixed_shards()`, `"s3://warehouse/db/events"`, `&sample_storage()`, `4, 8192, 2, 0.6, 200, 8`). A future edit to one copy alone would leave the two all-agg-kinds fixtures captured under different tuning from the ten pre-existing ones while both families still pass against their own committed files — a silent divergence in exactly the seam this plan's byte-identity claim rests on.
- Fix: In crates/lakehouse-engine/src/adapter/pushdown/dispatch_golden.rs, delete the `build_dispatch_sql` call from `dispatch_sql_with_col_types` and route it through the existing helper instead. Add a `col_types: Vec<(String, String)>` parameter to `dispatch_sql_with_pushdown_req` (line 185) directly after `proj_types`, replace its hardcoded `base_col_types()` argument with that parameter, and update its four call sites (lines 177, 423, 501, 509) to pass `base_col_types()`. Then reduce `dispatch_sql_with_col_types` to `fn dispatch_sql_with_col_types(request: &Json, col_types: Vec<(String, String)>) -> String` whose body is `dispatch_sql_with_pushdown_req(request, &pd(request), Vec::new(), Vec::new(), col_types, None, None)`. Growing the shared helper to seven parameters is the accepted trade for having one `build_dispatch_sql` call site — do not introduce a parameter struct. Both all-agg-kinds fixtures and all ten pre-existing `dispatch_golden` fixtures MUST stay byte-identical; run `cargo test -p lakehouse-engine --lib dispatch_golden` to confirm.

#### [DEAD_FLEXIBILITY] `dispatch_sql_with_col_types`' `filter` and `limit` parameters are never varied
- Location: lines 645-650 (signature), call sites at lines 690-695 and 705-710
- Issue: both call sites pass `None, None` for `filter` and `limit`. The two parameters exist only to mirror `dispatch_sql`'s signature and give the helper four arguments where two would do, past the guardrail's three.
- Fix: In crates/lakehouse-engine/src/adapter/pushdown/dispatch_golden.rs, remove the `filter: Option<String>` and `limit: Option<u64>` parameters from `dispatch_sql_with_col_types` and drop the `None, None` arguments from both call sites (`single_group_all_agg_kinds_matches_golden`, `grouped_all_agg_kinds_matches_golden`). Apply this together with the `[SHRINKABLE]` finding above so the final signature is `fn dispatch_sql_with_col_types(request: &Json, col_types: Vec<(String, String)>) -> String`.

### crates/lakehouse-engine/src/adapter/pushdown/single_group_agg.rs

#### [REDUNDANT_COMMENT] The statistical-branch comment restates its own doc comment and its table's
- Location: lines 292-295
- Issue: the four-line block above the `STAT_AGG_KINDS` lookup states three facts that are each already stated within twenty lines of it. "decompose into (cnt, sum, sum_sq) sufficient statistics over a bare source column" and "Anything but a bare `column` argument declines, which is what keeps the argument-less plan out of the scan" repeat `parse_agg_item`'s own doc comment (lines 232-242, which states the same rule and the same reason at more length); "resolved through the bare-column-only argument path" is `STAT_AGG_KINDS`' doc comment (lines 208-209); and "STDDEV and STDDEV_SAMP are the sample forms; VARIANCE / VAR_SAMP likewise" is now the literal content of the `STAT_AGG_KINDS` table itself and of `AggKind`'s variant docs in `scan/spec.rs`. Three copies of one rule is what this plan set out to remove.
- Fix: In crates/lakehouse-engine/src/adapter/pushdown/single_group_agg.rs, delete the four-line `//` comment block at lines 292-295 immediately above the `STAT_AGG_KINDS.iter().find(...)` lookup. Change no code and no doc comment — the rule stays recorded in `parse_agg_item`'s doc comment and in `STAT_AGG_KINDS`' doc comment.

### crates/lakehouse-engine/src/adapter/pushdown/support.rs

#### [MISSING_DESIGN_INTENT] The sole owner of the CAST rule does not say why the `VARCHAR(2000000)` default is exempt
- Location: lines 1502-1512 (`cast_to_declared_type`)
- Issue: this helper is now the single owner of the declared-type CAST rule for six call sites, and the per-site convention notes that surrounded those sites were deleted by task 1.6. Its doc comment states what it does and why a cast is needed at all, but not why `VARCHAR(2000000)` specifically is exempt — the one clause a reader cannot infer from the signature and the only clause that decides five of the six sites. The reason is recorded elsewhere and is not restated here: `crates/lakehouse-engine/src/types/mapping.rs:22-28` documents `VARCHAR(2000000)` as the catch-all the mapping returns "for every incompatible Arrow type rather than failing", and `mapping.rs:44` notes that incompatible types "surface as `VARCHAR(2000000)`, so the rendered string cannot separate the type". The string is therefore a fallback signal, not evidence Exasol declared that type.
- Fix: In crates/lakehouse-engine/src/adapter/pushdown/support.rs, extend `cast_to_declared_type`'s doc comment with one sentence stating why the default is exempt: `VARCHAR(2000000)` is the catch-all `crate::types::mapping` returns for any type it cannot map (see `mapping.rs:22-28`), so its presence signals "no usable declared type" rather than a type Exasol actually declared, and casting to it would mislabel the value. Do not re-add a list of the helper's call sites — task 1.6 deleted that convention note deliberately. Change no code.

### crates/lakehouse-engine/src/adapter/pushdown/file_resolution.rs

#### [OUTDATED_COMMENT] The `ScalarOverAggregate` arm's comment claims a mirror the code contradicts
- Location: lines 789-791
- Issue: the comment says the arm is "typed to the item's own declared type (mirrors the group-key/aggregate cast so the empty grouped shape never drifts from the non-empty wrapper)". It does not mirror them. The `GroupKey` and `Aggregate` arms twelve lines above (lines 782-787) render `CAST(NULL AS {ty})` unconditionally, while this arm now visibly routes through `cast_to_declared_type`, which returns a bare `NULL` when the declared type is `VARCHAR(2000000)`. plan.md § Consequences records that the unconditional form is deliberate there, "a bare `NULL` in `SELECT … FROM DUAL WHERE 1=0` carries no `VARCHAR` type and would fail Exasol's positional `selectListDataTypes` check" — so the difference the comment denies is the one a reader most needs to see, and the shared helper has made it structurally visible for the first time.
- Fix: In crates/lakehouse-engine/src/adapter/pushdown/file_resolution.rs, replace the three-line comment at lines 789-791 on `empty_grouped_sql`'s `GroupedSelectItem::ScalarOverAggregate` arm with a comment stating the actual rule: a scalar-over-aggregate column is NULL over zero rows and goes through the shared `cast_to_declared_type`, so unlike the `GroupKey` and `Aggregate` arms above — which cast unconditionally — it emits a bare `NULL` when the item's declared type is the `VARCHAR(2000000)` default. Change no code: the arm's rendered output MUST stay byte-identical, and all twelve `dispatch_golden` fixtures MUST still pass.

## Expert fixes

### crates/lakehouse-engine/tests/e2e_capability_test.rs

#### [MISSING_BOUNDARY_TEST] Two of the three paths task 1.7 re-routes have no automated end-to-end guard
- Location: line 1274 (`e2e_stddev_over_expression_falls_back_and_returns_correct_value`) is the only integration test added for the behavior change
- Issue: task 1.2 measured the expression-argument statistical aggregate as pushed-and-broken on three reachable select-list paths, not one: ungrouped `detect_aggregates` (capture 1), grouped `detect_group_by_aggregates` (capture 3), and grouped scalar-over-aggregate `classify_scalar_over_aggregate` (capture 4), each failing with `sqlCode 22002`, `grouped partial aggregate SQL error: Schema error: No field named .` Only the ungrouped path gained an end-to-end guard. The two grouped paths are guarded solely by `grouped_stat_aggregate_over_expression_argument_declines` and `scalar_over_stat_aggregate_with_expression_argument_declines` in `grouped_agg.rs`, which assert that detection returns `None`. That proves the decline happens; it does not prove Exasol then returns the correct per-group statistic through the Tier 1b qualified wrapper, which is the half of the change a user observes. plan.md § Manual Testing assigns both grouped after-states to a hand-run query, and a hand run is not a CI guard — the same reasoning plan.md task 1.7 already applies to the ungrouped path ("Re-running task 1.2's two queries by hand supplements that test and does NOT substitute for it").
- Fix: Add `e2e_grouped_stddev_over_expression_falls_back_and_returns_correct_value` to crates/lakehouse-engine/tests/e2e_capability_test.rs, modelled on `e2e_stddev_over_expression_falls_back_and_returns_correct_value` at line 1274. Run `SELECT MOD(id, 4), STDDEV(score + id) FROM <vs_table()> GROUP BY MOD(id, 4) ORDER BY 1` — the exact shape of task 1.2's capture 3 — and first assert via `explain_virtual_sql` that the generated SQL contains no `PARTIAL_stat_`, so the test fails if the grouped decline regresses. Then build the reference WITHOUT a second aggregate query: read `SELECT id, score FROM <vs_table()>` back through a plain projection, group those rows in Rust by `id % 4`, compute each group's sample standard deviation of `score + id`, and assert each returned row matches its group's reference within a 1e-6 relative tolerance. Computing the reference through any aggregate query would make the test pass over wrong behavior — the failure mode this fix exists to prevent — so the reference MUST come only from projected rows. Assert the returned row count equals the number of distinct `id % 4` groups so a dropped group cannot pass. Per CLAUDE.md the test MUST fail, not skip, when the Docker Exasol container is absent, matching `setup_e2e`'s existing behavior. Add no production code and change no existing test.
