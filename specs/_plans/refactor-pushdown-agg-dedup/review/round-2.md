# Plan Review Findings: refactor-pushdown-agg-dedup (round 2)

## Summary
- Axes checked: 6/6
- Total findings: 5 (Blockers: 2, Advisory: 3)
- Intent Fidelity blockers: 0

## Round-1 Blocker Recheck

- Resolved: [UNSTATED_ASSUMPTION] `parse_agg_item` has five callers, not two — task 1.7's second paragraph now enumerates all five with the behavior each produces, and every citation checks out against the code: `single_group_agg.rs:78` (`SingleGroupItem::Aggregate(parse_agg_item(item)?)`), `grouped_agg.rs:215` (`let plan = parse_agg_item(item)?` inside `detect_group_by_aggregates`), `:399` (`aggregates.iter().map(parse_agg_item).collect()`, the last line of `classify_scalar_over_aggregate`), `:427` (`render_scalar_over_merge`), `:988` (`render_having_over_merge`). The `position(…)` lookups the paragraph relies on exist at `grouped_agg.rs:428` and `:989`. The delta's fifth AND clause enumerates all five sites and states an outcome at each. `vs-adapter/pushdown-planning-grouped-agg-scalar-over-aggregate` is now assessed in plan.md § Features, and the assessment is accurate: the recorded feature carries exactly four scenarios (`spec.md:41`, `:52`, `:61`, `:69`), none naming a statistical aggregate, and its § Background bullet 4 (`spec.md:33-37`) is the decline-reason list the plan says gains a fifth reason. Two new tests are named in task 1.7 and both appear in § Verification § Scenario Coverage. One residual weakness in the HAVING test is raised below as ADVISORY, not as an unresolved blocker.
- Not resolved: [UNSTATED_ASSUMPTION] Stop asserting the capture's conclusion before task 1.2 measures it — § Impact and the delta's Background bullets were fixed, but the same unvalidated conclusion still stands as settled fact in plan.md § Context (line 15) and in decision-log § [7]'s Rationale, and task 1.2's new correction step names neither. Decision [7] carries `Promotes to ADR: yes`, so its text reaches the permanent library, which is the exact harm the round-1 finding named. Raised below as a BLOCKER.
- Resolved: [TRACEABILITY_GAP] A byte-identity claim needs a baseline at every seam the refactor rewrites — task 1.1 now captures a third and fourth pre-refactor fixture through the scan seam, and both are feasible as written: `build_partial_agg_sql` is exported `#[cfg(test)]` at `scan/mod.rs:44` and defined at `partial_agg.rs:285`, `build_grouped_partial_agg_sql` is `pub` at `partial_agg.rs:203` with the `(group_keys, aggregates, aliased_table, filter)` signature task 1.1's "one group key and no filter" needs, and both live in the same module as the test that will assert them, so no visibility widens. The named select list covers all ten `AggKind` variants (`spec.rs:29-42`) and therefore every arity. Task 1.3's "Both fixtures from 1.1" is corrected to "All FOUR fixtures" and states which pair covers the scan side it rewrites. § Verification § Scenario Coverage carries the new row.
- Resolved: [TRACEABILITY_GAP] Every test named as verification needs a task that creates it — all five previously orphaned tests now have a creating task: `partial_columns_arity_per_agg_kind`, `is_counter_marks_the_four_count_columns`, and `partial_column_name_renders_role_and_ordinal` in task 1.3 with literal expected values and an explicit prohibition on reading `partial_columns()` back; `cast_to_declared_type_skips_the_varchar_default_and_absent_type` as task 1.6's opening failing-test-first step covering all three arms; `e2e_stddev_over_expression_falls_back_and_returns_correct_value` as a required deliverable of task 1.7 with the manual re-run demoted to a supplement in writing and the fail-rather-than-skip requirement stated. All five are confirmed absent from the codebase, so each can fail first. Every other test the § Verification table names was confirmed to exist (`bare_column_aggregates_unchanged_regression` `single_group_agg.rs:465`, `partial_sql_uses_rendered_expression_argument` `partial_agg.rs:632`, `partial_agg_sql_mixed_column_order_and_indices` `:560`, `stat_aggregate_null_fallback_row_has_three_values` `:824`, `stat_aggregate_emits_three_partial_columns` `grouped_agg.rs:3571`, `e2e_stddev_variance_pushdown` `e2e_capability_test.rs:1197`).

## Premortem

Two failure stories against the revised artifacts, each routed into the taxonomy below.

1. **The merged spec library and its ADR permanently record a query failure that never happened.** Task 1.2's capture returns "the query already succeeds". The implementer executes task 1.2's final step exactly as written — rewrites § Impact's first two paragraphs and the delta's Background bullets 2-3 to branch two, deletes the pending marker — and stops there, because that is the whole instruction. plan.md § Context still says "The whole query errors", and decision-log [7], flagged `Promotes to ADR: yes`, still says "the whole query fails" and "the shape is reachable rather than theoretical". `/speq:record` promotes [7] into an ADR. → Feasibility, `[UNSTATED_ASSUMPTION]`.
2. **A real user-visible change ships documented as "nothing changes".** Task 1.2 captures only `SELECT STDDEV(score + id)` and `SELECT VARIANCE(score * 2)` — both ungrouped, both bare aggregates. Exasol turns out not to push an expression argument in that shape, so the capture reads "success" and § Impact is rewritten to branch two: "NOTHING changes for a user". But task 1.7's newly added path (3) is a different shape — `SELECT region, SQRT(STDDEV(a + b)) … GROUP BY region`, reaching `classify_scalar_over_aggregate` — which task 1.7 asserts is "a real behavior change and today's second failure path" while nothing in the plan ever measures whether Exasol pushes *that* shape. → Feasibility, `[UNSTATED_ASSUMPTION]`.

## Intent Fidelity

No objection on `[INTENT_DRIFT]` — axis checked: the revision changed no scope. All four duplications named in issue #179 still map to tasks 1.5, 1.6, 1.4, and 1.3, and the added material (two scan-seam fixtures, three call-site descriptions, five named tests) serves the issue's own no-behavior-change constraint rather than substituting a different problem.

No objection on `[SCOPE_REDUCTION]` — axis checked: nothing was dropped in the revision. The withdrawn Background-only delta on `pushdown-planning-grouped-agg-scalar-over-aggregate` is the second option round-1's own `Fix:` line sanctioned, and § Features states the withdrawal, the validator error that forced it, and the feature it recorded the reason in instead.

No objection on `[SCOPE_CREEP]` — axis checked: the revision added no unrequested work. Round-1's `[SCOPE_CREEP]` ADVISORY on task 1.7's "fold in if cheap" condition stands unaddressed by design and carries to the PR; it is not re-raised here.

## Feasibility

No objection on `[EFFORT_MISESTIMATION]` — axis checked: the revision grew tasks 1.1, 1.3, 1.6, and 1.7, and each states its new work explicitly rather than hiding it. Task 1.3 is now larger still (four failing tests plus five rewired sites); that is round-1's `[TASK_GRANULARITY]` ADVISORY, which the planner declined on the record in decision-log § [plan-review] and which carries to the PR.

No objection on `[HIDDEN_DEPENDENCY]` — axis checked: the two new fixture seams exist and are reachable (`scan/mod.rs:44`, `partial_agg.rs:203`, `:285`); `include_str!` from `partial_agg.rs` resolves the new `src/scan/testdata/partial_agg_golden/` path; the new `capabilities.rs` citations are correct (`AGGREGATE_HAVING` at `:188`, `FN_AGG_STDDEV` through `FN_AGG_VAR_SAMP` at `:176-181`); `request_shape.rs:133`, `grouped_agg.rs:553`, and `:1068` all carry the routing the revision cites; `speq plan validate` passes on all three deltas with 0 errors and `speq feature validate` reports 0 errors library-wide.

No objection on `[NFR_IGNORED]` — axis checked: the revision touches no security, concurrency, or migration surface. The added E2E test is the migration-of-behavior guard the round-1 finding asked for.

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: plan.md § Context paragraph 4 (line 15); decision-log.md § [7] Rationale (line 67); plan.md § Implementation Tasks task 1.2, final-step paragraph (line 109)
- Issue: round-1's blocker is fixed in two of four places, and the two that remain are the ones that outlive the plan. § Impact now correctly separates code inspection from the query-level outcome and states both branches, and the delta's Background bullet 2 carries the `TASK 1.2 CAPTURE PENDING` marker. But plan.md § Context still asserts "The whole query errors where a decline would have returned the correct answer natively", and decision-log § [7] still asserts "the scan then renders `COUNT(\"\")`, which DataFusion rejects — the whole query fails" and "so the shape is reachable rather than theoretical". Reachability is precisely what task 1.2 measures and what CLAUDE.md § Verification discipline forbids settling from a capability registry: "No assumptions about SQL capabilities, syntax, or pushdown reachability without checking them against a running Exasol instance." Decision [7] carries `Promotes to ADR: yes`, so `/speq:record` promotes that text into the permanent library — the round-1 finding's stated harm ("the recorded spec permanently documents a bug that never existed") is therefore still live, just relocated from the delta to the ADR. Task 1.2's new final step closes only "§ Impact's first two paragraphs … and the `aggregate-extensions` delta's § Background bullets 2 and 3", so neither remaining assertion is on any correction list, and if the capture returns branch two the plan ships with § Context contradicting § Impact.
- Fix: In plan.md § Context paragraph 4, replace "The whole query errors where a decline would have returned the correct answer natively" with a statement scoped to code inspection plus a forward reference, e.g. "DataFusion rejects that SQL; whether Exasol pushes the shape at all, and therefore whether a query ever reached the rejection, is task 1.2's capture." In decision-log.md § [7] Rationale, replace "which DataFusion rejects — the whole query fails" with "which DataFusion rejects" and replace "so the shape is reachable rather than theoretical" with "so nothing between the advertisement and the scan constrains the argument shape; whether Exasol pushes it is task 1.2's capture." In plan.md task 1.2's final-step paragraph, extend the rewrite list from two artifacts to four: "§ Impact's first two paragraphs, § Context paragraph 4, decision-log § [7]'s Rationale, and the `aggregate-extensions` delta's § Background bullets 2 and 3", and state that decision [7] MUST NOT reach `/speq:record` as an ADR while it asserts an outcome the capture did not establish.

#### [UNSTATED_ASSUMPTION] BLOCKER
- Location: plan.md § Implementation Tasks task 1.7, paragraph 2 clause (3); plan.md § Implementation Tasks task 1.2; plan.md § Impact paragraph 1; plan.md § Manual Testing rows 5-6
- Issue: the revision introduced a second reachability claim and no step measures it. Task 1.7 clause (3) now states of `classify_scalar_over_aggregate`: "This is a real behavior change and today's second failure path: such an item currently classifies successfully on the malformed plan, gets three `EMITS` columns from the `DOUBLE PRECISION` default, and fails in the scan." Verified correct by inspection — `classify_scalar_over_aggregate` ends in `aggregates.iter().map(parse_agg_item).collect()` (`grouped_agg.rs:399`), and `detect_group_by_aggregates` folds each nested plan with `None` for the declared type (`:264`), so the default applies. What is NOT established is that Exasol pushes `SQRT(STDDEV(a + b))` over a GROUP BY, and task 1.2 does not test it: its two queries are `SELECT STDDEV(score + id)` and `SELECT VARIANCE(score * 2)`, both ungrouped bare aggregates reaching only path (1). § Manual Testing's two `aggregate-extensions` rows repeat the same ungrouped query, and the new E2E test `e2e_stddev_over_expression_falls_back_and_returns_correct_value` runs the same ungrouped query. So the plan asserts a user-visible failure on a shape it never sends to a database, in the one plan whose § Impact promises "At most one user-visible change" and whose task 1.2 will rewrite that paragraph to a single branch. If Exasol pushes the grouped scalar-over-aggregate shape but not the ungrouped one, task 1.2 records "success", § Impact is rewritten to branch two ("NOTHING changes for a user"), and a real user-visible change ships documented as none. This is the same defect class as the finding above, at a location the revision created.
- Fix: In plan.md § Implementation Tasks task 1.2, add two more capture queries covering the grouped paths task 1.7 now claims: `SELECT region, SQRT(STDDEV(score + id)) FROM <vs_table> GROUP BY region` (path 3, `classify_scalar_over_aggregate`) and `SELECT region, STDDEV(score + id) FROM <vs_table> GROUP BY region` (path 2, `detect_group_by_aggregates`), each with its `EXPLAIN VIRTUAL` output, and state that the capture must record per-path whether Exasol pushes the shape. In task 1.7 clause (3), replace "This is a real behavior change and today's second failure path" with a claim scoped to what inspection establishes plus the same task-1.2 gate the other paths carry, e.g. "By inspection such an item classifies successfully on the malformed plan, takes the `DOUBLE PRECISION` default, and renders SQL the scan rejects; whether Exasol pushes this grouped shape is task 1.2's capture." In plan.md § Impact paragraph 1, state that the branch verdict is per-path across the three reachable paths rather than one verdict for the plan. In plan.md § Manual Testing, add a row for the grouped scalar-over-aggregate query before and after the change.

## Requirement Quality

No objection on `[REQUIREMENT_CONFLICT]` — axis checked via `speq plan validate` and `speq feature get`. The revised `aggregate-extensions` delta does not contradict `pushdown-planning-grouped-agg-scalar-over-aggregate`: that feature's four scenarios each name a decomposable shape and none names a statistical aggregate, verified against `specs/vs-adapter/pushdown-planning-grouped-agg-scalar-over-aggregate/spec.md:41-75`. The new `PartialAggColumn` name fragments in `scan-partial-agg-column-contract` § Scenarios reproduce the existing literals exactly — `count`, `count`, `sum`, `min`, `max`, `avg_sum`, `avg_cnt`, `stat_cnt`, `stat_sum`, `stat_sumsq`, checked against `partial_agg.rs:368-401` — so the byte-identity requirement is self-consistent. `speq feature validate` reports 0 errors library-wide.

No objection on `[COMPLETENESS_GAP]` — axis checked: the revision introduced no new gap. Round-1's ADVISORY on the "seven" versus six Background-bullet count in § Record Notes and § Features still stands (the delta's `DELTA:NEW` block holds six bullets, `spec.md:16-21`) and carries to the PR unaddressed; it is not re-raised here.

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: vs-adapter/pushdown-planning-aggregate-extensions/spec.md § Scenarios, the fifth `*AND*` clause (line 34)
- Issue: the clause that replaced "BOTH detection entry points" is one ~250-word AND step carrying five distinct call-site requirements, and its closing sub-claim is wrong for the one pre-change input that is actually reachable. It ends: "`render_scalar_over_merge` … and `render_having_over_merge` … SHALL return `None` … which is the outcome those two already produce through their `AggregatePlan` lookup against the detected plans, so their observable behavior is UNCHANGED". For `SELECT region, STDDEV(a + b) FROM t GROUP BY region HAVING STDDEV(a + b) > 1`, today `detect_group_by_aggregates` produces the malformed plan, `render_having_over_merge` parses the HAVING node to that same plan, and `plans.iter().position(…)` (`grouped_agg.rs:989`) FINDS it — so the function returns `Some(merged)`, not `None`. "Already produce" holds only when the select list omits the aggregate. The request-level reasoning in task 1.7 is sound (the select list is declined earlier by (2) or (3)), but the spec clause states it at function level, where it is false, and this is normative text that will be recorded permanently. Against INCOSE GtWR the clause is also not singular: a reader cannot tell which of the five sub-requirements a failing test refutes.
- Fix: In the `aggregate-extensions` delta, split the fifth `*AND*` clause into one clause per call site, each naming the site and its outcome. In the clause for `render_scalar_over_merge` and `render_having_over_merge`, replace "which is the outcome those two already produce through their `AggregatePlan` lookup against the detected plans, so their observable behavior is UNCHANGED" with the request-level statement that is actually true: "these two produce no request-level change of their own, because a select list that could place such a plan in `plans` is itself declined by `detect_group_by_aggregates` or `classify_scalar_over_aggregate`; pre-change their `position` lookup succeeded for exactly that select list."

## Task Breakdown

No objection on `[TASK_GRANULARITY]` — axis checked: the revision added no task and merged none. Round-1's ADVISORY on task 1.3's size stands, is now slightly larger, and carries to the PR with the planner's recorded refusal; it is not re-raised here.

#### [TRACEABILITY_GAP] ADVISORY
- Location: plan.md § Implementation Tasks task 1.7, paragraph 3 (`having_over_stat_aggregate_with_expression_argument_declines`)
- Issue: one of the two tests added to close round-1's blocker cannot fail before the production edit under the construction the task describes, and the plan's own analysis says so. Task 1.7 opens paragraph 3 with "Write the failing tests first, then the production edit" and lists four tests, including "a grouped request whose HAVING compares a statistical aggregate over an arithmetic argument, asserting `render_having_over_merge` returns `None`". The task never says the request's SELECT list must also carry that aggregate — and without it, `plans` does not contain the malformed plan, `plans.iter().position(…)` (`grouped_agg.rs:989`) already returns `None` today, and the test passes before the edit. Clause (4)-(5) of the same task states this outright: "both already declined these items at their `plans.iter().position(…)` lookup, which no malformed plan satisfies unless the select list carries the same shape". So the most natural reading yields a vacuous test that asserts identical behavior before and after, violating `/speq:code-guardrails`' failing-test-first rule while appearing to satisfy it. The sibling test `scalar_over_stat_aggregate_with_expression_argument_declines` has no such problem: `classify_scalar_over_aggregate` returns `Some(vec![malformed])` today, so it fails first as written.
- Fix: In plan.md § Implementation Tasks task 1.7 paragraph 3, specify the construction that fails first: "a grouped request whose SELECT list AND HAVING both carry the same statistical aggregate over an arithmetic argument (for example `SELECT region, STDDEV(a + b) … GROUP BY region HAVING STDDEV(a + b) > 1`) — the only shape whose `plans` list contains the malformed plan today, so `render_having_over_merge` returns `Some` before the edit and `None` after." Add one sentence stating that a variant whose SELECT list omits the aggregate already declines today and MUST NOT be used as the failing-first test.

## Design Depth

No objection on `[SHALLOW_DESIGN]` — axis checked against the Quick Diagnostic: the revision changed no interface. `AggKind::partial_columns()` keeps its one-sentence responsibility, and the two new fixtures add test data rather than an abstraction.

No objection on `[INFORMATION_LEAKAGE]` — axis checked: the revision added no cross-module decision. Round-1's ADVISORY on the unowned `EMITS`-type dimension stands and carries to the PR; it is not re-raised here.

No objection on `[TACTICAL_SHORTCUT]` — axis checked: the revision took no shortcut with a deferred follow-up. The one deferral it records (task 1.3 not split) is an explicitly declined ADVISORY, logged in decision-log § [plan-review], not a silent one.

No objection on `[BOUNDARY_VIOLATION]` — axis checked: the two new fixtures are asserted inside `scan/partial_agg.rs`'s own test module against a `#[cfg(test)]` export that already exists (`scan/mod.rs:44`) and a `pub` function (`partial_agg.rs:203`), so no production visibility widens and no dependency edge is added.

## Prose Quality

No objection on `[PROSE_UNCLEAR]` — axis checked: no sentence in the revised governed sections is unclear enough to make a requirement non-actionable. Task 1.7's five-call-site paragraph is long but each clause names a function, a line, and an outcome.

#### [PROSE_BLOAT] ADVISORY
- Location: plan.md § Impact paragraph 1, sentence 2 (line 96)
- Issue: the rewritten § Impact fixed round-1's em-dash violation but introduced a 52-word sentence against `/speq:writing-guardrails`' 25-word cap, in the section that skill names as PR-facing content: "What code inspection establishes: a query selecting a statistical aggregate over an expression (`STDDEV(a + b)`, `VARIANCE(LENGTH(name))`) produces an `AggregatePlan` carrying neither a `column` nor an `arg_expr`, `validate_agg_col_types` admits it on the `DOUBLE PRECISION` default, three `EMITS` columns are sized from that default, and the scan renders `COUNT(\"\")`, which DataFusion rejects." Four independent findings ride one sentence, so an architect scanning § Impact cannot lift any one of them out.
- Fix: In plan.md § Impact paragraph 1, split sentence 2 into a lead-in of 25 words or fewer plus one short sentence or sub-bullet per code-inspection finding: the plan carries neither `column` nor `arg_expr`; `validate_agg_col_types` admits it on the `DOUBLE PRECISION` default; three `EMITS` columns are sized from that default; the scan renders `COUNT("")`, which DataFusion rejects.
