# Plan Review Findings: refactor-pushdown-join-rendering-dedup (round 2)

## Summary
- Axes checked: 6/6
- Total findings: 4 (Blockers: 1, Advisory: 3)
- Intent Fidelity blockers: 0

## Round-1 Blocker Recheck

- **Resolved:** [REQUIREMENT_CONFLICT] `vs-adapter/pushdown-module-structure` pins the signature task 3 changes — `vs-adapter/pushdown-module-structure | CHANGED` is in plan.md § Features (line 82) with the reason stated (line 87), and `vs-adapter/pushdown-module-structure/spec.md` is authored as `DELTA:CHANGED` on the exact recorded scenario name (merge-by-name will land). A mechanical diff of the delta's restatement against recorded `specs/vs-adapter/pushdown-module-structure/spec.md:129-139` shows exactly one hunk: recorded bullet 138 replaced by the `column_tables(expr: &Json) -> (HashSet<String>, bool, bool)` return-form pin plus a separate clause re-scoping "compile unedited" to `collect_side_column_names` alone. Every other bullet is carried verbatim — the case-folding bullet (137) and the test gate (139) are byte-identical (verified by `diff`). The re-scope's blast radius is complete: `grep -rn "referenced_side_columns\|conjunct_single_side" specs/ --include=spec.md` outside `_plans/` returns line 138 and nothing else, so no other recorded clause depended on the retired guarantee. § Verification and § Manual Testing each gained a row for the amended scenario, and `cargo test -p lakehouse-engine dispatch_golden` resolves (the tests live in `adapter/pushdown/dispatch_golden.rs`, whose module path the filter matches).
- **Resolved (spec half); its accompanying gate fix introduced a new BLOCKER:** [REQUIREMENT_CONFLICT] `vs-adapter/pushdown-planning-selectlist-expressions` names the wrapper task 4.3 deletes — the feature is in § Features (line 83) with its reason (line 88), and the `DELTA:CHANGED` restates the recorded "A widened derived projection routes to a native wrapper on every path" scenario with exactly one change: `(render_expression_exasol_safe, reached by render_selectlist_item_qualified → render_expression_qualified)` becomes `(render_expression_exasol_safe, reached by render_expression_qualified)`. A `diff` of recorded lines 94-106 against delta lines 21-32 shows that single hunk; the `n_scan_join_select_items` refusal-site clause the feature depends on is carried verbatim. The grep-gate half is where the substitution went wrong — see Requirement Quality BLOCKER below.
- **Resolved:** [REQUIREMENT_CONFLICT] the case-folding prohibition was falsely attributed to `specs/_decision/037` — every case-folding site (plan.md § Context line 15, § Consequences line 70, task 1.1, decision-log [1]) now cites the two real sources and states positively that `037` is silent, so the citation cannot be restored as a cleanup. All three claims independently verified: the `support.rs` quotation matches lines 1302-1308 verbatim, including the `ß` → `SS` example; `grep -in "case.fold\|to_uppercase\|to_ascii_uppercase\|unicode\|ß" specs/_decision/037-refactor-pushdown-collect-walk-dedup.md` returns nothing, confirming the file is silent; and the four surviving `037` citations map to real ADR headings in that file — "Fold by deleting the wrapper, not by leaving a pass-through" (line 29) and "`walk_column_nodes` visits `column` nodes only, narrowing the issue's suggested `walk_json`" (line 3).
- **Resolved:** [REQUIREMENT_CONFLICT] the "exactly one owner" guarantee was unsatisfiable for `selectList` — the joins delta's first *THEN* is narrowed to `filter`, `groupBy`, `orderBy`, `having` (line 31) and a new *AND* records the retained `selectList` exception as by-design (line 32). The claim that `selectList` is the only clause of the five with two named sites is now true against the code: `referenced_column_projection`'s sole `selectList` reference is the walk task 6.4 replaces (its projection build iterates `all_cols`), and `referenced_side_columns`' guard is the second site. One residual — the exception never reaches the doc comment task 6.3 writes; filed as ADVISORY below.

## Intent Fidelity

no objection — axis checked. The revision added no work and dropped none: both new deltas reconcile recorded specs the plan already changes, the only task edited is 4.4's gate wording, and decision-log § Review Findings records all four directions without reopening an interview answer. The three interview decisions still hold unchanged (six decline sites in task 2.2, existing goldens as primary net plus the one justified addition, finding 6 in scope at tasks 4.1-4.3). No design reversal was smuggled in under a BLOCKER fix — the `pushdown-module-structure` delta explicitly retains the tuple return form that round-1 advisory 8 disputed, and says so.

## Feasibility

no objection — axis checked. Re-verified against HEAD every code fact the revision newly leans on: the six decline literals sit at `joins/sql_builders.rs:217,338,355,638,658,689` and share the `join pushdown declined: <phrase>; this is a hard error, not a native re-plan` frame, with `joins/mod.rs:64`'s `{detail}` template the distinct seventh; `collect_all_column_names` is `pub(super)` in `support` (line 1337), which reaches `joins::rendering`, so task 6.2's non-ASCII test compiles where the Verification table sites it; `collect_side_column_names` is private in `rendering.rs:234` and task 6.5 calls it with its existing signature, so the re-scoped "compile unedited" guarantee is satisfiable; `referenced_side_columns` (`rendering.rs:263`) matches task 6.5's described guard, condition-collect, and null filters. The amended scenario adds no scenario to `pushdown-module-structure` (stays at 9) and the joins feature reaches 7, so `/speq:record`'s >10-scenario threshold does not trip. `speq feature validate` reports 0 errors on all three features (warnings only, for AND-step count). The one feasibility defect the revision introduced is an ordering dependency on `/speq:record`; it is filed under Requirement Quality because it is stated as a pass/fail gate.

## Requirement Quality

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: plan.md § Implementation Tasks, task 4.4; § Verification → Manual Testing row 2; § Verification → Scenario Coverage row 8
- Issue: the substituted gate still cannot pass at the moment it runs. Task 4.4 requires "Zero hits … under `crates/` and under every recorded feature spec (`specs/<domain>/`)" and permits only two exception classes, then adds "A hit in `specs/vs-adapter/pushdown-planning-selectlist-expressions/spec.md` means that feature's delta (this plan's § Features) was not authored or not merged, and fails the gate." That file is a recorded feature spec under `specs/<domain>/` and provably holds one hit right now (`grep -rn` returns `specs/vs-adapter/pushdown-planning-selectlist-expressions/spec.md:104`). Task 4.4 runs inside `/speq:implement`; the delta that removes that hit is merged by `/speq:record`, which runs *after* implementation is verified. So at gate time the hit exists by construction, the gate's own text declares that a failure, and the implementer has no passing move: merging is `recorder-agent`'s operation, and hand-editing the recorded spec both exceeds implementation scope and duplicates the delta the recorder will replace by scenario name. The two exception classes themselves are sound and verified (`specs/_decision/001-migrate-legacy-decision-log.md` holds exactly two `render_selectlist_item_qualified` mentions at lines 2717 and 2730 and is an archived migration record; every other `specs/` hit is under this plan's own directory, `review/` included) — the substitution fixed two unsatisfiable arms and left a third. decision-log § Review Findings (BLOCKER 2 entry) names this defect class itself: "an unqualified 'returns nothing' would have been unsatisfiable against those two, which is the same defect class as BLOCKER 4."
- Fix: In plan.md task 4.4, delete the sentence beginning "A hit in `specs/vs-adapter/pushdown-planning-selectlist-expressions/spec.md` means…" and restate the pass condition as: "Zero hits are required under `crates/`. Under `specs/`, zero hits are required in every recorded feature spec EXCEPT `specs/vs-adapter/pushdown-planning-selectlist-expressions/spec.md`, whose single `render_selectlist_item_qualified` hit is expected until `/speq:record` merges this plan's delta for that feature — at this gate, verify instead that `specs/_plans/refactor-pushdown-join-rendering-dedup/vs-adapter/pushdown-planning-selectlist-expressions/spec.md` exists and contains `reached by \`render_expression_qualified\``. Hits are also permitted in `specs/_decision/001-migrate-legacy-decision-log.md`, an immutable archived record, and in this plan's own `specs/_plans/refactor-pushdown-join-rendering-dedup/` files including `review/`, which name both wrappers precisely to record their deletion." Mirror that condition verbatim into § Manual Testing row 2's Expected Output. In § Verification → Scenario Coverage row 8, replace "no recorded spec names the deleted hop" with "the authored delta removes the hop from the recorded dialect chain when `/speq:record` merges it".

#### [COMPLETENESS_GAP] ADVISORY
- Location: `crates/lakehouse-engine/src/adapter/pushdown/support.rs:1304` (no task covers it); `vs-adapter/pushdown-module-structure/spec.md` delta § Scenarios, *GIVEN*; `vs-adapter/pushdown-joins-module-structure/spec.md` delta § Background, second bullet
- Issue: the rename `collect_column_tables` → `column_tables` leaves three stale references the revision did not sweep, one of them in the artifact the BLOCKER-3 fix just promoted to load-bearing authority. `walk_column_nodes`' doc comment reads "`collect_column_tables` and `collect_side_column_names` in `joins/rendering.rs` fold with `to_ascii_uppercase`. Those two MUST NOT be unified" — after task 3.1 that names a function that does not exist, and task 4.4's grep gate covers only the two deleted wrapper names, so nothing catches it. Task 4.4 does fix the equivalent stale mention at `joins/rendering.rs:97`, so the omission is an oversight rather than a decision. In the spec deltas the same item is named two ways post-merge: the `pushdown-module-structure` delta's *GIVEN* keeps `collect_column_tables` while its changed *AND* pins `column_tables`, and the joins delta's Background bullet describes the change using only the old name.
- Fix: Add to plan.md task 3.1: "Update `walk_column_nodes`' doc comment in `adapter/pushdown/support.rs` (line 1304) to name `column_tables`, leaving the rest of the case-folding paragraph verbatim." In the `vs-adapter/pushdown-module-structure` delta's *GIVEN*, write the walk as "`column_tables` (`collect_column_tables` before this delta)", and in the `vs-adapter/pushdown-joins-module-structure` delta's Background bullet write "`collect_column_tables` returning its three outputs as `column_tables`". If task 4.4's grep is extended to `collect_column_tables`, apply the same record-time exception to `specs/vs-adapter/pushdown-module-structure/spec.md` that the BLOCKER above establishes for the select-list spec.

#### [COMPLETENESS_GAP] ADVISORY
- Location: plan.md § Implementation Tasks, task 6.3
- Issue: BLOCKER 4's fix landed in the spec but not in the code artifact a future refactorer reads. Task 6.3 still instructs: "Doc-comment it as the single owner of 'which clauses of a pushdown request can name a source column'" — the unqualified claim the delta's new *AND* just retracted for `selectList`. The delta states the exception is "a retained exception rather than an incomplete reduction … so a later reader does not 'finish' the reduction", but that reader is reading `referenced_clause_values`' doc comment, which as instructed claims sole ownership and never mentions the guard. Folding the guard in is then the natural cleanup, and it gives the projection path the short-circuit the same scenario forbids — the exact failure the fix was written to prevent.
- Fix: In plan.md task 6.3, append to the doc-comment instruction: "and state that `referenced_side_columns` deliberately keeps its own absent/empty-`selectList` short-circuit before this walk, so `selectList` is named twice by design; that guard MUST NOT be folded in here, because doing so would give `referenced_column_projection` a short-circuit that `vs-adapter/pushdown-joins-module-structure`'s 'One clause walk feeds both wrapper column-narrowing routines' scenario forbids it."

#### [AMBIGUOUS_REQUIREMENT] ADVISORY
- Location: `vs-adapter/pushdown-module-structure/spec.md` delta § Background, third bullet; recorded `specs/vs-adapter/pushdown-module-structure/spec.md:135` (restated verbatim as delta line 23)
- Issue: the delta asserts compliance with a clause it silently softens. Its Background says "No visibility changes on either side … so no `use` path widens and this feature's 'narrowest visibility that compiles' rule holds unedited", while the recorded clause it restates verbatim reads "so NO item's visibility widens and no join-module `use` path **changes**". Tasks 3.2, 4.4, and 6.4 each change `sql_builders.rs`' `use super::rendering::{…}` list. The fair reading scopes the recorded consequence to declaring the primitive in `support` rather than to any future edit of a joins `use` line, so this is not a conflict — but the delta resolves it by paraphrase rather than by saying so, leaving a later auditor who greps the recorded wording with an apparent violation and no recorded answer.
- Fix: In the `vs-adapter/pushdown-module-structure` delta § Background, replace "so no `use` path widens and this feature's 'narrowest visibility that compiles' rule holds unedited" with "so no item's visibility widens. The restated clause's 'no join-module `use` path changes' consequence scopes to declaring the primitive in `support`; issue #181 does edit the `use super::rendering::{…}` list inside `joins` (tasks 3.2, 4.4, 6.4), which widens no path and adds no cross-module reach."

## Task Breakdown

no objection — axis checked. The revision added no task and reordered none, so the round-1 verification of the WBS still stands. Both amended scenarios have implementing tasks and Verification rows: the `pushdown-module-structure` amendment maps to task 3 (row 7, proven by the four goldens plus the two `dispatch_golden` decline-wrapper assertions), and the select-list amendment maps to task 4.3 (row 8, proven by `golden_grouped_qualified_fallback_sql_unchanged` plus the 4.4 gate — whose pass condition the BLOCKER above corrects). No new parallelization claim: the "Parallelization: None" section and its three hard orderings are untouched and remain correct.

## Design Depth

no objection — axis checked. The revision changed no interface, module boundary, or dependency direction — both new deltas are spec reconciliation, and § Key interfaces, § Patterns, and § Consequences carry the same five reductions. The tuple return form round-1 advisory 8 disputed is retained deliberately and the delta pins it normatively with its rationale (three fresh values per use, one call site inside a loop), so the decision is now recorded rather than implicit. Nothing new leaks across a boundary: `referenced_clause_values` stays `pub(super)` inside `joins/rendering.rs` on an import seam `sql_builders.rs` already uses, `join_render_decline` and `shard_side` stay private to their single caller file, and the change remains confined to VS-side SQL assembly with no I/O and no execution logic, so mission's "VS stays thin" holds.

## Prose Quality

no objection — axis checked. The revision's new prose is confined to spec Background bullets and Gherkin scenario steps, which `/speq:writing-guardrails` places outside the governed set, plus decision-log § Review Findings entries whose Finding/Direction-change prose is front-loaded and names the artifact, section, and change in each case. The one governed edit — plan.md § Features' two new explanatory bullets — leads with the conclusion ("in scope because each pins normatively a function this plan changes") and stays inside the sentence cap. Checked and dismissed: the BLOCKER-3 review entry carries `Promotes to ADR: yes` on a Finding/Direction-change entry, which `/speq:spec-merge`'s ADR field mapping expects to read as Decision/Alternatives; precedent shows `recorder-agent` handles exactly this shape — `specs/_recorded/2026-07-28-refactor-pushdown-expr-rewrite-primitive/decision-log.md`'s two `[plan-review]` entries became well-formed ADRs at `specs/_decision/037-refactor-pushdown-expr-rewrite-primitive.md:107` and `:145` with the review prefix dropped — so this is not a defect.
