# Open Questions: refactor-pushdown-node-count

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan refactor-pushdown-node-count` locally, or re-run `/speq:plan-pr` after commenting.

- [ ] **Missing ADR supersede pointer.** Decision-log entries [2] and [6] reverse an existing Accepted ADR (`source-cluster-node-count-from-udfcontext-node-count-not-a-connect-back-select-nproc-supersedes-adr-006`), which mandates recording `CLUSTER_NODES`. Neither entry names it. Without a `Supersedes` pointer, `/speq:record` will publish two contradictory Accepted ADRs with no link between them. Proposed fix: add a closing Rationale sentence to decision [2] naming the superseded ADR slug and stating what it retains (the `UdfContext::node_count()` source and the `0 => 1` floor).
- [ ] **Stale test-retarget instruction in task 1.3.** The task still tells the implementer to retarget `build_adapter_notes_merges_existing`'s probe key to `NOTE_PARALLELISM_FACTOR` — but that test has no such key, and its actual `CLUSTER_NODES`-survives assertion (`adapter/mod.rs:1502-1506`) now points the wrong way after the plan's own fix to actively remove the key via `notes.remove()`. Left as written, the merged (indivisible) task 1.3 ends red. Proposed fix: drop this test from the retarget instruction; instead have it keep the fixture's inherited `CLUSTER_NODES` entry, assert the key is now absent, and update its doc comment and the plan's Scenario Coverage table accordingly.

Both findings and their exact proposed fixes are in `review/round-2.md`; round 1 was resolved in full (4/4 blockers), round 2 surfaced these 2 new blockers plus 10 advisory items (rollback/rollout asymmetry, an ADR-count threshold note, prose trims, and coverage-mapping corrections) that don't block landing the plan — see the PR comment for the advisory list.
