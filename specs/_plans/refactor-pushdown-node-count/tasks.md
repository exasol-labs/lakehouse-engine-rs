# Tasks: refactor-pushdown-node-count

## Phase 2: Implementation (Group A)
- [x] 1.1 Add `cluster_nodes_from_context(ctx: &dyn UdfContext) -> usize` in `crates/lakehouse-engine/src/adapter/mod.rs`, applying the `0 => 1` floor. Write its two unit tests first — `cluster_nodes_from_context_defaults_to_one_when_node_count_zero` (`0 => 1`) and `cluster_nodes_from_context_passes_through_reported_node_count` (`N => N` for `N = 4`) — reusing the existing `StubCtx` and `NoopCtx` test doubles.

## Phase 2: Implementation (Group B)
- [x] 1.2 Capture the node count in `dispatch`'s pushdown arm next to `ctx.script_schema()`, add a `cluster_nodes: usize` parameter to `handle_pushdown_request`, and delete its `adapter_note(request, NOTE_CLUSTER_NODES)` read. Update the arm's explanatory comment, which currently cites `resolve_cluster_nodes`, and rewrite the comment block at `adapter/mod.rs:370-372` so it names only `PARALLELISM_FACTOR` and the other note-carried values, not `CLUSTER_NODES`.

## Phase 2: Implementation (Group C)
- [x] 1.3 Stop writing the note, start removing it, and reconcile the tests in the same step. Production side (`crates/lakehouse-engine/src/adapter/mod.rs`): drop the `cluster_nodes` parameter from `build_adapter_notes`, replace its `NOTE_CLUSTER_NODES` insert with `notes.remove(NOTE_CLUSTER_NODES);`, keep the `NOTE_CLUSTER_NODES` constant as that removal key, reduce `resolve_cluster_nodes(ctx, props) -> (u32, u32)` to `resolve_nr_of_cores(props) -> u32` (dropping the unused `ctx` param), update `build_adapter_notes`'s doc comment (currently lists `CLUSTER_NODES` among carried keys), and rewrite the channel-rationale comment above the constant to describe a removal key rather than a write channel — state that the constant and the `remove` call exist solely to evict a key persisted by adapter versions before this change, and that both can be deleted once every deployed virtual schema has refreshed at least once on this version or later. Reconcile all 19 `build_adapter_notes` call sites (1 production, 18 test). Test side, same file: delete `create_response_carries_cluster_nodes_property` and `adapter_notes_cluster_nodes_round_trips`; retarget the `adapter_note` probe key in `adapter_note_absent_or_unparseable_yields_none` to `NOTE_PARALLELISM_FACTOR`; in `build_adapter_notes_merges_existing` (`adapter/mod.rs:1473`), keep the fixture's inherited `CLUSTER_NODES` entry, replace the `parsed[NOTE_CLUSTER_NODES] == Some("3")` assertion (lines 1502-1506) with an assertion that the key is absent, and update the doc comment to state merge-not-clobber for every key except `CLUSTER_NODES`, which is removed; rename `adapter_notes_carry_cluster_nodes_and_parallelism_factor` to `adapter_notes_carry_parallelism_factor` and drop its `CLUSTER_NODES` assertion; delete `cluster_nodes_defaults_to_one_when_node_count_zero` and `cluster_nodes_passes_through_reported_node_count` (superseded by task 1.1's two tests); update the three `resolve_cluster_nodes` test call sites to the new `resolve_nr_of_cores` signature: `nr_of_cores_from_available_parallelism_when_unavailable` (`adapter/mod.rs:1702`), `nr_of_cores_property_overrides_auto_detect` (`:2194`, also deleting its `assert_eq!(nodes, 3u32, ...)`), and `nr_of_cores_property_falls_back_to_auto_detect` (`:2245`, also deleting its `assert_eq!(nodes, 1u32)`); invert the surviving `CLUSTER_NODES` assertions in `table_map_merges_with_existing_notes` and `create_vs_records_table_map_in_adapter_notes` to assert the key is absent; add `adapter_notes_omit_cluster_nodes`, `refresh_notes_drop_inherited_cluster_nodes` (a `refresh` request whose incoming `adapterNotes` carry `CLUSTER_NODES` yields notes without it, every other inherited key preserved), and `pushdown_ignores_persisted_cluster_nodes_note` (a `pushdown` request whose `adapterNotes` carry `CLUSTER_NODES` = "9", called with `cluster_nodes = 1`, asserting the shard count derived is `1 × PARALLELISM_FACTOR`).

## Phase 2: Implementation (Group D)
- [x] 1.4 Update the IPROC / NPROC row of the Domain Glossary at `specs/mission.md:75`: replace "captured once at `createVirtualSchema` from `UdfContext::node_count()` (the UDF handshake)" with a statement that the shard-count node count is read from `UdfContext::node_count()` per pushdown request. Leave that row's `NPROC()`/`IPROC()` clauses and every other mission section unchanged.
- [x] 1.5 Replace the E2E test `create_vs_records_cluster_nodes_property` in `crates/lakehouse-engine/tests/e2e_scan_test.rs` with `create_vs_omits_cluster_nodes_from_adapter_notes` (asserts `ADAPTER_NOTES` parses, carries `PARALLELISM_FACTOR` and `NR_OF_CORES`, and carries no `CLUSTER_NODES` key).
- [x] 1.6 Add the E2E test `pushdown_shards_from_handshake_node_count_without_note` in `crates/lakehouse-engine/tests/e2e_scan_test.rs`: run `EXPLAIN VIRTUAL` over a multi-file scan against the note-free virtual schema and assert the generated SQL still carries the `LAKEHOUSE_DISTRIBUTE_FILES` fan-out with `AS shards(shard_key, files) GROUP BY shard_key)`, following the assertion style already used at `e2e_scan_test.rs:1088`.
- [x] 1.7 Update the `cluster_nodes` doc comment on `handle_pushdown` in `crates/lakehouse-engine/src/adapter/pushdown/mod.rs` to name `ctx.node_count()` captured in `dispatch` as the source.

## Phase 2: Implementation (Group E)
- [x] 1.8 Run the verification checklist: `cargo test`, `cargo clippy --all-targets`, `cargo fmt`, `make test-e2e`.

## Phase 4: Review Fixes
- [x] 4.1 In `crates/lakehouse-engine/src/adapter/mod.rs`, delete the assertion-free test `pushdown_ignores_persisted_cluster_nodes_note` with its doc comment (lines 2614-2647): it re-implements the production computation in its own body and asserts two literals against each other, so it passes under the full note-read regression. Substitute no replacement assertion — the negative invariant is unobservable below the database, because the derived shard count is visible only in the generated SQL, which `handle_pushdown` reaches after `CatalogSession::resolve` and `resolve_file_list`. Establish the surviving guards by injecting each note-read regression shape and recording what fails. Then correct `specs/_plans/refactor-pushdown-node-count/plan.md` § Verification: drop the test from the two § Scenario Coverage rows that name it and from § Implementation Tasks task 1.3, and state which guard covers each regression shape, naming the one shape only § Manual Testing catches. [expert]
- [x] 4.2 In `crates/lakehouse-engine/tests/e2e_scan_test.rs`, rewrite the doc comment on `pushdown_shards_from_handshake_node_count_without_note` to state only what the test guards: dropping the persisted `CLUSTER_NODES` note did not break shard-fan-out emission (`pushdown` still plans a `LAKEHOUSE_DISTRIBUTE_FILES` fan-out with no adapterNotes node count available). Add a sentence stating the test cannot distinguish the handshake node-count source from the pre-refactor absent-note default, because the two-file `events` fixture clamps `shard_count` to 2 for every node count >= 1, and name the plan's four-node staging gate (§ Manual Testing, `parallelism/work-unit-sharding (GATE: four-node staging)`) as the only check that does. Delete the claim that the test proves live derivation. Replace the `e2e_scan_test.rs:1088` line-number citation with a bare reference to `ordered_topn_pushes_down_matches_single_node` by name.
- [x] 4.3 In `crates/lakehouse-engine/tests/e2e_scan_test.rs`, drop the `ORDER BY id` clause from `pushdown_shards_from_handshake_node_count_without_note` so it exercises the plain projection-scan pushdown shape instead of the unrelated ORDER-BY-without-LIMIT fallback: change the statement to `let sql = format!("SELECT id, name, score FROM {}", vs_table());`. Leave both existing `assert!` markers unchanged.
- [x] 4.4 In `crates/lakehouse-engine/src/adapter/mod.rs`, rewrite the dispatch comment in the `Some("pushdown")` arm (currently at lines 151-158) to separate two distinct reasons: (a) `ctx.connection()`, reached via `resolve_connection_config`, is a connect-back round-trip that may block on the UDF host and therefore must not run inside the tokio runtime; (b) `ctx.script_schema()` and `cluster_nodes_from_context(ctx)` are plain handshake-metadata field reads, captured here not because they block but so the async planning body reads no ambient state and takes no dependency on the UDF delivery mechanism. Delete the "all three must happen before the runtime is built" clause. Keep the existing note that `script_schema` qualifies the scan/distributor/merge UDF names in the generated pushdown SQL.
- [x] 4.5 In `crates/lakehouse-engine/src/adapter/mod.rs`, replace the first paragraph of the `resolve_nr_of_cores` doc comment (currently at lines 952-953) with a single sentence stating the promise without the vestigial "no SQL round-trip, no connect-back session" contrast, e.g. `/// Per-node CPU core count used to derive the AUTO parallelism, DataFusion threading, and S3 connection budgets.` Keep the second paragraph (override-then-auto-detect precedence and the `0` = "unknown, caller applies the floor" contract) unchanged.

## Phase 5: Verification
- [x] 5.1 Automated checks (5a) — cargo test, clippy, fmt, cross-musl-udf-build, test-e2e all green
- [x] 5.2 Scenario coverage audit (5b) — all 16 plan-listed scenarios have passing tests
- [x] 5.3 Manual verification (5c) — 3/5 rows covered by automated E2E equivalents; 2/5 (four-node staging gate, legacy-.so REFRESH check) NOT RUN — infra unavailable in this sandbox, flagged in verification-report.md

## Phase 6: Report
- [x] 6.1 Generate verification-report.md
