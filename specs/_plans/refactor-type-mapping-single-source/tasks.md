# Tasks: refactor-type-mapping-single-source

## Phase 2: Implementation (Group A — parallel)
- [x] 1 Fold the Arrow arm list in `types/mapping.rs` behind one private classifier; make `arrow_to_exasol_type` and `needs_json_fallback` read from it, keeping both signatures. Add the unit test proving `Utf8` and out-of-range `Decimal128` share a type string but differ on the JSON flag. [expert]
- [x] 2 Split `arrow_value_at`'s `Int64 | UInt32 | UInt64` arm in `scan/convert.rs` into three flat top-level arms; delete the inner `match dt` and the `unreachable!()`. Add the unit test covering `Int64`, `UInt32`, `UInt64` below `i64::MAX`, and `UInt64` above it.

## Phase 2: Implementation (Sequential after Group A)
- [x] 3 Move `exasol_type_to_json` from `adapter/mod.rs` to `types/mapping.rs` as `pub(crate)`; update its one call site and relocate its two unit tests unedited.
- [x] 4 Move `exasol_type_from_json` from `adapter/pushdown/support.rs` to `types/mapping.rs` as `pub(crate)`; update all eight call sites across `support.rs`, `grouped_agg.rs`, `file_resolution.rs`, and `joins/planning.rs`, and relocate its three unit tests unedited.
- [x] 5a Make `parse_decimal_args` `pub(crate)`; delete `exasol_type_to_json`'s inline `DECIMAL` parser in its new `types/mapping.rs` home and call `parse_decimal_args` instead. Serialize the parsed scale as a SIGNED JSON number, never cast to unsigned. Add the three characterization tests pinning its divergence classes — absent scale, precision or scale outside `u8`/`i8` range, negative scale. Add the doc note on `arrow_type_from_tag` recording why its `decimal128(` parse stays separate. [expert]
- [x] 5b Delete `sum_emit_type`'s inline `DECIMAL` parser (`adapter/pushdown/grouped_agg.rs`) and call `parse_decimal_args` instead. Add one characterization test per representative input listed in spec.md scenario 2's `sum_emit_type` clause, plus a test for each of the two invariants stated there. Note in the test module that `sum_emit_type` GAINS a trimming step it lacks today. Do NOT add an uppercasing step. [expert]
- [x] 6 Add `pub enum ExaTypeClass { Character, Date, Decimal, Other }` and `pub fn classify_exa_type(&str)` to `types/mapping.rs`, reproducing the three pushdown guards' exact predicates. Document #265 as the intended first consumer. Add the per-variant unit test including bare `DECIMAL`. Do not touch `support.rs`'s guards.
- [x] 7 Update the `types/mapping.rs` module doc comment to name the three representations the module owns. Then run the full gate: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`.

## Phase 3: Verification
- [x] V1 Run automated checks (build, test, e2e, lint, format)
- [x] V2 Scenario coverage audit
- [x] V3 Manual verification (grep checks, diff stat)
- [x] V4 Generate verification report

## Phase 4: Review Fixes
- [x] 4.1 In crates/lakehouse-engine/src/types/mapping.rs, delete the pre-match `if let DataType::Decimal128(p, s)` block and its 2-line comment in `compatible_exasol_type`, and instead add `DataType::Decimal128(p, s) if *p <= 36 && *s <= 36 => return Some(format!("DECIMAL({p},{s})")),` as an arm of the existing `match dt`, placed immediately after the `DataType::Timestamp(_, _)` arm and before the `_ => return None` arm.
- [x] 4.2 In crates/lakehouse-engine/src/types/mapping.rs, convert every `///` line in the leading module doc block to an inner doc comment `//!` so the block documents the module rather than the `use` item it currently binds to. Change no wording. Verify with `cargo doc -p lakehouse-engine --no-deps` that the `types::mapping` module page shows the three-representation text.
- [x] 4.3 In crates/lakehouse-engine/src/types/mapping.rs, replace the "The input is expected to be already upper-cased and trimmed." sentence in `parse_decimal_args`' doc comment with a statement of the actual contract: the input must be upper-cased and the `DECIMAL(` prefix must start at offset 0 (leading or trailing whitespace around the whole string yields `None`), while whitespace around each argument is trimmed before parsing.
- [x] 4.4 In crates/lakehouse-engine/src/scan/convert.rs, delete the now-stale two-line comment above the `Int64` arm and the redundant single-line comment restating the overflow check, keeping only the one comment explaining the UInt64-overflow-to-Numeric branch. Rename the test `int64_uint32_uint64_arms_produce_expected_values` to `int64_uint32_uint64_convert_identically_through_flat_arms`, matching the plan's Verification table. Change no assertion.
- [x] 4.5 In crates/lakehouse-engine/src/adapter/mod.rs, add `use crate::types::mapping::exasol_type_to_json;` to the existing `use` block and change the fully-qualified inline call to call `exasol_type_to_json(ty)` unqualified.
- [x] 4.6 In crates/lakehouse-engine/src/adapter/pushdown/grouped_agg.rs, delete the seven single-input `sum_emit_type` tests already reproduced verbatim as rows of the two table-driven invariant tests (`sum_emit_type_trims_an_untrimmed_scale`, `sum_emit_type_canonicalizes_a_leading_plus_scale`, `sum_emit_type_declines_a_non_numeric_scale`, `sum_emit_type_declines_a_scale_outside_i8`, `sum_emit_type_declines_a_three_argument_input`, `sum_emit_type_declines_a_precision_outside_u8`, `sum_emit_type_declines_a_non_numeric_precision`), keeping `sum_emit_type_absent_scale_widens_to_a_scale_zero_decimal` and both tables. Verify each input against its table row before deleting; fold the per-divergence-class rationale into the two table docs and trim the `// NOTE` block to the trimming-step consequence it alone states. [expert]
