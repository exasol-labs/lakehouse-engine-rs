# Feature: Storage Backend Enum

Gives the storage-backend decision exactly one home: a `StorageBackend` enum whose variant IS the backend, so every other module asks the enum for what it needs instead of deciding for itself which backend it is talking to.

## Background

* **This delta lands the second variant, `Adls`, and is issue #275 — slice C of six (A-F).** It SUPERSEDES this feature's "Scope fence: ONE variant" bullet, which named the Azure variant as the declared successor slice and forbade it only in slice B. Everything slice B built is now exercised for the first time: with two variants, every `match` has two arms, every method's dispatch is observable, and the wire tag discriminates rather than merely existing.
* **The variant carries an account name and exactly one credential.** `Adls { account_name: String, cred: AdlsCred }` with `AdlsCred` holding an account-key state or a SAS state and no third state, so an Azure backend that names two credentials — or none — is unrepresentable rather than validated away at every use site. A struct variant rather than a wrapper struct: unlike `S3`, there is no pre-existing Azure credential type whose serde contract must be preserved, so a wrapper would be a type introduced for symmetry alone.
* **`AdlsCred` redacts its payload in `Debug`.** `ConnectionCreds` already does this for `secret_key`, `session_token`, `token`, and `client_secret`, and the three new CONNECTION fields join that manual impl. Doing the same on the wire type costs six lines and leaves issue #135 (credentials in cleartext in query plans) with strictly less to fix rather than strictly more.
* **The iceberg config keys are the crate's own named constants, verified against the resolved dependency, not spelled inline.** `iceberg::io::ADLS_ACCOUNT_NAME` = `"adls.account-name"`, `ADLS_ACCOUNT_KEY` = `"adls.account-key"`, `ADLS_SAS_TOKEN` = `"adls.sas-token"` (`iceberg-0.10.0/src/io/storage/config/azdls.rs:34-38`, re-exported through `iceberg::io`), mirroring the existing `S3_*` imports. `iceberg-storage-opendal-0.10.0/src/azdls.rs:46-85` reads exactly these strings from the `FileIO` props map.
* **`OpenDalStorageFactory::Azdls` is a UNIT variant** (`iceberg-storage-opendal-0.10.0/src/lib.rs:108-134`) — no fields, unlike `S3 { customized_credential_load }`.
* **The Azure arm MUST NOT emit `adls.connection-string`.** `iceberg-storage-opendal-0.10.0/src/azdls.rs:49-54` fails `FileIO` construction hard when that key is present.
* **`adls.account-name` doubles as a wrong-account guard, deliberately.** When it is set, `iceberg-storage-opendal` compares it against the account in each URI and raises `DataInvalid` on a mismatch (`azdls.rs:163-170`). Because `validate_creds` requires `account_name` for every Azure CONNECTION, the key is always present and the guard is always armed: a table whose location names a different storage account fails loud instead of attempting an unauthorised read.
* **The container comes from the URI, not from props.** `iceberg-storage-opendal` reads the filesystem/container from the URI userinfo and derives the endpoint from the URI host (`azdls.rs:195-207`, `azdls.rs:244-267`), so ONE `FileIO` serves every container in the account and neither key needs to be configured. Both `abfss://` and `abfs://` are accepted (`azdls.rs:144-159`).
* **The engine's object store is the asymmetry this slice has to close.** `MicrosoftAzureBuilder::with_url` on `abfss://<container>@<account>.dfs.core.windows.net` sets BOTH the account and the container (`object_store-0.13.2/src/azure/builder.rs:662-682`), so the built store is CONTAINER-scoped. DataFusion, however, keys the registry by scheme + `host:port` ONLY — `get_url_key` reads `Position::BeforeHost..Position::AfterPort` (`datafusion-execution-54.1.0/src/object_store.rs:268-274`), which EXCLUDES userinfo — so the key identifies the ACCOUNT. Two containers of one account therefore collapse onto one registered store, and without a guard a broadcast join's dimension side would be read out of the fact side's container with no error.
* **`ListingTableUrl::object_store()` is the finer notion and already sees the container.** It slices `Position::BeforeScheme..Position::BeforePath` (`datafusion-datasource-54.1.0/src/url.rs:323-327`), which INCLUDES userinfo. `validate_uniform_object_store_files` compares exactly that per data file and per delete file, so a file list that MIXES containers WITHIN one side is already rejected today and this delta does not edit that function's rule. What is missing is the ACROSS-side case, where two sides each internally uniform resolve to two different stores under one key.
* **Credential setters, verified against `object_store-0.13.2`:** a shared account key is `with_access_key` (`src/azure/builder.rs:730-734`), NOT `with_account_key`; a raw SAS string is `with_config(AzureConfigKey::SasKey, …)` (documented at `src/azure/builder.rs:252-262`, split by `split_sas` at `:1077-1094`, leading `?` optional), NOT the typed `with_sas_authorization`, which takes pre-split query pairs. In `build()` an access key BEATS a SAS (`:990` vs `:1021`); `AdlsCred` makes setting both unrepresentable, so that precedence can never be reached.
* **`MicrosoftAzureBuilder::with_url` accepts exactly FOUR `abfss://` host suffixes, and this slice does not widen that.** With a non-empty userinfo it splits the host once on `.` and matches the remainder against `dfs.core.windows.net`, `blob.core.windows.net`, `dfs.fabric.microsoft.com`, or `blob.fabric.microsoft.com`; every other host returns `Error::UrlNotRecognised` (`object_store-0.13.2/src/azure/builder.rs:660-682`). The account segment must also contain no further `.` (the `validate` closure at `:655-658`). A sovereign-cloud endpoint (`*.dfs.core.chinacloudapi.cn`), a private endpoint, and a custom-domain account therefore FAIL at `build()`. That is an upstream capability boundary, not a defect this slice introduces or hides: the arm surfaces the error redacted rather than special-casing the host, and `plan.md` names it a Non-Goal.
* **`s3a://` is the one existing input whose registered store key CHANGES.** `FileEntry::path` is documented as an absolute S3 **or s3a** URI (`crates/lakehouse-engine/src/scan/spec.rs:445`, `:730`). The deleted `extract_bucket_from_files` returned only the host, and its caller rebuilt the key as `format!("s3://{bucket}")` — rewriting an `s3a` scheme to `s3`. The lookup side never did: `register_file_list` takes `ListingTableUrl::parse(&first_abs).object_store()`, preserving `s3a`. Since `get_store` matches `scheme://host:port` exactly, the two disagree and an `s3a://` scan errors today with "No suitable object store found". `side_store_url` returns the scheme verbatim, so the key and the lookup agree and `s3a://` starts working. The S3 characterization gate is unaffected: every committed fixture uses `s3://`.
* **The `with_client_options` ordering hazard is identical to the S3 builder's** (`src/azure/builder.rs:859-863`): it REPLACES the whole `ClientOptions`. The Azure arm sets no `allow_http`, so nothing is currently at risk, but the same ordering discipline applies to any later setter that layers onto `ClientOptions`.
* **Iceberg spec compliance is checked and unchanged.** `apache/iceberg` `format/spec.md` defines data-file field `100 file_path` as "Full URI for the file with FS scheme" (required in v1, v2, and v3), and path resolution as "If the path starts with a URI scheme, it is absolute and is used without modification. If the path does not start with a URI scheme, the resolved path is the table location followed by the relative path joined by the URI separator character `/`." Both rules are scheme-agnostic, and `relativize_path_to_root` and `reconstruct_abs_uri` already implement exactly them. This delta therefore PINS their `abfss://` behaviour with tests and changes neither. The spec mandates no scheme-specific reader behaviour beyond that: it "only requires that file systems support the following operations: In-place write, Seekable reads, [and] Deletes". This delta introduces no deviation and fixes none; it changes no file pruning, no snapshot or manifest reading, no delete-file handling, and no type mapping.
* **`datafusion-scan/scan-execution-memory-and-credentials` needs no amendment.** Its two credential-passthrough scenarios are explicitly about vended S3 credentials and stay accurate, and its Background already assigns "how that store is derived and registered without the scan path naming a backend" to this feature.
* No new dependency is introduced and no dependency version changes: `object_store`'s `azure` feature and `iceberg-storage-opendal`'s `opendal-azdls` feature were both enabled by slice A (issue #273).
* Every scenario is a pure refactor. Every scenario of `vs-adapter/connection-credentials`, `vs-adapter/connection-credentials-catalog-auth`, `vs-adapter/rest-catalog-oauth-auth`, `vs-adapter/pushdown-planning*`, `vs-adapter/pushdown-file-pruning`, `datafusion-scan/scan-execution*`, and the S3 E2E suite stays behaviorally accurate, and those suites are the characterization gate that makes "S3 behavior unchanged" falsifiable.
* **`StorageProps` itself is NOT edited.** The struct, its field set, its field order, its serde attributes, its `Default` impl, its `secret_values` body, and its three unit tests are untouched. `StorageBackend::S3` WRAPS it as the variant payload. Every `StorageProps { .. }` construction literal in production and test code stays as written; only the places that HOLD, PASS, or MATCH a storage backend gain the wrapping layer. Inlining the seven fields onto the variant was rejected: it would edit the one type whose serde encoding `vs-adapter/catalog-crate-structure` pins field-for-field, for no gain the wrapper does not already deliver.
* **Three of the four methods live on the enum; the fourth cannot.** `vs-adapter/catalog-crate-structure` records that `lakehouse-catalog`'s manifest MUST NOT declare `object_store` or `datafusion` as a direct dependency. DataFusion object-store registration therefore cannot be a method on a catalog-crate type. Issue #274 states the same split ("Only `register_object_store` ... stays engine-side"), so the enum declares `secret_values`, `catalog_storage_props`, and `file_io`, and the engine owns one backend-dispatching registration function in `crates/lakehouse-engine/src/scan/object_store.rs`. The issue's `register_object_store(env, sample_uri)` is illustrative shorthand, not a literal signature. Only two of the three methods are `pub`: `catalog_storage_props`' consumers are both in-crate.
* **The crate boundary forces two owners.** The enum owns WHICH backend; the engine's single registration function owns that backend's object store. Before this feature four engine-side sites independently know the backend is S3 — `extract_bucket`'s `url.host()` bucket derivation, `build_s3_store`, `register_bucket_store`, and the `build_s3_file_io` call. After it, one.
* **`catalog_storage_props` is the single source of the six iceberg S3 config keys.** The keys are `S3_ENDPOINT`, `S3_REGION`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_SESSION_TOKEN`, and `S3_PATH_STYLE_ACCESS`; the first four are inserted only when non-empty, the session token only when `Some`, and path-style always. `file_io` folds that map into the `FileIOBuilder` and `build_rest_catalog` merges it into its props map, so neither declares a key of its own. `decision-log.md` [3] records the line-by-line evidence that the two pre-refactor mappings are identical.
* **A `HashMap` return is order-safe for both consumers.** `catalog_storage_props`' entries reach `build_rest_catalog` via `HashMap::extend` and `file_io` via repeated `FileIOBuilder::with_prop`; both are keyed inserts, so the map's nondeterministic iteration order cannot change either result.
* **The scan-spec wire gains a variant tag, and that is safe here.** `CommonScanSpec.storage` is serialized into the scan-driving SQL literal, so wrapping it changes that literal's `storage` value from `{...}` to `{"s3":{...}}`. `datafusion-scan/scan-execution-spec-reconstitution` already records that "the same `.so` produces and consumes the spec within one deploy (there is no cross-version wire-compatibility requirement)", which is exactly the property this change relies on. `vs-adapter/catalog-crate-structure`'s "the serde encoding of `StorageProps` MUST be unchanged field-for-field" clause also stays TRUE and is not amended: the payload's own encoding is byte-identical, and the tag is the enclosing enum's.
* **The wire tag is externally tagged and lowercase, and it lands in this slice.** `#[serde(untagged)]` MUST NOT be used. Every non-`storage` byte of every golden staying identical is this slice's proof that nothing but the tag moved. `decision-log.md` [1] carries the argument against the untagged wire.
* **`extract_bucket` is deleted, not branched.** It derives the bucket from `Url::host_str()`, which on an `abfss://` URI yields the storage account rather than the container. `extract_bucket_from_files` keeps the same logic but survives ONLY as the S3 arm's private derivation, unreachable from the backend-agnostic call site.
* **Iceberg REST compliance is checked and unchanged.** `select_credential_source` and `merge_vended_into_storage` implement the `storage-credentials`-before-`config` rule and the longest-`prefix` rule of `apache/iceberg` `open-api/rest-catalog-open-api.yaml` (main), and their bodies move into the S3 arm VERBATIM. This feature introduces no deviation and fixes none, and it changes no file pruning, no snapshot or manifest reading, no delete-file handling, and no type mapping. `decision-log.md` [8] carries the normative quotations.
* This crate's CI gate is `cargo clippy --workspace --all-targets -- -D warnings`, under which an item left without a caller is a build failure. That gate, not preference, is what forces each deleted function's callers to be repointed in the same change.
* No new dependency is introduced and no dependency version changes.
* **This delta replaces the "ONE selection site" fence with a "TWO selectors on disjoint inputs" fence, and is issue #276 — slice D of six (A-F).** Slice C's clause declaring `storage_block` "the ONLY place a backend is SELECTED FROM INPUT" is superseded, not bent: `vs-adapter/pushdown-planning-cloud-credentials` now selects the backend from the table location's URI scheme when `use_vended_credentials` is true. Pretending that is not a selection would leave the fence technically intact and factually false, which on a credentials path is worse than no fence.
* **One selector per credential mode, chosen by ONE boolean at ONE site, on DISJOINT inputs.** `storage_block` selects from the CONNECTION credential shape and runs only when vending is disabled. `resolve_vended_storage` selects from the `loadTable` response's table location and runs only when vending is enabled. `resolve_file_list` holds the single `if creds.use_vended_credentials` that picks between them. Neither selector reads the other's input, neither can override the other, and there is no path on which both run — so the count of DECISION POINTS is still one even though the count of SELECTORS is two.
* **Deleting `resolve_vended_storage`'s backend parameter is what makes the disjointness provable rather than asserted.** Before this delta it took a `&StorageBackend` and overlaid vended values field-by-field onto the payload `storage_block` had chosen, which is precisely a second selector reading the first's output. Removing the parameter turns "the vended selector does not consult the static one" into a signature-level property no future edit can quietly reverse.
* **Keeping ONE selector was considered and rejected on two counts.** Passing the URI scheme into `storage_block` would require the scheme at CONNECTION-parse time, but the scheme is only known after `loadTable`, which runs later, once per table, and never at all on the `createVirtualSchema` path — so a single selector would have to be deferred past `loadTable` for every path including the non-vended one, and would still have no answer for a request that resolves no table. Making the vended arm mutate the payload `storage_block` returned was rejected by this feature's own existing clause: "reaching into the payload to finish construction is exactly the knowledge this feature removes."
* **The count of modules permitted to name a variant is UNCHANGED at five.** `resolve_vended_storage` was already on that list; only its documented ROLE changes, from overlaying credentials onto a caller-selected backend to selecting one from the anchor scheme. No module joins the list and none leaves it, so the fence this feature exists to hold is narrowed in prose and identical in extent.
* **`allow_http` is threaded in as a resolved virtual-schema property, so the vended selector takes one new non-CONNECTION parameter.** Slice C's reading is UNCHANGED: `allow_http` "arrives from the `ALLOW_HTTP` VS property and is consumed only by the S3 payload", and "the Azure backend carries no HTTP-scheme knob at all". Deriving the value from the vended `s3.endpoint`'s scheme instead was considered and REJECTED as a security regression — the property defaults to false, so a derivation would let a catalog vending `http://…` put vended credentials in the clear with no operator control. The parameter is NOT a CONNECTION value, so it does not weaken the rule that the vended selector reads no CONNECTION storage field. Adding a fourth method to the enum to expose `allow_http` was likewise rejected: it would leave the vended selector reading a CONNECTION-derived value, which is the exact coupling this delta removes.
* **`StorageProps`, `AdlsCred`, the wire encoding, and all three enum methods are UNEDITED.** No variant is added or removed, no field changes, no serde attribute changes, and the externally-tagged lowercase `{"s3":{…}}` / `{"adls":{…}}` encoding is byte-identical. A vended ADLS backend serializes exactly as a static one does, so every committed golden SQL fixture and scan-spec JSON fixture passes unedited.
* **The `AdlsCred` two-state invariant is preserved and newly exercised.** The vended ADLS arm assembles the SAS state and never the account-key state, so "both" and "neither" stay unrepresentable and `object_store`'s access-key-beats-SAS precedence stays unreachable. Slice C recorded that precedence as the reason the enum exists in this shape; a vended SAS is the first input that reaches the SAS state from anywhere other than the CONNECTION.
* No dependency is added and no dependency version changes.
* This delta (plan `add-native-unity-catalog-client`, issue #318) admits a third backend-selection site for native Unity Catalog credential vending. It supersedes both the enum's two-selector invariant and its five-module variant-naming list (now six), but does NOT wire a new selector dispatch: `resolve_uc_vended_storage` is unit-tested only, and the catalog-kind-to-credential-family dispatch is deferred to #319/#320. The two existing selectors — `storage_block` (CONNECTION shape, static, when `use_vended` is false) and `resolve_vended_storage` (URI scheme, vended, when `use_vended` is true) — stay unchanged.
* **This delta moves vended POLICY and CONSTRUCTION into the shared home and is issue #330.** It supersedes the recorded clause that the shared home "SHALL own ONLY the scheme-to-variant-kind classification and MUST NOT construct a `StorageBackend` value", and the clause that `resolve_vended_storage` "MUST NOT take a `StorageBackend`, a `ConnectionCreds`, or any other CONNECTION-derived value as a parameter". Both were written when the only shared thing was scheme classification; both are what let the two vended selectors drift.
* **The fork was at the wrong seam, and the drift is measurable.** `vended.rs` and `unity/vended.rs` resolve the same output from different wire inputs. Only `classify_vended_scheme` was shared; everything after it was copied. Scheme extraction, storage-host derivation, ADLS account-name derivation, the plaintext `http://` endpoint gate, and the `path_style` default were copied and stayed consistent. The `abfs://` plaintext consent gate and the S3 store-address check were NOT copied and became gaps: the Unity ADLS arm takes no `allow_http` parameter at all, so `abfs://` is always accepted and silently read over HTTPS.
* **The governing principle, stated once so a third catalog kind's author reads it rather than re-deriving it:** a catalog kind MAY fork on HOW a value is read off the wire; it MUST NOT fork on WHAT makes the resulting value acceptable. Consent gates, address rules, and target-variant decisions live in one shared home both kinds call.
* **Moving construction SHRINKS this feature's own invariant rather than widening it.** The exhaustive list of modules permitted to name a `StorageBackend` variant goes from SIX to FOUR: both vended selectors drop off it. That is the opposite direction from the last two deltas to this feature, which each added a module.
* **The repointed probe is strictly stronger than the two it replaces.** Today each selector must merely MENTION every variant name. After the repoint, the shared home must CONSTRUCT every variant, each selector must DISPATCH every vended kind, and the two enums' variant-name sets must be equal — so a third `StorageBackend` variant fails a probe at every one of the three levels instead of at two mention sites that can drift apart.
* **`resolve_uc_vended_storage` has no production caller today** — verified by reference search: its only callers are `crates/lakehouse-catalog/src/unity/vended_tests.rs`, `crates/lakehouse-catalog/tests/catalog_public_surface.rs`, and the crate-root re-exports. Delta scan execution reaches it in #319/#320. The `abfs://` gap is therefore latent, not live, which is why this is a follow-up rather than a #327 blocker — and why it must land BEFORE #319/#320, when the supersede cost grows.
* No dependency is added and no dependency version changes. `StorageProps`, `AdlsCred`, the variant set, the wire encoding, and all three enum methods are UNEDITED, so every committed golden SQL fixture and scan-spec JSON fixture passes unedited.

## Scenarios

### Scenario: One enum names the storage backend and answers every backend-specific question

* *GIVEN* the one-variant `StorageBackend` slice B landed, whose three methods `secret_values`, `catalog_storage_props`, and `file_io` each have a single S3 arm
* *WHEN* the Azure backend lands
* *THEN* `lakehouse-catalog` SHALL declare a SECOND variant, `Adls { account_name: String, cred: AdlsCred }`, beside the unmodified `S3(StorageProps)`, and a `pub` enum `AdlsCred` with EXACTLY TWO states — an account key and a SAS token — each carrying its secret as a `String`
* *AND* `AdlsCred` SHALL implement `Debug` MANUALLY so the wrapped secret is replaced by a redaction marker, matching what `ConnectionCreds` already does for `secret_key`, `session_token`, `token`, and `client_secret`
* *AND* `AdlsCred` SHALL derive `Clone`, `PartialEq`, `Eq`, `Serialize`, and `Deserialize`, because `StorageBackend` derives all five and stops compiling without them
* *AND* all three methods SHALL gain an Azure arm, and NO method SHALL gain a catch-all `_` arm, so a third backend is a build failure at every site with a decision to make rather than a silent fall-through
* *AND* `catalog_storage_props` SHALL emit `iceberg::io::ADLS_ACCOUNT_NAME` when `account_name` is non-empty, and EXACTLY ONE of `iceberg::io::ADLS_ACCOUNT_KEY` and `iceberg::io::ADLS_SAS_TOKEN` chosen by the `AdlsCred` state, and SHALL emit NO other key — in particular no `adls.connection-string`, which fails `FileIO` construction hard, and no `s3.*` key
* *AND* `catalog_storage_props` SHALL name those keys through the `iceberg::io::ADLS_*` constants rather than string literals, so this module stays the SINGLE source of every iceberg storage config key it emits, exactly as it already is for the six S3 keys
* *AND* `file_io` SHALL select the unit factory variant `OpenDalStorageFactory::Azdls` and SHALL configure it from `catalog_storage_props` alone, so the Azure props map and the Azure `FileIO` cannot drift
* *AND* `secret_values` SHALL return EXACTLY ONE value for the Azure variant — the account key or the SAS token — and SHALL return an EMPTY list when that value is the empty string, because every returned value is fed to literal-value error redaction and redacting `""` would strip every character of the error it guards
* *AND* `secret_values` MUST NOT return `account_name`, because a storage account name is not a secret and appears in every `abfss://` URI the engine handles
* *AND* the S3 arm of all three methods SHALL be UNEDITED, and `StorageProps` SHALL remain unedited, so the six S3 config keys, their inclusion conditions, and the S3 secret list are the same values in the same order as before this delta

### Scenario: Every consumer holds a backend and no consumer names one

* *GIVEN* the seventeen production sites that carry storage config today — in `lakehouse-catalog`: `resolve_vended_storage` (`vended.rs`), `build_rest_catalog` (`session.rs`), `list_namespace_tables` and `list_namespace_tables_unsigned` (`namespace.rs`); in the adapter: `storage_block` (`connection.rs`), `resolve_connection_config`, `handle_pushdown_request`, and `redact_error` (`adapter/mod.rs`), `handle_pushdown` and `build_dispatch_sql` (`pushdown/mod.rs`), `resolve_file_list` (`pushdown/file_resolution.rs`), `plan_join` (`pushdown/joins/mod.rs`), `resolve_one_join_side` plus the `pub` field `ResolvedJoinSide::effective_storage` and `ResolvedJoinSide::new`'s matching parameter (`pushdown/joins/planning.rs`); and in the scan: `CommonScanSpec.storage` and its `impl Default` initializer (`scan/spec.rs`), `register_file_list` (`scan/raw_scan.rs`), and `PositionalDeleteScanTable::new` (`scan/positional_deletes.rs`)
* *WHEN* the refactor lands
* *THEN* each of those SHALL hold, accept, or return `StorageBackend` in place of `StorageProps`, so the backend travels as one value from the CONNECTION object to the scan spec, and no site in that chain SHALL unwrap the payload to hand a bare `StorageProps` onwards
* *AND* the ONLY modules permitted to match on a `StorageBackend` variant or read its S3 payload SHALL be these six, exhaustively: the enum's own methods (`storage.rs`); the engine's single object-store registration function (`scan/object_store.rs`); `resolve_vended_storage` (`vended.rs`), which SELECTS the variant from the anchor's URI scheme and constructs the backend the vended credential source describes; `storage_block` (`connection.rs`), which constructs the backend at the CONNECTION-parsing entry point; `CommonScanSpec`'s manual `impl Default` (`scan/spec.rs`), which re-wraps a placeholder payload without choosing a backend from any input and so is a construction site but not a selection site; and `resolve_uc_vended_storage` in the `unity` module of `lakehouse-catalog` (plan `add-native-unity-catalog-client`, issue #318), which constructs `StorageBackend::S3` or `StorageBackend::Adls` from the Unity Catalog vended response's storage-location URI scheme. A test-only payload unwrapper or variant construction MAY appear in ANY `#[cfg(test)]` module of either crate, not only in a support module. No other production module SHALL name a variant, with the source-level variant probe covering BOTH vended selectors as the compensating gate
* *AND* there SHALL be EXACTLY TWO sites that SELECT a backend FROM INPUT ON THE VENDING-VERSUS-STATIC AXIS, one per credential mode, reading DISJOINT inputs: `storage_block`, whose variant depends on the CONNECTION credential shape and which runs only when `use_vended_credentials` is false; and `resolve_vended_storage`, whose variant depends on the table location's URI scheme and which runs only when `use_vended_credentials` is true. A `Default` impl that always re-wraps a fixed `S3` placeholder is NOT a selection under this clause, because its variant depends on no input. `resolve_uc_vended_storage` is a THIRD selection site, admitted below, reading a disjoint Unity Catalog credential-vending input outside this two-site axis
* *AND* exactly ONE site SHALL choose between the two Iceberg-path selectors — the `use_vended_credentials` branch in `resolve_file_list` — so the number of decision points on that axis stays ONE; the catalog-kind-to-credential-family dispatch that would choose `resolve_uc_vended_storage` is deferred to #319/#320, per the scenario below
* *AND* `resolve_vended_storage` MUST NOT take a `StorageBackend`, a `ConnectionCreds`, or any other CONNECTION-derived value as a parameter, so the two selectors' input disjointness is enforced by its signature rather than by inspection of its body; its resolved `ALLOW_HTTP` parameter is a virtual-schema property and not a CONNECTION value, so it does not breach that disjointness
* *AND* because the vended selector matches on a URI SCHEME rather than on a `StorageBackend`, adding a third variant SHALL NOT break its build, so a source-level probe in `crates/lakehouse-catalog/tests/catalog_public_surface.rs` SHALL EXTRACT the variant list from this enum's own source — already reachable there through the `CATALOG_SOURCES` `include_str!` table — and assert every extracted name appears in `vended.rs`, as the compensating gate for the compile-time failure that site cannot provide; a HARDCODED variant list SHALL NOT satisfy this clause, because it keeps passing after a third variant is added
* *AND* because the selected variant is PER TABLE LOCATION, a join SHALL carry one backend PER SIDE rather than collapsing onto one whole-spec value, and each side SHALL be read through a store built from its own backend, as `vs-adapter/pushdown-planning-join` and `datafusion-scan/scan-execution-join` require — so no consumer ever holds a backend that names one side while another side's files are read through it. SUPERSEDES the earlier clause requiring the join planner to reject a join whose sides differ in backend variant or ADLS `account_name`: that guard is deleted, because per-side carriage removed the collapse it compensated for and backend identity was never what the scan's addressing depends on
* *AND* `storage_block` SHALL take the resolved `allow_http` value as a parameter and return a fully-formed `StorageBackend`, and `resolve_connection_config` SHALL NOT mutate `allow_http` on the returned value afterwards, because reaching into the payload to finish construction is exactly the knowledge this feature removes
* *AND* `build_rest_catalog` SHALL merge `catalog_storage_props`' entries into its props map and MUST NOT insert an S3 config key of its own
* *AND* `resolve_vended_storage` SHALL keep its name and its `pub` status while returning `Result<StorageBackend, UdfError>`, and it SHALL remain the crate's ONLY vended entry point with every mechanism step crate-private
* *AND* `lakehouse_engine::scan::spec` SHALL re-export `StorageBackend` beside its existing `StorageProps` re-export, at `pub` visibility, so the scan layer's consumers reach it on the path they already import

### Scenario: The vended selector reaches the SAS state without widening the enum

* *GIVEN* the `Adls { account_name: String, cred: AdlsCred }` variant and its two-state `AdlsCred`, whose account-key and SAS states make "both" and "neither" unrepresentable
* *WHEN* the vended selector resolves an ADLS backend from a `loadTable` response
* *THEN* it SHALL construct the SAS state and MUST NOT construct the account-key state, so `object_store`'s access-key-beats-SAS precedence in `build()` stays unreachable
* *AND* `AdlsCred`, `StorageProps`, the enum's variant set, and all three of `secret_values`, `catalog_storage_props`, and `file_io` SHALL be UNEDITED, because a vended SAS is a new INPUT to the existing SAS state rather than a new state
* *AND* the wire encoding of both variants MUST be byte-identical to before this delta, so nesting changes what encloses a backend and never its own bytes — every scan-spec JSON fixture passes UNEDITED, and of the committed golden SQL fixtures the six `empty_*` pass UNEDITED while the eighteen credential-bearing ones are regenerated by `vs-adapter/scan-spec-credential-reference` for their enclosing `storage` value alone
* *AND* `secret_values` SHALL return the vended SAS as this backend's single secret value, so the value-based redaction `resolve_file_list` already applies covers a vended SAS exactly as it covers a static one

### Scenario: The scan registers its object store without naming the backend

* *GIVEN* `register_side_store`, whose S3 arm derives its store key with the private `extract_bucket_from_files` (`Url::host_str()` on the side's first file), and whose sibling `validate_uniform_object_store_files` derives the same notion independently with `ListingTableUrl::object_store()` — a duplication slice B recorded and DEFERRED to the slice that adds the second backend
* *WHEN* the Azure backend lands
* *THEN* that deferral SHALL be discharged: `extract_bucket_from_files` SHALL be DELETED and replaced by ONE backend-agnostic derivation that returns the side's store URL as the `scheme://userinfo@host:port` slice of that side's first reconstructed file URI — the SAME slice `ListingTableUrl::object_store()` takes — so the two notions agree by construction instead of by inspection
* *AND* that one derivation SHALL be the ONLY store-key derivation in the module, with each arm reading what its builder needs OUT of the returned URL — the S3 arm the host as the bucket name, the Azure arm the whole URL — and NO arm re-parsing the file URI its own way
* *AND* the `Url` that derivation returns for an `s3://` file list MUST equal the value the deleted function's caller produced — `s3://<bucket>` — so every existing assertion on `register_side_store`'s returned URL passes UNEDITED
* *AND* for an `s3a://` file list — the OTHER absolute scheme `FileEntry::path` documents (`crates/lakehouse-engine/src/scan/spec.rs:445` and `:730`) — the derivation SHALL return `s3a://<bucket>`, which DELIBERATELY CHANGES the registered key from the `s3://<bucket>` that the deleted `format!("s3://{bucket}")` produced
* *AND* that change SHALL be recorded as a latent-defect FIX and not a regression, because the rewritten key was one DataFusion never looks up: `register_file_list` resolves the side's store through `ListingTableUrl::parse(&first_abs).object_store()`, which PRESERVES the `s3a` scheme, while `get_store` is an exact match on `scheme://host:port` with no scheme fallback (`datafusion-execution-54.1.0/src/object_store.rs:255-274`) — so an `s3a://` file list fails TODAY with "No suitable object store found for s3a://…", and no `s3a://` scan has working behavior to preserve
* *AND* the Azure arm SHALL pass that URL to `MicrosoftAzureBuilder::with_url`, which sets the account and the container from it, and SHALL pass the account host through VERBATIM rather than reconstructing `<account>.dfs.core.windows.net`, because reconstruction would discard whatever host the file URI actually names
* *AND* the arm MUST NOT compensate for `MicrosoftAzureBuilder`'s OWN host restriction: `with_url` accepts an `abfss://` host bearing exactly one of four suffixes — `dfs.core.windows.net`, `blob.core.windows.net`, `dfs.fabric.microsoft.com`, `blob.fabric.microsoft.com` — and returns `UrlNotRecognised` for every other host (`object_store-0.13.2/src/azure/builder.rs:660-682`), so a sovereign-cloud host (`*.dfs.core.chinacloudapi.cn`), a private endpoint, or any other Azure-compatible host SHALL surface that build error through the arm's redaction, naming no credential value
* *AND* the Azure arm SHALL apply the resolved connection budget through the SAME `ClientOptions` helper the S3 arm uses, so the warm-connection ceiling is one decision for both backends
* *AND* the Azure arm SHALL set the account key via `with_access_key` OR the SAS token via the SAS config key, selected by the `AdlsCred` state, and SHALL set NEITHER when the other is set
* *AND* the Azure arm SHALL redact the backend's `secret_values` out of any builder error before surfacing it, through the SAME two-step redaction the S3 arm applies, so a malformed account key or SAS never reaches an error message
* *AND* the Azure arm SHALL wrap the built store in the caller-supplied-size HEAD decorator and register it under the derived URL, returning `Some(url)`, and SHALL return `None` without re-registering when the registry already holds that key — the same contract the S3 arm has
* *AND* a NEW whole-spec precondition SHALL reject a scan spec whose sides resolve to DIFFERENT object stores that DataFusion would register under the SAME registry key, checked ONCE before any registration and skipping a join whose dimension file list is empty
* *AND* that precondition SHALL compare, per side, the registry key DataFusion computes (scheme + `host:port`, EXCLUDING userinfo) against the store URL the side actually needs (scheme + userinfo + `host:port`), and SHALL return a `UdfError::User` naming both conflicting store URLs when two sides share the former and differ in the latter
* *AND* that precondition SHALL be BACKEND-AGNOSTIC and MUST NOT match on a storage-backend variant, because it states a property of DataFusion's registry key rather than of any one backend; an `s3://` URI carries no userinfo, so the two notions coincide and the check can never fire for an S3 spec
* *AND* `validate_uniform_object_store_files` SHALL STILL be unedited, because `ListingTableUrl::object_store()` already includes the userinfo the registry key drops, so a file list mixing containers WITHIN one side is already rejected — for data files and for their associated delete files alike — and only the ACROSS-side case was missing

### Scenario: The scan-spec wire carries the backend as a tagged variant

* *GIVEN* the externally-tagged lowercase encoding slice B landed, under which the S3 backend serializes as `{"s3": {…}}`
* *WHEN* a storage backend is serialized into the shard-invariant common blob
* *THEN* the backend value SHALL be `{"s3": {…}}` or `{"adls": {"account_name": …, "cred": {…}}}`, whose single lowercase variant key names the backend and whose `cred` value is itself a tagged object naming the account-key or SAS state
* *AND* that backend value SHALL appear on the wire ONLY inside the scan-spec storage wrapper specified by `vs-adapter/scan-spec-credential-reference`, so the `storage` key of the common blob no longer holds a bare backend: the `connection` variant carries a reference and no credential at all; the `sealed` variant carries the connection name and this same backend encoding as AES-GCM ciphertext; and the `inline` variant carries it in plaintext for host-test spec construction ONLY — the adapter's selection function cannot return it
* *AND* the backend value's OWN encoding MUST be byte-identical to before this delta, so nesting changes what encloses it and never its own bytes — the EIGHTEEN credential-bearing committed golden SQL fixtures are REGENERATED for the enclosing `storage` value alone, while the SIX `empty_*` golden fixtures, every scan-spec JSON fixture, and the existing external-vantage serde test all still pass UNEDITED
* *AND* the encoding MUST round-trip: deserializing a serialized backend SHALL yield an equal value, including which `AdlsCred` state it holds
* *AND* the backend enum MUST STILL NOT be declared `untagged`, and the wrapper enum MUST NOT be either, and the decode-side test that pins this SHALL be EXTENDED rather than replaced: its `{"azure": …}` case SHALL be KEPT — still proving that `azure` is not the ADLS variant key, `adls` is — and its S3-shaped-payload-under-`adls` case SHALL be KEPT, still proving that a payload naming a variant it does not match is rejected rather than resolved by trial deserialization, with a wrapper case ADDED, because a connection reference and an inline S3 backend are both JSON objects and trial deserialization would decide between them by field-name coincidence. That extension is admitted by CLASS FOUR of the "Adding the Azure backend leaves the S3 path unchanged" scenario, which bounds it, and by no other class
* *AND* the wrapper MUST NOT expose any method returning secret values or a credential payload, so a scan-side site left reading the unresolved wire value fails to compile rather than yielding an empty secret set
* *AND* a scan-spec deserialization failure MUST NOT contain any credential value — no storage access key, secret key, session token, Azure account key, or SAS token — while the connection NAME MAY appear, because it is not a secret and the resolution error specified by `vs-adapter/scan-spec-credential-reference` names it; a sealed payload that fails base64 decoding or AEAD authentication SHALL likewise produce an error naming the connection and the failed operation, carrying neither the payload nor any plaintext

### Scenario: No storage credential type prints its payload through Debug

* *GIVEN* the storage types that hold a credential value — `StorageProps`, `StorageBackend`, `AdlsCred`, and `ConnectionCreds`
* *WHEN* any of them is formatted with the `Debug` formatter
* *THEN* the rendered text MUST NOT contain the value of `access_key`, `secret_key`, `session_token`, `token`, `client_secret`, `account_key`, `sas_token`, or an `AdlsCred` payload, and SHALL render a fixed placeholder in each such position instead
* *AND* the guard SHALL sit on the TYPE rather than on each formatting site, because a use site added later cannot be reviewed by a test that does not yet exist
* *AND* the non-secret fields of those types — `endpoint`, `region`, `path_style`, `allow_http`, `warehouse`, `account_name`, `client_id`, `oauth2_server_uri`, `scope`, `use_sigv4`, and `use_vended_credentials` — SHALL still render their values, so `Debug` stays useful for diagnosing a misconfigured store address
* *AND* the label-based redaction SHALL additionally cover the exact literal `"sas":`, the serialized `AdlsCred::Sas` wire key, and MUST NOT be given a bare `sas` pattern, because the matcher redacts from a matched label to the next delimiter and would destroy unrelated text
* *AND* every production site that composes the two redaction passes SHALL apply the VALUE pass before the LABEL pass, the order this crate's own redaction module documents as required for a SAS token, so no site keeps the inverted composition

### Scenario: S3 behavior is unchanged across the refactor

* *GIVEN* the pre-refactor unit, integration, and S3 E2E suites
* *WHEN* the suites run against the refactored layout
* *THEN* every test MUST pass, and no assertion MUST be weakened, disabled, or deleted to accommodate a type change — a test edit is permitted ONLY to wrap a constructed value in `StorageBackend::S3`, to change a helper's return type, or to update a `storage` value on the wire
* *AND* the catalog request URLs, the header set on each request, the OAuth2 grant request body, and the props map `build_rest_catalog` builds MUST be identical to the pre-refactor shapes for the same query on every auth mode
* *AND* the file list resolved for a given table and the vended-versus-static storage resolved from a given `loadTable` response MUST be field-for-field identical to the pre-refactor output
* *AND* the scan-driving SQL generated for a given pushdown request MUST be byte-identical to the pre-refactor output EXCEPT for the `storage` VALUE — a WIDENING of the recorded variant-tag exception, because `vs-adapter/scan-spec-credential-reference` replaces the value with a connection reference or a sealed envelope rather than only re-tagging what encloses the backend's encoding
* *AND* `make test-e2e` MUST pass against a `.so` built from the refactored source, because the S3 E2E suite is this feature's declared characterization gate and a stale `.so` would make that gate vacuous
* *AND* no storage credential, catalog secret, or live bearer token SHALL appear in any returned SQL string or error message, on any path the refactor touches, and no VENDED STS key SHALL appear there in PLAINTEXT — a vended key travels only inside the AES-GCM-sealed envelope of `vs-adapter/scan-spec-credential-reference`, issue [#378](https://github.com/exasol-labs/lakehouse-engine-rs/issues/378), closed by this plan — SUPERSEDING the recorded clause, whose vended half asserted an absence that was FALSE before this plan
* *AND* the storage-credential half SHALL be read as STRENGTHENED rather than scoped: a CONNECTION-supplied storage credential is now referenced by name and appears in no returned SQL at all, which the recorded clause asserted and this plan is what first makes true

### Scenario: Adding the Azure backend leaves the S3 path unchanged

* *GIVEN* the unit, integration, and S3 E2E suites as they stand after slice B
* *WHEN* the Azure variant, its three enum arms, its object-store arm, and the new whole-spec store precondition land
* *THEN* every test MUST pass, and no assertion MUST be weakened, disabled, or deleted — a test edit is permitted ONLY in the FOUR classes below, and any other test edit invalidates the gate
* *AND* CLASS ONE — repointing a direct call on the deleted `extract_bucket_from_files` at its replacement; the repointed test MUST assert the SAME bucket value it asserts today
* *AND* CLASS TWO — completing a pattern that the SECOND VARIANT alone makes refutable, at the FIVE sites named in `plan.md` § Forced Edits table A and nowhere else; each such edit SHALL add a panicking `Adls` arm (or `let … else`) that names the site as S3-only, SHALL change NO assertion, and SHALL NOT introduce a `_` catch-all, so the site stays a build failure for a THIRD backend
* *AND* CLASS THREE — completing a `ConnectionCreds` struct literal that the THREE NEW FIELDS alone make incomplete, at the SEVEN sites named in `plan.md` § Forced Edits table B and nowhere else; each such edit SHALL supply the three new fields and SHALL change NO existing field value and NO existing assertion. It MAY ADD an assertion only at `crates/lakehouse-catalog/src/creds.rs`'s `Debug`-redaction test, and only to assert that the two new secret fields are redacted
* *AND* CLASS FOUR — the decode-side extension the "The scan-spec wire carries the backend as a tagged variant" scenario above MANDATES in its `untagged` clause; that extension SHALL KEEP every payload case the test asserts today, SHALL ADD only further rejection cases, and SHALL change NO existing assertion. Renaming the test function is permitted, because its case set widens past the S3 variant key it is named for
* *AND* NO class SHALL change, weaken, or delete an assertion about S3 behavior, so the gate still proves the S3 path unchanged rather than being edited into agreement with the delta
* *AND* the repointed, completed, and extended sites SHALL keep every existing S3 assertion byte-identical
* *AND* the scan-driving SQL generated for a given S3 pushdown request MUST be byte-identical to its pre-delta output EXCEPT for the `storage` VALUE: the recorded reason — that an S3 CONNECTION produces the same S3 backend and therefore the same `storage` value — held for THIS delta and no longer holds across `vs-adapter/scan-spec-credential-reference`, which replaces that value with a connection reference or a sealed envelope while leaving the S3 backend it derives from field-for-field identical, so the S3-path-unchanged property this clause proves is preserved and only its ONE permitted difference widens
* *AND* the props map `build_rest_catalog` builds, the `FileIO` an S3 backend produces, the file list resolved for a given table, and the vended-versus-static storage resolved from a given `loadTable` response MUST each be identical to their pre-delta output
* *AND* the sequence of values `register_side_store` returns across a whole `build_session_context` call MUST match its pre-delta sequence for every S3 spec shape — a single-table scan, a join whose two sides share a bucket, a join whose sides span two buckets, and a join whose dimension file list is empty
* *AND* the S3 E2E suite MUST pass against a `.so` built from the delta's source, because that suite is this feature's declared characterization gate and a stale `.so` would make the gate vacuous
* *AND* no storage credential, catalog secret, live bearer token, Azure account key, or static SAS token SHALL appear in any returned SQL string or error message on any path this delta touches, and no VENDED STS key and no VENDED SAS SHALL appear there in PLAINTEXT — a vended value travels only inside the AES-GCM-sealed envelope of `vs-adapter/scan-spec-credential-reference`, issue [#378](https://github.com/exasol-labs/lakehouse-engine-rs/issues/378), closed by this plan — SUPERSEDING the recorded clause, whose vended half asserted an absence that was FALSE before this plan while its static halves were true and are now enforced by reference rather than by redaction
* *AND* making that true for Azure requires BOTH redaction layers to be extended, because neither alone covers the manifest-read path: `redact_credentials` (`crates/lakehouse-catalog/src/redaction.rs:30-48`) SHALL gain the Azure LABELS it currently lacks — its pattern list is AWS-only today — and the `file_resolution.rs` error sites that call it ALONE SHALL first apply value-based `redact_secret_values` against the effective backend's `secret_values()`, matching what `redact_error` (`crates/lakehouse-engine/src/adapter/mod.rs:1012-1020`) already does elsewhere
* *AND* the object-store arm's redaction alone MUST NOT be treated as discharging that requirement, because this delta feeds `adls.account-key` and `adls.sas-token` into `FileIO` through `catalog_storage_props`, so an Azure secret can surface from a manifest-list or manifest-load failure that never reaches `register_side_store`

### Scenario: A Unity Catalog vended selector is admitted as a third backend-selection site

* *GIVEN* the invariant in "Every consumer holds a backend and no consumer names one" that there SHALL be EXACTLY TWO sites that select a backend from input — `storage_block` when `use_vended` is false and `resolve_vended_storage` when `use_vended` is true — with no third selector added without superseding that clause
* *WHEN* the native Unity Catalog credential-vending path lands, resolving a `StorageBackend` from a Unity Catalog temporary-table-credentials response
* *THEN* a THIRD backend-selection site, `resolve_uc_vended_storage`, SHALL be admitted, reading a DISJOINT input — the Unity Catalog temporary-table-credentials response shape, distinct from both the CONNECTION credential shape and the Iceberg REST `loadTable` response — and this scenario SUPERSEDES the "EXACTLY TWO sites" and "no third selector" clauses accordingly
* *AND* `resolve_uc_vended_storage` SHALL select the backend variant from the table location's URI scheme ALONE and MUST NOT take a `warehouse`, a `region`, an existing `StorageBackend`, or any other CONNECTION-derived value as a parameter, so the three selectors' input disjointness is enforced by its signature
* *AND* the Unity Catalog vended path SHALL terminate in a `StorageBackend` value and is NOT wired into a pushdown dispatch in this plan — Delta scan execution consumes it in #319/#320 — but the invariant is superseded now so the third selector is not a silent breach of the two-site rule
* *AND* the recorded clause naming "these five [modules], exhaustively" as the ONLY modules permitted to match on a `StorageBackend` variant or read its S3 payload — closing with "No other production module SHALL name a variant" — SHALL be SUPERSEDED to admit a SIXTH module, `resolve_uc_vended_storage` in the new `unity` module of `lakehouse-catalog`, which constructs `StorageBackend::S3` or `StorageBackend::Adls` from the Unity Catalog vended response's storage-location URI scheme; the amended exhaustive list is SIX — the enum's own methods (`storage.rs`), the engine's single object-store registration function (`scan/object_store.rs`), `resolve_vended_storage` (`vended.rs`), `storage_block` (`connection.rs`), `CommonScanSpec`'s manual `impl Default` (`scan/spec.rs`), and `resolve_uc_vended_storage` (`unity` module) — and no other production module SHALL name a variant, with the source-level variant probe covering BOTH vended selectors as the compensating gate (pinned in "The three-selector dispatch stays single-homed and probe-guarded" below)

### Scenario: The three-selector dispatch stays single-homed and probe-guarded

* *GIVEN* the admitted third selector `resolve_uc_vended_storage` and the "exactly ONE site chooses between selectors" clause of "Every consumer holds a backend and no consumer names one"
* *WHEN* the shared scheme-to-variant-kind classification and its source probe are wired, and the selector dispatch onto `resolve_uc_vended_storage` is left for #319/#320
* *THEN* the scheme-to-variant-KIND classification SHALL have ONE home shared by `resolve_vended_storage` and `resolve_uc_vended_storage`, mapping `s3`/`s3a` to the S3 variant kind and `abfs`/`abfss` to the ADLS variant kind in exactly one place rather than duplicated across the two vended selectors
* *AND* that shared home SHALL own ONLY the scheme-to-variant-kind classification and MUST NOT construct a `StorageBackend` value, while each vended selector constructs its OWN `StorageBackend` variant from its OWN disjoint credential family — `resolve_vended_storage` from the Iceberg REST vended response and `resolve_uc_vended_storage` from the Unity Catalog temporary-table-credentials response — so the single-home requirement and the probe's requirement that every variant name appear in `resolve_uc_vended_storage`'s own production source are simultaneously satisfiable rather than in conflict
* *AND* this plan SHALL NOT wire a new selector dispatch: `resolve_uc_vended_storage` is admitted and unit-tested but is reached by NO selector-choice site in #318, because a pushdown under the Unity Catalog kind is refused before any credential or file resolution (`catalog-kind-selection` § "A pushdown request under the Unity Catalog kind is refused as not yet executable"); the recorded "exactly ONE site chooses between those two selectors — the `use_vended_credentials` branch in `resolve_file_list`" clause therefore stays INTACT and is NOT superseded here, and the catalog-kind-to-credential-family two-level dispatch that would supersede it is DEFERRED to #319/#320, when Delta scan execution first reaches `resolve_uc_vended_storage` — marked as a deferral exactly as the third selector's scan wiring is deferred in the scenario above
* *AND* a source-level probe SHALL extract the variant list from `enum StorageBackend`'s own source and assert every extracted name appears in `resolve_uc_vended_storage`'s production source (`unity` module), and the existing probe asserting the same over `resolve_vended_storage`'s source (`vended.rs`) SHALL remain — so the source-probe gate covers BOTH vended variant-naming selectors — and a hardcoded variant list SHALL NOT satisfy either probe

### Scenario: Vended policy and construction move into the enum's own module

* *GIVEN* the recorded clause of "The three-selector dispatch stays single-homed and probe-guarded" that the shared scheme-to-variant-kind home "SHALL own ONLY the scheme-to-variant-kind classification and MUST NOT construct a `StorageBackend` value, while each vended selector constructs its OWN `StorageBackend` variant from its OWN disjoint credential family"
* *AND* the two policy rules that clause allowed to exist in ONE selector only — the `abfs://` plaintext consent gate, present in `resolve_vended_storage` and ABSENT from `resolve_uc_vended_storage`, and the S3 store-address check, present in `resolve_vended_storage` alone
* *WHEN* vended policy and construction are given one shared home in `storage.rs`, beside `classify_vended_scheme`
* *THEN* that MUST-NOT-construct clause SHALL be SUPERSEDED: the shared home SHALL own the neutral vended S3 value shape, the URI-scheme and storage-host derivations, the ADLS account-name derivation, both plaintext-transport consent gates, the S3 store-address rule, and EXACTLY ONE construction function per `StorageBackend` variant
* *AND* the recorded exhaustive list of modules permitted to match on a `StorageBackend` variant or read its S3 payload SHALL SHRINK from SIX to FOUR — the enum's own module (`storage.rs`), the engine's single object-store registration function (`scan/object_store.rs`), `storage_block` (`connection.rs`), and `CommonScanSpec`'s manual `impl Default` (`scan/spec.rs`) — because NEITHER vended selector names a variant after this delta
* *AND* each vended selector SHALL keep its OWN wire extraction and SHALL reduce to three steps: classify the location's scheme, read its own credential family off its own wire shape, hand the resulting neutral values to the shared construction function — `resolve_vended_storage` reading a flat `HashMap<String, String>` through `select_credential_source`, `resolve_uc_vended_storage` reading the typed temporary-table-credentials response
* *AND* a catalog kind MAY fork on HOW a value is read off the wire and MUST NOT fork on WHAT makes the resulting value acceptable, so every consent gate, every store-address rule, and every target-variant decision SHALL have exactly ONE home that both kinds call, and a second per-kind copy of any of them SHALL NOT be added
* *AND* the count of sites that SELECT a backend from input SHALL stay THREE and the count of sites that choose between the two Iceberg-path selectors SHALL stay ONE, because moving construction changes which module NAMES a variant and not which module CHOOSES a backend
* *AND* the shared construction functions SHALL be crate-private and MUST NOT join the crate's public surface, because they are mechanism steps of the one published vended entry point per catalog kind

### Scenario: The repointed source probe binds the shared home, both selectors, and both enums

* *GIVEN* the two recorded probes, each asserting that every variant name extracted from `enum StorageBackend`'s own source appears in one vended selector's production source — `vended.rs` and `unity/vended.rs`
* *WHEN* construction moves into the shared home, so neither selector's source names a `StorageBackend` variant and both recorded probes would fail
* *THEN* those probes SHALL be REPOINTED rather than deleted or weakened, and the repointed gate SHALL bind at THREE levels instead of two mention sites that can drift apart
* *AND* ONE probe SHALL assert that every variant name extracted from `enum StorageBackend`'s own source appears as a `StorageBackend::<Variant>` construction in the shared home's PRODUCTION source, which is non-vacuous because the enum's own method bodies spell their arms `Self::<Variant>` and only a construction expression spells the qualified form
* *AND* a per-selector probe SHALL assert that every variant name extracted from `enum VendedBackendKind`'s own source appears in EACH vended selector's production source, so a third vended kind still forces BOTH selectors to dispatch it
* *AND* a probe SHALL assert that the extracted variant-name SET of `VendedBackendKind` EQUALS that of `StorageBackend`, so adding a third `StorageBackend` variant fails that probe until a third kind exists, which fails the per-selector probes until both selectors dispatch it, which fails the construction probe until the shared home builds it
* *AND* every one of those probes SHALL extract its variant list from the enum's own source through ONE shared extractor parameterised by enum name, and a HARDCODED variant list SHALL NOT satisfy any of them
* *AND* every one of those probes SHALL read PRODUCTION source only — text from the first `#[cfg(test)]` onward MUST NOT satisfy it — exactly as the recorded probe already requires

### Scenario: The vended selectors take a store address that cannot carry a credential

* *GIVEN* the recorded clause that `resolve_vended_storage` "MUST NOT take a `StorageBackend`, a `ConnectionCreds`, or any other CONNECTION-derived value as a parameter, so the two selectors' input disjointness is enforced by its signature rather than by inspection of its body", and the matching clause requiring `resolve_uc_vended_storage` to take no `warehouse`, `region`, or existing `StorageBackend`
* *AND* the defect those clauses cause: real Databricks AWS vends short-lived credentials with NO endpoint and its credential type carries no region field at all, so a legal Databricks `s3://` table lands in exactly the state `resolve_vended_storage`'s "store address undetermined" error rejects
* *WHEN* credentials are split from addressing
* *THEN* both clauses SHALL be SUPERSEDED for ADDRESSING ALONE: each vended selector SHALL take one additional parameter carrying the CONNECTION's configured store `endpoint` and `region`, and carrying NOTHING else
* *AND* the credential half of the disjointness rule SHALL be UNCHANGED: an access key, a secret key, a session token, an Azure account key, and a SAS token SHALL come from the vended response ALONE on every vended path, and MUST NEVER be read from the CONNECTION under vending
* *AND* because the signature no longer enforces that half, it SHALL be enforced MECHANICALLY rather than by prose: the new parameter's type SHALL declare EXACTLY the two addressing fields, and a source-level probe SHALL assert that its declaration names no field spelled `access_key`, `secret_key`, `session_token`, `token`, `account_key`, `sas_token`, or `password`
* *AND* the type SHALL have EXACTLY ONE construction from the CONNECTION, declared beside it, so which CONNECTION fields are permitted to cross into vended resolution is one decision in one place rather than a struct literal repeated per call site
* *AND* that ONE-construction rule SHALL be enforced BY THE TYPE rather than by prose or by review: both addressing fields SHALL be declared NON-`pub`, reads SHALL go through accessor methods, and the type SHALL therefore admit outside the shared home only its `Default` and that one conversion — a call site building the value field-by-field, or mutating a `Default`, SHALL NOT COMPILE — and a source-level probe SHALL assert that the declaration keeps both fields non-`pub`, because widening them back to `pub` would silently restore the field-by-field construction this clause forbids
* *AND* passing a `ConnectionCreds`, a `StorageBackend`, or any other type carrying a credential field to either vended selector SHALL remain FORBIDDEN, so the superseded rule is narrowed to addressing and not dropped
* *AND* the disjointness rule's PURPOSE SHALL be restated in the spec rather than left to be inferred: it exists to stop a vended credential silently falling back to a static one, and it was never about addressing — without that sentence the next reader sees only "the vended selector now takes CONNECTION values" and reads the rule as abandoned
