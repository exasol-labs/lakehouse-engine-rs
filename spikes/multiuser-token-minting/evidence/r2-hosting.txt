
[1m== 1a  BucketFS[0m
   BucketFS presents a 2-certificate chain:
     subject=CN=exacluster.local X509v3 Subject Alternative Name:      DNS:*.exacluster.local, DNS:exacluster.local 
     subject=CN=exacluster.local 
   publishing via exapump --profile docker to the default bucket
     Uploaded /tmp/tmp.LnDnpOHI3q/bfs/jwks.json to engine-oidc/jwks.json
     Uploaded /tmp/tmp.LnDnpOHI3q/bfs/openid-configuration to engine-oidc/.well-known/openid-configuration
   anonymous read back (bucket is Public = True):
     HTTP/1.1 200 OK
     Content-Type: application/json
     X-Content-Type-Options: nosniff
   the same fetch with the chain root trusted, no -k:
     HTTP 200 over 1.1
   Lakekeeper, chain ROOT trusted via SSL_CERT_FILE:
     ACCEPTED
     "message":"Creating OIDC authenticator for oidc (https://exasol.exacluster.local:2581/default/engine-oidc)"
     "message":"Successfully added OIDC authenticator: oidc"
   negative control 1 — the LEAF trusted instead of the root:
     REJECTED
     Error: Failed to create required OIDC authenticator for oidc (https://exasol.exacluster.local:2581/default/engine-oidc): Failed to fetch openid configuration from https://exasol.exacluster.local:2581/default/engine-oidc/.well-known/openid-configuration: error sending request for url (https://exasol.exacluster.local:2581/default/engine-oidc/.well-known/openid-configuration)
   negative control 2 — a hostname outside the certificate's SAN:
     REJECTED
     Error: Failed to create required OIDC authenticator for oidc (https://exasol:2581/default/engine-oidc): Failed to fetch openid configuration from https://exasol:2581/default/engine-oidc/.well-known/openid-configuration: error sending request for url (https://exasol:2581/default/engine-oidc/.well-known/openid-configuration)

[1m== 1a.2  Authorize real users through the BucketFS-hosted issuer[0m
   whoami -> {"id":"oidc~11111111-1111-4111-8111-111111111111","name":"alice (via Keycloak)"}
   alice: alice_table 200  bob_table 404   (expect 200 / 404)
   bob:   alice_table 404  bob_table 200   (expect 404 / 200)
   Verdict for 1a: WORKS, with zero new components. BucketFS already runs inside
   the Exasol cluster the engine is deployed on, it serves the files anonymously
   when the bucket is Public = True, and it returns the right content type.
   Two conditions, and both look identical when violated — reqwest reports only
   "error sending request for url", with no TLS detail at any RUST_LOG level:
     * the provider URI must use a name the BucketFS certificate covers. The
       Docker image's cert is CN=exacluster.local with SAN *.exacluster.local, so
       `https://exasol:2581/...` fails however the trust is configured.
     * Lakekeeper must trust the ROOT of the chain. BucketFS presents two
       certificates that share the subject CN=exacluster.local and both carry
       CA:TRUE; trusting the leaf is not enough.
   The trust itself is ordinary: Lakekeeper honours SSL_CERT_FILE and the system
   bundle at /etc/ssl/certs/ca-certificates.crt. A deployment whose Exasol
   certificate comes from the customer's own PKI needs no special handling at all
   if that PKI is already in the catalog host's trust store.
   removed the published files from BucketFS

[1m== 1b  Object storage (the bucket the lakehouse data already lives in)[0m
   published to s3://engine-oidc (public read)
   anonymous read back:
     HTTP/1.1 200 OK
     Content-Type: application/json
     X-Content-Type-Options: nosniff
   Lakekeeper against the object-storage URI:
     ACCEPTED
     "message":"Creating OIDC authenticator for oidc (http://minio:9000/engine-oidc)"
     "message":"Successfully added OIDC authenticator: oidc"
   Verdict for 1b: WORKS. Zero new components: the discovery document and JWKS
   are two static objects in a bucket the deployment already has, served with
   the right content type, reachable by Lakekeeper over the same network path it
   already uses for table data.
   Caveat: the objects must be PUBLICLY readable (a JWKS is public by design,
   but it is a policy change on that prefix), and the URL must be stable —
   rotating it invalidates every token in flight.
